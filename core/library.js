// Shared generic chapters: employer- and candidate-neutral. Packs add their own material through an overlay (see docs/content-schema.md).
window.PREP_CORE = {
 "system-design-fundamentals": {
  "id": "system-design-fundamentals",
  "title": "System design fundamentals",
  "day": 1,
  "level": "danger",
  "levelLabel": "Asked in almost every interview",
  "why": "Before anyone asks you about drift or feature stores, they will ask where the load balancer goes, what happens when the cache is stale, how you would shard that table, and how the service stays up during a deploy. In real interviews these plain engineering words come up more often than the machine-learning ones: caching read-through against write-through, database indexing and sharding, CAP and ACID, pagination, rate limiting, circuit breakers, no-downtime deployment. Your answer needs both halves. Describe the ordinary service first, then the extra pieces that make it an <i>ML</i> system.",
  "learn": [
   {
    "id": "sdf-0",
    "part": "field",
    "title": "Key terms",
    "body": "<ul>\n<li><b>Client, service, dependency</b> - who calls, what answers, and what the answerer needs in order to answer. Almost every design question is a question about one of these three.</li>\n<li><b>Network layers</b> - network software is described in layers; <b>layer 4</b> is where a connection is only an address and a port, and <b>layer 7</b> is where the request itself is readable (see <a href=\"#networking-basics/nb-layers\">The network layers, all seven of them</a>).</li>\n<li><b>Load balancer</b> (<a href=\"#apis-and-communication/apc-f5\">Load balancing algorithms and health checks</a>) - the component that spreads incoming requests across healthy replicas of a service. How much of the request it is allowed to read - the address and port only, at <a href=\"#networking-basics/nb-layers\">layer 4</a>, or the request itself, at <a href=\"#networking-basics/nb-layers\">layer 7</a> - is the distinction that comes up most.</li>\n<li><b>Health check</b> (<a href=\"#apis-and-communication/apc-f5\">Load balancing algorithms and health checks</a>) - the probe that decides whether a replica should be sent traffic.</li>\n<li><b>Replica</b> - one running copy of a service. <b>Horizontal scaling</b> adds replicas; <b>vertical scaling</b> makes one replica bigger.</li>\n<li><b>Stateless</b> - the service keeps nothing about a client between requests, so any replica can serve any request. State lives in a store, or travels in the request.</li>\n<li><b>Cache</b> (<a href=\"#caching/cache-1\">Where caches live</a>) - a fast copy of data kept close to the reader. <b>Hit rate</b> is the number that decides whether it is worth its complexity.</li>\n<li><b>TTL</b> (<a href=\"#caching/cache-5\">TTL expiry and background reapers</a>) - time to live: how long a cached value may be served before it must be refetched.</li>\n<li><b>CDN</b> - content delivery network: a globally distributed cache serving cacheable content from a location near the user.</li>\n<li><b>Message queue</b> - a durable buffer between a producer and a consumer, so the producer does not wait for the consumer and a spike becomes a backlog rather than an error.</li>\n<li><b>Asynchronous processing</b> (<a href=\"#queues-and-streams/qs-f1\">Why hand off work asynchronously</a>) - accepting work now and doing it later, returning an acknowledgement rather than a result.</li>\n<li><b>Index</b> (<a href=\"#sql-databases/sql-f2\">Indexes: B-tree, composite, covering</a>) - a database structure that makes lookups on a column fast, paid for with slower writes.</li>\n<li><b>Replication</b> (<a href=\"#distributed-coordination/dc-3\">Replication and quorums</a>) - keeping copies of data on more than one node.</li>\n<li><b>Sharding</b>, also called partitioning (<a href=\"#nosql-partitioning-ids/npi-f3\">Sharding and partitioning</a>) - splitting data across nodes by a shard key, so each node holds a slice.</li>\n<li><b>CAP</b> - under a network <b>partition</b>, a distributed store must choose between remaining <b>consistent</b> and remaining <b>available</b>. It is a statement about partitions, not about normal operation.</li>\n<li><b>ACID</b> - atomicity, consistency, isolation, durability: the guarantees a transactional database gives so concurrent work does not corrupt state.</li>\n<li><b>Consistency level</b> - how fresh a read is guaranteed to be. <b>Strong</b> means you always see the latest write; <b>eventual</b> means you will see it soon; <b>read-your-writes</b> means at least your own writes are visible to you.</li>\n<li><b>Idempotency</b> - doing an operation twice has the same effect as doing it once. Usually implemented with a caller-supplied <b>idempotency key</b> and a record of keys already applied.</li>\n<li><b>At-least-once / at-most-once / exactly-once</b> - delivery guarantees. Real transports give at-least-once; <b>exactly-once</b> is an end-to-end effect you build from at-least-once delivery plus idempotent writes.</li>\n<li><b>Rate limiting</b> - capping how much a caller may send in a window, usually with a <b>token bucket</b>: tokens refill at a fixed rate, each request spends one, an empty bucket means rejection.</li>\n<li><b>Backpressure</b> (<a href=\"#queues-and-streams/qs-f5\">Queue lag and backpressure</a>) - letting a saturated component slow or reject its callers instead of queueing without bound.</li>\n<li><b>Load shedding</b> (<a href=\"#apis-and-communication/apc-f8\">Tail latency, hedged requests and load shedding</a>) - deliberately dropping or degrading lower-value work under overload.</li>\n<li><b>Circuit breaker</b> (<a href=\"#distributed-coordination/dc-8\">Circuit breakers and timeouts</a>) - a wrapper that stops calling a failing dependency once it has failed enough, and fails fast instead.</li>\n<li><b>Exponential backoff with jitter</b> - retrying after a delay that doubles each time, with randomness added so many clients do not retry in lockstep.</li>\n<li><b>Pagination</b> (<a href=\"#nosql-partitioning-ids/npi-f7\">Keyset pagination vs offset pagination</a>) - returning a large result in bounded pages rather than all at once.</li>\n<li><b>Blue-green</b> and <b>canary</b> - two no-downtime release patterns: run the new version alongside the old and switch traffic in one move, or send it a small slice first and ramp.</li>\n<li><b>Multi-region</b> - running in more than one geographic region: <b>active-active</b> with both serving, or <b>active-passive</b> with one held for disaster recovery.</li>\n<li><b>Observability</b> (<a href=\"#sre-practices/srep-f1\">Observability: logs, metrics, traces, and the golden signals</a>) - being able to answer questions about a running system from metrics, logs and traces.</li>\n<li><b>SLI, SLO, error budget</b> (<a href=\"#sre-practices/srep-f2\">SLIs, SLOs and error budgets</a>) - an SLI is a service level indicator, an SLO is a service level objective, and the error budget is the failure that objective permits.</li>\n<li><b>Percentile (p50, p95, p99)</b> (<a href=\"#counting-and-sketches/cs-f7\">Quantile sketches</a>) - the value below which that share of measurements fall, so p99 is the slow tail rather than the average.</li>\n<li><b>Back-of-envelope</b> - rough arithmetic on requests, bytes and machines. Saying a rough number out loud is much better than saying nothing.</li>\n</ul>",
    "deeper": "<p>There is a reason these words are tested so heavily even for machine-learning roles. A model in production is a service, and it fails the way a service fails far more often than the way a model fails: a dependency times out, a cache goes stale, a deploy drops requests, one shard gets hot, a retry storm turns a blip into an outage. An engineer who can only think about the model will be paged for things they cannot debug.</p>\n<p>The other reason is that these words save the interviewer time. They are shorthand. Say \"read-through with a jittered TTL and a single-flight refresh\" and you have covered a paragraph in four words, so the conversation can move on to the interesting part. Being fluent here buys you time for the parts you actually want to talk about.</p>",
    "check": {
     "question": "What does the CAP theorem actually constrain?",
     "options": [
      "That a distributed system can never be both consistent and available",
      "That during a network partition a distributed store must choose between consistency and availability, saying nothing about behaviour when there is no partition",
      "That consistency and latency are always in direct conflict",
      "That any system with replicas must give up atomicity"
     ],
     "answer": 1,
     "explain": "CAP is a statement about behaviour under partition. Outside a partition a system can offer both; the theorem forces the choice only when nodes cannot reach each other."
    }
   },
   {
    "id": "sdf-f1",
    "part": "field",
    "title": "The request path: balancers, health checks, and why stateless matters",
    "viz": "load-balancer-replicas",
    "body": "<p>Every design answer should start by tracing one request from the client to the answer and back. Do it out loud, hop by hop, and the structure of the rest of the answer follows.</p>\n<ul>\n<li><b>Client to edge.</b> DNS resolves a name, the client opens a connection, and it arrives at a load balancer. At <a href=\"#networking-basics/nb-layers\">layer 7</a> that balancer can route by path or header, terminate TLS, and apply rate limits before anything expensive happens. On Kubernetes this is an Ingress or a gateway in front of a Service.</li>\n<li><b>Edge to replica.</b> The balancer picks a healthy replica - round robin, least connections, or a hash on some key when locality matters. It knows which replicas are healthy because each one answers a <a href=\"#apis-and-communication/apc-f5\">readiness check</a>. A replica that fails readiness is removed from rotation without being killed, which is exactly what you want during a slow start or a dependency blip.</li>\n<li><b>Replica to dependencies.</b> The service calls its stores and downstreams. This is where the latency actually goes, and where every failure mode in the later sections lives.</li>\n<li><b>Back out.</b> The response returns, and anything not needed to produce it - logging, metric emission, writing the request to a stream - should be off the critical path.</li>\n</ul>\n<p><b>Why statelessness matters most.</b> If a replica remembers a client, then traffic must be pinned to it, a lost replica loses work, and adding capacity does not help the sessions already stuck elsewhere. Push state into a store and every replica becomes interchangeable, which is what makes horizontal scaling, rolling deploys and instant failover all possible with one property.</p>\n<p><b>The no-downtime deploy.</b> New replicas start, pass readiness, and take traffic; old replicas stop accepting new requests, finish what they hold, then exit. The two details people miss are that the old replicas need a drain period longer than the slowest request still running, and that the balancer must be told to stop routing <i>before</i> the process starts shutting down, or you drop the requests in between.</p>",
    "deeper": "<p>Two extra points worth having ready. First, what a readiness check should actually test is a real trade-off, and it is worth raising yourself rather than waiting to be asked (<a href=\"#apis-and-communication/apc-f5\">Load balancing algorithms and health checks</a>).</p>\n<p>Second, sticky routing is not always wrong. Hashing on a key keeps a client's requests on the same replica, so that replica's cache stays warm. That can be a big win when each replica holds something expensive - a loaded model, a large local cache. The cost is that adding or removing replicas reshuffles the keys and the warm data has to be rebuilt. So choose it deliberately and work out how big the reshuffle will be, instead of ending up with it by accident.</p>",
    "check": {
     "question": "During a rolling deploy, requests are occasionally dropped. Which explanation is most likely?",
     "options": [
      "The new replicas are larger than the old ones",
      "Old replicas began shutting down before the load balancer stopped routing to them, and requests already in progress had no drain period",
      "The service is stateless, so requests had nowhere to go",
      "The readiness probe is checking a dependency"
     ],
     "answer": 1,
     "explain": "A no-downtime release needs the balancer to remove the replica first, then a drain window longer than the slowest request still running, before the process exits."
    }
   },
   {
    "id": "sdf-f2",
    "part": "field",
    "title": "Caching: where it belongs in the request path",
    "viz": "caching-read-through",
    "body": "<p>Almost every design reaches a point where the obvious database read is too slow or too expensive, and a cache is the answer. In a broad design round three things have to be said about it, in this order.</p>\n<ul>\n<li><b>Where the copy lives.</b> In the client, at the network edge in a content delivery network, inside the service process, or in a shared store such as Redis. That choice sets how close the copy is to the reader and how many readers see it go stale at once (<a href=\"#caching/cache-1\">Where caches live</a>).</li>\n<li><b>Which pattern reads and writes it.</b> Name the one you intend - cache-aside, read-through, write-through or write-behind - and say why you picked it (<a href=\"#caching/cache-2\">Cache-aside and read-through</a>, <a href=\"#caching/cache-3\">Write-through and write-behind</a>).</li>\n<li><b>What happens when the copy is wrong or gone.</b> Expiry by TTL, invalidation on write, or a change stream the cache follows; and what the service does on a restart with a cold cache (<a href=\"#caching/cache-5\">TTL expiry</a>, <a href=\"#caching/cache-7\">Invalidation</a>, <a href=\"#caching/cache-6\">Cache stampede</a>).</li>\n</ul>\n<p>The sentence that ties those three together is that a cache is a replica you built by hand. Every consistency question that applies to database replication applies to it as well; you have simply chosen to own the copying yourself. Said out loud, that shows you know the cost you are taking on and not only the speed you are buying.</p>\n<p><b>Where the CDN fits.</b> A content delivery network is the same idea pushed to the network edge, for content that is cacheable and shared: static assets, public pages, sometimes an API response with a short TTL. It does nothing for a personalised, authenticated, per-request computation - which is most of a model scoring path - so mention it for the parts of the product it actually serves.</p>",
    "deeper": "<p>One practical note that sounds like experience: prefer deleting a key to updating it on write. An update writes a value derived from what the writer knows, which may not be the value a reader would compute, and it silently diverges. A delete forces the next reader to recompute from the source of truth, and the cost is one miss.</p>\n<p>The other thing an interviewer listens for is whether you know what your hit rate has to be. A cache that quietly becomes load-bearing turns a restart into an outage, because the database was never sized for the traffic the cache was absorbing. Knowing that number, and knowing what happens at zero, is the difference between a cache you added on purpose and one you depend on without having decided to.</p>",
    "check": {
     "question": "Why does putting a cache in front of a database bring consistency problems with it?",
     "options": [
      "Because caches store data in a different format from the database",
      "Because the cache is a second copy of the data that the application maintains itself, so it can disagree with the source in the same ways a database replica can",
      "Because caches are always slower to write than to read",
      "Because a cache can never hold the whole dataset"
     ],
     "answer": 1,
     "explain": "A cache is a replica you built by hand. Once a second copy exists, the open questions are how fresh it is and what happens when the two disagree - exactly the questions replication raises."
    }
   },
   {
    "id": "sdf-f2b",
    "part": "field",
    "viz": "api-performance-tricks",
    "title": "Seven ways to make an API faster before you scale it",
    "body": "<p>Before adding replicas, remove waste on the request path. These seven are the standard checklist, and an interviewer can ask about any one of them in a design or craft round.</p>\n<ul>\n<li><b>Caching</b> (<a href=\"#caching\">Caching</a>) - keep hot reads in memory (Redis, or in-process) so repeated requests never reach the database; decide the invalidation rule at the same time you add the cache.</li>\n<li><b>Connection pooling</b> - opening a database connection per request costs a handshake and a socket; a pool of reused connections turns that into a queue wait measured in microseconds. Size it to the database's limits, not to request volume.</li>\n<li><b>Avoid the N+1 problem</b> - N+1 means one query for the list plus one more query per item, so a list of 50 items costs 51 round trips instead of one; fetch the children in one joined or batched query. In ML services this shows up as one feature lookup per entity instead of one batched lookup.</li>\n<li><b>Pagination</b> (<a href=\"#nosql-partitioning-ids/npi-f7\">Keyset pagination vs offset pagination</a>) - never return an unbounded list; page by cursor rather than by offset on a large table.</li>\n<li><b>Fast JSON serialisation</b> - serialising large responses is CPU on the hot path; use a fast serialiser (orjson in Python) and trim the response to fields the client uses.</li>\n<li><b>Payload compression</b> - gzip or Brotli on responses above a few kilobytes cuts transfer time for the client; skip it for tiny payloads where CPU outweighs bytes saved.</li>\n<li><b>Asynchronous logging</b> - write logs to an in-memory buffer flushed to disk or a collector in batches; synchronous disk writes on the request path add latency and jitter at p99.</li>\n</ul>\n<p>The order matters in an interview: profile first, then fix the biggest hop. A latency budget tells you which of these seven is worth doing.</p>",
    "deeper": "<p><b>Where these hide in ML serving.</b> N+1 becomes per-entity feature fetches (batch them into one online-store call). Serialisation becomes large float arrays in JSON (send compact types or binary). Connection pooling applies to the feature store client and the model server client alike. Caching applies to features that change slowly (a customer's tenure) but not to ones that change per event (velocity counters). Async logging matters twice: request logs and prediction logs, both of which you need for monitoring but neither of which may sit on the latency-critical path.</p>",
    "check": {
     "question": "A list endpoint returns 50 orders and then runs one query per order for the customer name. What is the fix called?",
     "options": [
      "Add a cache in front of the database",
      "Fix the N+1 problem by fetching the customers in one joined or batched query",
      "Add pagination",
      "Compress the payload"
     ],
     "answer": 1,
     "explain": "That pattern is one query plus N follow-ups; the fix is a single join or a batched lookup by the set of customer ids. Caching would hide it, not fix it."
    }
   },
   {
    "id": "sdf-f3",
    "part": "field",
    "title": "Databases: which family to choose, and the two acronyms",
    "viz": "db-replication-sharding",
    "body": "<p>Choose on access pattern, not on familiarity.</p>\n<ul>\n<li><b>Choosing a family.</b> Relational when relationships and transactions matter and the query patterns will change. Key-value or wide-column when the access pattern is known, single-entity and enormous. Document when the unit of read and write is a whole nested object. Search when the query is ranked text. Graph when traversal depth is the point. Say the access pattern out loud, then name the family - never the reverse.</li>\n<li><b>ACID</b> gives you atomic multi-row changes and isolation between concurrent transactions. The part worth knowing beyond the acronym is that isolation has levels, and that the default in most engines is not the strictest one, so anomalies like a non-repeatable read are possible unless you ask for more.</li>\n<li><b>CAP</b> applies when the network splits. If your store keeps serving both sides of a partition, it is giving you availability and you will have divergence to reconcile; if it refuses on the minority side, it is giving you consistency and you will have unavailability. For money and tax, the default should be consistency, and you should say why: a wrong answer is worse than a missing one in a domain where the answer is a legal claim.</li>\n</ul>\n<p><b>The three mechanisms you will be asked to name.</b> An <a href=\"#sql-databases/sql-f2\">index</a> makes reads on a predicate fast and every write slower. <a href=\"#distributed-coordination/dc-3\">Replication</a> keeps copies of the data on more than one node, for availability and for read scaling. <a href=\"#nosql-partitioning-ids/npi-f3\">Sharding</a> splits the data across nodes by a shard key when one node can no longer hold it. Name which of the three you are reaching for, and why.</p>",
    "deeper": "<p>A useful, quick order of escalation when someone asks how you would scale a store, because it shows you reach for cheap options first: measure the actual slow query; add or fix the index; add read replicas and move read traffic to them; add a cache in front of the hot read path; denormalise the data into the form the read wants; and only then shard. Sharding is last because it is the only step that changes the application's model of its own data and is hard to reverse.</p>\n<p>Two additions that show operational experience. Connection pools are a real scaling limit - a thousand replicas each holding a pool will exhaust a database's connection count long before its CPU, which is why a proxy or pooler exists. And the read-replica trick has a trap: after a write, reading from a lagging replica shows the old value, so a request that writes and then reads must be pinned to the primary or use a read-your-writes guarantee.</p>",
    "check": {
     "question": "A team needs one enormous store read by a single key, with an access pattern that is fixed and well understood. Which family fits, and why?",
     "options": [
      "A graph store, because the dataset is large",
      "A key-value or wide-column store, because the access pattern is single-entity and fixed, which is what those families are built for",
      "A search index, because a single-key lookup is a text query",
      "A relational database, because transactions are always required"
     ],
     "answer": 1,
     "explain": "Pick the family from the access pattern. Fixed, single-entity, very large reads are what key-value and wide-column stores are built for; relational earns its place when relationships, transactions and changing query patterns matter."
    }
   },
   {
    "id": "sdf-f4",
    "part": "field",
    "title": "Queues, async work, idempotency and exactly-once",
    "viz": "message-queue-async",
    "body": "<p>The moment a design has work that does not need to finish before the response, a queue belongs in it - and the moment a queue is in it, the consumer has to be idempotent.</p>\n<ul>\n<li><b>What a queue actually buys.</b> Decoupling, so a slow consumer does not fail the producer. Absorption, so a spike becomes a backlog instead of a wall of errors. Retry, because a failed message can be redelivered. Fan-out, because several consumers can read the same stream for different purposes.</li>\n<li><b>What it costs.</b> Ordering becomes a per-key property rather than a global one. Latency becomes end-to-end and harder to see. The backlog is now a thing that can grow without bound, so queue depth and consumer lag need alerts of their own. And you inherit duplicates.</li>\n<li><b>Why duplicates are guaranteed.</b> A consumer that processes a message and then dies before acknowledging it will be redelivered the same message. The alternative - acknowledge first, then process - loses messages instead. Every real system therefore chooses at-least-once and makes the effect of processing idempotent.</li>\n<li><b>How idempotency is implemented.</b> Derive a stable key from the message - a natural id, or a hash of the fields that define it - and record applied keys in the same store as the effect, so the check and the write commit together. If they commit separately you have moved the race rather than removed it. For aggregates, prefer operations that are naturally idempotent: set a value rather than increment it, or make the increment carry the key it came from.</li>\n<li><b>Exactly-once, said correctly.</b> There is no exactly-once delivery over an unreliable network. What exists is exactly-once <i>effect</i>: at-least-once delivery plus idempotent or transactional writes on the consumer. Some stream processors give you this within their own boundary and call it effectively-once; the moment you write to an external system it is your problem again.</li>\n<li><b>The dead-letter queue.</b> A message that fails repeatedly must go somewhere other than back on the queue, or one poison message blocks the pipeline forever. Retries with a cap, then a dead-letter destination, then an alert on its depth.</li>\n</ul>",
    "deeper": "<p>The distinction to keep straight is between message ordering and effect ordering. Most brokers guarantee order only within a partition or a key, so if your correctness depends on two events being applied in order, they have to share a key - which then constrains your parallelism, because a key is processed by one consumer at a time. That trade, between per-key ordering and throughput, is one of the more genuinely interesting choices in a streaming design and it is worth raising yourself.</p>\n<p>Second, the queue changes how a failure looks. Synchronously, an outage is visible as errors immediately. Asynchronously, an outage is invisible until someone notices that results are old, because the producer is happily accepting work into a growing backlog. That is why consumer lag matters more than error rate as an alert on an asynchronous path, and saying so shows you have run one.</p>",
    "check": {
     "question": "Which statement about exactly-once processing is correct?",
     "options": [
      "A durable broker with acknowledgements provides exactly-once delivery",
      "Exactly-once is an end-to-end effect built from at-least-once delivery plus idempotent or transactional writes at the consumer",
      "Exactly-once requires synchronous processing without a queue",
      "Exactly-once is achieved by acknowledging the message before processing it"
     ],
     "answer": 1,
     "explain": "Delivery over an unreliable network cannot be exactly-once. The achievable property is exactly-once effect: redelivery is expected, and the consumer makes reprocessing harmless."
    }
   },
   {
    "id": "sdf-f5",
    "part": "field",
    "title": "Overload and failure: timeouts, retries, breakers, limits, backpressure",
    "viz": "circuit-breaker-states",
    "body": "<p>How a design behaves under overload is the difference between one that works and one that survives. Most answers never get this far, which is why getting there stands out.</p>\n<ul>\n<li><b>Timeouts.</b> Every remote call has one, and it must be smaller than the caller's own remaining budget. A call with no timeout is a call that can hold a thread forever, and enough of those is an outage. The related discipline is a deadline that propagates: the edge sets the budget, each hop passes on what is left, and a hop with nothing left fails fast instead of doing work nobody will wait for.</li>\n<li><b>Retries.</b> Retry only what is safe to retry - which means idempotent, which means the idempotency key from the previous section. Retry with exponential backoff and <b>jitter</b>, because synchronised retries are how a brief blip becomes a sustained overload. And retry with a budget: a cap on the fraction of traffic that may be retries, so a failing dependency cannot be given three times its normal load at its worst moment.</li>\n<li><b>Circuit breaker</b> (<a href=\"#distributed-coordination/dc-8\">Circuit breakers and timeouts</a>). When a dependency has failed enough recently, stop calling it, fail fast, and return the fallback until it recovers. The point worth making in an overview is that the breaker is as much for the dependency as for the caller: it gets a chance to recover instead of being held down by its own callers.</li>\n<li><b>Rate limiting.</b> Protects the service and shares capacity between callers. Token bucket is the standard: tokens accrue at a fixed rate up to a burst size, each request spends one, empty means reject. Reject with a clear status and a retry-after hint, so a well-behaved client backs off instead of hammering.</li>\n<li><b>Backpressure</b> (<a href=\"#queues-and-streams/qs-f5\">Queue lag and backpressure</a>). The system-level version of the same idea: when saturated, refuse or slow intake rather than accepting unbounded work.</li>\n<li><b>Load shedding and graceful degradation</b> (<a href=\"#apis-and-communication/apc-f8\">Tail latency, hedged requests and load shedding</a>). Under overload, drop what matters least - low-priority traffic, optional enrichment, expensive personalisation. In a model scoring path the natural degradation is a cheaper answer - a smaller model, request-only features, a rules fallback - marked as degraded so the consumer can treat it accordingly. The rule to state: the answer to a failed dependency is a worse answer, not no answer.</li>\n<li><b>Cascading failure.</b> The pattern that connects all of the above: one slow dependency fills the caller's threads, which makes the caller slow, which fills <i>its</i> callers, until the whole path is down and nothing is obviously broken. Timeouts, breakers, retry budgets and bounded queues exist specifically to stop the propagation.</li>\n</ul>",
    "deeper": "<p>The subtlest of these is the retry storm, and it is worth being able to explain the arithmetic. If every client retries three times on failure, then a dependency that starts failing immediately sees up to four times its normal request rate, at exactly the moment it has least capacity. Backoff spreads that in time, jitter stops the spread being synchronised, and a retry budget caps the total. Teams usually implement the first, sometimes the second, and rarely the third - so naming the budget counts for a lot.</p>\n<p>The other one worth remembering is that a <a href=\"#apis-and-communication/apc-f5\">health check</a> can cause an outage by itself. If the readiness probe calls a shared dependency and that dependency slows, every replica fails readiness at the same moment and the whole fleet leaves rotation, turning a degraded system into a dead one. The fix is to separate \"am I able to serve at all\" from \"is my downstream healthy\", and to let a replica keep serving degraded rather than removing itself.</p>",
    "check": {
     "question": "A dependency starts returning errors and the whole platform goes down, although the dependency is only used for an optional enrichment step. What is the most likely mechanism?",
     "options": [
      "The dependency was sharded incorrectly",
      "Calls to it had no timeout and no breaker, so caller threads filled waiting on it and the saturation cascaded upstream",
      "The cache TTL was too short",
      "The load balancer was sending each request to the next replica in a fixed rotation"
     ],
     "answer": 1,
     "explain": "This is cascading failure. An unbounded wait on a slow dependency consumes the caller's concurrency, which propagates saturation upstream. Timeouts, circuit breakers and bounded queues exist to contain it."
    }
   },
   {
    "id": "sdf-f6",
    "part": "field",
    "title": "APIs, pagination, regions, and what you actually promise",
    "viz": "pagination-cursor",
    "body": "<p>The last of the general topics: the contract you expose, how you survive losing a region, and how you say what \"working\" means.</p>\n<ul>\n<li><b>API design.</b> Nouns and verbs that match the domain, predictable status codes, a versioning story, and an error format a client can act on - a machine-readable code, a human message, and whether retrying will help. Backwards compatibility is the real discipline: add fields, do not repurpose them; make new fields optional; never change the meaning of an existing one. REST at the edge for readability and tooling; a typed binary protocol like gRPC internally where the latency budget is small and both sides are yours.</li>\n<li><b>Pagination</b> (<a href=\"#nosql-partitioning-ids/npi-f7\">Keyset pagination vs offset pagination</a>). Never return an unbounded list, and cap the page size on the server rather than trusting what the client asked for.</li>\n<li><b>Multi-region.</b> Two options. <b>Active-passive</b>: one region serves, the other is kept warm and current, and failover is a routing change plus a promotion of the store - simpler, with a real recovery time and possible data loss bounded by replication lag. <b>Active-active</b>: both serve, which needs either data partitioned so each region owns its records, or a store that accepts writes in both places and reconciles - which is where conflict resolution and the CAP choice become concrete. The honest answer for most services is active-passive with a rehearsed failover, and the honest thing to say is that a failover you have never practised is a plan, not a capability.</li>\n<li><b>Observability</b> (<a href=\"#sre-practices/srep-f1\">Observability: logs, metrics, traces, and the golden signals</a>). Metrics, logs and traces, and what each of the three answers. A model service adds a fourth stream that a general service does not have: the prediction log - inputs, features used, model version, score, decision - which is what makes the quality of past decisions auditable later, and it is the one an interviewer will expect you to bring up yourself.</li>\n<li><b>SLIs, SLOs, error budgets</b> (<a href=\"#sre-practices/srep-f2\">SLIs, SLOs and error budgets</a>). A service level indicator is what you measure, a service level objective is the target for it over a window, and the error budget is the failure that target permits. Quote percentiles rather than averages, because a mean hides the slow tail that customers actually experience.</li>\n</ul>",
    "deeper": "<p>A nuance on error budgets that is worth one sentence: the budget is measured against the promise, not against perfection, which means a service comfortably inside its budget is allowed to take risks - ship faster, run failure exercises, deploy on a Friday. Describing it as a budget you may spend rather than a target you must not miss is the actual idea, and it is a good thing to be able to explain in plain words.</p>\n<p>On multi-region, one asymmetry to keep ready. Stateless services are easy to run in several regions; the state is the hard part. So the real question in any multi-region design is what happens to the store, and the useful follow-up question to ask yourself is what you would do if the two regions disagreed - which record wins, who notices, and how it is repaired. Being able to say \"I would rather have a rehearsed active-passive failover than an active-active design whose conflict resolution nobody has tested\" is a defensible, senior position.</p>",
    "check": {
     "question": "Why is a rehearsed active-passive failover often the more defensible multi-region answer?",
     "options": [
      "Because an active-active design is always more expensive to run",
      "Because the hard part of running in more than one region is the state, and a failover nobody has practised is a plan rather than a capability",
      "Because active-passive gives every user lower latency",
      "Because active-active cannot be used with a relational database"
     ],
     "answer": 1,
     "explain": "Stateless replicas are easy to run in several regions; the store is the difficulty. Active-active needs either data partitioned so each region owns its records or conflict resolution someone has actually tested, and an untested failover is not a capability."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "sdf-a1",
    "type": "design",
    "title": "Design a no-downtime, autoscaled scoring API",
    "viz": "autoscaling-queue",
    "prompt": "Design an online scoring API that must never be down, including during releases, and that must absorb a load profile which rises by two orders of magnitude on predictable dates. Cover: the request path from client to model and back; how a release happens with no dropped requests; how it scales, and what the autoscaler cannot do for you; what is cached and how the cache stays correct; what happens when each dependency is slow and when it is down; how a caller is rate limited; and what you promise as an SLO and how you measure it. Do the capacity arithmetic out loud. Say what you would build first.",
    "timeboxSec": 2400,
    "rubric": "Marking guide, out of 10. (1) The answer asks first about: request volume now and at peak, latency target and percentile, what the caller does with the answer, whether staleness is acceptable, and what already exists - then states a scope. (2) Request path traced hop by hop from client through <a href=\"#networking-basics/nb-layers\">layer-7</a> routing to replica to dependencies and back, with non-essential work kept off the critical path. (3) No-downtime release described concretely: a readiness check before a replica gets traffic, connection draining longer than the slowest request still running, balancer removal before process shutdown, and blue-green or canary with rollback as a traffic switch. (4) Scaling: statelessness named as the enabling property; horizontal scaling on a metric correlated with load; the limits stated explicitly - the pod autoscaler schedules pods but does not create nodes, and a model server's warm-up can exceed the autoscaler's reaction time - so predictable peaks are pre-scaled with warm headroom rather than handled reactively. (5) Capacity arithmetic performed out loud: measured single-replica throughput at the target percentile rather than at saturation, target load divided by it, headroom for failure domains, and the same multiple checked against every downstream, with the observation that capacity is set by the smallest link. (6) Caching: what is cached, which pattern, the TTL or invalidation mechanism, the write-then-invalidate race named, and stampede protection through jittered expiry or single-flight refresh. (7) Failure handling per dependency: a timeout smaller than the remaining budget, retries only on idempotent calls with backoff, jitter and a retry budget, a circuit breaker with a half-open probe, and a defined degraded mode that returns a worse answer rather than no answer. (8) Rate limiting described with an algorithm (token bucket), a scope (per caller), and a clear rejection response with a retry-after hint; backpressure through bounded queues named as the system-level counterpart. (9) SLO stated as an indicator plus a target over a window at a named percentile, with the error budget interpreted as spendable, and the observability to measure it - metrics, traces and a prediction log carrying model version. (10) Closed with two or three explicit trade-offs and a stated first thing to build. Deduct for: starting with technology; no drain or readiness detail in the release plan; assuming the autoscaler solves the peak; no capacity arithmetic; caching with no consistency story; happy-path-only dependencies; an availability number quoted with no cost attached; claiming to have run multi-region active-active, or to have built resilience machinery by hand. {{HONESTY}}",
    "model": "<p><b>Clarify.</b> \"Four questions. What is normal request rate and what is the peak multiple, and is the peak on a known date? What is the latency target, at which percentile? What does the caller do if I return a degraded answer or an error - is there a sane default on their side? And how stale can a feature be before the answer is wrong? I will assume a few thousand requests per second normally, around a hundred times that at a predictable seasonal peak, a p99 target in the low hundreds of milliseconds, a caller that can act on a degraded answer, and features that may be seconds old but not hours. Scope: the online path, its scaling and release, and the operational contract. I am not designing the training pipeline.\"</p>\n<p><b>Request path.</b> Client to a <a href=\"#networking-basics/nb-layers\">layer-7</a> balancer, which terminates TLS, authenticates, applies the per-caller rate limit and routes on path. Then a stateless scoring replica: fetch features for the entities in the request in one batched call, apply the last-mile transformation, run inference, apply the policy layer, return. The prediction log - inputs, feature values, model version, score, decision - is emitted asynchronously, off the critical path, as is metric emission.</p>\n<p><b>No-downtime release.</b> New replicas start and stay out of rotation until a readiness probe passes, and that probe is a real one - model artifact loaded, store reachable - but not so deep that a slow downstream can take the whole fleet out at once. Traffic moves blue-green: the new version takes traffic in one routing change and gives it back in one. Old replicas are removed from the balancer first, then given a drain window longer than the slowest request still running, then exit. Rollback is that same traffic switch, so it is seconds rather than a rebuild, and the model and its feature contract revert together - reverting a model onto a changed feature definition is a second incident, not a fix.</p>\n<p><b>Scaling, and the arithmetic.</b> Statelessness is what makes this possible at all: nothing about a caller lives in the replica, so any replica serves any request and capacity is a number rather than a topology. I would measure one replica's throughput at the p99 target - not at saturation, because throughput measured at full utilisation is a latency violation with a number attached - and suppose that is 300 requests per second. For 100,000 requests per second that is around 334 replicas, plus headroom for a lost zone and for the variance between measurement and reality, so call it 450. Then the part people skip: the feature store, the policy service and anything else downstream must take the same multiple, because the system's capacity is the smallest link, and it is rarely the one you scaled.</p>\n<p><b>What the autoscaler cannot do.</b> Horizontal pod autoscaling on the metric most correlated with load handles within-day variation. It does not handle the seasonal ramp, for two reasons: the pod autoscaler schedules pods but does not create nodes, so a scale-up can succeed on paper and have nowhere to run; and a model server's warm-up - loading weights, first-request compilation - can be longer than the autoscaler's whole reaction cycle. Since the peak is on a known date, I pre-scale on the calendar and hold warm headroom, keeping the autoscaler for the noise rather than for the event. Node capacity is held ahead of time, with low-priority placeholder workloads that get preempted when real traffic needs the room.</p>\n<p><b>Caching.</b> The online feature store is the cache in this design: latest value per entity, read-through from the perspective of the scoring service. TTLs carry jitter so a popular entity's entries do not all expire together, and a refresh is single-flight - one caller repopulates while the rest serve the previous value. Where a value must be correct immediately, the writer invalidates, and I would say out loud that write-then-invalidate is not atomic: a reader that missed just before the write can repopulate the old value just after the delete. If that race matters, the cache follows a change stream instead, and then it is a replica with a lag I can monitor rather than a race I hope loses.</p>\n<p><b>Dependencies, slow and down.</b> Every call has a timeout smaller than the budget left at that hop, and the deadline propagates so a hop with no budget left fails fast rather than doing unwanted work. Retries only on idempotent calls, with exponential backoff, jitter, and a retry budget capping retries as a fraction of traffic - otherwise a failing dependency receives several times its normal load at its worst moment. A circuit breaker in front of each dependency: open after a failure threshold, fail fast to the fallback, half-open with a single probe. And a defined degraded mode - on a feature-store timeout, score with request-only features and mark the decision degraded so the policy layer treats it conservatively; if the model server is unavailable, fall back to rules. A worse answer, never no answer.</p>\n<p><b>Rate limiting and backpressure.</b> Token bucket per caller at the edge, with a burst allowance, rejecting with a clear status and a retry-after hint so a well-behaved client backs off instead of hammering. Internally, bounded queues with rejection rather than unbounded ones, because an unbounded queue does not remove overload, it hides it until latency collapses.</p>\n<p><b>What I promise.</b> An SLI of the fraction of requests answered within the latency target, and correctly - counting a degraded answer separately so it cannot be quietly used to buy the number. An SLO stated at a percentile over a rolling window, with the error budget treated as spendable: inside budget we ship and run failure drills, outside it reliability outranks features. Metrics for rate, errors, duration and saturation; traces to answer which hop; the prediction log with model version for everything after the fact.</p>\n<p><b>Trade-offs and first build.</b> Pre-scaling costs money for capacity that sits idle and buys survival of a ramp faster than any reactive system. A managed store costs control and buys consistency between the two read paths. The degraded path costs decision quality and buys availability. If I had one thing to build first: the degraded path plus a rehearsed failure drill against it, because until you have actually turned a dependency off in a test, everything else on the whiteboard is just a claim.\"</p>"
   },
   {
    "id": "sdf-a2",
    "type": "followup",
    "title": "Interview question: read-through against write-through, and keeping the cache in sync",
    "prompt": "An interviewer asks: \"What is the difference between a read-through and a write-through cache, and how do you keep a cache and its database in sync?\" Answer in first person, in about 90 seconds. Name the patterns, then the sync mechanisms, then the race that the obvious mechanism does not solve. If you need the detail first, it is in <a href=\"#caching/cache-2\">Cache-aside and read-through</a>, <a href=\"#caching/cache-3\">Write-through and write-behind</a> and <a href=\"#caching/cache-7\">Invalidation</a>.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) read-through defined as the application reading only from the cache, with the cache loading from the database on a miss, so the miss path pays database latency and the loading logic lives in one place; (2) write-through defined as writing to cache and database together, so data written that way is never stale, paid for with latency on every write whether or not the value is read; (3) at least one of cache-aside or write-back mentioned to show the family is understood, with write-back correctly identified as the one that can lose acknowledged data; (4) the three sync mechanisms named - TTL expiry, invalidation on write, and change propagation from a database change stream - each with its consequence; (5) the write-then-invalidate race named explicitly: the two steps are not atomic, so a concurrent reader that missed before the write can repopulate the stale value after the invalidation; (6) a stated preference for deleting a key over updating it in place, because an updated value derived by the writer can diverge from what a reader would compute; (7) the point that a cache is a replica you chose to maintain yourself, so replication-lag reasoning applies; (8) one sentence on the question that chooses the pattern - how stale is too stale. Common mistakes: describing only the two named patterns and stopping; asserting that invalidation makes the cache consistent; no mention of the race; recommending write-back without naming the durability cost; claiming to have built a cache layer when you have not - the honest version is that the online feature store was the managed equivalent. {{HONESTY}}",
    "model": "<p>\"Read-through means the application only talks to the cache. On a miss, the cache itself loads from the database, stores the value and returns it - so the loading logic lives in one place rather than at every call site, and the first reader after a miss pays the full database latency. Cache-aside is the same latency profile with the application doing the loading itself.</p>\n<p>Write-through means every write goes to the cache and the database as one operation, so anything written that way is never stale in the cache. You pay for it on the write path, twice per write, whether or not anyone reads the value. The third one in the family is write-back, where writes go into the cache and are flushed later - fastest, and the only one that can lose data you have already acknowledged.</p>\n<p>On keeping them in sync, there are really three mechanisms. Expiry: give entries a TTL and accept staleness bounded by it. Invalidation on write: the writer deletes the key. Or change propagation: the cache follows the database's change stream, which is the most robust because it stops pretending the cache is not a replica and gives it a lag you can actually monitor.</p>\n<p>The part I would flag is that invalidation on write is not as safe as it looks. The write and the delete are two steps and they are not atomic, so a reader that missed just before the write commits can repopulate the key with the old value just after the delete, and now you are stale with no TTL to save you. That is the argument for the change stream, or for a short TTL as a backstop even when you do invalidate. And I would rather delete a key than update it, because an updated value derived by the writer can quietly diverge from what a reader would have computed.</p>\n<p>The way I would actually pick between them is to ask one question first: how stale is too stale? That number chooses the pattern.</p>\n<p>And I would be straight about what I have actually done: if I have not built a cache layer myself I say so, and name the nearest thing I did run - a managed online store on the request path, say - where the same reasoning applies but the mechanism was someone else's.\"</p>"
   },
   {
    "id": "sdf-a3",
    "type": "explain",
    "title": "Trace one request and work out what it costs",
    "prompt": "Take any service you know and trace one request from the client to the answer and back, naming every hop. For each hop, state the timeout, what happens when it is slow, what happens when it is down, and which metric would tell you. Then do the capacity arithmetic for ten times the current load, and name the component you expect to break first and why.",
    "timeboxSec": 600,
    "rubric": "Must-haves: (1) the path traced hop by hop, starting at name resolution or the balancer and ending with the response, with any non-essential work explicitly placed off the critical path; (2) a timeout per hop, each smaller than the budget remaining at that point, with the deadline described as propagating; (3) for every hop, all three answers given - slow, down, and how you would know - with the metric named rather than \"monitoring\"; (4) at least one degraded behaviour that returns a worse answer rather than an error, and one place where failing fast is the correct choice; (5) capacity arithmetic that starts from measured single-replica throughput at the target percentile rather than at saturation, multiplies out, adds headroom for a lost failure domain, and applies the same multiple to downstreams; (6) a named first bottleneck with a mechanism - connection pool exhaustion, a hot shard or hot key, fan-out amplifying the tail, an accelerator's fixed capacity unit, or the store rather than the service - not a guess; (7) the observation that capacity is set by the smallest link and that scaling the service alone can make things worse by loading a saturated dependency harder. Common mistakes: a component list rather than a request path; \"we would monitor it\" with no metric; no arithmetic; scaling by guesswork; forgetting that a retry storm is itself a load multiplier. {{HONESTY}}",
    "model": "<p><b>The path.</b> Client resolves the name and connects to a <a href=\"#networking-basics/nb-layers\">layer-7</a> balancer, which terminates TLS, authenticates, applies the per-caller rate limit and routes on path. The balancer picks a replica that is passing readiness. The replica fetches features for the request entities in one batched call, applies the last-mile transformation, runs inference, applies the policy layer, and returns. The prediction log and metric emission go out asynchronously, off the critical path.</p>\n<p><b>Hop by hop, at a 200 millisecond budget.</b> Balancer and authentication: a few milliseconds, timeout short; when it is slow, everything is slow uniformly, which is the easiest signature to spot - watch edge p99 against replica p99, and if they diverge the problem is in front of the service. Feature fetch: the largest and most variable term, budget maybe 60 milliseconds, timeout 60 with a deadline that leaves room for inference; slow means the tail dominates and fan-out amplifies it, since the slowest of several entity lookups sets the response, so I batch the reads into one call; down means the degraded path - score on request-only features, mark the decision degraded. Metrics: store p99, error rate, and the share of requests served degraded. Inference: budget maybe 40 milliseconds; slow usually means queueing behind saturated replicas rather than the model itself being slower, so watch the number of requests in progress and saturation, not just duration; down means fall back to rules. Policy: cheap, but it is a remote call in most designs, so a timeout and a conservative default. Everything after the answer is off the path by construction, so it can be slow without being felt - and that is exactly why it must be monitored separately, because nothing will page for it.</p>\n<p><b>Ten times the load.</b> Measure single-replica throughput at the p99 target, not at saturation - suppose 300 requests per second. Current 3,000 requests per second is 10 replicas plus headroom, so 14. Ten times is 30,000, so 100 replicas, plus a lost-zone allowance and variance, call it 135. Then the same multiple against every downstream: the feature store must serve ten times the read rate at the same percentile, the policy service ten times the calls, and the connection count from 135 replicas has to be inside whatever the store's pooler allows.</p>\n<p><b>What breaks first, and why.</b> Not the model. In a tabular path, inference is often the smallest term. I would expect the feature store first, for two compounding reasons: it takes the whole traffic multiple, and connection pool exhaustion arrives before CPU saturation does when replica count grows - a hundred replicas each holding a pool is a very different client than ten. Second candidate is a hot key: one popular entity concentrating reads onto one partition, which shows up as a p99 that grows while the mean stays flat. Third is the retry path, because if any of the above starts failing and retries are uncapped, the dependency receives several times its normal load at its worst moment - which is why the retry budget matters as much as the replica count.</p>\n<p>The general statement: capacity is the smallest link, and scaling the service alone can make things worse by pointing more load at whatever was already saturated.\"</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "ml-lifecycle-platform": {
  "id": "ml-lifecycle-platform",
  "title": "The ML platform, end to end",
  "day": 1,
  "level": "warning",
  "levelLabel": "You have built this - learn their words for it",
  "why": "<i>Reliably scale and operate ML systems in production</i> is a sentence you will see in a great many job descriptions. What it really asks about is the platform: what the stages are, what runs automatically, what has to pass a check first, who can do it without asking permission, and how long it takes to get from a working notebook to a served model. Most engineers have built a good deal of this without ever calling it a platform. Use the standard names for those stages and a deep-dive interviewer hears a platform engineer rather than someone who only writes models.",
  "learn": [
   {
    "id": "mlp-0",
    "part": "field",
    "title": "Key terms",
    "body": "<p>Learn these once and the rest of the course reads easily.</p>\n<ul>\n<li><b>ML lifecycle</b> - the ordered stages a model moves through: data ingestion, feature engineering, training, evaluation, registration, deployment, serving, monitoring, retraining. A platform is whatever makes those stages repeatable for someone who did not build them.</li>\n<li><b>MLOps</b> - short for machine learning operations: applying software delivery discipline to that lifecycle: version control, automated tests, continuous integration and delivery, monitoring, rollback.</li>\n<li><b>Continuous integration (CI)</b> (<a href=\"#mlops-tooling/mot-f6\">CI and CD for ML</a>) - automatically building and testing every change when it is pushed.</li>\n<li><b>Continuous delivery (CD)</b> - automatically getting a tested change into an environment, where a check has to pass before it moves on to the next one.</li>\n<li><b>Release check</b> (often called a gate) - a condition that must pass before something moves forward: a test, a metric threshold, a human approval.</li>\n<li><b>Environment</b> - an isolated copy of the system: development, pre-production, production. They differ in data, traffic, and how much damage a mistake can do.</li>\n<li><b>Artefact</b> - a built, versioned, immutable thing: a container image, a trained model file, a pipeline definition.</li>\n<li><b>Model registry</b> (<a href=\"#mlops-tooling/mot-f3\">Experiment tracking and the registry</a>) - the versioned store of model artefacts with metadata, lineage and approval state, from which deployments are made.</li>\n<li><b>Lineage</b> - the recorded chain from a served prediction back through model version, training run, code commit and data snapshot.</li>\n<li><b>Reproducibility</b> - being able to rebuild the same artefact from the same declared inputs.</li>\n<li><b>Declarative</b> - you state the desired end state and a controller makes reality match it, rather than running steps yourself.</li>\n<li><b>GitOps</b> - short for Git Operations: the declarative pattern where Git holds the desired state and a controller continuously reconciles the running system to it.</li>\n<li><b>Infrastructure as code (IaC)</b> - cloud resources declared in files under version control rather than clicked in a console.</li>\n<li><b>Paved road</b> - a supported default path with guardrails built in, that a team can take without asking permission. Large platform organisations use the phrase for data, persistence, ML and GenAI (generative AI) alike.</li>\n<li><b>Self-serve</b> - a team can go from idea to production on the paved road without a platform engineer in the loop.</li>\n<li><b>Guardrail</b> - a check built into the road rather than added by review: a blocking data-quality test, a mandatory approval, an enforced encryption policy.</li>\n<li><b>Time to production</b> - elapsed time from a working notebook to a served model. The headline metric of a platform.</li>\n<li><b>Champion and challenger</b> - the model currently serving, and the candidate model trying to replace it.</li>\n</ul>",
    "deeper": "<p>Interviewers like to ask about one difference in particular: <b>pipeline</b> versus <b>platform</b>. A pipeline is one team's automated path for one model. A platform is the reusable version, where the common parts - packaging, promotion, monitoring, rollback - are pulled out so a second team gets them for free.</p>\n<p>Some of your own work probably crosses that line, even if you never called it that. One team's automated path for one model is a pipeline. A way of shipping that other teams picked up without your help is a platform contribution. Saying which is which, in those words, earns you real credit and costs you nothing.</p>",
    "check": {
     "question": "What most cleanly distinguishes a platform from a pipeline?",
     "options": [
      "A platform runs on Kubernetes; a pipeline does not",
      "A platform factors out the common parts so a second team gets packaging, promotion, monitoring and rollback without rebuilding them",
      "A platform is owned by a central team; a pipeline is owned by a product team",
      "A platform handles more data volume"
     ],
     "answer": 1,
     "explain": "Reuse by a team that did not build it is the defining property. Ownership and technology are incidental."
    }
   },
   {
    "id": "mlp-f1",
    "part": "field",
    "title": "The nine stages, and what fails at each",
    "viz": "ml-lifecycle",
    "body": "<p>Walk the lifecycle once, naming each stage and its characteristic failure. These nine stages underpin almost every ML system design answer.</p>\n<ul>\n<li><b>1. Data ingestion.</b> Events or records arrive from producers. <i>Fails as:</i> schema changes without notice, duplicates, late arrivals, silent volume drops.</li>\n<li><b>2. Feature engineering.</b> Raw data becomes model inputs. <i>Fails as:</i> the training version and the serving version diverge.</li>\n<li><b>3. Training.</b> A model is fitted on a labelled dataset. <i>Fails as:</i> leakage, an unrepresentative sample, an unpinned dependency.</li>\n<li><b>4. Evaluation.</b> The candidate model is measured against a holdout and against the incumbent. <i>Fails as:</i> one aggregate number that hides a broken segment.</li>\n<li><b>5. Registration.</b> The artefact is versioned with metadata and lineage. <i>Fails as:</i> a model in production that nobody can trace to a commit.</li>\n<li><b>6. Approval.</b> A human or a policy authorises promotion. <i>Fails as:</i> either a rubber stamp or a bottleneck.</li>\n<li><b>7. Deployment.</b> The version is rolled out under a strategy. <i>Fails as:</i> an all-at-once switch with no way back.</li>\n<li><b>8. Serving and monitoring.</b> Predictions are made and watched. <i>Fails as:</i> watching only whether the service is up, never whether it is right.</li>\n<li><b>9. Retraining.</b> A new candidate is produced on a trigger. <i>Fails as:</i> a cron job nobody owns, retraining on data the model itself influenced.</li>\n</ul>\n<p>Put it another way: the model occupies one of nine boxes. Most production incidents live in the other eight, which is the argument of <i>Hidden Technical Debt in Machine Learning Systems</i>.</p>",
    "deeper": "<p>Most people skip two of these stages, and good interviewers check for exactly those two: <b>registration</b> and <b>approval</b>. They are the difference between a system you can audit and one you cannot, which matters more in financial software than almost anywhere else.</p>\n<p>Registration answers \"which exact artefact is serving, built from which commit, on which data window\". Approval answers \"who decided this could serve, and on what evidence\". In a tax or payments context, an interviewer will assume you know that a model decision may have to be explained to a regulator, an auditor or a customer months later.</p>",
    "check": {
     "question": "Which stage most directly answers an auditor asking why a specific customer was scored the way they were, three months ago?",
     "options": [
      "Evaluation, because the metrics are stored there",
      "Registration, because it records which artefact served, built from which commit and which data window",
      "Retraining, because it shows how the model has changed since",
      "Serving, because the request logs contain the score"
     ],
     "answer": 1,
     "explain": "Request logs give the score but not the provenance. Registration plus lineage is what lets you reconstruct exactly which model, code and data produced it."
    }
   },
   {
    "id": "mlp-f2",
    "part": "field",
    "title": "Paved roads, self-serve, and why adoption is the hard part",
    "body": "<p>A platform that nobody adopts is a hobby. The pattern that solves adoption has a widely used name: the <b>paved road</b> - the supported default path with guardrails built in.</p>\n<p>The mechanism is simple. Make the safe path the easy path. If the compliant way to deploy a model is also the fastest way, nobody routes around it. If compliance is a review meeting and speed is a shell script, everyone routes around it and the platform team becomes a police force.</p>\n<p>A second published idea pairs with it: <b>fixed, flexible, free</b> - declaring explicitly where the company must converge, where teams may customise, and where engineers may choose freely. That way of putting it matters because it makes convergence negotiable in a bounded way instead of a fight about every tool.</p>\n<p>Platform teams describe a self-serve modelling platform in remarkably similar terms: one automated workflow from end to end, a low-code or no-code route with a simple interface that a team can use on its own, and guardrails built in that hold the standard on data quality, model quality and responsible AI.</p>",
    "deeper": "<p>The counter-argument you should be able to make yourself: paved roads go stale. A road optimised for last year's workload becomes the reason this year's workload is hard, and the fixed tier is where that damage accumulates.</p>\n<p>The usual defences are an explicit escape hatch with a review, a stated review schedule for what sits in the fixed tier, and treating the road as a product with users and feedback rather than as a policy. An interviewer who has run a platform will be glad you brought this up without being asked, because it is the problem every platform team eventually hits.</p>",
    "check": {
     "question": "A team routes around your paved road because it is slower than their own scripts. What is the correct first response?",
     "options": [
      "Add a policy requiring platform review before deployment",
      "Treat the speed gap as a platform defect and close it, because adoption follows the fastest safe path",
      "Escalate to their manager for non-compliance",
      "Fork the road so each team maintains its own variant"
     ],
     "answer": 1,
     "explain": "Guardrails only hold when the guarded path is also the quickest. Enforcement without speed produces shadow tooling."
    }
   },
   {
    "id": "mlp-f3",
    "part": "field",
    "title": "CI for models: three kinds of test, not one",
    "viz": "data-contract-validation",
    "body": "<p>Software CI asks one question: does the code still work. ML CI has to ask three, because an ML system can break without any code changing.</p>\n<ul>\n<li><b>Data tests.</b> Run on the training and serving inputs. Schema conformance, null rates, value ranges, category cardinality, row counts against expectation, freshness. These catch the most common production breakage, which is an upstream change nobody told you about.</li>\n<li><b>Model tests.</b> Run on the candidate artefact. Metrics against a frozen holdout, comparison against the incumbent, metrics on named slices rather than only in aggregate, behavioural checks (a known-fraudulent pattern must score high), and a check that the model is not degenerate - constant output, or an output distribution far from the previous release.</li>\n<li><b>Integration tests.</b> Run on the deployed path, not the artefact. Load the model in the serving container, fetch features the way production fetches them, score a fixed set of records, and assert the outputs match the offline scores within a tolerance. This is the single most valuable test in an ML system, because it is the one that catches training-serving skew before customers do.</li>\n</ul>\n<p>Mature platforms give the second and third their own names: a <b>model sanity check</b> suite for catching systematic error and bias before production, and an automated performance pipeline that compares a release candidate's key metrics against a certified <b>golden baseline</b>.</p>",
    "deeper": "<p>One detail from published accounts of these checks is worth remembering. The first version's tolerances are usually too strict - a one-millisecond difference fails the pipeline - so per-team tolerances get added later. Teams that get this right report certification time falling by roughly three quarters, from a working day to a couple of hours.</p>\n<p>The lesson is that an automated check with badly chosen thresholds is worse than no check at all, because it teaches people to override it. So when you propose a check, propose the tolerance and say who will tune it, in the same breath.</p>",
    "check": {
     "question": "Which test family most directly catches training-serving skew?",
     "options": [
      "Data tests, because they check schema and null rates",
      "Model tests, because they compare metrics against the incumbent",
      "Integration tests that score fixed records through the real serving path and compare to offline scores",
      "Load tests, because skew appears under concurrency"
     ],
     "answer": 2,
     "explain": "Skew is a difference between the offline and online computation paths, so only a test that exercises the online path and compares against the offline result can see it."
    }
   },
   {
    "id": "mlp-f4",
    "part": "field",
    "title": "GitOps for models: what is actually in Git, and what rollback means",
    "viz": "gitops-loop",
    "body": "<p>GitOps means the desired state of the system lives in Git and a controller continuously reconciles reality to it. For an ML system, be precise about what \"the desired state\" contains.</p>\n<ul>\n<li><b>In Git:</b> the serving deployment manifest, including the model version reference; the pipeline definition; the feature definitions; the infrastructure declarations; scaling and routing policy; alert and dashboard definitions; the approval record via pull request.</li>\n<li><b>Not in Git:</b> the model weights themselves, which live in artefact storage or a registry and are referenced by immutable version; training data, which is referenced by snapshot or window; secrets, which come from a secret manager.</li>\n</ul>\n<p>The payoff is that deployment, audit and rollback become one mechanism. Who changed the serving model, when, and with whose approval is a Git history. Rollback is a revert, and the controller reconciles.</p>\n<p>Two ML-specific cautions. First, a model rollback that leaves new feature definitions live is not a rollback - the old model then reads inputs it never saw in training. Version the model and its feature contract together. Second, reverting a manifest does not undo effects that have already left the system: refunds already declined, emails already sent, a retraining set already polluted by the bad model's own outputs.</p>",
    "deeper": "<p>The GitOps tooling most teams meet was largely built inside large platform organisations and donated to open source: a continuous-delivery controller that reconciles many clusters with enterprise single sign-on and role-based access control, a workflow engine, and a progressive-delivery controller. The same reconciliation idea is often extended to cloud resources through a resource controller, so infrastructure is declared in Git too.</p>\n<p>Be precise about which of those you have actually run. Running the delivery controller is not the same as running the workflow engine or the progressive-delivery controller. Saying where your experience stops, and then giving an example that genuinely matches, is stronger than blurring the two.</p>",
    "check": {
     "question": "Why is reverting only the model version an incomplete ML rollback?",
     "options": [
      "Because the model artefact is immutable and cannot be reverted",
      "Because the restored model may now be reading feature definitions that changed after it was trained",
      "Because Git cannot store binary model files",
      "Because the controller will re-apply the newer version automatically"
     ],
     "answer": 1,
     "explain": "The model and its feature contract are one unit. Rolling back one and not the other puts the old model on inputs it never saw during training."
    }
   },
   {
    "id": "mlp-f5",
    "part": "field",
    "title": "Environments, reproducibility and the registry",
    "body": "<p>Three mechanics that make the difference between a demo and a system.</p>\n<p><b>Environments.</b> Development, pre-production and production differ in three ways that matter: what data they can see, what traffic they take, and what they can break. The common mistake is a pre-production environment with synthetic data, which validates nothing about the real distribution. The common fix is data that matches production with the sensitive fields masked, plus a shadow path in production for anything that only real traffic can prove.</p>\n<p><b>Reproducibility.</b> To rebuild a model you need four pinned things: the code commit, the data window or snapshot identifier, the configuration and hyperparameters, and the environment image. Randomness needs a recorded seed. Miss any one and \"we cannot reproduce the model that is serving\" becomes a real incident, usually discovered during an audit.</p>\n<p><b>The registry.</b> Every trained artefact gets an immutable version, its metrics, its lineage back to commit and data, its approval state, and a stage label such as candidate, production or archived. Deployments reference a registry version, never a file path. That single rule is what makes rollback and audit possible.</p>",
    "deeper": "<p>Be careful about naming an employer's registry. Public engineering writing routinely names a training platform, a workflow engine or an experiment tracker while <b>naming no model registry product, no versioning scheme and no artefact promotion path.</b> Claiming to know it would be making something up, and the people who run a deep dive are exactly the ones who would notice.</p>\n<p>What to say instead: \"I do not know what you use internally; the properties I would look for are immutable versions, lineage to commit and data, an approval state, and deployment by reference rather than by file path.\"</p>",
    "check": {
     "question": "Which four things must be pinned for a training run to be reproducible?",
     "options": [
      "Model architecture, learning rate, batch size, epochs",
      "Code commit, data window or snapshot, configuration and hyperparameters, environment image",
      "Cluster size, region, node type, autoscaler settings",
      "Dataset size, feature count, label distribution, holdout ratio"
     ],
     "answer": 1,
     "explain": "Hyperparameters are only one of the four. Without the commit, the data window and the image, the same hyperparameters produce a different artefact."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "mlp-a1",
    "type": "explain",
    "title": "Exercise: name the nine stages from memory in 3 minutes",
    "prompt": "Without notes, name the nine lifecycle stages in order and give one typical failure for each. Then say which two stages most people skip, and why those two matter especially in financial software.",
    "timeboxSec": 180,
    "rubric": "Must-haves: (1) all nine stages named in a defensible order - ingestion, feature engineering, training, evaluation, registration, approval, deployment, serving and monitoring, retraining; (2) one specific failure per stage, not a generic \"it breaks\" - for example silent schema change at ingestion, skew at features, leakage at training, an aggregate metric hiding a broken slice at evaluation, an untraceable artefact at registration, a rubber stamp or a bottleneck at approval, an irreversible all-at-once switch at deployment, monitoring uptime but not correctness at serving, and an unowned cron job or feedback-loop contamination at retraining; (3) registration and approval identified as the commonly skipped pair; (4) the financial-software reason stated: a scored decision may have to be explained to an auditor, a regulator or a customer months later, which needs provenance and an approval record. Common mistakes: collapsing registration into deployment; omitting approval entirely; giving failures that are all \"the model gets worse\"; listing tools instead of stages. {{HONESTY}}",
    "model": "<p>Ingestion - a producer changes a field type and nobody is told. Feature engineering - the training transform and the serving transform drift apart. Training - leakage from a feature that encodes the label. Evaluation - one aggregate AUC (area under the curve, a ranking-quality score) that hides a segment where the model is worse than random. Registration - a model serving in production that cannot be traced to a commit. Approval - either nobody looks, or one person becomes a queue. Deployment - a full switch with no route back. Serving and monitoring - dashboards showing latency and uptime but nothing about whether the decisions are right. Retraining - a scheduled job with no owner, quietly training on data the model's own decisions shaped.</p>\n<p>The two most often skipped are registration and approval. In financial software they matter more than anywhere else, because a decision - a declined refund, a flagged return, a tax recommendation - may have to be explained months later to an auditor, a regulator or the customer. Without a registry entry and an approval record you can show the score but not the provenance, and the provenance is what is actually being asked for.</p>"
   },
   {
    "id": "mlp-a2",
    "type": "design",
    "title": "Exercise: design the checks a model must pass before release",
    "viz": "canary-rollout",
    "prompt": "Design the automated checks a candidate model must pass before it can replace the incumbent in a fraud-scoring service. Say what data it is evaluated on, what it is compared against, which metrics, at what thresholds, what is checked per slice, what is checked about the serving path, and what happens when a check fails. Then say what these checks cannot catch.",
    "timeboxSec": 480,
    "rubric": "Must-haves: (1) two evaluation sets named - a frozen holdout for comparability across releases and a recent window for current relevance - with the reason for each; (2) the incumbent scored on exactly the same rows, so the comparison is paired rather than against a historical number; (3) metrics appropriate to imbalanced fraud data - precision at a fixed recall or recall at a fixed alert volume, precision-recall AUC, and calibration - with an explicit statement that raw accuracy is useless here; (4) an operating-point constraint tied to a business quantity such as analyst review capacity or customer friction, not just a threshold-free metric; (5) per-slice checks named with example slices - channel, device type, geography, new versus returning customer, transaction size band - and a hard block if any named slice regresses beyond tolerance; (6) serving-path checks: the candidate model loaded in the serving container, features fetched the production way, fixed records scored, and outputs compared to offline scores within a tolerance; plus a latency check at the target percentile; (7) behavioural or invariance checks - known patterns must score above a bound, and a sanity check that the output distribution has not shifted implausibly; (8) failure behaviour: block promotion, publish the diff, and require a named human override that is recorded; (9) what these checks cannot catch, stated explicitly - concept drift after deployment, feedback effects from the model's own decisions, harms not represented in the labels, and anything whose ground truth has not arrived yet because of label latency. Bonus: says the tolerances need an owner, and cites the published golden-baseline certification pattern as a precedent. Common mistakes: accuracy as the metric; a single aggregate threshold with no slices; no serving-path test; no statement of what the checks miss; claiming you had checks like these when you did not. {{HONESTY}}",
    "model": "<p><b>Data.</b> Two sets. A frozen holdout that never changes, so releases are comparable over time, and a recent window, so the candidate model is judged on the world as it is now. Both built with point-in-time correct features, so no row contains information that was not knowable at the label timestamp.</p>\n<p><b>Comparison.</b> The incumbent is scored on the same rows in the same run. Everything is a paired difference, never a comparison against a number from a previous quarter.</p>\n<p><b>Metrics.</b> Fraud is heavily imbalanced, so accuracy is meaningless. I would require precision at a fixed recall, recall at the alert volume the review team can actually absorb, precision-recall AUC as the threshold-free summary, and calibration, because the score feeds a decision policy rather than a human eyeball.</p>\n<p><b>Slices.</b> Aggregate improvement with a regressed slice is the classic silent failure. Named slices: channel, device type, geography, new versus returning customer, and transaction size band. Any named slice regressing beyond tolerance is a hard block, not a warning.</p>\n<p><b>Serving path.</b> Load the candidate model in the actual serving container, fetch features the way production fetches them, score a fixed record set, and assert the outputs match the offline scores within tolerance. That is the skew check, and it is the one I would insist on keeping. Plus a latency check at the target percentile against the budget.</p>\n<p><b>Behaviour.</b> A small set of known patterns that must score above a bound, and a distribution check so a degenerate or wildly shifted output is caught before a human sees it.</p>\n<p><b>On failure.</b> Promotion is blocked, the metric diff is published to the pull request, and an override requires a named human and is recorded. The tolerances have an owner and a review schedule - published accounts of checks like these describe first versions so strict that a one-millisecond difference failed the pipeline.</p>\n<p><b>What it cannot catch.</b> Anything that happens after deployment: concept drift, adversaries adapting, feedback effects where the model's own decisions shape the next training set. It also cannot see harms the labels do not encode, and it cannot judge recent performance where labels have not arrived yet - with chargeback-style labels that is weeks. These checks are necessary but not enough, which is why shadow and canary releases exist.</p>"
   },
   {
    "id": "mlp-a3",
    "type": "followup",
    "title": "Follow-up question: \"How would you get another team to adopt your platform?\"",
    "prompt": "An interviewer asks: \"You said the GitOps standard was adopted org-wide. How did that actually happen, and what would you do differently?\" Answer in first person, in about 90 seconds. Talk about mechanism, not persuasion, and include something that did not work.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) the adoption mechanism stated as making the safe path the fast path, rather than as evangelism or policy; (2) at least two concrete mechanisms - working templates a team can copy, migrating the first team's workload himself, removing a real pain such as manual rollback, or making the default path require less work than the alternative; (3) a stated obstacle and how it was handled, or an honest statement of what did not work; (4) an explicit acknowledgement that a paved road can go stale, with a defence such as an escape hatch with review, a review schedule for what is fixed, or treating the road as a product with users; (5) spoken in the first person, separating what you did from what the organisation did; (6) under about 110 seconds of spoken content. Common mistakes: describing adoption as a mandate; claiming universal enthusiasm with no friction; listing tools rather than mechanisms; no reflection on the downside of standardisation. {{HONESTY}}",
    "model": "<p>\"It was not persuasion, it was making the standard route the shortest one. The pitch was never 'please adopt GitOps'; it was 'your rollback is currently a person at a keyboard at eleven at night, and on this path it is a revert'. That is a pain people already have.</p>\n<p>Concretely: working templates rather than documentation, so a team started from something that ran; declaring cloud resources through an infrastructure controller so infrastructure moved into the same review flow as code, which removed a whole class of drift; and doing the first migration myself rather than handing over a guide.</p>\n<p>What did not work at first was leading with the tooling. That invites a debate about tools. It worked when I led with the outcome - traceability and reversibility - and let the tooling be an implementation detail.</p>\n<p>What I would do differently is plan for the road going stale. A standard that is right this year becomes the reason next year's workload is awkward, so I would want an explicit escape hatch with a review, and a stated schedule for revisiting what is mandatory. Otherwise the platform team slowly turns into a compliance function, which is how these things stop being used.\"</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "data-features": {
  "id": "data-features",
  "title": "Data and features",
  "day": 1,
  "level": "warning",
  "levelLabel": "You have used a feature store, not built one",
  "why": "Almost every ML design question touches this material: feature stores with online and offline consistency, training-serving skew, labels that arrive late, idempotency, data consistency. A feature store is built for three things - availability, consistency, usability - and consistency here means that features are computed the same way for training and for inference. If you have used a managed store rather than built one, that is still a strong place to be. Just never let it sound like you built the store.",
  "learn": [
   {
    "id": "df-0",
    "part": "field",
    "title": "Key terms",
    "viz": "streaming-feature-pipeline",
    "body": "<p>This is the longest list of terms in the course. Learn them precisely - almost any one of them can become a question.</p>\n<ul>\n<li><b>Feature</b> (<a href=\"#data-and-generalization/dag-f4\">Feature engineering</a>) - a single input value the model reads at prediction time. It always has an entity it belongs to and a timestamp at which it was true.</li>\n<li><b>Entity</b> - the thing a feature hangs off: a customer, a card, a device, a session, a tax return.</li>\n<li><b>Feature view</b> (Feast's word), also called a <b>feature set</b> - a named group of features computed together on one entity, registered once and reused.</li>\n<li><b>Offline store</b> - the full history of every feature value over time. Read for training and for batch scoring. Optimised for large scans.</li>\n<li><b>Online store</b> - the latest value per entity, read on the request path. Optimised for single-key millisecond reads.</li>\n<li><b>Materialisation</b> - copying computed values into the online store so they are there before a request needs them.</li>\n<li><b>Point-in-time correctness</b> - building a training row using only values that were knowable at the label's timestamp. Also called an as-of join.</li>\n<li><b>Leakage</b> (<a href=\"#data-and-generalization/dag-f5\">Data leakage: train-test, temporal, and target</a>) - a training feature containing information from after the prediction moment.</li>\n<li><b>Training-serving skew</b> - the training feature and the serving feature are computed differently, so the model meets a different distribution in production.</li>\n<li><b>Label</b> - the ground truth you are trying to predict. <b>Label latency</b> - the delay before it arrives.</li>\n<li><b>Backfill</b> - recomputing historical feature values after a definition change or a bug fix.</li>\n<li><b>Streaming</b> - processing events as they arrive, continuously. <b>Batch</b> - processing a bounded set on a schedule.</li>\n<li><b>Event time</b> - when the thing happened. <b>Processing time</b> - when your system saw it. They differ, and almost every streaming bug lives in that gap.</li>\n<li><b>Window</b> - a bounded slice of a stream over which an aggregate is computed. Tumbling windows do not overlap, sliding windows do, session windows are defined by gaps in activity.</li>\n<li><b>Watermark</b> (<a href=\"#counting-and-sketches/cs-f2\">Bucketed time-window aggregation</a>) - the engine's estimate of how far event time has advanced, used to decide when a window may be closed.</li>\n<li><b>Late data</b> - an event arriving after its window closed. You either drop it, or reopen and correct.</li>\n<li><b>At-least-once delivery</b> - the messaging guarantee that an event may be delivered more than once but never lost. <b>Idempotency</b> - processing the same event twice has the same effect as processing it once.</li>\n<li><b>Data contract</b> - an agreed, enforced schema and quality guarantee between a data producer and its consumers.</li>\n<li><b>Lineage</b> - the recorded path from a feature back to the raw data and code that produced it.</li>\n</ul>",
    "deeper": "<p>The single idea underneath most of this list is that <b>every value has two timestamps</b>: when it became true, and when your system learned it. Training must respect the first. Serving is constrained by the second. Almost every subtle bug in feature engineering is a place where those two got confused.</p>\n<p>Say that once in the interview and you have covered leakage, late data, watermarks and point-in-time joins in a single sentence. That is exactly the kind of summary a deep-dive interviewer notices.</p>",
    "check": {
     "question": "Why does every feature value need two timestamps rather than one?",
     "options": [
      "For time-zone conversion between regions",
      "Because training must use when the value became true, while serving is limited by when the system learned it - and confusing them causes leakage or skew",
      "Because the online and offline stores use different clocks",
      "To support retention policies and deletion"
     ],
     "answer": 1,
     "explain": "Event time governs correctness of the training set; processing time governs what is available at inference. Conflating them produces leakage offline and skew online."
    }
   },
   {
    "id": "df-f1",
    "part": "field",
    "title": "What a feature store is for, and what it is not",
    "viz": "feature-store-two-stores",
    "body": "<p>A feature store exists to solve four problems, and it is worth being able to say all four, because most people name only the last one.</p>\n<ul>\n<li><b>Consistency.</b> One definition, computed once, read by both training and serving. This is the defence against skew and it is the reason the thing exists.</li>\n<li><b>Reuse and discovery.</b> Features computed by one team are findable and usable by another, with metadata, ownership and lineage attached. Without this, every team recomputes card velocity slightly differently.</li>\n<li><b>Point-in-time correct training data.</b> The store can answer \"what was this feature's value at this exact past moment\", which is what makes an as-of join possible at all.</li>\n<li><b>Low-latency serving.</b> A single-key read fast enough to sit inside a request budget.</li>\n</ul>\n<p>Published platform goals map onto this almost exactly: feature availability as close to real time as possible, feature consistency so that features are calculated through the same mechanism for training and inference, and feature usability meaning discovery, metadata, lineage and governance. Such platforms typically expose four interfaces - registration, publishing, the online and offline stores, and serving - and organise everything around a <b>feature set</b>, a logical grouping on one focal entity.</p>\n<p>What a feature store is <i>not</i>: a general database, a data warehouse, or a place to put slow joins. If a feature needs a five-second computation, the store does not fix that - it just moves where the five seconds happen.</p>",
    "deeper": "<p>Two extra points from published platform write-ups are worth remembering. Writes can be made cheaper by storing one composite feature vector per feature set rather than writing frequently to individual keys, which is a real engineering choice about write amplification in a key-value store. And <b>last mile transformations</b> - transformations applied to stored base features immediately before the model is invoked - are the standard answer to \"what about features that depend on the request itself\".</p>\n<p>That second idea is the one to have ready. Request-time features such as \"amount relative to this customer's median\" cannot be precomputed for every possible amount, so you store the base statistic and do the last-mile arithmetic in the request path, using the same code in training and serving.</p>",
    "check": {
     "question": "What is the primary purpose of a feature store?",
     "options": [
      "To make feature reads fast enough for a low-latency request path",
      "To give training and serving one shared definition, so the model sees the same computation in both places",
      "To store training datasets so they do not have to be regenerated",
      "To version model artefacts alongside their features"
     ],
     "answer": 1,
     "explain": "Consistency is the reason the pattern exists. Low latency is a property of the online half, not the purpose of the whole."
    }
   },
   {
    "id": "df-f2",
    "part": "field",
    "title": "Point-in-time correctness, and the join that gets it wrong",
    "viz": "point-in-time-join",
    "body": "<p>This is the single most useful idea in the subject, and the one people most often get wrong in interviews.</p>\n<p>Suppose you are training a fraud model. Each training row is a transaction with a label that arrived later. You want features such as \"number of transactions on this card in the previous hour\". If you build that row by joining the current feature table on card identifier, you get <i>today's</i> value of card velocity, not the value at the moment of that transaction. The model then trains on information from the future.</p>\n<p>The correct operation is an <b>as-of join</b>: for each training row, take the feature value with the greatest timestamp that is still less than or equal to the row's event timestamp. Feast calls this a point-in-time join; the concept is identical everywhere.</p>\n<p>Two refinements matter in practice. First, the cut-off should be the moment the value was <i>knowable</i>, not the moment it was true - if a value takes ten seconds to arrive, a feature timestamped one second before the transaction was not actually available. Second, the label's own timestamp must be excluded from the feature window, or you leak the outcome into its own prediction.</p>\n<p>The symptom of getting this wrong is unmistakable: excellent offline metrics, mediocre production performance, and a model that appears to degrade immediately after launch.</p>",
    "deeper": "<p>A harder version an interviewer may push you towards: how do you make an as-of join efficient over billions of rows. The usual answers are to partition both sides by entity and sort by time so the join becomes a merge; to bound the lookback window so you are not scanning all history; and to precompute the feature at the event grain during ingestion, so training rows are assembled at write time rather than reconstructed at read time.</p>\n<p>That last option is the streaming answer: computing features in the stream as events arrive means the value attached to an event is by construction the value as of that event. Say it that way and the point-in-time question becomes a description of your own architecture rather than a theory question.</p>",
    "check": {
     "question": "A fraud model shows excellent offline metrics and mediocre production performance from day one. What is the first thing to check?",
     "options": [
      "Whether the serving hardware is underpowered",
      "Whether the training rows were built with a point-in-time correct join, or leaked future feature values",
      "Whether the model needs more trees",
      "Whether concept drift has already occurred"
     ],
     "answer": 1,
     "explain": "Immediate underperformance at launch, rather than gradual decay, points at leakage or skew rather than drift. Drift takes time; a bad join is wrong from the first request."
    }
   },
   {
    "id": "df-f3",
    "part": "field",
    "title": "Training-serving skew: five causes and their fixes",
    "viz": "training-serving-skew",
    "body": "<p>Skew is when the feature the model was trained on and the feature it receives in production are not the same quantity. It has a small number of recurring causes.</p>\n<ul>\n<li><b>Two implementations.</b> Training features written in one language or framework, serving features in another. <i>Fix:</i> one definition, one computation, both paths reading the store - the stated goal of every feature store, one mechanism for training and inference.</li>\n<li><b>Different data freshness.</b> Training uses a settled table; serving uses a value that is seconds old and sometimes missing. <i>Fix:</i> train on the value as it would have been available, including its staleness, and treat missing as a real category rather than as a null to be imputed differently.</li>\n<li><b>Different missing-value handling.</b> Training imputes the median; serving substitutes zero. <i>Fix:</i> encode the imputation inside the shared transform, and test it.</li>\n<li><b>Filtering differences.</b> The training set silently drops rows the serving path keeps - refunds, test accounts, bot traffic. <i>Fix:</i> make the filter explicit and apply it in both places, or not at all.</li>\n<li><b>Time-of-day or seasonal sampling.</b> Training data sampled from one window, production traffic spread across the day or the tax year. <i>Fix:</i> sample across the full cycle, and check slice metrics by time.</li>\n</ul>\n<p>The detection method is the same regardless of cause: score a fixed set of records offline and again through the deployed serving path, and compare. Any difference beyond floating-point tolerance is skew, and it should be a blocking test in continuous integration (CI) - the automated pipeline that builds and tests every change when it is pushed.</p>",
    "deeper": "<p>Published debugging procedures for when online and offline scores disagree share a template worth copying. Confirm the scores are actually arriving where you think - permissions, input access, pipeline bugs, infrastructure failure. Rescore a representative sample of online traffic through the offline model. Compare score distributions across bins; a difference of around 20 percent across score bins is a widely used red-flag threshold. Then inspect the input features for missing features, changed null rates and distribution shift.</p>\n<p>Note the ordering: infrastructure, then scores, then features. It goes cheapest and most likely first, which is the mark of someone who has actually been paged for this.</p>",
    "check": {
     "question": "Which single test most reliably detects training-serving skew before release?",
     "options": [
      "Comparing the training and validation loss curves",
      "Scoring a fixed record set both offline and through the deployed serving path and asserting the outputs match within tolerance",
      "Monitoring the production score distribution for a week",
      "Checking feature null rates in the offline store"
     ],
     "answer": 1,
     "explain": "Skew is by definition a discrepancy between the two computation paths, so the test must exercise both and compare. The others detect it late or not at all."
    }
   },
   {
    "id": "df-f4",
    "part": "field",
    "title": "Label latency in fraud and tax models",
    "viz": "label-latency",
    "body": "<p>In a fraud system the label does not arrive with the prediction. A chargeback can arrive weeks after the transaction it disputes. In a tax product, whether a recommendation was right may only be known when a return is accepted, amended or audited. This delay changes four things.</p>\n<ul>\n<li><b>You cannot measure quality today.</b> Precision and recall for this week's model are unknown until the label window closes. So you need <b>proxy metrics</b> that move immediately: score distribution, alert volume, approval rate, rate of manual overrides, feature null rates.</li>\n<li><b>Your training set has a maturity boundary.</b> Recent rows have incomplete labels: a transaction not yet charged back may be good, or may simply be young. Training on them teaches the model that recent fraud is legitimate. The fix is a <b>maturity window</b> - only rows older than the label horizon count as labelled - and being explicit that you are trading recency for correctness.</li>\n<li><b>How often you retrain is limited by labels, not by compute.</b> There is no point retraining daily if labels only arrive monthly. What can move faster is the decision threshold and the rules layer around the model.</li>\n<li><b>Evaluation must be windowed.</b> Comparing a model trained last month against one trained last week on the same recent data is unfair to whichever one had less mature labels.</li>\n</ul>",
    "deeper": "<p>Two harder wrinkles worth having. First, <b>censoring</b>: an unlabelled recent row is not a negative, it is censored, and treating censored rows as negatives biases the model toward under-detecting fraud. Options are to exclude them, to weight them, or to model the delay distribution explicitly and treat the label as partially observed.</p>\n<p>Second, <b>feedback contamination</b>: if the model blocks a transaction, no chargeback can ever arrive for it, so the label for blocked cases does not exist. Training naively on what is left teaches the model about the traffic it allowed, not about fraud. The standard mitigations are holding out a small random unblocked control, and logging the score and decision for every request so the selection can be modelled later. Bringing this up without being asked shows real judgement in any fraud design discussion.</p>",
    "check": {
     "question": "Why can training on the most recent transactions make a fraud model worse?",
     "options": [
      "Recent data is noisier because volumes are higher",
      "Recent rows have immature labels, so genuinely fraudulent transactions that have not yet been charged back are learned as legitimate",
      "The model overfits to seasonality",
      "The feature store has not materialised the recent values yet"
     ],
     "answer": 1,
     "explain": "Label latency censors recent rows. Treating not-yet-labelled as negative teaches the model that recent fraud is fine, which is exactly backwards."
    }
   },
   {
    "id": "df-f5",
    "part": "field",
    "title": "Streaming mechanics: windows, watermarks, duplicates, backfills",
    "viz": "idempotency-key",
    "body": "<p>How a streaming feature pipeline works, in the words an interviewer will use.</p>\n<p><b>Windows.</b> Aggregates over a stream need a bounded slice. Tumbling windows are fixed and non-overlapping - transactions per card per minute. Sliding windows overlap, giving smoother features at higher cost. Session windows close after a gap in activity, which suits per-user browsing behaviour.</p>\n<p><b>Watermarks and late data.</b> The engine estimates how far event time has advanced and closes windows when the watermark passes them. Events arriving after that are late. You choose: drop them, or allow a lateness bound and emit a corrected result. For a fraud feature read at request time, a corrected value that arrives after the decision is useless for that decision but still matters for the training set - which is a good, concrete thing to say out loud.</p>\n<p><b>Duplicates.</b> Most streaming systems guarantee at-least-once delivery, so the same event can appear twice. The standard fix is an idempotency key - a stable identifier derived from the event, checked against a short-lived store of recently seen keys, with the window sized to the realistic redelivery horizon rather than forever.</p>\n<p><b>Backfills.</b> When a feature definition changes, history must be recomputed with the new definition or training data mixes two definitions. The safest pattern is to write the new definition as a new versioned feature, backfill it, compare, and cut over - rather than mutating the existing one in place.</p>",
    "deeper": "<p>The trade-off underneath all four is the same: <b>completeness against latency</b>. Waiting longer for late events makes a feature more correct and the decision later. In a 200 millisecond budget you cannot wait, so you accept an approximate value at decision time and a corrected value in the offline store - and then you must make sure training uses the value as it was at decision time, not the corrected one, or you have manufactured skew while trying to be accurate.</p>\n<p>That last sentence is the kind of follow-through a deep-dive interviewer is looking for. It is also true of the systems you have built, so it is something you can defend.</p>",
    "check": {
     "question": "A late event corrects a feature value after the decision was already made. What should the training set use for that decision's row?",
     "options": [
      "The corrected value, since it is more accurate",
      "The value as it was at decision time, because using the corrected value would train on information the model will never have at inference",
      "The average of the two values",
      "Drop the row, since it is ambiguous"
     ],
     "answer": 1,
     "explain": "Training must reflect what serving can actually see. Substituting the corrected value creates skew, however tempting the accuracy improvement looks."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "df-a1",
    "type": "explain",
    "title": "Exercise: explain point-in-time correctness in 90 seconds",
    "prompt": "Explain point-in-time correctness to an interviewer who has not used a feature store, using a concrete fraud example. Say what the wrong join does, what the right one does, what the symptom of getting it wrong looks like, and one way to make it efficient at scale.",
    "timeboxSec": 150,
    "rubric": "Must-haves: (1) a concrete example with an entity, a feature and a label - for instance card velocity on a transaction with a chargeback label; (2) the wrong join described precisely as joining on entity key and picking up the current value, thereby importing information from after the prediction moment; (3) the right operation named as an as-of or point-in-time join, taking the latest feature value at or before the row's event timestamp; (4) the knowability refinement - the cut-off should be when the value was available, not merely when it was true; (5) the symptom named: strong offline metrics with immediate, not gradual, production underperformance, which distinguishes leakage from drift; (6) at least one efficiency technique - partition and sort both sides by entity and time so the join is a merge, bound the lookback window, or compute the feature at the event grain during ingestion so the as-of value is attached by construction. Common mistakes: describing it as \"not using future data\" with no mechanism; confusing it with a train/test time split; omitting the symptom; claiming you built the join machinery yourself. {{HONESTY}}",
    "model": "<p>\"Take a fraud model. Each training row is a transaction, and the label - was this charged back - arrives weeks later. One of the features is how many transactions this card has seen in the previous hour.</p>\n<p>The wrong way to build that row is to join the feature table on card identifier. You get the card's velocity as it is today, which includes everything that happened after the transaction you are trying to predict. The model learns from the future, and the offline metrics look wonderful.</p>\n<p>The right operation is an as-of join: for each row, take the feature value with the largest timestamp that is still at or before the transaction's event time. And I would push the cut-off slightly earlier still, to when the value was actually knowable - if an aggregate takes ten seconds to arrive, a value timestamped one second before the transaction was not available to the scorer.</p>\n<p>The symptom of getting it wrong is distinctive. It is not gradual decay, which is drift. It is a model that is excellent offline and mediocre from the very first day in production.</p>\n<p>To make it efficient: partition both sides by entity and sort by time, so the join becomes a merge rather than a scan; bound the lookback window; or, best of all, compute the feature at the event as it arrives, which is what a streaming feature pipeline does - then the as-of value is attached to the event by construction.\"</p>"
   },
   {
    "id": "df-a2",
    "type": "design",
    "title": "Exercise: design the label pipeline for delayed chargebacks",
    "viz": "label-latency",
    "prompt": "Design the labelling and retraining strategy for a fraud model where chargebacks arrive between 7 and 90 days after the transaction, and where blocked transactions never generate a chargeback at all. Cover: what counts as labelled, how you build a training set, how you measure quality before labels arrive, how often you retrain, and what bias the blocking introduces.",
    "timeboxSec": 600,
    "rubric": "Must-haves: (1) a maturity window defined - only rows older than the label horizon count as labelled - with the explicit trade of recency for correctness, and a stated choice of horizon tied to the 7-to-90-day distribution; (2) recent unlabelled rows described as censored rather than negative, with at least one handling option: exclude, weight, or model the delay distribution; (3) proxy metrics for the pre-label period named concretely - score distribution, alert volume, approval or block rate, manual override rate, feature null rates - with the point that they detect change, not correctness; (4) how often you retrain justified by label availability rather than compute, with the observation that the decision threshold and any rules layer can move faster than the model; (5) the selection bias from blocking named explicitly: blocked transactions produce no label, so the surviving data describes allowed traffic, not fraud; (6) at least one mitigation for that bias - a small randomised unblocked control, logging score and decision for every request so selection can be modelled, or reviewing a sample of blocked cases manually - with an acknowledgement of the cost or risk of the control; (7) evaluation windowed so two models are compared over comparable label maturity. Bonus: mentions that a model retrained on its own filtered output degrades over time; mentions seasonality interacting with the maturity window. Common mistakes: treating unlabelled as negative; retraining nightly with no label justification; no mention of blocking bias; proposing a randomised control with no acknowledgement of the fraud loss it accepts. {{HONESTY}}",
    "model": "<p><b>What counts as labelled.</b> I would set a maturity window from the observed delay distribution - if 95 percent of chargebacks arrive within 60 days, rows older than 60 days are labelled and anything newer is censored, not negative. Calling a young transaction legitimate is the fastest way to teach a model that recent fraud is fine.</p>\n<p><b>Training set.</b> Labelled rows only, with features built point-in-time correct as of the transaction. I would keep the censored rows visible in the pipeline rather than filtering them silently, because their volume tells me whether the delay distribution itself has shifted.</p>\n<p><b>Before labels arrive.</b> Proxy metrics, and I would be explicit that they detect change rather than correctness: the score distribution against last week and against the same week last year, alert volume against review capacity, block and approval rates, manual override rate from the review team, and feature null rates. A sudden shift in any of them is a reason to go and look, not a conclusion on its own.</p>\n<p><b>How often.</b> Retraining is bounded by labels, not compute, so something like monthly, aligned to the maturity window. What can move faster is the threshold and the rules layer - those respond to a new attack pattern in days, and they are also easier to reverse.</p>\n<p><b>The blocking bias.</b> This is the part I would raise unprompted. A blocked transaction never produces a chargeback, so it has no label at all. Train naively on what is left and the model learns about the traffic we allowed, which is not the same as learning about fraud, and it gets narrower with every retrain because it is filtering its own future training data.</p>\n<p><b>Mitigations.</b> Log the score and the decision for every request, so the selection can be modelled later rather than reconstructed. Keep a small randomised control of low-score-margin cases that are allowed through, sized so the expected fraud loss is acceptable and agreed with the business, not decided by me. And sample blocked cases for manual review, which is slower but carries no loss. Evaluation windows must line up too - comparing a model on 30-day-mature labels against one on 60-day-mature labels flatters whichever saw fewer chargebacks.</p>"
   },
   {
    "id": "df-a3",
    "type": "followup",
    "title": "Follow-up question: \"Your model looks fine offline but the online scores differ. What do you do?\"",
    "prompt": "An interviewer asks: \"A model scores differently online than it does offline on the same records. Walk me through your debugging.\" Answer in first person, in about 90 seconds, ordered cheapest and most likely first.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) an explicit ordering principle - cheapest and most likely first - rather than an unordered list; (2) infrastructure and plumbing checked first: are the scores arriving where expected, permissions, input access, a pipeline or deployment failure, and is the serving artefact actually the version being compared; (3) rescoring a representative sample of online traffic through the offline model, so the comparison is on identical inputs; (4) comparing score distributions across bins rather than only means, with a stated tolerance for what counts as a red flag; (5) then inspecting inputs: missing features, changed null rates, distribution shift, and different missing-value handling between paths; (6) the five skew causes available if needed - two implementations, freshness differences, imputation differences, filtering differences, sampling differences; (7) a preventive close: this comparison should be a blocking integration test in CI, not a debugging session. Common mistakes: starting with the model rather than the plumbing; comparing means only; never mentioning feature nulls or freshness; no preventive step. {{HONESTY}}",
    "model": "<p>\"I would go cheapest and most likely first, because the boring explanations are usually right.</p>\n<p>First, plumbing. Are the online scores actually arriving where I think they are - permissions, write failures, a partially rolled-out deployment? And is the artefact serving the version I am comparing against, or did a promotion happen that I have not accounted for?</p>\n<p>Second, make the comparison honest. Take a representative sample of real online requests, with the exact feature values that were used, and rescore them through the offline model. If they now match, the model is fine and the difference is in the inputs. If they still differ on identical inputs, it is the artefact or the runtime - a library version, a different preprocessing step baked into the serving container.</p>\n<p>Third, compare distributions rather than means. Bin the scores and look at the shape; a mean can be stable while the tails have moved, and in fraud the tail is the entire product. A difference of roughly twenty percent across score bins is the kind of thing I would treat as a red flag rather than noise.</p>\n<p>Fourth, the inputs. Missing features, changed null rates, a value that is fresher or staler online than the training set assumed, imputation done differently in the two paths, or a filter applied in training that serving does not apply.</p>\n<p>And then the preventive point: this comparison should not be a debugging session. Scoring a fixed record set through both paths and asserting they match belongs in CI as a blocking test.\"</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "serving-and-scale": {
  "id": "serving-and-scale",
  "title": "Serving and scale",
  "day": 2,
  "level": "good",
  "levelLabel": "Your strongest area - get the numbers right",
  "why": "A serving round is where your own real-time work lines up most closely with what the big consumer platforms publish about themselves. Write-ups of seasonal-peak products describe going from a few thousand to a few hundred thousand transactions per second inside a two-hour window, weekly scale tests run in production at three times the expected load, and long lists of exactly what broke at that scale. Their scale is bigger than most engineers have handled. The engineering is the same, though, and being able to talk about it in the same terms - without claiming someone else's numbers as your own - is one of the strongest things you can do in a serving round.",
  "learn": [
   {
    "id": "ss-0",
    "part": "field",
    "title": "Key terms",
    "body": "<p>Precision matters here more than anywhere else. A vague latency claim is the easiest thing in the world for an interviewer to pick apart.</p>\n<ul>\n<li><b>Latency</b> - time from request to response, for one request. Always quote it with a percentile.</li>\n<li><b>Throughput</b> - requests or events handled per unit time. Often written TPS, transactions per second, or RPS, requests per second.</li>\n<li><b>Percentile (p50, p95, p99)</b> (<a href=\"#counting-and-sketches/cs-f7\">Quantile sketches</a>) - the value below which that share of requests fall: p50 is the median, p95 is the value 95 percent of requests beat, and p99 means 99 percent were faster - the slow tail, not the average. The mean is nearly useless for user experience because it hides the tail.</li>\n<li><b>Tail latency</b> - the slow end of the distribution. It dominates perceived reliability, and it worsens when a request depends on several services at once.</li>\n<li><b>Latency budget</b> - the total allowed time, split explicitly across hops, with headroom deliberately unallocated.</li>\n<li><b>Saturation</b> - how close a resource is to its limit. Queues form as saturation approaches one, and latency rises sharply and non-linearly rather than gradually.</li>\n<li><b>Queueing</b> - what couples throughput and latency. Below saturation they are independent; above it, adding load adds waiting time faster than it adds work.</li>\n<li><b>Horizontal scaling</b> - more replicas. <b>Vertical scaling</b> - bigger replicas. Serving usually scales horizontally; a single large model may force vertical.</li>\n<li><b>Horizontal pod autoscaler (HPA)</b> - the Kubernetes controller that changes replica count based on an observed metric, with a stabilisation window to prevent oscillation.</li>\n<li><b>Cluster autoscaler</b> - adds and removes nodes. Distinct from HPA, and the reason HPA can schedule pods that have nowhere to run.</li>\n<li><b>Headroom</b> - spare capacity held deliberately so that a scale-up has time to complete before saturation.</li>\n<li><b>Cold start</b> - the delay before a new replica can serve: image pull, process start, model load, cache fill, compilation.</li>\n<li><b>Warmup</b> - doing that work before the replica is put into rotation. A readiness probe should mean warm, not merely running.</li>\n<li><b>Batching</b> - grouping requests into one model call. <b>Dynamic batching</b> waits a few milliseconds to collect a batch; <b>continuous batching</b>, used in LLM (large language model) serving, adds and removes sequences from a running batch.</li>\n<li><b>Caching</b> - reusing a previous result. Only valid when the same input recurs and the answer is still fresh enough.</li>\n<li><b>Capacity planning</b> - deciding how much to hold, from measured single-replica throughput and a forecast peak.</li>\n<li><b>Load test, stress test, soak test</b> - at expected load, beyond it until something breaks, and at moderate load for hours to find leaks.</li>\n<li><b>Failover</b> - shifting traffic away from a failed region or zone. <b>Active-active</b> - both regions serve. <b>Active-passive</b> - one serves, the other stands by for disaster recovery.</li>\n</ul>",
    "deeper": "<p>The one piece of theory worth carrying is why latency explodes rather than degrades. In a queueing system, waiting time grows roughly in proportion to one divided by one minus utilisation. At 50 percent utilisation a small load increase barely matters. At 90 percent the same increase can double the wait. At 95 percent it is much worse again.</p>\n<p>This is why headroom is not waste and why autoscaling alone is insufficient: by the time a metric shows saturation, the queue has already formed, and the new replica still has to start and warm. Being able to say that in one sentence is what makes a capacity argument sound like engineering rather than superstition.</p>",
    "check": {
     "question": "Why is autoscaling on its own an insufficient answer to a sharp traffic ramp?",
     "options": [
      "Because autoscalers only scale on CPU",
      "Because latency degrades non-linearly near saturation and a new replica still needs time to start and warm, so headroom is needed to buy that time",
      "Because Kubernetes limits the number of replicas",
      "Because scaling up costs more than over-provisioning"
     ],
     "answer": 1,
     "explain": "Queueing makes the failure sudden rather than gradual, and scale-up is not instantaneous. Headroom is what covers the gap between detection and readiness."
    }
   },
   {
    "id": "ss-f1",
    "part": "field",
    "title": "Decomposing the latency budget",
    "viz": "latency-budget-bar",
    "body": "<p>When asked about latency, never answer with one number. Decompose. For a real-time scoring service, the hops are roughly these.</p>\n<ul>\n<li><b>Ingress and authentication</b> - gateway, TLS termination, authorisation.</li>\n<li><b>Feature fetch</b> - one or more reads from the online store. Often the largest and most variable component, especially if several entities are involved.</li>\n<li><b>Last-mile transformation</b> - request-time arithmetic on the fetched base features.</li>\n<li><b>Model inference</b> - for a gradient-boosted tree, typically small. For a deep model, the dominant term.</li>\n<li><b>Post-processing and policy</b> - thresholds, rules, reason codes.</li>\n<li><b>Response, logging and emission</b> - including any asynchronous write you must not put on the critical path.</li>\n</ul>\n<p>Three rules follow. First, allocate the budget deliberately and leave headroom unallocated - a 200 millisecond budget fully allocated is a budget already breached. Second, anything that does not need to be on the critical path should be asynchronous: logging, feature writes, downstream notification. Third, every hop needs a timeout smaller than the remaining budget, and a defined behaviour when it fires.</p>\n<p>That last point is what separates a design answer from a wish. \"Feature store call times out at 30 milliseconds, and on timeout we score with the request-only features and mark the decision as degraded\" is engineering. \"The feature store is fast\" is not.</p>",
    "deeper": "<p>One point worth bringing up yourself: <b>fan-out multiplies tail latency</b>. If a request needs five independent feature reads and each has a p99 of 20 milliseconds, the chance that at least one of them is slow is much higher than 1 percent - so the request's p99 is far worse than any individual call's p99.</p>\n<p>The standard mitigations are to reduce fan-out by batching reads into one call, to hedge by issuing a duplicate request after a short delay and taking the first response, and to make some reads optional so a slow one can be abandoned. The feature-set idea - one composite feature vector per set rather than many individual writes and reads - is partly this argument, expressed on the write side.</p>",
    "check": {
     "question": "A request makes five independent feature reads, each with a p99 of 20 ms. What is true of the request's p99?",
     "options": [
      "It is 20 ms, since the reads are parallel",
      "It is much worse than 20 ms, because the chance that at least one read is slow is far higher than one percent",
      "It is 100 ms, since latencies add",
      "It is 4 ms, since parallelism divides latency"
     ],
     "answer": 1,
     "explain": "Fan-out amplifies the tail: the slowest of several calls determines the response, so the probability of hitting some call's tail rises with the number of calls."
    }
   },
   {
    "id": "ss-f2",
    "part": "field",
    "title": "Autoscaling: what HPA does, and where it stops",
    "viz": "autoscaling-queue",
    "body": "<p>The horizontal pod autoscaler observes a metric, compares it to a target, and changes replica count. Three things about it are worth knowing precisely, because it is asked about in almost every serving round.</p>\n<p><b>The metric choice matters more than the target.</b> CPU is the default and is often the wrong metric for an inference service, where the bottleneck may be memory bandwidth, a downstream call, or queue depth. The principle is to scale on the metric most correlated with load, and to size minimum and maximum replicas from the lowest and highest observed throughput over a recent window - seven days is a common choice - with a healthy buffer.</p>\n<p><b>The stabilisation window prevents oscillation.</b> Without damping, an autoscaler that reacts to every spike will add replicas, see the metric fall, remove them, and thrash. Scale-up is usually allowed to be fast and scale-down deliberately slow.</p>\n<p><b>HPA schedules pods; it does not create nodes.</b> If the cluster autoscaler cannot add nodes fast enough, HPA produces pending pods and no extra capacity. The standard fix is to hold spare nodes warm with low-priority pause pods that get preempted when real workloads need the space.</p>",
    "deeper": "<p>Two further limits worth naming. First, a model server's cold start can exceed the useful reaction time - if loading weights takes 40 seconds, an autoscaler reacting in 30 seconds is still a minute behind the traffic. That is an argument for pre-scaling on a schedule when the peak is predictable, which a tax season very much is.</p>\n<p>Second, scaling out does not help if the bottleneck is shared: a saturated online store, a connection-pool limit, or a rate-limited downstream API. Adding replicas then makes things worse by adding concurrent pressure on the same constrained resource. The correct instinct when someone proposes scaling out is to ask what the actual bottleneck is first.</p>",
    "check": {
     "question": "Peak is predictable, arriving every year in the same weeks. What does that change about scaling strategy?",
     "options": [
      "Nothing - autoscaling handles predictable and unpredictable load identically",
      "Predictability allows scheduled pre-scaling and pre-warmed headroom, which removes the reaction delay that reactive autoscaling cannot avoid",
      "It means you can run at higher utilisation safely",
      "It means vertical scaling becomes preferable"
     ],
     "answer": 1,
     "explain": "Reactive scaling always lags. When the peak is known in advance, capacity can be in place and warm before the traffic arrives."
    }
   },
   {
    "id": "ss-f3",
    "part": "field",
    "title": "Batching, caching, warmup: three ways to raise throughput",
    "viz": "request-batching",
    "body": "<p>Three techniques trade a little latency for a lot of throughput. Know what each costs.</p>\n<p><b>Batching.</b> One model call serving many requests amortises per-call overhead and uses hardware far better, especially on a GPU. The cost is waiting: a dynamic batcher holds requests for a few milliseconds to fill a batch. That is affordable only if the wait fits the budget, so batching windows are usually small in a real-time path and large in an offline one. For LLM serving, continuous batching adds and removes sequences from a running batch rather than waiting for the whole batch to finish, which is why it improves throughput far more than naive batching.</p>\n<p><b>Caching.</b> Free throughput when the same input recurs and the answer stays valid. Ask two questions before proposing it: what is the hit rate, and what is the staleness tolerance. In fraud scoring, caching a score by card is dangerous - the whole point is that the next transaction differs - but caching a slowly-changing base feature is entirely reasonable.</p>\n<p><b>Warmup.</b> A new replica must load weights, fill caches and often compile before it is fast. If it takes traffic while cold, the tail latency spike looks like an incident. The fix is to make readiness mean warm: load the model and run a few synthetic inferences before reporting ready.</p>",
    "deeper": "<p>A fourth option most people forget: <b>make the model cheaper</b>. A distilled or smaller model, quantisation, or a cascade where a cheap model handles the easy majority and only ambiguous cases reach the expensive one. In fraud, a cascade is natural because the overwhelming majority of traffic is obviously fine.</p>\n<p>The cascade also has an operational benefit worth naming: it gives you a ready-made degraded mode. If the expensive stage is unavailable, the cheap stage still produces a decision, which is exactly the graceful-degradation behaviour a reliability question is asking for.</p>",
    "check": {
     "question": "When is caching model scores keyed on the customer a bad idea in fraud detection?",
     "options": [
      "Whenever the cache is in memory rather than in a shared store",
      "Almost always, because the decision depends on the current transaction, so a cached score answers a different question",
      "Only when the cache exceeds available memory",
      "Only when the model is retrained frequently"
     ],
     "answer": 1,
     "explain": "A per-customer cached score discards the very thing being scored. Cache slowly-changing inputs instead, never the decision."
    }
   },
   {
    "id": "ss-f4",
    "part": "field",
    "title": "Peak season, scale testing, and multi-region",
    "viz": "multi-region-failover",
    "body": "<p>Products with a sharp seasonal peak - a tax-preparation product in filing season, a retailer on its busiest shopping day, a ticketing platform on sale day - have published enough about peak readiness to make a reliable pattern.</p>\n<ul>\n<li><b>The requirement.</b> A growth target with a time bound: for example, from a few thousand to a few hundred thousand transactions per second within a two-hour window, with every microservice above 99.99 percent availability.</li>\n<li><b>What peak looks like.</b> Known calendar dates, often two of them in a season, so the ramp is predictable rather than a surprise.</li>\n<li><b>The testing practice.</b> Weekly scale tests <i>in the production environment</i> at three times anticipated load, covering east-west traffic splits, traffic spikes, and regional and availability-zone failures. Notably, such tests also exercise the people: teams tune monitoring thresholds, collect metrics, prepare log queries and keep runbooks current.</li>\n<li><b>The capacity buffer.</b> Spare nodes held warm by low-priority pause pods, preempted when real workloads need the capacity, with the amount and timing configurable.</li>\n<li><b>Multi-region, per service.</b> Some services single-region with the other region for disaster recovery; others active-active across both, with the ability to scale one region up for disaster recovery.</li>\n</ul>\n<p>The pattern to take from this is that peak readiness is a rehearsal problem, not a capacity problem. Buying capacity is easy; knowing that failover works, that the alerts fire, and that the runbook is current requires practising it.</p>",
    "deeper": "<p>Published lists of what actually broke at that scale are unusually valuable, because they are specific and not what most people would guess. DNS first: a cluster DNS cache defaulting to 1,000 entries and needing 10,000; concurrent DNS queries per pod capped at 150 and raised to 500; DNS pods confined to a handful of master nodes and hitting the cloud provider's per-node API rate limit during pod boot storms, fixed by spreading them across more reserved nodes. </p>\n<p>Then the autoscaling gap, fixed with pause pods. Then a node bootstrap that downloaded packages from the internet, which broke the cluster autoscaler when one package vanished upstream, fixed by baking dependencies into a custom machine image. Then a credential service running as a DaemonSet that was not drained on upgrade. Then load-balancer health-check storms from putting every node in every target group. Then log collection, where a per-node agent could not keep up because log volume scales with pods, not nodes, so the fix was a per-pod sidecar.</p>\n<p>The general lesson, and the one to say out loud: at scale, the things that break are shared infrastructure with implicit limits - DNS, cloud API rate limits, log collection - not the application. Removing external dependencies from the boot path is a principle worth stating in its own right.</p>",
    "check": {
     "question": "A per-node log-collection DaemonSet stops keeping up as a cluster grows. Why?",
     "options": [
      "Because nodes ran out of disk space",
      "Because log volume scales with the number of pods, while a per-node agent scales with the number of nodes, and bin-packing put more pods on each node",
      "Because the log backend rate-limited them",
      "Because DaemonSets cannot be autoscaled"
     ],
     "answer": 1,
     "explain": "The mismatch is between what generates load (pods) and what the collector scales with (nodes). Moving to a per-pod sidecar realigns the two."
    }
   },
   {
    "id": "ss-f5",
    "part": "field",
    "title": "Capacity planning and load testing, concretely",
    "viz": "scale-test-3x",
    "body": "<p>How to answer \"how many replicas do you need\" without hand-waving.</p>\n<ol>\n<li><b>Measure one replica.</b> Find the maximum sustained throughput of a single replica at which the p99 still meets the budget. Not the maximum it can do - the maximum it can do <i>while still being fast enough</i>.</li>\n<li><b>Divide the forecast peak by that number.</b> This gives the replicas needed at peak.</li>\n<li><b>Add headroom.</b> Enough that steady-state utilisation sits well below saturation, and enough that a scale-up has time to happen.</li>\n<li><b>Add failure-domain redundancy.</b> If a zone can be lost, the surviving zones must carry the whole peak.</li>\n<li><b>Check the downstream.</b> The online store, the database and any external API each need the same treatment. The system's capacity is the smallest of them.</li>\n<li><b>Test it.</b> Load test at expected peak, stress test beyond it to find the breaking point and confirm it is the component you expected, soak test for hours to catch leaks.</li>\n<li><b>Cost it.</b> Express it as cost per thousand predictions so the trade-off is discussable with someone who does not read dashboards.</li>\n</ol>\n<p>Steps one and two can be automated for model endpoints: open-source performance-sizing tools deploy candidate configurations, send test traffic, and recommend instance type and count against a target peak throughput, an allowed response time and an allowed error rate, with cost estimates.</p>",
    "deeper": "<p>Two refinements that separate a good answer from a great one. First, the forecast peak is itself uncertain, so capacity should be planned against a high quantile of the demand forecast rather than its mean - under-capacity and over-capacity have very different costs, and saying which is worse in this domain is a business question you should ask rather than assume.</p>\n<p>Second, load tests lie in a specific way: synthetic traffic is usually too uniform. Real traffic has hot keys, correlated bursts and a long tail of unusual requests. Replaying recorded production traffic, or at least shaping the synthetic load to match the observed key distribution, is what makes a load test predictive rather than reassuring.</p>",
    "check": {
     "question": "What is the correct measurement of single-replica capacity?",
     "options": [
      "The maximum requests per second it can process before erroring",
      "The maximum sustained requests per second at which the target latency percentile is still met",
      "The average requests per second observed in production",
      "The CPU limit divided by the average CPU per request"
     ],
     "answer": 1,
     "explain": "Capacity is throughput subject to the latency objective. Throughput measured without the latency constraint overstates what you can actually use."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "ss-a2",
    "type": "design",
    "title": "Exercise: prepare a scoring service for a 60x seasonal ramp",
    "viz": "scale-test-3x",
    "prompt": "A real-time scoring service normally handles 5,000 requests per second. Twice a year it must reach 300,000 requests per second, with the ramp happening over about two hours. Design the readiness plan: capacity, scaling mechanism, what you test and how, what you freeze, what degrades, and what you would measure to know you are ready.",
    "timeboxSec": 720,
    "rubric": "Must-haves: (1) capacity derived from measurement - single-replica sustained throughput at the target latency percentile, multiplied by the forecast peak, plus headroom and failure-domain redundancy - rather than a guessed replica count; (2) the recognition that peak is predictable, so scheduled pre-scaling and warm headroom replace pure reactive autoscaling, with the reason given: reaction time plus cold start exceeds the ramp's slope in the worst case; (3) HPA described accurately - it schedules pods but does not create nodes - with a node-level answer such as warm spare nodes held by low-priority preemptible placeholder pods; (4) downstream dependencies sized too, with the explicit statement that system capacity is the smallest link: online store, database, any external API, connection pools; (5) a testing plan that includes load at peak, stress beyond peak to find and confirm the breaking component, soak for hours, and failure injection covering zone or region loss - not volume alone - plus the point that tests should exercise alerts and runbooks, not only infrastructure; (6) a change freeze near the peak with a stated exception process; (7) a defined degraded mode with a trigger, such as a cascade whose cheap stage serves when the expensive stage saturates, or a rules fallback; (8) readiness criteria that are measurable: a passing test at some multiple of forecast peak, a rehearsed failover, current runbooks, alert thresholds tuned for seasonal baselines. Bonus: notes that synthetic load is too uniform and proposes replaying recorded traffic or matching the key distribution; notes seasonal cost trade-off of holding headroom only in season; notes traffic-mix change at peak affecting model slices. Common mistakes: \"we would autoscale\" with no node story; no downstream sizing; testing volume only; no degraded mode; claiming to have run this when you have not. {{HONESTY}}",
    "model": "<p><b>Capacity.</b> Measure one replica's sustained throughput at the p99 target - not its maximum, its maximum while still fast enough. Divide 300,000 by that number to get peak replicas, then add headroom so steady-state utilisation stays well away from saturation, and enough zone redundancy that losing a zone still leaves peak capacity. Then do the same arithmetic for every downstream: the online store, the database, connection pools, any external API. System capacity is the smallest link, and scaling the service against a saturated store makes things worse.</p>\n<p><b>Scaling mechanism.</b> A 60-fold ramp in two hours is fast, and reactive autoscaling lags: detection, then pod scheduling, then node provisioning, then model load and warmup. So the plan is scheduled pre-scaling on the known calendar, plus warm headroom. HPA stays for the within-day variation, on the metric most correlated with load rather than on CPU by default, with a stabilisation window so it does not thrash. Crucially, HPA schedules pods but does not create nodes, so I would hold warm spare nodes - the standard approach is low-priority pause pods that get preempted when real workloads need the capacity. And readiness must mean warm: weights loaded, caches filled, a few synthetic inferences run.</p>\n<p><b>Testing.</b> Load test at forecast peak, stress test beyond it until something breaks and confirm the breaking component is the one I predicted, soak test for several hours to catch leaks and connection exhaustion. Then failure injection: lose a zone, lose a region, kill the online store, and confirm the degraded path behaves. The published practice I would copy is weekly tests in production at three times anticipated load, including regional and availability-zone failures, with the point that the tests exercise the humans too - thresholds tuned, log queries prepared, runbooks current. I would also shape the synthetic load to match the real key distribution, because uniform synthetic traffic is reassuring rather than predictive.</p>\n<p><b>Freeze and degrade.</b> Change freeze from a stated date before the peak, with a named exception path for security fixes. Degraded mode defined in advance: a cascade where a cheap model handles the obvious majority, which both cuts cost at peak and doubles as the fallback if the expensive stage saturates; and a rules layer beneath that, so the answer to a total model failure is a worse decision rather than no decision.</p>\n<p><b>Readiness criteria.</b> A passing load test at three times forecast peak; a failover rehearsed end to end this season, not last; runbooks updated within the last month; alert thresholds set against seasonal baselines rather than annual averages, so January traffic does not page anyone by itself; and a costed plan for holding the headroom only during the season.</p>\n<p>I should say that this is a design, not a rerun: I have not operated a service at this peak, and I did not run weekly production scale tests at three times load.</p>"
   },
   {
    "id": "ss-a1",
    "type": "explain",
    "title": "Exercise: allocate a 150 ms budget",
    "prompt": "You are given a 150 millisecond end-to-end budget for a real-time risk decision at p99. Allocate it across hops, state the timeout for each hop and what happens when each timeout fires, and say how much headroom you leave and why. Then say which single hop you would attack first if the budget were missed.",
    "timeboxSec": 360,
    "rubric": "Must-haves: (1) hops named and given explicit numeric allocations summing to less than 150 ms, with the remainder identified as deliberate headroom and a reason given for leaving it; (2) a timeout per hop, each smaller than the remaining budget, with a defined behaviour on expiry rather than a generic \"it fails\"; (3) the feature fetch treated as the largest and most variable allocation, with fan-out addressed - batch the reads, or make some reads optional; (4) logging, feature writes and any downstream notification explicitly moved off the critical path; (5) a stated degraded decision path, for example scoring on request-only features and marking the decision degraded, with the consequence acknowledged; (6) the percentile respected throughout - allocations reasoned about at p99, not at the mean; (7) a first-attack choice with a reason, normally the feature fetch, justified by size and variance rather than by preference. Bonus: notes that fan-out amplifies tail latency so the request p99 is worse than any single call's p99; mentions hedged requests. Common mistakes: allocations summing to exactly the budget; timeouts with no fallback; treating model inference as the dominant cost for a gradient-boosted tree; ignoring the percentile. {{HONESTY}}",
    "model": "<p>\"All of this is at p99, not the mean. Allocation: ingress, TLS and authorisation 15 milliseconds; feature fetch 45; last-mile transformation 10; model inference 20 for a gradient-boosted tree; policy and rules 10; response serialisation 10. That is 110, leaving 40 milliseconds of deliberate headroom - because network variance, garbage collection pauses and an occasional slow node are real, and a budget allocated to the last millisecond is a budget already breached.</p>\n<p>Timeouts, each smaller than what remains. Feature fetch times out at 50 milliseconds, and on expiry we score with request-only features and mark the decision degraded, which the policy layer can treat more conservatively. Model inference times out at 30, and on expiry we fall back to the rules layer. The policy layer has no external calls, so it does not need one.</p>\n<p>Off the critical path: request logging, feature writes back to the store, and any downstream notification. All asynchronous, none blocking the response.</p>\n<p>Fan-out is the thing I would watch. If a request needs five entity lookups at 20 milliseconds p99 each, the request's p99 is much worse than 20, because the slowest one sets the response. So I would batch the reads into a single call, and make any non-essential read abandonable.</p>\n<p>If the budget were missed, I would attack the feature fetch first - it is the largest allocation and the most variable, so it has the most headroom to recover and it dominates the tail. Only after that would I look at the model, and for a tree model the answer there is usually fewer trees or a cascade rather than faster hardware.\"</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "reliability-ops": {
  "id": "reliability-ops",
  "title": "Reliability and operations",
  "day": 2,
  "level": "warning",
  "levelLabel": "Strong on keeping it up, thinner on keeping it right",
  "why": "An ordinary service has one health question: is it up. An ML service has two: is it up, and is it right. Most people skip the second one, and it is the one that separates a machine-learning engineer from a backend engineer who once deployed a model. Answering it properly means service level objectives for ML, drift, proxy metrics while you wait for labels, the checks a model must pass before release, gradual rollout, rolling back model and features together, and what you actually do when precision drops at two in the morning.",
  "learn": [
   {
    "id": "ro-0",
    "part": "field",
    "title": "Key terms",
    "body": "<p>Most of this vocabulary comes from site reliability engineering. The ML-specific parts are marked.</p>\n<ul>\n<li><b>SLI, SLO and SLA</b> (<a href=\"#sre-practices/srep-f2\">SLIs, SLOs and error budgets</a>) - a service level indicator is a measured number describing quality, a service level objective is the target that number must hold, and a service level agreement is an objective with a contractual consequence.</li>\n<li><b>Error budget</b> (<a href=\"#sre-practices/srep-f2\">SLIs, SLOs and error budgets</a>) - the shortfall an objective permits, and something you are meant to spend deliberately.</li>\n<li><b>Golden signals</b> (<a href=\"#sre-practices/srep-f1\">Observability and the golden signals</a>) - latency, traffic, errors, saturation.</li>\n<li><b>Observability</b> (<a href=\"#sre-practices/srep-f1\">Observability: logs, metrics, traces</a>) - being able to answer questions about the system you did not anticipate.</li>\n<li><b>Decision quality</b> <i>(ML-specific)</i> - whether the predictions are correct: precision, recall, calibration, business outcome.</li>\n<li><b>Data drift</b> <i>(ML-specific)</i> - the input distribution moves away from the training distribution.</li>\n<li><b>Concept drift</b> <i>(ML-specific)</i> - the relationship between inputs and label changes, even when inputs look unchanged.</li>\n<li><b>Proxy metric</b> <i>(ML-specific)</i> - a fast-moving stand-in for a delayed quality metric: score distribution, alert rate, override rate.</li>\n<li><b>Canary</b> - a small traffic share sent to a new version while metrics are compared.</li>\n<li><b>Shadow</b> <i>(ML-relevant)</i> - the new version scores real traffic but its output is discarded, so it can be compared with no customer risk.</li>\n<li><b>Blue-green</b> - two full environments with an atomic traffic switch and an instant switch back.</li>\n<li><b>Progressive delivery</b> (<a href=\"#sre-practices/srep-f4\">Deployment and migration safety</a>) - the umbrella term for canary, blue-green and staged rollout with an automated abort.</li>\n<li><b>Blast radius</b> - how much of the system, or how many customers, one failure can reach.</li>\n<li><b>Runbook</b> - the written, tested procedure for a specific alert.</li>\n<li><b>MTTD and MTTR</b> - mean time to detect and mean time to recover. Detection is usually the larger and more improvable half.</li>\n<li><b>Postmortem</b> - a blameless written analysis after an incident, with actions that have owners and dates.</li>\n<li><b>Chaos or failure injection</b> - deliberately breaking something in a controlled way to verify the system and the people behave as designed.</li>\n</ul>",
    "deeper": "<p>Most of that vocabulary is general site reliability practice, taught in <a href=\"#sre-practices/srep-f2\">SLIs, SLOs and error budgets</a>. What a model service adds is a second budget a general service has no need for: a quality budget. If the objective is precision above some level at a fixed alert volume, then the allowed shortfall is a budget too, and it governs how aggressively you promote new models. Where an employer's public sources do not describe an error-budget framework, present this as your practice rather than as theirs.</p>",
    "check": {
     "question": "What is the second budget worth proposing for a model service, alongside the usual error budget?",
     "options": [
      "A budget for how many experiments the team may run each quarter",
      "A quality budget: if the objective is precision above some level at a fixed alert volume, the allowed shortfall is a budget too, and it governs how aggressively new models are promoted",
      "A budget for the money spent on training hardware",
      "A budget for how many features a model is allowed to read"
     ],
     "answer": 1,
     "explain": "Availability and latency are only half of what a model service promises. Stating the allowed shortfall in decision quality the same way turns model promotion into a decision anyone can check."
    }
   },
   {
    "id": "ro-f1",
    "part": "field",
    "title": "Two health questions, and why the second one is delayed",
    "viz": "two-dashboards",
    "body": "<p>Split every ML monitoring conversation into two questions and say so out loud. It immediately organises the answer.</p>\n<p><b>Question one - is it up.</b> Latency at the target percentile, error rate, traffic volume, saturation, dependency health, replica count against expectation. This is ordinary service monitoring, and it is available in seconds.</p>\n<p><b>Question two - is it right.</b> Precision, recall at the operating point, calibration, and the business outcome the model exists to move. This question is <i>delayed</i>, because labels are delayed, and in fraud that delay is weeks.</p>\n<p>The consequence is that day-to-day operations run on the first question plus proxies for the second. Useful proxies: the score distribution compared to last week and to the same period last year; alert or block volume against review capacity; the rate at which human reviewers override the model; feature null rates and freshness; and the share of requests taking the degraded path.</p>\n<p>Be explicit about what a proxy can and cannot tell you. A shifted score distribution means <i>something changed</i>. It does not mean the model got worse - a genuine change in traffic mix produces the same signal. Proxies trigger investigation, not rollback.</p>",
    "deeper": "<p>A third question worth naming if the conversation goes deep: <b>is it fair and explainable</b>. Slice metrics by protected or business-relevant segments, monitor for divergence, and keep the ability to produce reason codes for an individual decision. In a financial product this is not an optional extra - a declined transaction or a flagged return may need an explanation to a customer or a regulator.</p>\n<p>Platform teams increasingly build responsible-AI practices into the published model workflow as a check in their own right, with a stakeholder sign-off step and a responsible-AI check before deployment. Saying that you would want that check among the automated checks before release, rather than as a review afterwards, counts for a lot.</p>",
    "check": {
     "question": "The production score distribution shifts noticeably overnight. What is the correct first action?",
     "options": [
      "Roll back the model immediately",
      "Investigate, because the shift means something changed - which may be traffic mix, an upstream data change, or the model - and a proxy triggers investigation rather than rollback",
      "Retrain on the new distribution",
      "Raise the decision threshold to compensate"
     ],
     "answer": 1,
     "explain": "Proxy metrics detect change, not defect. Acting on them without diagnosis risks reacting to a legitimate shift in traffic."
    }
   },
   {
    "id": "ro-f2",
    "part": "field",
    "title": "SLOs for an ML service: what to promise",
    "viz": "latency-budget-bar",
    "body": "<p>Writing an SLO for a model service means promising things on both questions. A workable set for a real-time risk scorer looks like this.</p>\n<ul>\n<li><b>Availability.</b> The share of requests that receive a decision, including the degraded path. Note the subtlety: a rules-based fallback still counts as a decision, so availability and quality are separated deliberately.</li>\n<li><b>Latency.</b> A percentile and a threshold over a window: \"99 percent of decisions within 200 milliseconds over 30 days.\"</li>\n<li><b>Freshness.</b> The age of the features used, at a percentile. Stale features are a silent quality failure that no latency metric will reveal.</li>\n<li><b>Degraded-path share.</b> The fraction of decisions made without full features or without the primary model, capped. This is the one people never propose, and it is the best single sign of hidden decay.</li>\n<li><b>Quality, on a lagged window.</b> Precision at a fixed alert volume, or recall at a fixed precision, measured over a matured-label window and reported with the lag stated.</li>\n</ul>\n<p>Two cautions. First, do not promise a quality number over a window shorter than the label delay; it cannot be measured and the promise is empty. Second, availability targets are scope-specific. Published figures illustrate this: a company may state a goal above 99.99 percent for its microservices while a write-up of its ML platform reports 99.9 percent. Different scopes, different numbers, not to be conflated.</p>",
    "deeper": "<p>Note what an employer's sources usually do <i>not</i> contain, because a confident invention here would be caught. <b>Error-budget frameworks, on-call rotation structures and serving latency percentiles</b> for a model scoring path are rarely published; a source may say only \"milliseconds\" for an online feature lookup, and third-party summaries often carry a p99 (see <a href=\"#serving-and-scale\">Serving and scale</a> for what a percentile means) figure their own author flags as a generic industry assumption rather than company data.</p>\n<p>So the right approach is: describe the SLO structure you would propose, cite their availability language accurately with its scope, and say plainly that their latency objectives are not public. That is a stronger answer than a number, because it shows you are careful with your sources in front of people who know the real ones.</p>",
    "check": {
     "question": "Which SLO best detects a model that is fully available but quietly degrading?",
     "options": [
      "Latency at p99",
      "The share of decisions taking the degraded path, plus feature freshness",
      "Request error rate",
      "Replica count against expected"
     ],
     "answer": 1,
     "explain": "A service on stale features or on its fallback path still answers every request quickly and without errors. Only freshness and degraded-path share expose it."
    }
   },
   {
    "id": "ro-f3",
    "part": "field",
    "title": "Drift: what it is, how to detect it, what to do about it",
    "viz": "drift-monitor",
    "body": "<p>Two kinds, and they need different responses.</p>\n<p><b>Data drift</b> is a change in the inputs: a new device type, a redesigned checkout, an upstream field that starts arriving null. Detect it by comparing recent feature distributions to a reference, per feature, using a distribution distance or a simple summary comparison of means, quantiles and null rates. It is cheap and immediate because it needs no labels.</p>\n<p><b>Concept drift</b> is a change in the relationship: the same inputs now imply a different outcome, because fraud rings adapted or a policy changed. It cannot be detected from inputs alone - you need labels, or a strong proxy such as the override rate of human reviewers.</p>\n<p>What to do is a sequence of checks, not a reflex. Confirm the change is real rather than a monitoring artefact. Determine whether it is expected seasonality - in a tax business this matters enormously, and the baseline should often be the same period last year rather than last month. Check whether it is an upstream data defect, which is the most common cause and is fixed upstream, not by retraining. Only then consider a model response: threshold adjustment first, because it is fast and reversible, then retraining, then a redesign of features.</p>",
    "deeper": "<p>Two practical traps. First, per-feature drift tests on a wide feature set generate constant false alarms - with a hundred features and a five percent test threshold, several fire every day by chance. The fixes are to monitor a small set of features that matter most, to require persistence over several windows before alerting, and to alert on the model's output distribution rather than on every input.</p>\n<p>Second, a drift alert is not a retraining trigger by itself. Retraining on drifted data with immature labels can be actively harmful, especially where the model's own decisions shaped which labels exist. State the trigger properly: sustained drift, plus a measurable drop in quality or a strong proxy, plus mature labels, plus the checks the new model has to pass.</p>\n<p>On the employer's side, the honest position is usually that they publish having a model monitoring service that detects drift and automates retraining, and that models are set up for drift monitoring and alerting when they are deployed - but <b>no source states the statistic, window, threshold or trigger policy</b>. Do not invent one.</p>",
    "check": {
     "question": "A drift alert fires on eight features simultaneously. What is the most likely cause?",
     "options": [
      "The model has degraded and needs retraining",
      "An upstream data defect or pipeline change, since simultaneous multi-feature shifts usually share one source",
      "Concept drift caused by adversaries",
      "The drift threshold is set too loosely"
     ],
     "answer": 1,
     "explain": "Correlated shifts across many features point to a common upstream cause - a schema change, a failed job, a partial outage - far more often than to genuine model decay."
    }
   },
   {
    "id": "ro-f4",
    "part": "field",
    "title": "Progressive rollout and rollback that actually works",
    "viz": "canary-rollout",
    "body": "<p>The order of safety, cheapest and safest first.</p>\n<ol>\n<li><b>Offline checks.</b> The release checks from the platform chapter: frozen holdout plus recent window, incumbent scored on the same rows, slice checks, serving-path comparison.</li>\n<li><b>Shadow.</b> The candidate model scores real production traffic; its output is logged and discarded. This catches skew, latency problems and distribution surprises with zero customer risk. For an ML system it is the single most valuable stage, and it is uniquely available to ML because scoring can be separated from acting.</li>\n<li><b>Canary.</b> A small traffic share takes the candidate model's decisions. Define the abort criteria before you start - error rate, latency, score distribution, and any fast business proxy - and automate the abort.</li>\n<li><b>Ramp.</b> Increase in stages with a soak period at each, long enough for the relevant proxies to move.</li>\n<li><b>Full switch.</b> Blue-green, with the previous version kept warm long enough to switch back instantly.</li>\n</ol>\n<p><b>Rollback for ML is wider than for software.</b> You must revert the model version <i>and</i> the feature definitions it depends on, together, or the restored model reads inputs it never trained on. You must also consider what cannot be reverted: decisions already made, notifications already sent, and a training set already contaminated by the bad model's own outputs. Naming that last one counts for a lot.</p>",
    "deeper": "<p>The open-source progressive-delivery controllers most teams use were built for exactly this: blue-green and canary deployments, with automated rollbacks driven by operational anomaly detection.</p>\n<p>Where to be careful is the word \"automated\": if your rollback was a human decision followed by a revert, say that. Automated abort on a canary is a design you would build, not necessarily one you have operated.</p>",
    "check": {
     "question": "Why is shadow deployment particularly valuable for ML rather than for ordinary software?",
     "options": [
      "Because models are larger artefacts and need longer soak times",
      "Because scoring can be separated from acting, so a model can be exercised on real traffic with real features while its output is discarded",
      "Because ML services have higher error rates",
      "Because it avoids the need for a canary stage"
     ],
     "answer": 1,
     "explain": "The prediction and the action are separable, so the whole online path can be validated - including skew and latency - with no customer consequence."
    }
   },
   {
    "id": "ro-f5",
    "part": "field",
    "title": "Incidents: runbooks, blast radius, and the 2 a.m. answer",
    "viz": "incident-scenario-spine",
    "body": "<p>The question \"what do you do at two in the morning when precision drops\" is really asking three things: do you have a procedure, do you know how to limit damage, and do you understand that the fastest fix is rarely retraining.</p>\n<p><b>The procedure.</b> Stabilise first, diagnose second. Stabilising means reducing harm now: raise the threshold to cut false positives, route to the previous model version, or fall back to rules. All three are reversible in minutes; retraining is not.</p>\n<p><b>Then diagnose in cost order.</b> Did something deploy? Is an upstream feed broken or stale? Are feature null rates or freshness abnormal? Has the traffic mix changed? Is it seasonal? Only after all of those does the model itself become the leading hypothesis.</p>\n<p><b>Blast radius.</b> Decide in advance what one bad version can touch. A fraud model that can decline transactions has a larger blast radius than one that only queues cases for review, and a design that routes uncertain cases to humans rather than acting on them is a reliability decision as much as a product one.</p>\n<p><b>Afterwards.</b> A blameless postmortem with a timeline, contributing causes, and actions that have owners and dates. The test of a good postmortem is whether the same alert would now be resolved faster by someone who was not there.</p>",
    "deeper": "<p>Published incident discipline is worth citing accurately when you rely on it: incidents rated P1 to P4, a central operations centre that spins up incident calls, and a platform team taking central responsibility for one class of incident and for learning from it.</p>\n<p>What is usually <i>not</i> published is an on-call rotation structure or a postmortem process for ML specifically. Cite what a source states; do not extrapolate a process it has not described.</p>",
    "check": {
     "question": "Precision has dropped sharply overnight. What is the correct first move?",
     "options": [
      "Retrain the model on the most recent data",
      "Stabilise with a reversible action - raise the threshold, revert to the previous version, or fall back to rules - then diagnose",
      "Roll back the entire cluster to yesterday's state",
      "Wait for the labels to mature before acting"
     ],
     "answer": 1,
     "explain": "Stabilisation must be fast and reversible. Retraining is slow, is not reversible in the same sense, and may be the wrong fix if the cause is upstream data."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "ro-a1",
    "type": "design",
    "title": "Exercise: write the SLOs for a risk-scoring service",
    "viz": "two-dashboards",
    "prompt": "Write the full set of service level objectives for a real-time risk-scoring service whose labels arrive between 7 and 90 days later. For each: the indicator, the target with percentile and window, why it exists, and what you do when it is breached. Include at least one objective that most people omit, and say which objectives you would refuse to promise and why.",
    "timeboxSec": 600,
    "rubric": "Must-haves: (1) availability defined as the share of requests receiving a decision, with the deliberate note that a degraded or rules-based decision still counts, so availability and quality are separated; (2) latency stated properly as a percentile, a threshold and a window, not as a bare number; (3) feature freshness included as an objective with a percentile, and the reason given - stale features are a quality failure invisible to latency and error metrics; (4) degraded-path share included and identified as the commonly omitted one, with the reasoning that a service can be available, fast and error-free while quietly running on its fallback; (5) a quality objective stated on a lagged window with the lag made explicit, for example precision at a fixed alert volume over a matured-label window; (6) a stated refusal to promise a quality objective over a window shorter than the label delay, on the grounds that it cannot be measured; (7) a breach response per objective that distinguishes reversible stabilisation from investigation and from a fix; (8) error budget mentioned as the mechanism converting the ship-or-stabilise decision into arithmetic. Bonus: proposes a quality budget analogous to the error budget, flagged as your own practice rather than the employer's; notes that published availability figures differ by scope - a microservice goal above 99.99 percent versus 99.9 percent reported for an ML platform - and must not be conflated; notes that serving latency percentiles are usually not public. Common mistakes: latency without a percentile; a same-day precision SLO; no freshness objective; no breach response; inventing an employer latency figure. {{HONESTY}}",
    "model": "<p><b>Availability.</b> Indicator: share of requests that receive a decision, including the degraded path. Target: 99.95 percent over 30 days. Why: the caller must always get an answer; a timeout is worse than a conservative answer. Breach: page, stabilise by widening the fallback, then diagnose.</p>\n<p><b>Latency.</b> Indicator: end-to-end decision time. Target: 99 percent within 200 milliseconds over 30 days. Why: the decision sits inside a customer-facing transaction. Breach: check saturation and fan-out first, then the feature read, then the model.</p>\n<p><b>Feature freshness.</b> Indicator: age of the features used at decision time. Target: 95 percent of decisions using features under some agreed age. Why: stale features are a silent quality failure that latency and error rate cannot see. Breach: investigate the pipeline, not the model.</p>\n<p><b>Degraded-path share.</b> Indicator: fraction of decisions made without full features or without the primary model. Target: under 1 percent over 7 days. Why: this is the one most people omit, and it is the best indicator of hidden decay - a service can be fully available, fast and error-free while quietly running on rules. Breach: treat as a real incident even though no other metric moved.</p>\n<p><b>Quality, lagged.</b> Indicator: precision at the fixed alert volume the review team can absorb, over a window where labels have matured. Target: above an agreed level, reported with the lag stated on the dashboard. Why: it is the actual purpose of the system. Breach: investigate, then stabilise with the threshold before considering a retrain.</p>\n<p><b>What I would refuse to promise.</b> Any quality objective over a window shorter than the label delay. With chargebacks arriving between 7 and 90 days out, a weekly precision SLO cannot be measured, and promising it teaches people to trust a number that is not real. I would report proxies for that period and label them as proxies.</p>\n<p><b>The mechanism.</b> Each objective carries an error budget, which turns \"should we ship\" into arithmetic. I would also propose a quality budget on the lagged metric governing how aggressively we promote new models - that is my own practice, not something the company has published.</p>\n<p><b>One source caution.</b> Companies publish different availability figures for different scopes - a goal above 99.99 percent for microservices in one engineering write-up, and 99.9 percent reported for an ML platform in another. They are different scopes, and serving latency percentiles are frequently not public at all.</p>"
   },
   {
    "id": "ro-a2",
    "type": "followup",
    "title": "Follow-up question: \"It is 2 a.m. and precision has dropped. Go.\"",
    "prompt": "An interviewer says: \"You are paged at two in the morning. Fraud precision has dropped sharply and the review queue has tripled. What do you do?\" Answer in first person, about 90 seconds, in the order you would actually act.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) stabilise before diagnose, stated explicitly as a principle rather than implied; (2) the immediate reversible actions named with their effect: raise the decision threshold to cut alert volume, revert to the previous model version, or fall back to the rules layer - and the acknowledgement that raising the threshold trades recall for volume, which is a business decision to flag rather than a free win; (3) a diagnosis order that goes cheapest and most likely first: recent deployments or config changes, upstream feed health and freshness, feature null rates and distributions, traffic mix, seasonality, and only then the model; (4) the observation that a sudden shift is far more often an upstream data defect than model decay, with the corollary that retraining is the slowest and usually wrong first action; (5) recognition that precision \"dropping\" overnight is suspicious given label latency, so the answer should establish what is actually being measured - a proxy such as review-queue volume or override rate, not matured precision; (6) communication: who is told, and that the review team's capacity is now the binding constraint; (7) afterwards: a blameless postmortem with owners and dates, and the test that the next person could resolve it faster. Common mistakes: retraining first; diagnosing before stabilising; not questioning whether real precision could even be known overnight; no mention of the recall trade when raising the threshold; no follow-through. {{HONESTY}}",
    "model": "<p>\"First, one clarifying thought I would have immediately: with chargeback labels arriving weeks later, I cannot actually know tonight's precision tonight. So what has dropped is a proxy - probably the review team's confirm rate, or the override rate - and the queue tripling is the hard fact. Worth saying out loud, because it changes what I trust.</p>\n<p>Then stabilise before diagnosing. The reversible actions are all fast: raise the decision threshold to bring alert volume back inside what the review team can absorb, revert to the previous model version, or fall back to the rules layer. I would raise the threshold first because it is the least disruptive, and I would say out loud that it trades recall for volume - we will miss more fraud while it is up, and that is a business call I flag rather than make silently.</p>\n<p>Then diagnose cheapest and most likely first. Did anything deploy - model, features, config, an upstream service? Is a feed stale or partially failed? Are feature null rates or distributions abnormal? Has the traffic mix changed - a promotion, a new channel, a bot? Is it seasonal, which in a business with a calendar is a real possibility? And only after all of that does the model become the leading hypothesis. In my experience a sudden shift like this is an upstream data defect far more often than model decay, and retraining is both the slowest option and frequently the wrong one.</p>\n<p>Communication: tell the review team, because their capacity is now the binding constraint, and tell whoever owns the business metric that recall is temporarily lower.</p>\n<p>Afterwards, a blameless postmortem with a timeline and actions that have names and dates. The test I would hold it to is whether someone who was not there could now resolve the same alert faster.\"</p>"
   },
   {
    "id": "ro-a3",
    "type": "design",
    "title": "Exercise: design the rollout and rollback plan",
    "viz": "rollback-path",
    "prompt": "Design the full rollout plan for a new version of a production fraud model, from the offline checks to full traffic, and the rollback plan for each stage. State the abort criteria at each stage, who or what decides, and what cannot be undone.",
    "timeboxSec": 600,
    "rubric": "Must-haves: (1) the stages in order with a purpose each: offline checks, shadow, canary, staged ramp with soak, full switch; (2) shadow justified specifically for ML - scoring can be separated from acting, so skew, latency and distribution can be validated on real traffic with zero customer risk; (3) abort criteria defined per stage and defined in advance, covering error rate, latency at the target percentile, score distribution divergence, degraded-path share and a fast business proxy such as alert volume; (4) an explicit statement of who or what aborts - automated where the criterion is measurable in minutes, human where it is not - and honesty that automated abort is something you would build rather than something you have run; (5) soak duration justified by how long the relevant proxy takes to move, rather than by a round number; (6) rollback specified as reverting the model version and its feature contract together, with the reason; (7) what cannot be undone named: decisions already taken, notifications sent, and training data contaminated by the bad model's own outputs, plus a mitigation such as marking the affected window in the training pipeline; (8) the previous version kept warm so the switch back is immediate rather than a cold start. Bonus: cites the origin of the open-source progressive-delivery controllers accurately as theirs, not yours; notes that a canary on a low-base-rate problem needs enough traffic or enough time for any difference to be visible at all. Common mistakes: no shadow stage; abort criteria invented after the rollout starts; rolling back the model only; no statement about irreversible effects; claiming to have run automated canary abort when you have not. {{HONESTY}}",
    "model": "<p><b>1. Offline checks.</b> Frozen holdout plus recent window, incumbent scored on the same rows, aggregate and per-slice metrics, and a serving-path comparison where fixed records are scored through the real container with production-style feature fetches. Fail means no promotion, with the diff published to the pull request.</p>\n<p><b>2. Shadow.</b> The candidate model scores live traffic; output logged, discarded. This is the stage I would most insist on keeping, because in ML the prediction can be separated from the action, so I get skew detection, real latency and a real score distribution at zero customer risk. Abort criteria: any offline-to-online score mismatch beyond tolerance, latency above budget at the target percentile, or a score distribution materially different from the incumbent on the same traffic. Duration: long enough to cover a full daily cycle, because traffic mix changes by hour.</p>\n<p><b>3. Canary.</b> One percent of decisions. Abort criteria fixed before starting: error rate, latency percentile, score-distribution divergence, degraded-path share, and alert volume against review capacity. Automated abort where the criterion moves in minutes; a human decision where the evidence comes in slower. I should be clear that automated canary abort is something I would build - what I have operated is blue-green through a service mesh with metric alerting and a human decision to switch.</p>\n<p><b>4. Ramp.</b> One, five, twenty-five, fifty percent, with a soak at each stage long enough for the relevant proxy to actually move. On a low-base-rate problem like fraud, one percent of traffic may simply not contain enough positives to say anything, so the soak is set by how long it takes to gather enough evidence, not by a round number.</p>\n<p><b>5. Full switch.</b> Blue-green, previous version kept warm long enough that switching back is instant rather than a cold start.</p>\n<p><b>Rollback.</b> Revert the model version and its feature definitions together. Reverting only the model leaves it reading inputs it never trained on, which is a different failure that looks the same.</p>\n<p><b>What cannot be undone.</b> Decisions already made - declines, holds, notifications sent to customers. And the training set: every decision the bad version made shapes which labels will exist, so I would mark that window in the training pipeline rather than silently absorbing it. That contamination is the part people forget, and it outlives the incident by a retraining cycle.</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "forecasting-uq": {
  "id": "forecasting-uq",
  "title": "Forecasting and uncertainty",
  "day": 1,
  "level": "warning",
  "levelLabel": "Intervals and backtests, not just the model",
  "why": "A forecast is judged by the decision it feeds. So the metric you pick, the way you backtest, and the interval you publish all matter more than which model family you chose. And as soon as another system reads your number and acts on it, the uncertainty has to travel with it.",
  "learn": [
   {
    "id": "uq-0",
    "part": "field",
    "title": "Key terms",
    "body": "<ul>\n<li><b>Time series</b> — a sequence of values recorded in time order, where the order carries information. <i>Daily units sold in a store; hourly electricity load.</i></li>\n<li><b>Forecast</b> — an estimate of future values of that series, made from what you know now. <i>Next week's daily sales.</i></li>\n<li><b>Horizon</b> — how far ahead you forecast, in periods. <i>A 14-day horizon means you predict each of the next 14 days.</i></li>\n<li><b>Lag</b> — the value of the series some number of periods earlier, used as an input. <i>Sales 7 days ago is the lag-7 feature.</i></li>\n<li><b>Feature</b> — any input column the model reads. <i>Day of week, a public-holiday flag, price, weather, a lag.</i></li>\n<li><b>Trend</b> — the slow long-run direction of the series. <i>Demand drifting up 3% a year.</i></li>\n<li><b>Seasonality</b> — a pattern that repeats on a fixed period. <i>Weekly: every Saturday is high. Yearly: every December is high.</i></li>\n<li><b>Univariate</b> — you forecast a series using only its own history. <b>Multivariate</b> — you also use other variables. <i>Sales from sales alone, versus sales from sales plus price plus weather.</i></li>\n<li><b>Ordinary least squares (OLS)</b> — the standard way to fit a linear regression: pick the coefficients that make the sum of squared residuals as small as possible. <b>Driver-based regression</b> — regressing the target on explanatory variables (drivers) rather than on its own past values.</li>\n<li><b>Residual</b> — the gap between an actual observation and what the fitted model said it would be. <b>Residual standard error</b> — the typical size of those gaps, and the scale factor in every interval formula below.</li>\n<li><b>Point forecast</b> — a single number per period. <i>\"Tuesday: 412 units.\"</i></li>\n<li><b>Prediction interval</b> — a range with a stated probability of containing the actual value. <i>\"Tuesday: 340 to 500 units, 90% of the time.\"</i></li>\n<li><b>Confidence interval for the mean response</b> — a range for where the <i>average</i> outcome at a given input sits. It carries only the uncertainty in the fitted coefficients. A prediction interval covers one <i>single</i> future outcome, so it must also carry the residual noise, and it is therefore always the wider of the two.</li>\n<li><b>Quantile</b> (<a href=\"#counting-and-sketches/cs-f7\">Quantile sketches</a>) — the value below which a stated fraction of outcomes fall. <i>The p90 is the level that demand stays below on 90% of days.</i></li>\n<li><b>Probabilistic forecast</b> — a forecast that describes the whole distribution of outcomes, not one number. A set of quantiles or an interval is the practical form of it.</li>\n<li><b>Resampling</b> — refitting a model many times on altered versions of the same data to see how much the answer moves. <b>Bootstrap</b> — resample the observations with replacement. <b>Jackknife</b>, or leave-one-out — refit repeatedly, dropping one observation each time. Both give you a spread of fitted values, and that spread estimates coefficient uncertainty.</li>\n<li><b>Stochastic</b> — uncertain or random from your point of view: you can know the distribution, not the actual value in advance. <b>Stochastic demand</b> is demand you can only know as a distribution, never as a number, until it happens.</li>\n<li><b>Backtest</b> — testing a forecasting method on history by pretending you were standing at a past date and only knew what was known then.</li>\n<li><b>Rolling origin</b> — the correct way to backtest a time series: walk the \"today\" point forward through history, refitting and re-forecasting at each step, instead of doing one train/test split.</li>\n<li><b>MAE</b> (mean absolute error, see <a href=\"#evaluation-and-selection/es-f5\">Regression and ranking metrics</a>) — average size of the error in the original units. <i>\"On average we are 38 units out.\"</i></li>\n<li><b>MAPE</b> (mean absolute percentage error) — the same as a percentage of actuals; unusable when actuals can be zero or near zero.</li>\n<li><b>sMAPE</b> — a symmetric variant of MAPE that is less brutal when actuals are small; still awkward near zero.</li>\n<li><b>Pinball loss</b> (also called quantile loss) — the scoring rule for a quantile forecast: it penalises being under and being over by different amounts, in proportion to which quantile you claimed to predict.</li>\n<li><b>Coverage</b> — the fraction of actual outcomes that really fell inside your stated interval. <i>A 90% interval that contains the truth 74% of the time has 74% coverage and is not calibrated.</i></li>\n<li><b>Calibrated</b> — your stated probabilities match reality. A calibrated 90% interval covers about 90% of outcomes.</li>\n</ul>\n<p>Primer references if any of these is genuinely new: <code>otexts.com/fpp3</code> (Hyndman and Athanasopoulos, the standard free textbook — chapters 1 to 5 and the chapter on prediction intervals), <code>nixtlaverse.nixtla.io</code> for the statsforecast package docs, and <code>scikit-learn.org</code> for the general regression vocabulary.</p>",
    "deeper": "<p>Two distinctions worth locking in now because they cause most confusion later. First, a prediction interval is not a confidence interval: a confidence interval is about uncertainty in a parameter you estimated, a prediction interval is about uncertainty in a single future outcome, and the second is always wider.</p>\n<p>Second, accuracy and calibration are different properties. A model can have excellent MAE and badly calibrated intervals, or terrible MAE and perfectly honest intervals. They are measured separately and improved separately.</p>",
    "check": {
     "question": "What does a well-calibrated 90% prediction interval promise?",
     "options": [
      "The forecast is accurate to within 10%",
      "About 90% of actual outcomes fall inside the interval",
      "The model was trained on 90% of the data",
      "The interval is 90% as wide as the full range of the data"
     ],
     "answer": 1,
     "explain": "Calibration is about coverage: a 90% interval should contain the realised value about 90% of the time. It says nothing directly about how accurate the central estimate is."
    }
   },
   {
    "id": "uq-f1",
    "part": "field",
    "title": "The four families of forecasting method, and when each wins",
    "body": "<p>Almost every production forecasting system is one of four families, or a blend. Know what each is for.</p>\n<ul>\n<li><b>Classical statistical models.</b> ARIMA (autoregressive integrated moving average — models the series from its own lags and past errors), ETS (exponential smoothing with explicit level, trend and seasonal components), Theta, seasonal naive. They fit one model per series, handle trend and seasonality explicitly, and emit prediction intervals from their own error structure. <i>Best when:</i> series are reasonably long and well behaved, you have hundreds rather than millions of them, and you want interpretability and intervals for free.</li>\n<li><b>Machine-learning regressors on engineered features.</b> Treat forecasting as tabular regression: build lag features, rolling means, calendar flags, promotions, prices, weather, then fit LightGBM, XGBoost or a linear model. One global model across many series is normal and usually beats per-series fitting when you have many related series. <i>Best when:</i> external drivers matter, you have many series that share structure, and you need one model to maintain rather than ten thousand.</li>\n<li><b>Deep forecasters that learn across series.</b> DeepAR (an autoregressive recurrent network that outputs distribution parameters) and the Temporal Fusion Transformer both emit quantiles directly. N-BEATS sits in the same family but is a point forecaster unless you fit it with a quantile loss, so do not describe it as probabilistic by default. <i>Best when:</i> you have very many series, strong shared patterns, and enough data and engineering appetite to justify the operational cost.</li>\n<li><b>Structural and special cases.</b> Prophet (decomposable trend plus seasonality plus holidays, tuned for business series with analysts driving it), hierarchical forecasting with reconciliation (when forecasts must add up across a product or region hierarchy), and intermittent demand methods such as Croston's for series that are mostly zeros. <i>Best when:</i> the structure of the problem, not the algorithm, is the hard part.</li>\n</ul>\n<p>The honest default in industry, for a demand series with real business drivers, is a gradient-boosted regressor on lag and calendar features, benchmarked against seasonal naive and against a classical model. If you cannot beat seasonal naive, you do not have a model, you have a pipeline.</p>",
    "deeper": "<p>The comparison that gets asked most often is <b>global versus local</b>. A local model fits each series independently: maximum flexibility, no sharing, and it falls apart on short or sparse series. A global model fits one model across all series with the series identity as a feature: it borrows strength across series, handles cold starts far better, and is one artefact to deploy and monitor. The M-competition results over the last decade pushed the industry firmly toward global models plus simple statistical benchmarks.</p>\n<p>The other one is <b>recursive versus direct multi-step</b>. Recursive feeds its own prediction back in to go one step further, which compounds error; direct trains a separate model per horizon step, which is more models but no error compounding. Bringing that trade-off up yourself shows you know the area.</p>",
    "check": {
     "question": "You have twelve thousand related retail series, many of them short, plus price and promotion data. Which approach is the sensible default?",
     "options": [
      "Fit an ARIMA model per series independently",
      "One global gradient-boosted regressor on lag, calendar and driver features, benchmarked against seasonal naive",
      "A separate deep network per series",
      "A simple average of the last 30 days for each series"
     ],
     "answer": 1,
     "explain": "Many related series with external drivers is the classic case for one global ML model with engineered features. Per-series fitting cannot borrow strength, and short series are exactly where borrowing matters most."
    }
   },
   {
    "id": "uq-f2",
    "part": "field",
    "title": "The software packages, by name",
    "body": "<p>Know what exists and what each is for. You will not be quizzed on APIs, but naming the main packages accurately is cheap credibility.</p>\n<ul>\n<li><b>statsmodels</b> — the general Python statistics library; ARIMA, SARIMAX, ETS, state-space models. The reference implementation, not the fastest.</li>\n<li><b>statsforecast</b> (Nixtla) — fast, parallel classical models — AutoARIMA, AutoETS, Croston, Theta — designed for thousands of series at once. Its siblings are <b>mlforecast</b> for ML models with automatic lag features and <b>neuralforecast</b> for deep models.</li>\n<li><b>sktime</b> — a scikit-learn-style unified interface across forecasting methods, with pipelines and cross-validation splitters built for time series.</li>\n<li><b>darts</b> — a single API spanning classical, ML and deep forecasters, with backtesting and probabilistic output built in.</li>\n<li><b>Prophet</b> — Meta's decomposable model; strong at business series with holidays, easy for analysts, weak when drivers matter more than seasonality.</li>\n<li><b>GluonTS</b> — Amazon's probabilistic forecasting toolkit; the home of DeepAR and related models, with quantile output built in.</li>\n<li><b>scikit-learn, LightGBM, XGBoost</b> — the tabular route. LightGBM and XGBoost both support quantile or pinball objectives natively, which is what makes the ML route able to emit intervals at all.</li>\n<li><b>MAPIE</b> and <b>crepes</b> — conformal prediction wrappers for scikit-learn-style models, including time-series-aware variants.</li>\n</ul>",
    "deeper": "<p>A practical note worth having: the packages differ far more in how they handle <b>many series</b> and <b>backtesting</b> than in the algorithms themselves. Anyone can fit an ARIMA. Fitting fifty thousand of them nightly, with rolling-origin validation and per-series fallbacks when a fit fails, is where the library choice actually pays.</p>\n<p>That point also matters when you have not used a given package: how the algorithm works is common knowledge, and the plumbing around it is where the real work sits.</p>",
    "check": null
   },
   {
    "id": "uq-f3",
    "part": "field",
    "title": "Evaluating a point forecast: metrics and backtesting",
    "viz": "rolling-origin-backtest",
    "body": "<p>Before you get to intervals, get the ordinary evaluation right, because most people get it wrong.</p>\n<p><b>Metrics.</b> <b>MAE</b> is in the original units and is easy to explain to a business. <b>RMSE</b> (root mean squared error) punishes large errors harder, so use it when big misses are disproportionately costly. <b>MAPE</b> is scale-free and therefore comparable across series, but it explodes near zero and it penalises over-forecasting more than under-forecasting, which quietly biases model selection. <b>sMAPE</b> softens that but does not fix it. <b>MASE</b> (mean absolute scaled error) divides your error by the error of a seasonal naive forecast, so a value below 1 means you beat the naive benchmark — it is the most defensible cross-series metric.</p>\n<p><b>Backtesting.</b> A single train/test split on a time series tells you almost nothing, because it measures one origin on one regime. The correct method is <b>rolling origin</b>: pick a starting point, forecast the horizon, step forward, refit or update, forecast again, and aggregate. Two variants: <i>expanding window</i>, where the training set keeps growing, and <i>sliding window</i>, where it stays a fixed length and old data drops off — sliding is right when the process drifts.</p>\n<p>Two rules that catch people out. Never shuffle a time series into random folds; that leaks the future into the training set. And make sure every feature was actually knowable at the forecast origin — a price that was only finalised later is leakage, and it produces a backtest you cannot reproduce in production.</p>",
    "deeper": "<p>Report error <b>per horizon step</b>, not only as a single aggregate. Error almost always grows with horizon, and an aggregate number hides whether your day-1 forecast is excellent and your day-14 forecast is worthless. If the downstream decision commits at day 1 and merely plans at day 14, that difference completely changes what you should optimise.</p>\n<p>Report per segment too. A global metric can look healthy while an entire product family or region is badly served, and the segments that are badly served are usually the ones with the least data — which is exactly where a decision system quietly does the most damage.</p>",
    "check": {
     "question": "Why is a single random train/test split wrong for a time series?",
     "options": [
      "It uses too little data for training",
      "Random folds leak future information into training, and one split measures a single origin and regime",
      "Time series models cannot be cross-validated at all",
      "It always overestimates the error"
     ],
     "answer": 1,
     "explain": "Order carries information. Random splits let the model see the future, and even a correct chronological single split evaluates one origin, which says nothing about stability across regimes."
    }
   },
   {
    "id": "uq-1",
    "part": "field",
    "title": "Why a point forecast is dangerous when a machine reads it",
    "viz": "flow-forecast-optimize-act",
    "body": "<p>A point forecast is fine when a human reads it, because a human silently applies their own error bar. It becomes dangerous when it is fed to an automated decision, because the decision has no way to know how wrong the number might be.</p>\n<p>Take a generic staffing example: a contact centre forecasts calls per half hour and staffs agents accordingly. A point forecast makes the plan <b>confidently wrong</b> — it staffs against one assumed future, absorbs the entire forecast error silently, and because the resulting plan looks feasible and optimal, nothing in the system flags that it was built on a guess.</p>\n<p>An interval changes the conversation. The plan can carry buffer, staff to a service-level quantile, or be tested against a range of plausible days rather than one.</p>",
    "deeper": "<p>The clearest way to describe it is <b>asymmetric cost</b>. Under-staffing a half hour means customers waiting or abandoning; over-staffing means paid idle time. Those costs are almost never equal, so the cost-minimising staffing level is not the mean of the demand distribution — it is a high quantile of it. This is the newsvendor result from operations research, and the optimal quantile is set by the ratio of under-cost to under-cost-plus-over-cost.</p>\n<p>You do not need the formula. You need the consequence: a point forecast cannot express that target no matter how accurate it is on average, because the mean is simply the wrong statistic for an asymmetric decision.</p>",
    "check": {
     "question": "Why is feeding a point forecast to a capacity optimizer risky even when the forecast is accurate on average?",
     "options": [
      "Optimizers cannot accept continuous inputs",
      "The plan commits to one assumed future with no error bar, and asymmetric costs make the mean the wrong target",
      "Point forecasts are always biased low",
      "It makes the optimization non-convex"
     ],
     "answer": 1,
     "explain": "Average accuracy is not the issue. The optimizer has no way to know how wrong the number might be, and when under-supply costs more than over-supply the right plan is built on a quantile, not the mean."
    }
   },
   {
    "id": "uq-2",
    "part": "field",
    "title": "Prediction intervals 1: quantile regression and pinball loss",
    "viz": "quantile-band-forecast",
    "body": "<p>Train the model to predict a specific percentile directly by swapping the loss function for <b>pinball loss</b>, which penalises under- and over-prediction asymmetrically. Want the p90? Penalise under-prediction nine times as hard as over-prediction, and the model learns to sit at the 90th percentile.</p>\n<p>This is the production-friendly route because it needs no extra machinery: LightGBM and XGBoost support a quantile objective natively, so you train one model per quantile — typically p10, p50 and p90 — and you are done. Linear quantile regression exists too, in statsmodels, if the relationship is simple enough.</p>",
    "deeper": "<p>Two practical wrinkles. Independently trained quantile models can <b>cross</b> — the p90 model predicting below the p50 model for some inputs, which is incoherent. You fix it by sorting the outputs per row, or by training a monotone-constrained joint model.</p>\n<p>And there is a cost story: three quantiles means three models to train, serve and monitor. In a real deployment that is three times as much to monitor for drift, which is a legitimate argument for the conformal route instead — wrapping a single point model's past errors into an interval.</p>",
    "check": {
     "question": "To make a gradient-boosted model predict the 90th percentile, you:",
     "options": [
      "Train on the mean and add 1.28 standard deviations",
      "Use pinball loss weighted so under-prediction is penalised nine times more than over-prediction",
      "Train on the top decile of the training data only",
      "Fit the residuals with a Gaussian and take its 90th percentile"
     ],
     "answer": 1,
     "explain": "Pinball loss with an asymmetric weight makes the model target a specific quantile directly. The Gaussian and standard-deviation options assume a distribution shape you have not verified."
    }
   },
   {
    "id": "uq-3",
    "part": "field",
    "title": "Prediction intervals 2: conformal prediction",
    "viz": "coverage-vs-width",
    "body": "<p><b>Conformal prediction</b> is a wrapper that turns any point model's past errors into an interval. Hold out a calibration set, collect the absolute residuals, take their (1 minus alpha) quantile, and your interval is the prediction plus or minus that value. It is <b>distribution-free</b> — it assumes nothing about the shape of the error distribution — and it comes with a finite-sample coverage guarantee.</p>\n<p>The catch is the assumption it does make: <b>exchangeability</b>, meaning the calibration errors and the future errors are drawn from the same pool in a way where order does not matter. Time series violates that — next Tuesday is not exchangeable with last March — so the textbook guarantee does not straightforwardly hold on forecasts.</p>",
    "deeper": "<p>The mitigations to name: calibrate on a <b>rolling window</b> of recent residuals rather than a fixed historical set, or use an <b>adaptive</b> variant (adaptive conformal inference, EnbPI) that widens the interval when recent empirical coverage slips below nominal and narrows it when coverage runs high. The MAPIE package implements time-series-aware variants.</p>\n<p>And know what <b>marginal</b> coverage means, because it is a common thing to be asked about: coverage holds on average across all predictions, not conditionally for any given input. Your intervals can be reliably 90% overall while being systematically too narrow for one hard segment.</p>",
    "check": {
     "question": "What is the main reason a textbook conformal coverage guarantee does not simply hold for time-series forecasts?",
     "options": [
      "Conformal requires a Gaussian residual distribution",
      "Time series violates exchangeability, which the guarantee assumes",
      "Conformal cannot wrap tree-based models",
      "The calibration set has to be larger than the training set"
     ],
     "answer": 1,
     "explain": "Conformal is distribution-free but not assumption-free — it needs exchangeability, and temporal ordering with drift and seasonality breaks it. Rolling or adaptive calibration is the practical response."
    }
   },
   {
    "id": "uq-4",
    "part": "field",
    "title": "Prediction intervals 3: models that give you a distribution",
    "body": "<p>Some models emit a distribution by construction. Classical state-space models — ARIMA, ETS — produce prediction intervals from their own error structure, with no extra work. Deep probabilistic forecasters in the DeepAR family output quantiles or distribution parameters directly. Bayesian structural time-series models give you a full posterior predictive distribution.</p>\n<p>Worth naming so you cover the space, but be clear about the trade-off: their intervals are only as good as their distributional assumptions, which is exactly the thing the quantile and conformal routes avoid. A classical model's interval is usually too narrow in practice because it assumes the model form is correct.</p>",
    "deeper": "<p>A fourth route worth one sentence: <b>bootstrapping or ensembling the residuals</b> — resample historical errors and add them to the point forecast to build an empirical predictive distribution. It is crude, it makes no distributional assumption, and it is often the fastest thing to build when someone asks for an interval by Friday.</p>\n<p>Its weakness is the same as conformal's: it assumes the past error distribution still applies, so it degrades under drift in exactly the same way.</p>",
    "check": null
   },
   {
    "id": "uq-5",
    "part": "field",
    "title": "Evaluating an interval: the part most people get wrong",
    "viz": "coverage-vs-width",
    "body": "<p>You do not score an interval with RMSE. Score it with <b>pinball loss per quantile</b>, plus <b>empirical coverage</b> — does the nominal 90% interval actually contain 90% of outcomes on a rolling-origin backtest?</p>\n<p>And report coverage <b>together with interval width</b>, always. Coverage alone is trivially gamed: an infinitely wide interval has perfect coverage and is worth nothing. Saying that before you are asked counts for a lot.</p>",
    "deeper": "<p>Two refinements if pushed. Use <b>rolling-origin</b> backtesting — refit and re-forecast repeatedly as the origin advances — because a single train-test split on a time series tells you almost nothing about stability.</p>\n<p>And check coverage <b>per segment and per horizon</b>, not just globally. Aggregate 90% coverage can hide a segment sitting at 60%, and if that segment is the one the downstream decision relies on most, the global number was actively misleading.</p>",
    "check": {
     "question": "Your nominal 90% intervals achieve 91% empirical coverage on a rolling backtest. What must you check before calling this good?",
     "options": [
      "The RMSE of the interval midpoint",
      "The interval width, and coverage per segment and horizon",
      "Whether the residuals are normally distributed",
      "Nothing — 91% against a 90% target is a pass"
     ],
     "answer": 1,
     "explain": "Coverage without width is meaningless, since wide intervals trivially cover. And aggregate coverage can mask a badly under-covered segment or long horizon."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "uq-a0",
    "type": "explain",
    "title": "Exercise: compare three approaches for a retail demand series",
    "prompt": "Generic problem, no reference to any specific company. You are asked to forecast daily demand for 5,000 retail products across 200 stores, with price and promotion data available. Compare three approaches — a classical statistical model, a global gradient-boosted regressor on engineered features, and a deep probabilistic forecaster — and say which you would build first and why. Then name the benchmark you would hold all three to.",
    "timeboxSec": 240,
    "rubric": "Must-haves: (1) classical per-series models described correctly — ARIMA or ETS, explicit trend and seasonality, intervals from the model's own error structure, and the weakness that they do not use price or promotion drivers naturally and do not borrow strength across series; (2) the global ML route described correctly — one model across all series, lag and rolling features, calendar and driver features, series identity as a feature, LightGBM or XGBoost, with quantile objectives available for intervals; (3) the deep cross-series route named correctly — DeepAR or the Temporal Fusion Transformer for direct quantile output, with N-BEATS accepted only if the answer notes it is a point forecaster unless fitted with a quantile loss; higher engineering and operational cost; (4) a clear first choice with a stated reason, with the global ML route being the expected default given drivers and many related series; (5) seasonal naive named as the benchmark all three must beat, ideally with MASE as the metric. Bonus: mentions global versus local explicitly, or recursive versus direct multi-step. Common mistakes: naming an algorithm with no reason; claiming deep learning is the default; omitting the benchmark entirely; recommending per-series ARIMA for 5,000 times 200 series without noting the operational cost. {{HONESTY}}",
    "model": "<p><b>Classical.</b> AutoARIMA or AutoETS per series, via statsforecast so it parallelises. Handles trend and seasonality explicitly and gives prediction intervals for free. Two problems here: a million series is a lot of fits to maintain, and price and promotions are drivers these models do not take naturally. Good as a benchmark, weak as the product.</p>\n<p><b>Global gradient boosting.</b> One LightGBM model across all series. Features: lags at 1, 7, 28, rolling means and standard deviations, day of week, holiday flags, price, promotion flags, store and product identifiers as categoricals. Borrows strength across series so short and new products are handled sensibly, and it takes the drivers directly. For intervals, train the same feature set with a pinball objective at p10, p50 and p90. This is what I would build first.</p>\n<p><b>Deep probabilistic.</b> DeepAR or a Temporal Fusion Transformer via GluonTS or neuralforecast. Learns cross-series patterns and emits quantiles natively, and TFT handles known-future covariates like planned promotions cleanly. I would hold it back until the gradient-boosted version is in production and I know what it is failing at, because the operational cost is much higher and it frequently does not beat well-featured boosting on business series.</p>\n<p><b>Benchmark.</b> Seasonal naive — last week's same weekday — scored with MASE on a rolling-origin backtest. If a model cannot beat that, it is not a model. And I would report error per horizon step and per segment, not just aggregate.</p>"
   },
   {
    "id": "uq-a2",
    "type": "followup",
    "title": "Follow-up question: \"How do you know your intervals are calibrated?\"",
    "prompt": "An interviewer asks: \"How do you know your intervals are calibrated?\" Answer in first person, in about 90 seconds. Describe the whole test setup you would build, not just the metric.",
    "timeboxSec": 90,
    "rubric": "Must-haves: (1) rolling-origin backtest, not a single split; (2) empirical coverage per quantile against nominal; (3) interval width reported alongside coverage, with the explicit reason — coverage alone is gamed by wide intervals; (4) coverage broken out per segment and per horizon, not just aggregate; (5) a stated response for when coverage is wrong - recalibrate on a rolling window of recent residuals, or widen by the measured miss - rather than declaring the intervals broken. Common mistakes: naming RMSE or MAE as the interval metric; giving coverage without width; reporting only aggregate coverage; treating a single train-test split as a backtest. {{HONESTY}}",
    "model": "<p>\"Rolling-origin backtest — refit and re-forecast as the origin walks forward, because a single time split tells you almost nothing about stability. Then empirical coverage per quantile: does the nominal 90% interval actually contain 90% of realised outcomes. And I would always report width next to coverage, because coverage on its own is trivially gamed — make the interval wide enough and you cover everything and say nothing. I would also want coverage broken out by segment and by horizon, since aggregate 90% can easily hide a segment sitting at 60%, and that is usually the segment the decision layer relies on most.</p>\n<p>And if the coverage comes out wrong I would not throw the intervals away. Under-covering usually means the calibration window no longer matches the current regime, so I would recalibrate on a rolling window of recent residuals, or widen by the measured miss and keep watching. Over-covering means the band is wider than it needs to be, which is a real cost to whoever is planning against it.\"</p>"
   },
   {
    "id": "uq-a1",
    "type": "explain",
    "title": "Exercise: quantile regression vs conformal, in four sentences",
    "prompt": "Explain the difference between quantile regression and conformal prediction in four sentences, and say when you would pick each.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) quantile regression changes the loss (pinball) so the model predicts a percentile directly, one model per quantile; (2) conformal wraps any existing point model using held-out residuals, distribution-free; (3) conformal assumes exchangeability, which time series violates, so rolling or adaptive calibration is needed; (4) a stated choice rule — quantile when you own and can retrain the model, conformal when a point model already exists. Common mistakes: describing conformal as producing a Gaussian interval; saying quantile regression requires a neural network; omitting the exchangeability caveat; claiming conformal needs no calibration data. {{HONESTY}}",
    "model": "<p>Quantile regression changes the training objective — you swap in pinball loss so the model learns to predict a specific percentile directly, typically one model each for p10, p50 and p90. Conformal goes the other way: it leaves the model alone, takes a held-out calibration set, and builds the interval from the quantile of the absolute residuals, which makes it distribution-free and model-agnostic. The catch is that conformal's guarantee assumes exchangeability, and time series breaks that, so in practice you calibrate on a rolling window or use an adaptive variant that widens when recent coverage slips. I would pick quantile regression when I own the model and can retrain it, and conformal when there is already a point model in production I cannot or should not replace.</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "or-tooling": {
  "id": "or-tooling",
  "title": "Optimisation and operations research",
  "day": 1,
  "level": "warning",
  "levelLabel": "Write the model first, pick the solver second",
  "why": "Optimisation questions sort people who can call a solver from people who can write the model. Writing the model means naming the decision variables, stating the objective in real units, separating hard constraints from soft ones, and having a good reason to stop searching when you do.",
  "learn": [
   {
    "id": "or-0",
    "part": "field",
    "title": "Key terms",
    "body": "<ul>\n<li><b>Mathematical optimization</b> — choosing the best option from a set of allowed options, where \"best\" and \"allowed\" are both written down as mathematics. <i>Choose delivery routes that minimise total distance without exceeding vehicle capacity.</i></li>\n<li><b>Decision variable</b> — the thing you get to choose; the unknown the solver fills in. <i>Whether van 3 serves store 12: a yes/no variable.</i></li>\n<li><b>Objective function</b> — the single number you are minimising or maximising. <i>Total kilometres driven; total cost; negative profit.</i></li>\n<li><b>Constraint</b> — a rule the answer must obey. <i>Each store is visited exactly once; no van carries more than 20 pallets.</i></li>\n<li><b>Feasible solution</b> — any assignment of the decision variables that satisfies every constraint, however bad its objective value. <b>Infeasible</b> means no such assignment exists.</li>\n<li><b>Optimal solution</b> — the feasible solution with the best objective value. <i>Feasible means allowed; optimal means allowed and best.</i></li>\n<li><b>Model versus solver</b> — the <b>model</b> is the mathematics you write down (variables, objective, constraints). The <b>solver</b> is the engine that takes that model and finds a solution. You write a model; you run a solver. They are separate things and separate software.</li>\n<li><b>Linear</b> — the objective and constraints are weighted sums of the variables, with no products of variables and no curves. <i>3x + 5y, not x times y.</i></li>\n<li><b>LP (linear program)</b> — a model where the variables are continuous and everything is linear. Solved exactly and fast, even at large size.</li>\n<li><b>Integer variable</b> — a variable restricted to whole numbers. <b>Binary variable</b> — restricted to 0 or 1, which is how you encode a yes/no decision.</li>\n<li><b>MIP (mixed-integer program)</b> — an LP where some variables are integer or binary. This is where assignment, selection and yes/no decisions live, and it is dramatically harder than LP.</li>\n<li><b>CP (constraint programming)</b> — a different style where you describe the problem with logical and combinatorial constraints (all-different, no-overlap, precedence) and the engine searches by propagating those rules. <b>CP-SAT</b> is Google's constraint-programming engine built on a SAT (boolean satisfiability) solver; it is exceptionally strong on scheduling and assignment.</li>\n<li><b>Convex</b> — a problem whose structure guarantees that any local best is the global best. Convex problems are reliably solvable; non-convex ones are not.</li>\n<li><b>VRP (vehicle routing problem)</b> — decide which vehicle visits which stops in which order, subject to capacity, time windows and fleet size. TSP (the travelling salesman problem, visiting every stop once at minimum cost) is its single-vehicle ancestor.</li>\n<li><b>NP-hard</b> — a problem for which no known algorithm finds the proven best answer in time that scales reasonably with size. In practice: you solve small instances exactly, and for large ones you accept a good answer found within a time budget. Routing and most real scheduling are NP-hard.</li>\n<li><b>Heuristic</b> — a rule of thumb that produces a good answer quickly with no guarantee it is the best. <i>Always serve the nearest unvisited store.</i></li>\n<li><b>Local search</b> — start from a solution and repeatedly make small changes that improve it. <b>Local optimum</b> — a solution no small change improves, which may still be far from the global best.</li>\n<li><b>Metaheuristic</b> — a general strategy for escaping local optima and steering a local search. The four to know: <b>simulated annealing</b> (sometimes accept a worse solution, with a probability that shrinks the worse the move is and shrinks further as a \"temperature\" cools, so the search can climb out of a bad valley early and settle later); <b>genetic algorithm</b> (keep a population of solutions, combine and mutate the good ones); <b>tabu search</b> (forbid recently visited moves so the search cannot cycle); <b>guided local search</b> (penalise features of the current solution to push the search elsewhere).</li>\n<li><b>Exact versus heuristic</b> — an exact method can prove that its answer is optimal (or bound how far off it is); a heuristic or metaheuristic cannot. You trade the proof for scale and for freedom in what the objective may look like.</li>\n<li><b>Optimality gap</b> — how much better the true optimum could possibly be than the solution you have, expressed as a percentage. A 3% gap means \"no solution is more than 3% better than this one\". Exact solvers report it; metaheuristics do not.</li>\n<li><b>Time limit</b> — a wall-clock budget after which the solver returns the best feasible solution it has found. On any real decision with a deadline, the time limit is part of the specification, not a compromise.</li>\n<li><b>Warm start</b> — seeding the solver with a previous or hand-built solution so it starts from something good instead of from nothing. Usually faster, and it biases the answer toward the previous plan.</li>\n<li><b>Modeling library versus solver, by name.</b> Ways to <i>write</i> the model: <b>OR-Tools</b> (Google, includes its own engines), <b>PuLP</b>, <b>Pyomo</b>, <b>CVXPY</b> (convex problems only), <b>JuMP</b> (Julia), <b>AMPL</b>/<b>GAMS</b> (commercial languages). Engines that <i>solve</i> it: <b>Gurobi</b> and <b>CPLEX</b> (commercial, best-in-class MIP), <b>HiGHS</b>, <b>SCIP</b>, <b>CBC</b>, <b>GLPK</b> (open source, LP and MIP), <b>GLOP</b> (Google's open-source solver, linear programs only), <b>CP-SAT</b> (constraint programming), <b>OSQP</b>/<b>SCS</b>/<b>ECOS</b>/<b>Clarabel</b> (convex backends behind CVXPY).</li>\n</ul>\n<p>Primer references if any of these is genuinely new: <code>developers.google.com/optimization</code> (OR-Tools guide, the quickest tour of LP, MIP, CP-SAT and routing), <code>www.cvxpy.org</code> for the convex modeling style, and <code>coin-or.github.io/pulp</code> for the simplest possible model-then-solve example.</p>",
    "deeper": "<p>One more term you will hear and should not be surprised by: <b>relaxation</b>. Take a MIP and drop the requirement that the integer variables be whole numbers, and you get an LP that solves fast. Its answer is not usable — you cannot dispatch 2.6 vans — but it bounds how good the true optimum could possibly be. That bound is what the optimality gap is measured against, and it is why a solver can honestly tell you \"within 3% of the best achievable\" without ever having found the best.</p>\n<p>And one point worth carrying: optimization problems are usually easy to <i>write</i> and hard to <i>solve</i>. Most of the skill is in the writing — choosing variables that make the constraints expressible, and knowing which constraints are worth enforcing at all.</p>",
    "check": {
     "question": "Which statement is correct?",
     "options": [
      "CVXPY is a solver and Gurobi is a modeling language",
      "CVXPY is a modeling language for convex problems; Gurobi is a commercial LP/MIP/QP solver",
      "OR-Tools and Gurobi are both modeling languages with no solver of their own",
      "CP-SAT is a convex solver used by CVXPY"
     ],
     "answer": 1,
     "explain": "CVXPY expresses convex problems and hands them to a backend; Gurobi is the engine itself. Keeping modeling and solving separate is what makes a gap in tool experience easy to close."
    }
   },
   {
    "id": "or-1",
    "part": "field",
    "title": "The tools: which library, which engine, and why the split matters",
    "body": "<p><b>OR-Tools</b> is Google's toolkit and is unusual in that it is both a modeling layer and a set of engines: CP-SAT for constraint programming, a dedicated routing library for VRP and TSP built on local-search metaheuristics, GLOP for LP, and wrappers onto external MIP backends including SCIP, CBC and Gurobi.</p>\n<p><b>CVXPY</b> is a Python modeling language for <i>convex</i> problems only. You write the mathematics close to how you would on paper, and it canonicalises the model and hands it to a backend such as OSQP, SCS, ECOS or Clarabel. Its refusal to accept a non-convex formulation is a feature: it tells you your problem class before you waste a week.</p>\n<p><b>Gurobi</b> and <b>CPLEX</b> are commercial solvers — best-in-class LP, MIP and quadratic programming — driven through their own Python APIs (gurobipy, docplex) or through a modeling layer such as Pyomo or PuLP. They earn their licence fee on presolve, cutting planes and branching heuristics, not on syntax.</p>\n<p><b>PuLP</b> and <b>Pyomo</b> are pure modeling layers with no engine of their own: you write the model once and point it at CBC, HiGHS, Gurobi or CPLEX. That is the cleanest demonstration of why the split matters — the same model text runs on a free engine or a commercial one, and only the runtime changes.</p>\n<p>The consequence: the transferable skill is the <b>formulation</b>. Swapping solvers means learning a different API, plus knowing which engine suits the problem class.</p>",
    "deeper": "<p>The distinction has real consequences worth naming. A modeling layer determines how naturally you can express a constraint; the solver determines whether it finishes. A constraint that is painful to write in one layer may be a single built-in in another — CP-SAT's no-overlap and all-different constraints are the classic example, and expressing the same thing in a MIP takes a pile of auxiliary binaries.</p>\n<p>That is why \"which engine suits this problem class\" is a real question and not a preference. Heavy logical and combinatorial structure points at CP-SAT; a clean linear cost model with binaries points at MIP; continuous and curved but convex points at CVXPY; routing points at a specialised routing engine.</p>",
    "check": null
   },
   {
    "id": "or-2",
    "part": "field",
    "title": "Problem classes, and which engine suits which",
    "body": "<p>Five classes cover almost everything.</p>\n<ul>\n<li><b>LP</b> — continuous variables, linear objective and constraints. Solved exactly and fast. <i>Blending, flow, allocation of a divisible resource.</i></li>\n<li><b>MIP</b> — LP plus integer or binary variables. Where assignment, selection and yes/no decisions live, and where it gets hard. <i>Which facility to open; which task goes to which worker.</i></li>\n<li><b>CP / CP-SAT</b> — heavy combinatorial and logical structure: scheduling with precedence, all-different, no-overlap, disjunctions. <i>Shift rostering, job-shop scheduling.</i></li>\n<li><b>Convex (non-linear)</b> — continuous and curved but convex. <i>Portfolio optimization, least-squares with regularisation, some control formulations.</i> This is the CVXPY case.</li>\n<li><b>Routing</b> — VRP and its variants (capacitated, with time windows, with pickup and delivery). Formally a MIP, but in practice attacked with local-search metaheuristics under a time limit rather than solved to proven optimality, because the instances are large and NP-hard.</li>\n</ul>\n<p>Naming the class out loud before you start modeling shows you know the ground. It tells the interviewer you understand what you are about to hand a solver.</p>",
    "deeper": "<p>A useful extra: the <b>LP relaxation</b>. Drop the integrality requirement and you get a bound on how good the true optimum could possibly be. That bound is what an optimality gap is measured against, and it is why a solver can tell you \"this solution is within 2% of the best achievable\" without ever finding the best.</p>\n<p>Routing is the special case worth flagging every time: the standard tooling for it does not prove optimality at all. Knowing that, and saying it, is what separates someone who has run a routing solver from someone who has read about MIP.</p>",
    "check": {
     "question": "You are assigning tickets to engineers with binary assignment variables, a linear cost objective, and capacity limits. The problem class is:",
     "options": [
      "Linear programming, solved exactly and quickly",
      "Mixed-integer programming",
      "A convex program suited to CVXPY",
      "Unconstrained optimization"
     ],
     "answer": 1,
     "explain": "Binary decision variables with an otherwise linear model is textbook MIP. CP-SAT is also a reasonable engine for it, but the class is mixed-integer."
    }
   },
   {
    "id": "or-3",
    "part": "field",
    "title": "A four-step outline for writing the model from scratch",
    "body": "<p>Always these four, out loud, in this order. (1) <b>Decision variables</b> — what am I choosing, over what index sets, binary, integer or continuous? (2) <b>Objective</b> — minimising or maximising what, in what units? (3) <b>Constraints</b> — assignment or coverage, capacity, time or precedence, logical linking. (4) <b>Problem class</b> — which engine.</p>\n<p>Saying that outline out loud <i>is</i> most of the answer. It buys you thinking time and sounds confident even while you are still working it out. When people freeze on a formulation question it is almost always because they have no structure to follow, not because they do not know the material.</p>\n<p>Then the fifth step that separates practitioners from students: say which constraints you would make <b>soft</b>. A <b>hard constraint</b> must be satisfied — physically or legally impossible to violate. A <b>soft constraint</b> is a preference with a price: you allow it to be violated but you pay for the violation in the objective, via a slack variable and a penalty term. Reality violates hard constraints, and a model that returns \"infeasible\" at 6am when a van breaks down is operationally useless.</p>",
    "deeper": "<p>A common trap in the variables step: choosing variables that make the constraints hard to write. In routing, arc-based variables (does vehicle k travel directly from i to j) make capacity and flow easy but subtour elimination painful; sequence-based or position-based formulations shift the difficulty elsewhere. There is rarely a single right answer, but noticing that this choice decides how hard the rest will be is a sharp observation.</p>\n<p>And in the objective step, always state the <i>units</i>. \"Minimise cost\" is vague; \"minimise total kilometres, with lateness converted to kilometres at a stated exchange rate\" is a model someone can argue with — which is the point.</p>",
    "check": null
   },
   {
    "id": "or-4",
    "part": "field",
    "title": "A worked example: the assignment problem",
    "viz": "assignment-matrix",
    "body": "<p>Generic problem: assign tickets i to engineers j. Variables: x[i][j] binary, 1 if ticket i goes to engineer j. Objective: minimise the sum over i and j of c[i][j] times x[i][j], where c is expected handle time or a priority-weighted cost.</p>\n<p>Constraints: each ticket assigned exactly once — for every i, the sum over j of x[i][j] equals 1. Capacity — for every j, the sum over i of h[i] times x[i][j] is at most C[j]. Skill infeasibility is handled by simply not creating those variables, which is cheaper than adding constraints.</p>",
    "deeper": "<p>Then extend it out loud, because the extensions are where experience shows. <b>Fairness</b>: add a min-max load term — introduce a variable L for the maximum load, constrain every engineer's load to be at most L, and minimise L (possibly weighted alongside cost), or penalise deviation from the mean. <b>Soft capacity</b>: allow a slack variable above C[j] with a steep penalty, so overload is possible but expensive rather than infeasible.</p>\n<p>And name the linearisation trick if a product of variables appears: a binary times a binary can be replaced by a new binary with three linear constraints. You do not need to write them, only to know it is doable — that alone tells the interviewer you have modelled before.</p>",
    "check": {
     "question": "In the ticket-assignment model, the cleanest way to handle 'engineer j lacks the skill for ticket i' is to:",
     "options": [
      "Add a constraint forcing x[i][j] to zero",
      "Never create the variable x[i][j] in the first place",
      "Set c[i][j] to a very large number",
      "Make the whole model a CP-SAT problem"
     ],
     "answer": 1,
     "explain": "Not creating the variable shrinks the model rather than growing it. Forcing it to zero works but adds rows; a very large cost is worse still, because the solver can still pick that pair if nothing else is feasible."
    }
   },
   {
    "id": "or-5",
    "part": "field",
    "title": "Hard and soft constraints, and why the objective is only an approximation",
    "viz": "hard-vs-soft-constraint",
    "body": "<p>Hard constraints are things that are physically or legally impossible to violate. Almost everything else is a preference with a price. Getting this split wrong is the most common production failure in applied OR: a model full of hard constraints returns infeasible on a bad morning, and infeasible is not an answer an operator can use.</p>\n<p>The deeper point to say out loud: the objective is <i>always</i> an approximation of what the business wants. Nobody's true objective is \"minimise kilometres\" — it is some unwritten blend of cost, service, fairness and predictability, and you are encoding a proxy for it. Admitting that plainly sounds like experience, not weakness.</p>",
    "deeper": "<p>Which is why validation is two-sided. The mathematical side is the optimality gap against the relaxation bound. The business side is a planner looking at the solution and telling you it is wrong for a reason your objective never encoded — and that second kind of failure is far more common and far more expensive.</p>\n<p>The practical mechanism is to keep an inspection path: let a human see why a particular assignment was made, and which constraint or cost term drove it. Optimizers that cannot explain themselves get switched off.</p>",
    "check": null
   },
   {
    "id": "or-6",
    "part": "field",
    "title": "When you cannot reach optimality",
    "viz": "optimality-gap",
    "body": "<p>Four options, and they are the standard answer to \"the instance is too big\".</p>\n<ul>\n<li><b>Time limit</b> — take the best feasible solution found by the deadline. Specialised routing engines are built for exactly this.</li>\n<li><b>Decompose</b> — cluster first, then solve within clusters; or solve day by day rather than a whole week at once. You lose global optimality and gain tractability.</li>\n<li><b>Warm start</b> — seed from the previous solution, which is usually close. Faster, and it biases the answer toward continuity, which humans downstream usually want.</li>\n<li><b>Read the bound</b> — where the engine reports an optimality gap, use it. Knowing you are within a few percent turns \"we gave up\" into \"we made an informed stop.\"</li>\n</ul>",
    "deeper": "<p>Two more options worth naming if the conversation goes deeper. <b>Column generation</b> and <b>Benders decomposition</b> are the classical exact-but-scalable techniques — you solve a restricted problem and generate the missing pieces on demand. Name them as vocabulary you recognise, not as things you have implemented, and only if asked.</p>\n<p>And <b>problem reduction</b> is underrated: shrinking the instance before it reaches the solver, by pruning dominated options, aggregating identical entities, or fixing variables you already know the answer to, often beats any solver tuning.</p>",
    "check": {
     "question": "The solver hits its time limit with a 3% optimality gap. What does that gap mean?",
     "options": [
      "3% of constraints are violated",
      "The best possible solution is at most 3% better than the one you have, measured against the relaxation bound",
      "The solver is 3% confident in the answer",
      "3% of the decision variables are still fractional"
     ],
     "answer": 1,
     "explain": "The gap bounds how much better the true optimum could be, measured against the relaxation. It turns stopping early into an informed decision rather than a guess."
    }
   },
   {
    "id": "or-7",
    "part": "field",
    "title": "Metaheuristics versus exact solvers: how to choose",
    "viz": "exact-vs-metaheuristic-landscape",
    "body": "<p>When exact methods will not scale, you use a <b>metaheuristic</b>: a general-purpose search over solutions rather than a proof of optimality. The four to know by name are simulated annealing, genetic algorithms, tabu search and guided local search. All four are strategies for escaping a local optimum, and they differ mainly in how they decide where to look next.</p>\n<p>The judgement line to have ready. <b>Exact solvers buy guarantees</b> — provable feasibility, and an optimality gap so you know how much better it could have been. <b>Metaheuristics buy scale and objective freedom</b> — the objective can be non-linear, non-convex, or even stochastic, so you can score a candidate solution by simulating uncertain demand against it, which a MIP essentially cannot do.</p>\n<p>The grown-up answer is to use both rather than pick a winner: solve a small, tightly constrained sub-problem exactly inside a heuristic outer loop, and separately solve a scaled-down version to optimality purely as a yardstick for how far the heuristic's answers sit from the best possible. Building that benchmark, so the heuristic's cost is <i>measured</i> rather than assumed, is the strongest thing you can say on this topic.</p>",
    "deeper": "<p>A detail worth knowing because it makes the two families less separate than they look: many production \"exact\" solvers are themselves full of heuristics — branching rules, primal heuristics that go hunting for feasible solutions, restarts. The difference is not heuristic versus principled; it is whether the method carries a bound alongside its answer.</p>\n<p>And the practical rule of thumb for which to reach for: if you can write the objective as a weighted sum of linear terms and the constraints are clear-cut, start exact. If scoring a candidate requires simulation, or the objective contains a term you can only evaluate rather than express, start heuristic.</p>",
    "check": {
     "question": "What is the strongest general argument for a metaheuristic over an exact solver?",
     "options": [
      "It always finds a better solution",
      "The objective can be arbitrary — non-linear, non-convex, even evaluated by simulation — and it scales to instances an exact method cannot finish",
      "It requires no tuning",
      "Exact solvers cannot handle binary variables"
     ],
     "answer": 1,
     "explain": "Objective freedom plus scale is the real case. The price is giving up the feasibility guarantee and the optimality gap, which is exactly what you must be able to state."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "or-a1",
    "type": "formulate",
    "title": "Exercise: write the model from scratch, timed - tickets to engineers",
    "viz": "assignment-matrix",
    "prompt": "New problem, five minutes, out loud then written. Assign N incoming support tickets to M engineers subject to skill match and per-engineer capacity. Write: decision variables, objective, constraints, problem class. Then extend it with a fairness requirement and say which constraint you would make soft.",
    "timeboxSec": 300,
    "rubric": "Must-haves: (1) binary x[i][j] with stated index sets; (2) an objective with stated units — expected handle time or priority-weighted cost, minimised; (3) an exactly-once assignment constraint per ticket; (4) a capacity constraint per engineer; (5) skill handled by omitting variables or by fixing them to zero; (6) problem class named as MIP (CP-SAT accepted as an engine choice). Extensions expected: a min-max load term or deviation penalty for fairness, and at least one constraint identified as soft with a slack variable and penalty. Common mistakes: writing an inequality where the assignment constraint must be an equality; forgetting capacity entirely; leaving the objective in unstated units; declaring the class LP despite binary variables. {{HONESTY}}",
    "model": "<p><b>Variables.</b> x[i][j] binary for ticket i in 1..N and engineer j in 1..M, equal to 1 if i is assigned to j. Skill-infeasible pairs: I simply do not create the variable.</p>\n<p><b>Objective.</b> Minimise the sum over i and j of c[i][j] times x[i][j], where c[i][j] is expected handle time in minutes, optionally weighted by ticket priority.</p>\n<p><b>Constraints.</b> Assignment: for every ticket i, the sum over j of x[i][j] equals exactly 1. Capacity: for every engineer j, the sum over i of h[i] times x[i][j] is at most C[j], where h is estimated handling minutes and C is remaining capacity in the shift.</p>\n<p><b>Class.</b> Linear objective with binary variables, so this is a MIP. Given the logical and combinatorial flavour, CP-SAT would be a perfectly good engine.</p>\n<p><b>Fairness extension.</b> Introduce L as the maximum load, constrain each engineer's load to be at most L, and add a weighted L term to the objective — a min-max formulation that stops the solver dumping everything on the fastest engineer.</p>\n<p><b>Soft constraint.</b> I would make capacity soft: allow a slack s[j] above C[j] with a steep penalty in the objective. In a real queue you would rather someone runs 10% over than have the model return infeasible and hand the dispatcher nothing.</p>"
   },
   {
    "id": "or-a5",
    "type": "followup",
    "title": "Follow-up question: \"What if the problem is too large to solve to optimality?\"",
    "prompt": "An interviewer asks: \"What if the problem is too large to solve to optimality?\" Answer in first person, in about 90 seconds. Name the options concretely, and give the operational reason before the technical one.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) the operational reason first - a real decision has a deadline, so a proven optimum that arrives late is worth nothing, which makes the time limit part of the specification rather than a compromise; (2) a time limit with best-feasible acceptance, and the note that specialised routing engines run local-search metaheuristics rather than proving optimality at all; (3) decomposition - cluster then route, or solve day by day rather than a whole week - with the loss of global optimality named honestly; (4) warm starting from the previous solution, both for speed and because it biases the plan toward continuity; (5) reading the solver's bound where one exists, so stopping early is an informed decision rather than a guess; (6) an explicit exact-versus-metaheuristic judgement: exact methods buy provable feasibility and a measured gap, metaheuristics buy scale and an objective that may be non-linear or stochastic, and neither is treated as the inferior option; (7) at least one hybrid idea - solving a small crisply constrained sub-problem exactly inside a heuristic loop, or solving a scaled-down instance exactly purely as a yardstick. Common mistakes: listing techniques with no operational reasoning; describing metaheuristics as a compromise or as \"not real optimization\"; forgetting problem reduction and pre-filtering; claiming a bound where the engine does not produce one. {{HONESTY}}",
    "model": "<p>\"I would start from the clock rather than the algorithm. A dispatch or staffing decision has a deadline, and a proven optimum that arrives after it is worth zero - so a time limit is part of the specification, not a compromise. Set the limit, take the best feasible solution, and be explicit that a specialised routing engine is running local search under that limit rather than proving anything.</p>\n<p>Then the standard options. Shrink the instance before the solver sees it: prune dominated options, aggregate identical entities, fix variables whose answer is already known. Decompose along a natural boundary - cluster geographically then route within clusters, or solve day by day - accepting that you lose global optimality across the cut and saying so. Warm start from the previous solution, because it is mostly the same problem, which shortens the solve and biases the answer toward a plan people are already living with. And read the bound wherever the engine produces one, because knowing you are within a few percent turns 'we stopped early' into 'we made an informed stop'.</p>\n<p>On exact against heuristic, I would not treat one as the downgrade. Exact buys provable feasibility and a measured gap. A metaheuristic buys scale and objective freedom - you can score a candidate by simulating uncertain demand against it, which a mixed-integer model essentially cannot. Where I want both, I would solve a small crisply constrained sub-problem exactly inside the heuristic loop, and separately solve a scaled-down instance to optimality purely as a yardstick, so the heuristic's cost is measured rather than assumed.\"</p>"
   },
   {
    "id": "or-a0",
    "type": "drill",
    "title": "Exercise: name the tools and the problem classes",
    "prompt": "From memory and in one line each: what is a model versus a solver; name three modeling libraries and four solver engines and say which is which; what problem class is each of LP, MIP, CP-SAT, convex and routing best for; name four metaheuristics with a one-clause description each; and define optimality gap.",
    "timeboxSec": 180,
    "rubric": "Must-haves: (1) model versus solver stated correctly — the model is the mathematics you write, the solver is the engine that finds the solution; (2) at least three modeling libraries named — OR-Tools, CVXPY, PuLP, Pyomo, JuMP, AMPL — and at least four engines — Gurobi, CPLEX, HiGHS, SCIP, CBC, GLOP, CP-SAT, OSQP, SCS — correctly sorted into the two groups; (3) problem classes placed correctly: LP continuous and linear, MIP for yes/no and assignment decisions, CP-SAT for combinatorial and logical scheduling structure, convex for continuous curved problems via CVXPY, routing handled by specialised local-search engines rather than proven optimality; (4) four metaheuristics named with a distinguishing clause each — simulated annealing accepting worse moves with a probability set by how much worse the move is and by a cooling temperature, genetic algorithms as a population with crossover and mutation, tabu search forbidding recent moves, guided local search penalising features of the current solution; (5) optimality gap defined as how much better the true optimum could be, measured against a relaxation bound. Common mistakes: calling CVXPY a solver; calling Gurobi a modeling language; describing OR-Tools as only a routing library; defining the optimality gap as a violation count or a confidence level; treating metaheuristics as inferior to exact methods. {{HONESTY}}",
    "model": "<p>Model versus solver: the model is the mathematics — variables, objective, constraints; the solver is the engine that searches for an answer to it. Modeling libraries: OR-Tools, CVXPY (convex only), PuLP, Pyomo. Engines: Gurobi and CPLEX commercially, HiGHS, SCIP, CBC and GLOP open source, CP-SAT for constraint programming, OSQP and SCS as convex backends.</p>\n<p>Classes: LP for continuous linear allocation, solved exactly and fast. MIP for yes/no and assignment decisions — binaries make it hard. CP-SAT for scheduling with precedence, no-overlap and all-different structure. Convex via CVXPY for continuous curved problems where any local optimum is global. Routing — VRP and TSP — handled by specialised local-search engines under a time limit, not solved to proven optimality.</p>\n<p>Metaheuristics: simulated annealing accepts worse moves with a probability set by how much worse they are and by a temperature that cools over the run, so it can escape a local optimum; genetic algorithms keep a population and combine and mutate good solutions; tabu search forbids recently visited moves so the search cannot cycle; guided local search penalises features of the current solution to push the search elsewhere.</p>\n<p>Optimality gap: how much better the true optimum could possibly be than the solution in hand, measured against the relaxation bound — a 3% gap means nothing is more than 3% better.</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "control-theory": {
  "id": "control-theory",
  "title": "Control theory for software engineers",
  "day": 2,
  "level": "danger",
  "levelLabel": "Loops, not equations",
  "why": "Any system that predicts, decides, acts, and then measures the result of its own action is a control loop. The arguments worth having are about its four settings: how often it runs (its cadence), how far ahead it commits, how much you damp its reaction, and what makes it re-plan early.",
  "learn": [
   {
    "id": "ct-0",
    "part": "field",
    "title": "Key terms",
    "body": "<ul>\n<li><b>System</b>, or <b>plant</b> — the thing being steered. \"Plant\" is the traditional word and it survives even when there is no factory. <i>A heating system; a fleet of delivery vans; a running web service.</i></li>\n<li><b>State</b> — the set of facts about the system that matter for deciding what to do next. <i>Current temperature; where each van is and what it is carrying.</i></li>\n<li><b>Measurement</b>, or <b>observation</b> — what you can actually see of the state. Usually partial and noisy, and not the same thing as the state itself. <i>A thermometer reading; a GPS (global positioning system) ping thirty seconds old.</i></li>\n<li><b>Setpoint</b>, or <b>reference</b> — the value you want the state to reach or hold. <i>21 degrees; 60% CPU utilisation; every order delivered inside its window.</i></li>\n<li><b>Control input</b>, or <b>action</b> — the one thing you actually get to change. This is the only thing you choose; everything else you either observe or endure. <i>Boiler on or off; how many vans to dispatch; which worker takes which job.</i></li>\n<li><b>Disturbance</b> — anything that moves the system that you do not control. <i>Someone opening a window; a traffic jam; a demand spike.</i></li>\n<li><b>Feedback</b> — measuring the result of your own action and using that measurement in the next decision. This is the thing that makes it a loop.</li>\n<li><b>Open loop</b> — you compute a plan and execute it without checking. <b>Closed loop</b> — you re-measure and correct. <i>A sprinkler on a timer is open loop; a sprinkler with a soil-moisture sensor is closed loop.</i></li>\n<li><b>Controller</b> — the rule or algorithm that turns measurement plus setpoint into an action. It can be a formula, a lookup table, or an optimizer.</li>\n<li><b>Model</b> — whatever the controller uses to anticipate what will happen. Can be a physics equation, a learned forecast, or nothing at all (a purely reactive controller has no model).</li>\n<li><b>Horizon</b> — how far ahead the controller looks when it decides. <i>Plan the next 24 hours.</i></li>\n<li><b>Cadence</b>, or <b>tick</b> — how often the loop runs. <i>Every 15 seconds; every morning at 5am.</i></li>\n<li><b>Lag</b> — the delay between acting and seeing the effect of the action. Every real loop has it, and it is the root cause of most loop misbehaviour.</li>\n<li><b>Oscillation</b>, or <b>flapping</b> — the system swinging back and forth because the controller over-corrects, usually because of lag. <b>Thrash</b> or <b>churn</b> is the same failure in a planning system: the plan changes every time it is recomputed.</li>\n<li><b>Stability</b> — informally, the loop settles instead of swinging or running away. Proving it is a mathematical exercise; avoiding the swinging is an engineering one.</li>\n<li><b>Damping</b> — anything that deliberately slows the loop's reaction so it stops over-correcting. <i>A cooldown window; smoothing the input signal.</i></li>\n<li><b>Hysteresis</b> — requiring a bigger change to switch back than you required to switch in the first place, so a marginal signal cannot flip-flop the system. <i>A thermostat that heats below 20 but does not stop until 22.</i></li>\n<li><b>Stochastic</b> — uncertain or random from your point of view: you can know the distribution but not the actual value in advance.</li>\n</ul>\n<p>Primer references if any of these is genuinely new: <code>en.wikipedia.org/wiki/Control_theory</code> for the vocabulary map, <code>kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale</code> for a control loop written entirely in software terms, and <code>en.wikipedia.org/wiki/Model_predictive_control</code> for the one formal method worth recognising by name.</p>",
    "deeper": "<p>One distinction is worth locking in now: <b>state</b> versus <b>measurement</b>. You almost never observe the true state; you observe a partial, delayed, noisy view of it. Formal control handles this with a state estimator — a Kalman filter is the classic — that fuses noisy measurements with a model to produce a best guess.</p>\n<p>The software analogue is a feature pipeline: the raw signal is not the state, and freshness and noise in that pipeline degrade the controller exactly the way a bad sensor would. That analogy is worth one sentence in an interview and no more.</p>",
    "check": {
     "question": "In control vocabulary, which of these is the control input?",
     "options": [
      "The demand that arrived this hour",
      "The assignment you choose — which worker takes which job",
      "The average delivery time customers experienced",
      "The forecast of tomorrow's demand"
     ],
     "answer": 1,
     "explain": "The control input is the thing you actually choose. Arrivals are a disturbance, delivery time is an observed outcome, and the forecast is part of the model."
    }
   },
   {
    "id": "ct-1",
    "part": "field",
    "title": "What control theory is, at the level you need",
    "viz": "closed-loop",
    "body": "<p>Control theory is the mathematics of systems that observe, decide, act, and then observe the result of their own action. That is the whole definition. Nothing in it requires the loop to be made of hardware.</p>\n<p>It shows up at three levels, and only one of them is the one a decision-systems interview means:</p>\n<ul>\n<li><b>Hardware, continuous time.</b> Motors, thermostats, flight surfaces, chemical plants. Differential equations, PID (proportional-integral-derivative) controllers, tuning for stability. This is the level that makes the whole subject read like sensors and wiring, and it is the level almost every textbook is written at.</li>\n<li><b>Software infrastructure.</b> Autoscalers, rate limiters, congestion control, load shedding, retry backoff. The same ideas at discrete ticks, with no physics involved.</li>\n<li><b>Functional, or architectural.</b> A business decision loop: predict what is coming, decide what to do about it under constraints, do it, measure what happened, decide again. This is the level a job description means when it says \"optimal control\".</li>\n</ul>\n<p>You need the third level, described using vocabulary borrowed from the second. Say explicitly what you are ignoring, because knowing what is out of scope shows judgement: Laplace transforms, transfer functions, pole placement, Lyapunov stability, and PID tuning mathematics. None of that comes up in a Staff systems interview, and none of it is what the phrase means on a job description.</p>",
    "deeper": "<p>Why does a job description reach for the phrase at all? Because it is the only compact name for a system whose own output comes back as its input. Once a plan you publish changes the demand you observe, or an action you take changes tomorrow's starting position, you no longer have a prediction problem — you have a loop. Loops have properties predictions do not: they can oscillate, they can lag, they can keep correcting their own corrections.</p>\n<p>That is the entire reason the vocabulary exists.</p>",
    "check": {
     "question": "At which level does a decision-systems job description's 'optimal control' wording normally sit?",
     "options": [
      "Continuous-time hardware control — differential equations and PID tuning",
      "A functional decision loop: predict, optimize under constraints, act, observe, re-solve",
      "Formal stability analysis of a control law",
      "Signal processing over sensor data"
     ],
     "answer": 1,
     "explain": "It describes a business decision loop. The hardware level supplies useful vocabulary, but nobody is asking you to place poles or prove stability."
    }
   },
   {
    "id": "ct-2",
    "part": "field",
    "title": "The classic software control loop: an autoscaler",
    "viz": "autoscaling-queue",
    "body": "<p>The cleanest control loop in ordinary software engineering is a Kubernetes Horizontal Pod Autoscaler. It is worth walking through, because most backend engineers have already run one without ever calling it control.</p>\n<p>The HPA measures CPU utilisation across the pods of a deployment, compares it with a target — say 60% — and adds or removes replicas to close the difference. Then it measures again. Put the names on it:</p>\n<ul>\n<li><b>Plant</b> — the running deployment.</li>\n<li><b>State / measurement</b> — current CPU utilisation and current replica count.</li>\n<li><b>Setpoint</b> — 60% CPU.</li>\n<li><b>Control input / action</b> — the replica count. The only thing the controller changes.</li>\n<li><b>Disturbance</b> — incoming traffic.</li>\n<li><b>Feedback</b> — measuring utilisation again after your own scaling action.</li>\n<li><b>Controller</b> — the HPA's scaling algorithm.</li>\n<li><b>Damping</b> — the stabilization window, which refuses to scale down again for a few minutes. Without it the loop flaps: scale up, CPU drops, scale down, CPU spikes, scale up.</li>\n</ul>\n<p>Other software loops with the same anatomy: TCP congestion control (measurement is loss or delay, action is window size), a token-bucket rate limiter, adaptive load shedding, and retry with exponential backoff and jitter — where the jitter exists precisely to stop many independent loops synchronising into an oscillation.</p>",
    "deeper": "<p>The reason flapping is the classic failure here is <b>lag</b>. New pods take time to become ready; CPU takes time to settle. A controller that reacts as though the effect were instant keeps correcting for a change it has already made, and that is what oscillation is.</p>\n<p>Hold on to that intuition, because the same failure appears again in planning systems in a different form: a planner that re-plans faster than the plan takes effect will churn for exactly the same reason.</p>",
    "check": {
     "question": "In a Kubernetes HPA loop, which part is the control input?",
     "options": [
      "The CPU utilisation the autoscaler measures",
      "The replica count the autoscaler sets",
      "The incoming traffic",
      "The 60% CPU target"
     ],
     "answer": 1,
     "explain": "The replica count is the only thing the controller changes. CPU is the measurement, traffic is the disturbance, and the target is the setpoint."
    }
   },
   {
    "id": "ct-3",
    "part": "field",
    "title": "Open loop versus closed loop, and why it matters under uncertainty",
    "viz": "open-vs-closed-loop",
    "body": "<p>A single optimization solve produces a <b>plan</b>. Execute that plan without looking again and you are running <b>open loop</b>: nothing measures whether reality matched the plan, and nothing corrects it. Re-solve on fresh data and act on the new answer, and you are running <b>closed loop</b>.</p>\n<p>A system can be both, at different timescales. Take a generic daily delivery planner: across days it is closed loop, because each morning it re-solves against the real orders and the real available fleet, so yesterday's surprises are absorbed into today's plan. Within a day it is open loop, because once the vehicles leave nothing re-plans.</p>\n<p>Closed loop wins whenever the disturbance is <b>stochastic</b>. If you knew tomorrow's demand exactly, a single plan would be perfect and feedback would buy you nothing. You do not, so the value of the loop is that it absorbs error in your model instead of requiring you not to have any. That is the single most useful thing to understand about feedback: it is a substitute for model accuracy.</p>",
    "deeper": "<p>The more precise version is <b>loop rate versus disturbance rate</b>. A loop can only correct disturbances that are slower than the loop itself. If a planner re-solves once a day and demand shocks arrive hourly, the system is structurally blind to those shocks — no improvement to the optimizer changes that, because the problem is the cadence, not the solver.</p>\n<p>That gives you a precise way to argue for event-driven re-solving: not \"real time is better\", but \"the disturbance is faster than the loop\".</p>",
    "check": {
     "question": "A route plan is solved each morning and executed all day with no re-planning. During the day it is:",
     "options": [
      "Closed loop, because yesterday's outcomes were used as inputs",
      "Open loop, because nothing measures error and corrects the plan until the next solve",
      "Neither — batch systems have no loop",
      "Closed loop, because drivers can improvise"
     ],
     "answer": 1,
     "explain": "Using yesterday's outcomes closes the loop at a daily cadence. Within the day nothing corrects the plan, so the day itself runs open loop."
    }
   },
   {
    "id": "ct-4",
    "part": "field",
    "title": "Putting it together: forecast, optimise, act, observe",
    "viz": "flow-forecast-optimize-act",
    "body": "<p>Now put the two previous chapters inside the loop, because that combination is what \"optimal control\" means when a job description says it about a decision system.</p>\n<ul>\n<li><b>Forecast</b> — the demand model is the loop's <b>model of the near future</b>. It is what lets the system act before the demand arrives rather than after it.</li>\n<li><b>Optimize</b> — the constrained solver is the <b>controller</b>. It turns the predicted state plus the constraints into an action.</li>\n<li><b>Act</b> — the published plan, the dispatched routes, the assignment. That is the control input.</li>\n<li><b>Observe</b> — realised demand, no-shows, actual handling times, uncovered periods. That is the feedback, and it feeds both the next forecast and the next solve.</li>\n</ul>\n<p>Nothing in that list is exotic. Forecasting is a well-understood field; constrained optimization is a well-understood field; the loop is just the two of them wired together with a clock and a feedback path. Control theory is the name for that wiring, not for some extra component sitting outside both.</p>",
    "deeper": "<p>One hand-off is worth pointing out: the forecast is read by an optimizer, not by a human. That changes what a good forecast is. A human reading a number tolerates an unstated error bar; a solver does not — it commits resource against whatever number you hand it, with no way of knowing how wrong that number might be.</p>\n<p>That is the same argument as the forecasting chapter's asymmetric-cost point, arriving from the control side instead of the statistics side. Making both arguments together counts for a lot.</p>",
    "check": {
     "question": "In a forecast-optimize-act-observe loop, what plays the role of the controller?",
     "options": [
      "The demand forecast",
      "The constrained optimizer that turns predictions and constraints into an assignment",
      "The monitoring dashboard",
      "The published plan"
     ],
     "answer": 1,
     "explain": "The optimizer is what decides the action. The forecast is the model of the near future, the plan is the action itself, and realised demand is the feedback."
    }
   },
   {
    "id": "ct-5",
    "part": "field",
    "title": "The four design settings of any re-solving loop",
    "viz": "receding-horizon",
    "body": "<p>Nobody is going to ask you to prove stability. They will ask how you would set these four things.</p>\n<ul>\n<li><b>Cadence and staleness.</b> How often do you re-solve? A plan is only as good as the data it was built on, so the real question is how stale the inputs may get. Too slow and the loop cannot see the disturbances that matter. Too fast and you pay compute for a decision nobody can act on yet, and you destabilise the people downstream. What to say: \"I would set the cadence from how fast the inputs actually move, not from how fast the solver can run.\"</li>\n<li><b>Horizon versus commitment.</b> Plan over several periods, commit only the first. The horizon exists so today's decision accounts for tomorrow's consequences; the commitment window is short because the far end of the plan rests on the least reliable data. Get it wrong one way and you are myopic — you strand capacity in the wrong place at the end of every horizon. Get it wrong the other way and you have locked in a plan built on a forecast you have stopped believing. What to say: \"plan long, commit short, and be explicit about which decisions are already locked.\"</li>\n<li><b>Damping and stability, in software terms.</b> The failure is thrash: a plan that changes every time it is re-solved, until the people living under it stop trusting it. Four practical measures, none needing an equation — smooth the inputs so noise alone cannot drive a re-plan; use <b>hysteresis</b> so a marginal signal cannot flip the plan back and forth; penalise deviation from the previously published plan directly in the objective; and <b>warm start</b> the solver from the previous solution, which both shortens the solve and biases it toward a similar plan. What to say: \"people wanting predictable plans is not a nice-to-have, it is a stability requirement, and it belongs in the objective.\"</li>\n<li><b>Model-error monitoring as a re-plan trigger.</b> The loop should re-plan when the world disagrees with the model, not only when a timer fires. Track forecast error against realised outcomes and trigger an off-cycle re-solve when it crosses a threshold. What to say: \"I would make a re-plan an event driven by measured model error, with the scheduled run as the floor rather than the only mechanism.\"</li>\n</ul>",
    "deeper": "<p>Notice that three of the four settings are the same trade-off in different forms: responsiveness against stability. Faster cadence, shorter commitment and weaker damping all make the system react sooner and thrash more. That is the sentence to reach for if pushed on any one of them — you are not tuning four independent parameters, you are choosing one point on one trade-off.</p>\n<p>The fourth setting is the one that breaks the trade-off rather than sitting on it: triggering on measured model error lets the loop be slow when the world is calm and fast when it is not, which is strictly better than picking one fixed rate.</p>",
    "check": {
     "question": "People complain that their published plan keeps changing. Which change most directly addresses it?",
     "options": [
      "Improve the forecast model's accuracy",
      "Penalise deviation from the currently published plan in the objective, and add hysteresis so a marginal change cannot flip it back",
      "Lengthen the planning horizon",
      "Re-solve more frequently so the plan is always current"
     ],
     "answer": 1,
     "explain": "Churn is a stability problem, so the fix belongs in the objective and in the switching rule. Re-solving more often makes it worse, and a better forecast does not stop the plan from moving."
    }
   },
   {
    "id": "ct-6",
    "part": "field",
    "title": "The formal names: MPC and PID",
    "body": "<p>That re-solving loop has a name in the literature: <b>model predictive control</b>, or <b>MPC</b>. In one paragraph: use a model to predict the system over a short window ahead — the horizon — solve a constrained optimization for the best sequence of actions across that window, apply only the <b>first</b> action, throw the rest away, and re-solve next tick on fresh measurements. Discarding the tail and re-solving is called a <b>receding horizon</b>, and it is the one term worth carrying into a conversation.</p>\n<p>Recognize it; do not study it. The parts that make MPC a research field — proving stability, terminal cost design, robust and tube variants that plan against a set of possible models rather than one — are not what a decision-systems interview is testing, and reciting them invites a question you cannot win.</p>\n<p>One sentence on the other name you will hear. <b>PID</b> is the classic reactive hardware controller — it acts on the error now, on accumulated past error, and on how fast the error is changing — and the only thing worth knowing about it here is that it cannot express constraints. You cannot tell a PID controller \"never exceed 40 vehicles\". That limitation is precisely why optimization-based control exists. Nothing beyond that.</p>",
    "deeper": "<p>If pushed on what happens when the model is wrong, the vocabulary is <b>model mismatch</b>, and the formal family of answers is robust or tube MPC. Name it as a term you recognise and have not implemented, then give the practical version: re-solve more often, keep slack in the constraints that are really soft, and monitor for drift.</p>\n<p>That is the right trade-off in an interview. Naming the formal family costs one sentence and buys credibility; pretending to it costs the round.</p>",
    "check": {
     "question": "In a receding-horizon scheme, how much of the optimized action sequence is executed?",
     "options": [
      "The whole sequence, then you re-solve at the end of the horizon",
      "Only the first action, then you re-solve on fresh measurements",
      "The first half, to reduce solver load",
      "As much as the constraints allow"
     ],
     "answer": 1,
     "explain": "Applying only the first action and re-solving is what turns an open-loop plan into a closed-loop controller."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "ct-a2",
    "type": "drill",
    "title": "Exercise: the four design settings",
    "prompt": "From memory: name the four design settings of a re-solving decision loop. For each, say in one line what it is, what breaks if you get it wrong, and the sentence you would actually say on the call.",
    "timeboxSec": 120,
    "rubric": "Must-haves, all four: (1) cadence and input staleness — too slow and the loop cannot see the disturbances that matter, too fast and it churns a plan people are trying to live by, with the cadence set by how fast the inputs move rather than how fast the solver runs; (2) horizon versus commitment — plan long, commit short, failing either as myopia at the end of every horizon or as locking in a forecast you have stopped believing; (3) damping — thrash and churn, mitigated by smoothing the inputs, hysteresis so a marginal change cannot flip the plan back, penalising deviation from the published plan inside the objective, and warm starting from the previous solution; (4) model-error monitoring as a re-plan trigger, tracking forecast error against realised outcomes. Bonus: recognises that cadence, commitment and damping are one responsiveness-versus-stability trade-off rather than three independent parameters. Common mistakes: naming settings with no failure mode attached; treating churn as a user-interface complaint rather than an objective-function problem; proposing to fix thrash by re-solving more often. {{HONESTY}}",
    "model": "<p>Cadence and staleness: how often you re-solve, set by how fast the inputs really move rather than by how fast the solver runs — too slow and the disturbances that matter are invisible to the loop, too fast and you churn a plan people are trying to live by. Horizon versus commitment: plan over several periods so today's decision accounts for tomorrow, commit only the near one — get it wrong and you either strand capacity at the end of every horizon or lock in a forecast you have stopped believing. Damping: the fix for thrash — smooth the inputs, add hysteresis so a marginal signal cannot flip the plan back, penalise deviation from the published plan inside the objective, and warm start the solver from the previous solution. Model-error monitoring: track forecast error against realised demand and trigger an off-cycle re-solve when it crosses a threshold, so a re-plan is an event and not only a timer. And the first three are one trade-off, not three: responsiveness against stability.</p>"
   },
   {
    "id": "ct-a4",
    "type": "followup",
    "title": "Follow-up question: \"How do you stop the plan thrashing when the forecast is noisy?\"",
    "prompt": "An interviewer asks: \"How do you stop the plan thrashing when the forecast is noisy?\" Answer in first person. Name the measures concretely, say where the requirement comes from, and be clear that re-solving harder is not the answer.",
    "timeboxSec": 90,
    "rubric": "Must-haves: (1) treats churn as a stability requirement rather than a cosmetic complaint, and says the requirement comes from the people living under the plan; (2) names at least three concrete measures — smoothing or filtering the input signal, hysteresis so a marginal change cannot flip the plan back, an explicit penalty on deviation from the currently published plan inside the objective, warm starting the solver from the previous solution; (3) states that re-solving more often makes churn worse, not better; (4) distinguishes a change the loop should absorb quietly from one that genuinely warrants re-publishing, ideally via a trigger on measured model error; (5) stays operational rather than theoretical. Common mistakes: proposing a more accurate forecast as the fix; treating stability as a preference rather than something that belongs in the objective; claiming that a more frequent re-solve reduces churn. {{HONESTY}}",
    "model": "<p>\"I would treat that as a stability requirement rather than a complaint, because it comes from the people living under the plan. If it moves every time it is re-solved, they stop trusting it, and an untrusted plan is worse than a slightly suboptimal one.</p>\n<p>Four measures, roughly in order of how cheap they are. Smooth the input so noise alone cannot drive a re-plan. Add hysteresis — require a bigger swing to move back than you required to move in the first place, so a marginal signal cannot flip-flop the plan. Put an explicit penalty on deviation from the currently published plan into the objective, which is the honest version: if stability has value, it should be priced, not added afterwards. And warm start the solver from the previous solution, which shortens the solve and biases it toward a similar answer.</p>\n<p>And re-solving more often is not the fix — it is usually the cause. What I would add instead is a trigger: monitor forecast error against realised demand, and re-publish when the model is genuinely wrong rather than on every tick. And the cadence should come from the human requirement — the hour by which the plan has to be reliable — rather than from how fast the solver can run.\"</p>"
   },
   {
    "id": "ct-a1",
    "type": "explain",
    "title": "Exercise: explain the loop without the words control theory",
    "prompt": "Explain to a strong engineer who has never studied control how a forecast-driven planning system works as a loop. Four sentences. You may not use the words \"control theory\", \"MPC\" or \"receding horizon\" — describe the mechanism instead.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) a model predicts demand over a window ahead, and the reason given is that resources have to be placed before the work arrives; (2) a solve turns that prediction plus the real constraints into a concrete plan; (3) only the near part of the plan is committed, because the far end rests on the least reliable numbers; (4) the system measures what actually happened and re-solves on it, and the re-solving is what makes it a loop. Common mistakes to flag: reaching for the banned jargon anyway; describing a purely reactive system that only responds after the fact; omitting the constraints, which are the whole reason an optimizer is there; omitting the re-solve, which is the whole reason it is a loop; running well over four sentences. {{HONESTY}}",
    "model": "<p>You predict how much demand is coming over the next stretch of time — the next day, the next few hours — because you have to place people or vehicles before the work arrives, not after it. You hand that prediction, plus everything that is genuinely fixed — how many vehicles exist, who is available, which windows must be met — to a solver, and it produces a concrete plan. You commit the immediate part of that plan and leave the rest provisional, because the far end was built on the least reliable numbers. Then you measure what actually happened, feed it back into the prediction, and solve again — and it is the re-solving, not the solving, that lets the system survive being wrong.</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "rl-judgment": {
  "id": "rl-judgment",
  "title": "Reinforcement learning",
  "day": 2,
  "level": "warning",
  "levelLabel": "A judgement call before it is an algorithm",
  "why": "Reinforcement learning is mostly a judgement question. Knowing when a solver or a contextual bandit is the better answer, and what it takes to test a policy before it touches live traffic, is worth more here than knowing the algorithms.",
  "learn": [
   {
    "id": "rl-0",
    "part": "field",
    "title": "Key terms",
    "body": "<ul>\n<li><b>Agent</b> — the thing making decisions. <b>Environment</b> — everything else, which responds to those decisions. <i>A dispatcher is the agent; the city, the couriers and the customers are the environment.</i></li>\n<li><b>State</b> — what the agent knows about the situation when it decides. <i>Open orders, available couriers, current positions.</i></li>\n<li><b>Action</b> — what the agent chooses. <i>Which courier takes which order.</i></li>\n<li><b>Reward</b> — a single number the environment returns saying how good that turned out. <i>Completed deliveries minus cost minus lateness.</i></li>\n<li><b>Policy</b> — the rule that maps a state to an action. The policy <i>is</i> the thing RL learns. <i>\"If more than five orders are waiting, dispatch the nearest courier immediately.\"</i></li>\n<li><b>Episode</b> — one run from a start state to an end. <i>One delivery shift; one game.</i> Some problems are continuing and have no natural end.</li>\n<li><b>Return</b> — the total reward accumulated over an episode. <b>Discount factor</b> — how much you value reward later versus now, which is what makes an infinite future finite.</li>\n<li><b>MDP (Markov decision process)</b> — the standard mathematical framework for RL: states, actions, rewards, and transition probabilities, with the <b>Markov</b> assumption that the current state contains everything you need to decide — the past adds nothing beyond it. <i>If knowing where all the couriers are now is enough, and how they got there does not matter, the problem is Markov.</i></li>\n<li><b>Value function</b> — an estimate of the expected future return from a state (or from a state-action pair). It is how an agent judges a situation before the reward arrives.</li>\n<li><b>Value-based methods</b> — learn the value function and act greedily with respect to it. <i>Q-learning, DQN (deep Q-network).</i> <b>Policy-gradient methods</b> — learn the policy directly by nudging it toward higher return. <i>REINFORCE, PPO (proximal policy optimization).</i> <b>Actor-critic</b> — do both at once: an actor picks actions, a critic estimates value. <i>A2C (advantage actor-critic), SAC (soft actor-critic), PPO in practice.</i></li>\n<li><b>Exploration versus exploitation</b> — to find out whether a different action is better you have to sometimes take it, and that costs you real outcomes now. Balancing the two is the defining operational problem of RL.</li>\n<li><b>Bandit</b> — the simplest version: repeatedly choose one of several options and observe the reward for that option only. A <b>contextual bandit</b> adds a context you see before choosing. Crucially, in a bandit your choice does not change the next situation you face.</li>\n<li><b>Partial feedback</b> — you only ever observe the outcome of the action you took, never the counterfactual. This is what separates bandits and RL from supervised learning, where every example carries its own label.</li>\n<li><b>Online RL</b> — the agent learns by acting in the real environment. <b>Offline RL</b> (also called batch RL) — the agent learns only from previously logged decisions and never explores live.</li>\n<li><b>Off-policy evaluation (OPE)</b> — estimating how well a <i>new</i> policy would have performed, using data collected under an <i>old</i> policy. It requires knowing the probability with which each logged action was chosen.</li>\n<li><b>Propensity</b> — that logged probability. No propensities, no credible off-policy evaluation.</li>\n<li><b>Reward hacking</b> — the policy maximising the number you wrote instead of the outcome you wanted. <i>Reward completed deliveries and it quietly starves the hard ones.</i></li>\n<li><b>Libraries</b> — <b>Stable-Baselines3</b> (clean reference implementations of PPO, SAC, DQN), <b>Ray RLlib</b> (distributed, production-oriented), <b>Gymnasium</b> (the standard environment interface), <b>Vowpal Wabbit</b> (the standard tool for contextual bandits at scale), <b>d3rlpy</b> (offline RL).</li>\n</ul>\n<p>Primer references if any of these is genuinely new: <code>incompleteideas.net/book/the-book.html</code> (Sutton and Barto, free and canonical — chapters 1 to 3 are enough for this conversation), <code>spinningup.openai.com</code> for a practical tour of policy-gradient methods, and <code>vowpalwabbit.org</code> for the contextual-bandit approach used in industry.</p>",
    "deeper": "<p>One clarification that trips people up: the \"Markov\" assumption is about the <i>state representation</i>, not about the world. Almost no real problem is Markov in its raw observations; you make it approximately Markov by putting enough history into the state — recent demand, time since last event, current backlog. When someone says \"is this an MDP?\", the honest answer is usually \"it can be made into one, and how much history I have to stuff into the state is a design decision.\"</p>",
    "check": {
     "question": "What does the Markov assumption actually require?",
     "options": [
      "That the environment is deterministic",
      "That the current state contains everything needed to decide well — the earlier history adds nothing beyond it",
      "That rewards arrive immediately after each action",
      "That the same action always produces the same reward"
     ],
     "answer": 1,
     "explain": "Markov is a property of the state representation: given the state, the past is irrelevant to the future. It says nothing about determinism or reward timing."
    }
   },
   {
    "id": "rl-1",
    "part": "field",
    "title": "What RL actually contains, and which parts industry uses",
    "body": "<p>Reinforcement learning is a large research field of which industry uses a narrow slice. Knowing which slice, and why, is what actually counts.</p>\n<ul>\n<li><b>Tabular methods</b> — Q-learning, SARSA, on small discrete state spaces. Teaching material, essentially never production.</li>\n<li><b>Deep value methods</b> — DQN and descendants, for discrete action spaces with rich observations. Famous from games; sample-hungry and brittle to tune.</li>\n<li><b>Policy-gradient and actor-critic</b> — PPO, SAC, A2C. The main practical choices for continuous control and robotics, and the family behind RLHF (reinforcement learning from human feedback) for language models.</li>\n<li><b>Bandits and contextual bandits</b> — by a wide margin the most common industrial use: recommendation, ranking, pricing, ad selection, treatment assignment.</li>\n<li><b>Offline RL and off-policy evaluation</b> — the sub-field that exists because most businesses cannot let an untrained policy explore against real customers or real money. This is the part that matters most for any regulated or money-touching system.</li>\n<li><b>Model-based RL</b> — learn a model of the environment and plan inside it. Conceptually the bridge to model predictive control: both plan against a model over a horizon, one learns the model and the other is usually given it.</li>\n</ul>\n<p>The honest industry summary: RL that learns online in production is rare and expensive. Contextual bandits are common. Offline policy learning with careful evaluation is growing. And an enormous number of problems described as RL turn out to be prediction problems with a decision rule attached.</p>",
    "deeper": "<p>Worth knowing how this connects to optimization, because it is directly relevant to any decision-systems role. Model-based RL and model predictive control are near neighbours: both use a model to look ahead over a horizon and choose an action. The difference is where the model comes from and how the action is chosen — learned versus specified, and by a learned policy versus by solving a constrained optimization each tick.</p>\n<p>Saying that out loud shows you see the field as one connected subject rather than a list of techniques.</p>",
    "check": {
     "question": "Which RL family is most widely used in production industrial systems?",
     "options": [
      "Deep Q-networks trained online against live traffic",
      "Contextual bandits",
      "Tabular Q-learning",
      "Model-based planning with learned dynamics"
     ],
     "answer": 1,
     "explain": "Contextual bandits dominate industrial use because most decisions have no state carry-over, and they are far cheaper to build, evaluate and trust."
    }
   },
   {
    "id": "rl-2",
    "part": "field",
    "title": "What actually separates RL from supervised learning",
    "viz": "mdp-loop",
    "body": "<p>Only one thing really does: <b>delayed consequence</b>. The action you take now changes the situation you face later, so you cannot label each decision independently and train on it. That is the <b>credit assignment</b> problem — which of the last fifty actions earned the reward you just received?</p>\n<p>Delayed consequence also forces the exploration-versus-exploitation trade: to learn whether a different action is better, you have to sometimes take it, and that costs real outcomes.</p>\n<p>The practical test for whether a problem is genuinely an RL problem: <i>does taking this decision change the distribution of decisions you face next?</i> Repositioning supply, holding capacity in reserve, throttling, and setting prices all do. Scoring an incoming transaction mostly does not.</p>",
    "deeper": "<p>There is a second, quieter difference: the data is generated by the policy itself. In supervised learning the dataset is fixed and the model is fitted to it; in RL, changing the policy changes the data you will collect next, which is why RL training is unstable in ways supervised training is not.</p>\n<p>That is also why offline evaluation is hard: the logged data was generated by a different policy than the one you want to assess, and the further apart they are, the less the logs can tell you.</p>",
    "check": {
     "question": "Which single property most cleanly distinguishes an RL problem from a supervised one?",
     "options": [
      "The data arrives as a stream rather than a batch",
      "Actions taken now change the states and data you see later",
      "The model is a neural network rather than a tree ensemble",
      "There is no ground-truth label available"
     ],
     "answer": 1,
     "explain": "Delayed consequence — your action changing your future data distribution — is the defining property. Streaming data, model class and missing labels all occur in supervised settings too."
    }
   },
   {
    "id": "rl-3",
    "part": "field",
    "title": "Contextual bandits: the cheap middle option",
    "viz": "bandit-vs-rl",
    "body": "<p>A <b>contextual bandit</b> is RL with the state carry-over removed: you see a context, pick one of several actions, observe the reward for that action only, and the next context is unrelated to what you chose.</p>\n<p>This matters because most industry problems people call RL are actually bandits, and bandits are dramatically cheaper to build, evaluate and trust. Pointing out that difference before you are asked shows real judgement.</p>\n<p>The standard algorithms are worth knowing by name: <b>epsilon-greedy</b> (explore at random a fixed fraction of the time), <b>Thompson sampling</b> (sample from your posterior belief about each action's value and act on the sample), and <b>upper confidence bound</b> (act optimistically in proportion to your uncertainty). Thompson sampling is the usual production default because it is simple and behaves well.</p>",
    "deeper": "<p>The reason bandits are still not supervised learning is <b>partial feedback</b>: you only ever see the outcome of the action you took, never the counterfactual. That is why bandit systems need logged propensities — the probability with which each action was chosen — so you can reweight logged data and estimate what a different policy would have earned.</p>\n<p>Designing the logging correctly at the start is the difference between a system you can improve and one you can only replace. That is an infrastructure decision, not a modelling one, which makes it exactly the kind of thing a Staff engineer should raise.</p>",
    "check": {
     "question": "You choose which of three ranking layouts to show each user; the choice has no effect on what the next user sees. This is best modelled as:",
     "options": [
      "Full reinforcement learning with a discount factor",
      "A contextual bandit",
      "Standard supervised classification with complete labels",
      "An offline RL problem requiring a simulator"
     ],
     "answer": 1,
     "explain": "No state carry-over between decisions, but you only observe the reward for the action you took — that is exactly a contextual bandit."
    }
   },
   {
    "id": "rl-4",
    "part": "field",
    "title": "Offline RL and off-policy evaluation",
    "viz": "shadow-traffic",
    "body": "<p>You cannot let an untrained policy explore freely against real customers or real money. So the production path is <b>offline</b>: learn from logged decisions, then use <b>off-policy evaluation</b> to estimate what a new policy would have earned on that logged data, before it touches anything live.</p>\n<p>The standard estimators to name are <b>inverse propensity scoring</b> (reweight each logged outcome by how much more or less likely the new policy was to take that action), <b>self-normalised IPS</b> (the same, with the weights normalised to reduce variance), and <b>doubly robust</b> estimators (combine importance weighting with a learned reward model, so you get a good estimate if either component is right). You do not need their derivations — you need to know they exist and that they need logged action probabilities to work at all.</p>",
    "deeper": "<p>The failure mode to name is <b>distribution shift</b>: off-policy estimates get unreliable exactly where the new policy does something the old one rarely did, which is usually the interesting part. Importance weights explode, variance blows up, and the estimate quietly stops meaning anything.</p>\n<p>So the honest deployment story is a series of stages, not one big check: offline evaluation, then <b>shadow mode</b> — the candidate policy runs on real traffic and its decisions are logged but never acted on — then a small guarded ramp with a kill switch you have actually tested.</p>",
    "check": {
     "question": "Why does off-policy evaluation need the probability with which each logged action was chosen?",
     "options": [
      "To detect data quality problems in the logs",
      "To reweight logged outcomes so they estimate a different policy's value",
      "To set the discount factor correctly",
      "To decide the exploration rate for the new policy"
     ],
     "answer": 1,
     "explain": "Logged data came from the old policy. Reweighting by how likely each action was under old versus new policy is what lets logged outcomes stand in for the new policy's value."
    }
   },
   {
    "id": "rl-5",
    "part": "field",
    "title": "How to choose: solver, bandit, or RL",
    "body": "<p>Use <b>constrained optimization — LP, MIP, CP-SAT (Google's constraint-programming solver; see <a href=\"#or-tooling\">Optimisation and operations research</a>) or a routing engine</b> when the constraints are hard and known and the objective is writable. You get a feasible, auditable answer, and with an exact method a stated optimality gap.</p>\n<p>Reach for <b>bandits or RL</b> when the objective cannot be written in closed form, when dynamics are unknown or non-stationary, or when today's decision changes tomorrow's state.</p>\n<p>In production the answer is nearly always <b>both</b>: an optimizer in the decision loop, with learned models supplying its parameters — demand forecast, ETA (estimated time of arrival), acceptance probability, handling time. Learning belongs in the numbers the optimizer takes on faith, not in replacing the optimizer.</p>",
    "deeper": "<p>The strongest version of the argument: a solver gives you a feasibility guarantee, and you should be very reluctant to trade a feasibility guarantee for a learned policy. If vehicle counts and time windows are hard, a policy that \"usually\" respects them is not a better system, it is a worse one with better demos.</p>\n<p>Where learning genuinely earns its place inside that architecture is in the parameters — and, occasionally, in a heuristic that decides how to decompose or warm-start a large solve, which is a much less risky place to put a learned component.</p>",
    "check": {
     "question": "Constraints are hard and fully specified, the objective is writable, and decisions are near-independent. What should you build first?",
     "options": [
      "An offline RL policy trained on logged decisions",
      "A constrained optimizer, with learned models supplying its parameters",
      "A contextual bandit over candidate assignments",
      "A deep policy network with a shaped reward"
     ],
     "answer": 1,
     "explain": "Hard known constraints plus a writable objective is the classic case for a solver. Learning belongs in the parameters the solver consumes, not in replacing the feasibility guarantee."
    }
   },
   {
    "id": "rl-6",
    "part": "field",
    "title": "Reward design, and who owns it",
    "body": "<p>If asked for a reward function, do not invent one confidently. Reason out loud about the terms — completion, latency, cost, fairness — and then name <b>reward hacking</b> as the real risk: any term you can measure, a policy will over-optimize, usually by finding the degenerate case you did not think of.</p>\n<p>Then say the thing that marks out experience: the weighting between those terms is a business decision, and you would want the business to own it explicitly rather than have it buried in a config file by an engineer.</p>",
    "deeper": "<p>A concrete hacking example to have ready: reward completed deliveries and the policy quietly starves hard deliveries — remote addresses, awkward time windows — because easy ones score faster. The aggregate metric improves while the worst-served customers get worse.</p>\n<p>The mitigation you can speak to honestly is measurement, not clever reward shaping: track the tail, not just the mean, and alert on it. Note that this is exactly the same failure as an optimizer with a badly chosen objective: reward hacking and objective misspecification are the same problem in two vocabularies.</p>",
    "check": null
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "rl-a1",
    "type": "drill",
    "title": "Exercise: when to reach for RL, and when not to",
    "prompt": "From memory, list three conditions under which you would reach for a bandit or RL, and three under which you would use a solver instead. One line each, no explanation. Then say in one line what a contextual bandit is and why it is usually the right first step.",
    "timeboxSec": 120,
    "rubric": "Reach-for-RL side must include at least three of: objective not writable in closed form; unknown or hard-to-model dynamics; non-stationary environment; today's decision changes tomorrow's state; only partial feedback available. Solver side must include at least three of: hard and fully specified constraints; writable objective; need for a feasibility guarantee; need for auditability; near-independent decisions; need for a stated optimality gap. The bandit line must state the no-carry-over property and the cheaper evaluation and operational risk. Common mistakes: listing \"lots of data\" or \"deep learning available\" as an RL trigger; failing to name feasibility guarantee on the solver side; describing a bandit as \"RL with one step\" without the no-carry-over property; giving vague conditions that do not discriminate between the two. {{HONESTY}}",
    "model": "<p>RL or bandit: objective cannot be written down cleanly; dynamics unknown or non-stationary; today's action changes tomorrow's state. Solver: constraints hard and fully specified; objective writable and agreed with the business; you need a feasibility guarantee and an auditable decision with a stated gap.</p>\n<p>Contextual bandit: you see a context, choose one action, observe the reward for that action only, and your choice does not change the next context — which makes it far cheaper to evaluate and operate than full RL, so it is the right first step whenever the carry-over is genuinely absent.</p>"
   },
   {
    "id": "rl-a5",
    "type": "followup",
    "title": "Follow-up question: \"How would you evaluate a policy before deploying it?\"",
    "prompt": "An interviewer asks: \"How would you evaluate a policy before you deploy it?\" Answer in first person, in about 90 seconds. Give the stages in order, and name the prerequisite that has to be in place before any of it works.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) offline, off-policy evaluation on logged data as the first stage, with at least one estimator named - inverse propensity scoring, self-normalised IPS, or doubly robust; (2) the prerequisite stated explicitly and early: logged action probabilities, which have to be designed in before anyone asks for the policy, and which is a system-design decision rather than a modelling one; (3) the distribution-shift caveat - estimates degrade exactly where the new policy does something the old one rarely did, which is usually the interesting part, with importance weights exploding and variance following; (4) shadow mode next: the candidate runs on real traffic, its decisions are logged and discarded, and the incumbent still acts; (5) a small guarded ramp with abort criteria agreed before launch and a kill switch that has actually been tested; (6) the stages given in order rather than as one big check, with the reason each stage exists. Bonus: names what those stages still cannot catch - long-horizon effects, feedback loops, and anything whose outcome has not arrived yet. Common mistakes: jumping straight to a live A/B test; naming estimators without the propensity prerequisite; omitting the kill switch or the abort criteria; treating shadow mode and canary as the same stage. {{HONESTY}}",
    "model": "<p>\"A series of stages, and I would not skip any. First, offline: evaluate the candidate against logged decisions with an off-policy estimator - inverse propensity scoring, or doubly robust if I also have a reward model I trust. The thing I would say before any of that, though, is the prerequisite: this only works if we logged the probability with which each action was chosen. If nobody designed that in, the honest answer is that the logs cannot tell us what a different policy would have earned, and the first piece of work is the logging, not the policy.</p>\n<p>The caveat on the offline number is distribution shift. The estimate is reliable where the new policy behaves like the old one and unreliable exactly where it does something new, which is usually the part we care about - the importance weights blow up and the variance goes with them. So I treat the offline stage as a filter for obviously bad policies, not as the final answer.</p>\n<p>Second, shadow mode: the candidate scores real traffic with real features, its decisions are logged and thrown away, and the incumbent still acts. That is the cheapest place to find skew, latency surprises and distribution shocks, at zero customer risk. Third, a small guarded ramp with abort criteria written down before it starts and a kill switch someone has actually pulled in a drill. And I would be clear about what none of this catches: long-horizon effects, feedback loops, and anything whose outcome has not arrived yet.\"</p>"
   },
   {
    "id": "rl-a4",
    "type": "explain",
    "title": "Exercise: contextual bandits versus full RL",
    "prompt": "In four sentences, explain the difference between a contextual bandit and full reinforcement learning, and say why a bandit is usually the right first step in industry.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) bandits have no state carry-over — the action does not change the next context; (2) both share partial feedback: you only see the reward for the action you took; (3) full RL adds delayed consequence and therefore credit assignment across a sequence; (4) bandits are cheaper to build, evaluate and trust, so they are the right first step. Common mistakes: describing a bandit as \"RL with one step\" without naming the no-carry-over property; confusing partial feedback with missing labels; claiming bandits need no offline evaluation. {{HONESTY}}",
    "model": "<p>A contextual bandit sees a context, picks one action, and observes the reward for that action only — but what it picks does not change the next context it sees. Full RL adds exactly that carry-over: today's action shapes tomorrow's state, which brings credit assignment across a whole sequence and a much harder evaluation problem. Both share partial feedback, so both need logged action probabilities to reason counterfactually about a different policy. In industry the bandit is usually the right first step because most decisions genuinely have no carry-over, and you get most of the value at a fraction of the evaluation and operational risk.</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "genai-platform": {
  "id": "genai-platform",
  "title": "Generative AI in production",
  "day": 2,
  "level": "good",
  "levelLabel": "Often gets its own 30-minute round",
  "why": "Generative AI platform work now turns up in ML engineering interviews, and large companies have published a lot about how they run it. If you have real LLM (large language model) platform experience it is tempting to lead with it. Usually you should not. Most ML engineering roles still describe classical machine learning as the day-to-day work, so hold this back for the craft demonstration rather than opening with it. There is a second reason to know the material well. Many interview processes now include a separate 30-minute AI round where the questions are conceptual and quick: retrieval-augmented generation, vector databases and similarity, system prompts, temperature, agents, when to use AI and when not to, how misuse is prevented, and in some accounts solving a problem live with an AI tool. That round rewards short, correct, confident answers rather than depth. So you need two gears: held back in the craft demonstration, quick and fluent in the AI round.",
  "learn": [
   {
    "id": "gp-0",
    "part": "field",
    "title": "Key terms",
    "body": "<p>Enough to be precise, no more. Depth beyond this is not what this round is testing.</p>\n<ul>\n<li><b>Token</b> (<a href=\"#llm-inference-engineering/lie-f0\">What a token actually is</a>) - the unit a language model reads and writes, roughly a word fragment. Cost and latency are both measured per token.</li>\n<li><b>Prefill and decode</b> (<a href=\"#llm-inference-engineering/lie-f1\">How a token is generated: prefill versus decode</a>) - processing the input prompt, then generating output tokens one at a time.</li>\n<li><b>KV cache</b> (<a href=\"#llm-inference-engineering/lie-f2\">The KV cache: the memory formula</a>) - short for key-value cache: the stored state for tokens already processed. It is usually what limits how many requests one server can hold at once, which is why it appears in a platform conversation at all.</li>\n<li><b>Time to first token, tokens per second</b> (<a href=\"#llm-inference-engineering/lie-f9\">Measuring, cost, and observability</a>) - latency until output starts, and generation speed after it starts. The first is what a user reads as responsiveness, the second as fluency.</li>\n<li><b>Continuous batching</b> (<a href=\"#llm-inference-engineering/lie-f3\">Continuous batching</a>) - adding and removing sequences from a running batch as they start and finish.</li>\n<li><b>Fine-tuning</b> (<a href=\"#llm-fine-tuning-and-alignment/lft-f1\">When to fine-tune, when to prompt, when to retrieve</a>) - further training a base model on task data.</li>\n<li><b>Prompt engineering</b> (<a href=\"#genai-platform/gp-f2b\">Prompt engineering: the parts of a prompt, the techniques, and how a prompt is run in production</a>) - getting the behaviour you want purely through the text sent to a fixed model, with no training at all.</li>\n<li><b>RAG</b> - retrieval-augmented generation: retrieve relevant documents and put them in the prompt, so the model answers from supplied context rather than from memorised parameters.</li>\n<li><b>Guardrail</b> - a check on input or output enforcing policy: prompt injection defence, sensitive-data redaction, refusal rules, output validation.</li>\n<li><b>Model catalog</b> - the curated set of models available to teams, with metadata and access rules.</li>\n<li><b>Router</b> - the component choosing which model serves a request, by cost, capability or availability.</li>\n<li><b>Golden set</b> (<a href=\"#rag-and-agents/raa-f5\">Evaluating RAG: recall, faithfulness, and a golden set</a>) - a fixed evaluation set of prompts with agreed expected behaviour.</li>\n<li><b>LLM-as-judge</b> (<a href=\"#llm-fine-tuning-and-alignment/lft-f5\">Evaluating a fine-tuned model</a>) - using an LLM (large language model) to grade outputs.</li>\n</ul>",
    "deeper": "<p>The reason those two facts matter operationally: they mean an LLM service scales differently from a tree-model service. Throughput does not degrade gracefully - once the KV cache is full, new requests queue rather than slow slightly, so the jump in latency is sudden.</p>\n<p>It also changes what you monitor. For a gradient-boosted model you watch CPU and latency. For an LLM you watch cache occupancy, queue depth, and the split between time to first token and tokens per second, because those three tell you which side of the workload is saturated.</p>",
    "check": {
     "question": "Why does LLM serving throughput tend to drop suddenly rather than degrade smoothly?",
     "options": [
      "Because model weights are reloaded per request",
      "Because once the KV cache is full new requests must queue rather than merely run slower",
      "Because tokenisation is single-threaded",
      "Because GPUs throttle under sustained load"
     ],
     "answer": 1,
     "explain": "Concurrency is bounded by cache memory. Below the limit throughput scales; at the limit requests wait, so latency jumps rather than drifting."
    }
   },
   {
    "id": "gp-f1",
    "part": "field",
    "title": "The cost and latency controls, in order of impact",
    "viz": "llm-serving-batching",
    "body": "<p>If asked how to make an LLM feature affordable and fast, answer in this order. This is roughly the order of impact.</p>\n<ul>\n<li><b>Do not use a large model.</b> The largest available model is rarely the right default. A smaller model, or a fine-tuned smaller model, often matches task performance at a fraction of the cost.</li>\n<li><b>Shorten the prompt.</b> Cost and prefill latency are roughly linear in input length. Retrieved context is usually the largest and least disciplined part of a prompt.</li>\n<li><b>Cache.</b> (<a href=\"#llm-inference-engineering/lie-f7\">Prefix caching</a>) A repeated system prompt can share prefix computation, an identical request can be answered from a result cache, and retrieved passages can be cached too.</li>\n<li><b>Batch continuously.</b> The serving engine's job, and the single biggest throughput win on fixed hardware (<a href=\"#llm-inference-engineering/lie-f3\">Continuous batching</a>).</li>\n<li><b>Route.</b> Send easy requests to a cheap model and hard ones to an expensive one, with a defined escalation rule. This is the cascade idea from the serving chapter, applied to language models.</li>\n<li><b>Stream.</b> Streaming output does not reduce cost, but it changes perceived latency dramatically because time to first token replaces total generation time as what the user feels.</li>\n<li><b>Bound the output.</b> Generation dominates cost. Capping output length is often the simplest saving available.</li>\n</ul>",
    "deeper": "<p>The strategic version of this argument is the one to make: an LLM feature has a per-request marginal cost that classical ML mostly does not. A gradient-boosted model costs almost nothing per prediction once deployed; a large language model costs real money per call, every call, forever. That changes product design, not just engineering.</p>\n<p>It is also the honest way to describe self-hosted serving that replaces external API calls. The saving comes from moving a per-call variable cost onto fixed infrastructure - and the trade-off is that you now own capacity planning, upgrades and availability for something you previously rented.</p>",
    "check": {
     "question": "Which change usually gives the largest cost reduction for an LLM feature?",
     "options": [
      "Switching to a faster GPU",
      "Using a smaller or fine-tuned model where task performance allows, since cost scales with model size on every single call",
      "Increasing the batch size",
      "Compressing the response payload"
     ],
     "answer": 1,
     "explain": "Model size multiplies every call. Hardware and batching improve efficiency within a model choice; the choice itself dominates."
    }
   },
   {
    "id": "gp-f2",
    "part": "field",
    "title": "The four parts of a GenAI platform: catalog, router, guardrails, evaluation",
    "viz": "guardrails-io",
    "body": "<p>The four platform components of a GenAI (generative AI) capability, and what a mature published version of each looks like.</p>\n<ul>\n<li><b>Model catalog.</b> An LLM-agnostic approach with a curated catalog of third-party commercial, open-source and in-house models, and a stated schedule for revisiting the base model - some teams revisit theirs every few weeks.</li>\n<li><b>Runtime and orchestration.</b> A GenAI runtime described as an operating system: an extensible interface framework, a runtime managing resources, a planner likened to a scheduler, short-term memory and long-term storage, the models themselves, and a pluggable ecosystem of domain-specific capabilities compared to device drivers. The selling point is that observability, governance and cost attribution are provided by the platform itself, so teams do not build these themselves.</li>\n<li><b>Guardrails.</b> Registries and policies enforcing authorisation, data handling, legal, security, privacy and compliance, with agents and tools registered through a plugin registry that manages their lifecycle.</li>\n<li><b>Evaluation.</b> Manual expert evaluation blended with automatic evaluation against a predefined golden set or a prompt and criteria list, an internal leaderboard for new evaluations and models, and dashboards to keep resource costs inside budget when moving to production.</li>\n</ul>\n<p>The pattern to notice is that it is the same platform argument as the classical ML chapter: a paved road with guardrails, catalog and governance built in, so teams get compliance and cost attribution for free. Saying that out loud - that a GenAI platform is the same design philosophy applied to a new model class - is a better contribution than reciting component names.</p>",
    "deeper": "<p>What company sources usually do not say, so you do not overstep: <b>no GPU or inference-hardware detail, no LLM serving latency or cost figures, no token throughput, no model sizes, and no named third-party model vendors.</b> Third-party reports describing internal components do circulate, and should be labelled as third-party if used at all.</p>\n<p>The safe approach: describe the architecture they published, name the date, and say plainly that the operational numbers are not public. When the people interviewing you know the real numbers, being careful with your sources is worth far more than sounding knowledgeable.</p>",
    "check": {
     "question": "What is the safest way to reference an employer's internal GenAI platform in the interview?",
     "options": [
      "Describe its components in detail to show thorough research",
      "Name the architecture briefly with its publication date, and say plainly that operational figures such as latency, cost and hardware are not public",
      "Avoid mentioning it, since anything internal is off limits",
      "Reference recent third-party reporting, since it is more recent"
     ],
     "answer": 1,
     "explain": "Brief, dated, and explicit about the limits of the public record. Reciting components at length reads as memorisation; third-party reports need labelling."
    }
   },
   {
    "id": "gp-f2b",
    "part": "field",
    "title": "Prompt engineering: the parts of a prompt, the techniques, and how a prompt is run in production",
    "body": "<p>Prompt engineering is getting a model to behave the way you want purely through the text you send it, with no training of any kind. It is the cheapest control you have over how a model behaves, it changes in seconds, and it is the first thing to try before <a href=\"#rag-and-agents/raa-f1\">retrieval</a> or <a href=\"#llm-fine-tuning-and-alignment/lft-f1\">fine-tuning</a>. It is also the control people are vaguest about, so being precise about it is worth real credit in an interview.</p>\n<p><b>The parts of a prompt.</b> Almost every production prompt is built from the same five pieces, and naming them is what turns \"I wrote a prompt\" into an engineering description.</p>\n<ul>\n<li><b>System prompt.</b> The instruction placed above the conversation that sets role, constraints and output format for every turn. It is the part that does not change per request.</li>\n<li><b>Instruction.</b> What this particular request wants: the task, in the imperative, stated once and without hedging.</li>\n<li><b>Context.</b> The material the model is meant to answer from - retrieved passages, the record being processed, the conversation so far. This is usually the largest and least disciplined part of a prompt, and it is where cost goes.</li>\n<li><b>Examples.</b> Zero, one or a few worked cases showing the mapping from input to output.</li>\n<li><b>Output contract.</b> The exact shape the answer must take: a schema, a field list, an enumerated set of allowed values, or a stated way to decline.</li>\n</ul>\n<p><b>The techniques, and what each is actually for.</b> They are not interchangeable, and each buys one specific thing.</p>\n<ul>\n<li><b>Zero-shot.</b> The instruction alone. The right default: it is the cheapest, and modern instruction-following models are good at it. Start here and only add if measurement says you need to.</li>\n<li><b>Few-shot.</b> Two to five worked examples in the prompt. What it buys is <i>format</i> and edge-case handling far more than knowledge - it shows the model what a correct answer looks like, including the awkward cases. The cost is tokens on every single call, and examples that quietly become out of date.</li>\n<li><b>Chain of thought.</b> Asking the model to work through intermediate steps before giving an answer. It helps on arithmetic, multi-step reasoning and anything with a chain of conditions; it costs output tokens and latency, and on a model already trained to reason internally it can be redundant or even harmful. If the steps are not meant to reach the user, ask for them in a field the application strips.</li>\n<li><b>Role and constraint framing.</b> Telling the model what it is and what it may not do - \"you answer only from the passages provided; if they do not contain the answer, say so\". The valuable half is the constraint, not the role.</li>\n<li><b>Decomposition.</b> Splitting one hard prompt into a short chain of simple ones - extract, then classify, then format. Each step is separately testable and separately cheap, and a failure becomes attributable instead of mysterious. This is usually the right answer when a single prompt is being patched repeatedly.</li>\n<li><b>Self-check.</b> A second pass in which the model is asked to check its own answer against the stated rules. It catches format and omission errors well and factual errors poorly, because a model that was confidently wrong once is usually confidently wrong twice.</li>\n</ul>\n<p><b>What does not work, and is worth saying so.</b> Politeness, insistence and threats do nothing measurable. Vague quality words - \"be accurate\", \"be thorough\" - do nothing either, because they name no test the model can apply. Piling on instructions until one of them sticks produces a prompt nobody can safely edit later. And a longer prompt is not a better one: every added token costs money on every call and gives the model more to lose track of.</p>\n<p><b>Running a prompt in production.</b> This is the part that separates a platform answer from a hobbyist one.</p>\n<ul>\n<li><b>A prompt is a versioned file, not a setting.</b> It lives in source control, it is reviewed, it ships with a version number, and it can be rolled back on its own without redeploying the service. A prompt edited live in a console is an unreviewed production change.</li>\n<li><b>A prompt change is evaluated, not eyeballed.</b> Run the candidate against a fixed <a href=\"#rag-and-agents/raa-f5\">golden set</a> and compare, because a change that fixes the case in front of you very often breaks two you were not looking at.</li>\n<li><b>A prompt is tied to a model version.</b> Prompts are tuned against the model they were written for, so a base-model upgrade is a reason to re-run the evaluation rather than assume it carries over.</li>\n<li><b>A prompt is not a security boundary.</b> Text arriving in the context - a retrieved document, a user message, a tool result - can attempt to override the instructions above it. That is prompt injection, and the defences are input and output filtering, tool permissions and output validation, not a firmer instruction (<a href=\"#rag-and-agents/raa-f9\">Guardrails and safety</a>).</li>\n</ul>",
    "deeper": "<p>The measurement point is the one worth pressing hardest, because it is where most teams are weakest. Without a fixed evaluation set, prompt work is indistinguishable from superstition: someone changes a sentence, the example they were staring at now works, and nobody knows what happened to the other thousand cases. Thirty to a hundred labelled cases covering the ordinary path and the known awkward ones are enough to make prompt changes reviewable, and building that set is usually a better use of a week than another round of rewording.</p>\n<p>The second point is about cost, and it follows directly from the parts list. Everything you add to a prompt - the examples, the retrieved context, the reasoning you asked for - is paid for on every call, forever. That makes prompt length a design decision with a running bill attached, and it is the reason few-shot examples and long retrieved passages are the first places to look when a feature turns out to be too expensive.</p>",
    "check": {
     "question": "A team keeps adding sentences to one long prompt whenever a new failure appears, and each fix seems to break something else. What is the change most likely to help?",
     "options": [
      "Raise the temperature so the model explores more phrasings",
      "Split the task into a short chain of simple prompts - extract, then classify, then format - so each step is separately testable and a failure is attributable to one of them",
      "Add more few-shot examples to the same prompt",
      "Ask the model politely to be more accurate"
     ],
     "answer": 1,
     "explain": "A prompt that is patched repeatedly is doing too many jobs at once, which is why fixes interact. Decomposition makes each step separately testable and cheap; temperature, extra examples and politeness do not address the cause."
    }
   },
   {
    "id": "gp-f3",
    "part": "field",
    "title": "RAG on financial data: the guardrail questions that actually matter",
    "viz": "rag-pipeline",
    "body": "<p>Retrieval-augmented generation on financial or tax data is a common design theme, and it has a specific set of hard questions that have little to do with model quality.</p>\n<ul>\n<li><b>Whose data is in the index.</b> If one customer's documents can be retrieved into another customer's answer, nothing else matters. Retrieval must be scoped by tenant and by permission at query time, not filtered afterwards.</li>\n<li><b>Freshness and correctness of the corpus.</b> Tax rules change. An answer grounded in last year's guidance is confidently wrong, which is worse than being unsure. The corpus needs versioning and an effective-date concept.</li>\n<li><b>Grounding and citation.</b> The answer should be traceable to retrieved passages, and the system should be able to show them. This is both a trust feature and an audit requirement.</li>\n<li><b>Refusal and escalation.</b> A defined behaviour when retrieval returns nothing relevant or confidence is low: say so, or route to a human. In a domain with legal consequence, a confident guess is the worst outcome.</li>\n<li><b>Injection and exfiltration.</b> Retrieved content is untrusted input. Instructions embedded in a document must not be followed, and sensitive fields must not be echoed into an output that leaves the boundary.</li>\n<li><b>Evaluation.</b> A golden set with expected behaviours, including cases where the correct answer is a refusal. Measure retrieval quality separately from generation quality, since most failures are retrieval failures.</li>\n</ul>",
    "deeper": "<p>The evaluation split is worth expanding if asked. Retrieval is measured by whether the right passage was in the retrieved set at all - recall at some cut-off - and generation by whether the answer is faithful to what was retrieved. Conflating them hides the usual truth, which is that the generator was fine and the retriever missed.</p>\n<p>Faithfulness can be checked by an automatic judge, but only after you have checked that judge against human labels on a sample - otherwise you have replaced an unknown error rate with a differently unknown one. Published platform approaches blend manual expert evaluation with automatic evaluation against a golden set, which is the same instinct.</p>",
    "check": {
     "question": "What is the first design question for RAG on customer financial documents?",
     "options": [
      "Which embedding model gives the best retrieval quality",
      "How retrieval is scoped by tenant and permission at query time, so one customer's data can never reach another's answer",
      "How many passages to retrieve per query",
      "Whether to fine-tune the generator on domain data"
     ],
     "answer": 1,
     "explain": "Isolation is a correctness and legal requirement that no amount of retrieval quality compensates for, and post-hoc filtering is not sufficient."
    }
   },
   {
    "id": "gp-f4",
    "part": "field",
    "title": "The AI round: the concept questions, with short correct answers",
    "viz": "vector-similarity",
    "body": "<p>The AI round usually runs about 30 minutes and moves fast. What counts is whether your answer is right and short, not how much you know. Give the definition, then one consequence, then stop.</p>\n<ul>\n<li><b>What is retrieval-augmented generation?</b> Retrieve relevant passages from a corpus and put them in the prompt, so the model answers from supplied context rather than from what it memorised in training. Consequence: you can update knowledge by changing documents rather than by retraining, and most failures are retrieval failures rather than generation failures.</li>\n<li><b>What is a vector database?</b> A store of embeddings with an index for approximate nearest-neighbour search, so you can find the items closest in meaning to a query rather than matching keywords. Consequence: the index is approximate, so there is a trade-off between recall and latency, and adding filters such as tenant or date has to happen inside the search rather than after it.</li>\n<li><b>What is an embedding, and how is similarity measured?</b> A fixed-length vector of numbers positioning an item in a space where nearby means similar to the model that produced it. Similarity is usually cosine - the angle between two vectors, ignoring their length - or a dot product on normalised vectors, which is the same thing. Consequence: similarity is defined by the embedding model, so two different models give incomparable vectors and changing the model means reindexing everything.</li>\n<li><b>How does an image become a vector?</b> A vision model - a convolutional network or a vision transformer - processes the pixels and you take an internal layer's activations, usually the last one before the classifier, as the representation. Models trained to align images and text put both in one space, which is what lets you search images with a sentence. Consequence: the vector encodes whatever the training objective rewarded, so an embedding trained for classification and one trained for retrieval behave differently.</li>\n<li><b>What is a system prompt?</b> The instruction placed above the conversation that sets role, constraints and output format for every turn (<a href=\"#genai-platform/gp-f2b\">Prompt engineering</a>). Consequence: it is a production file - it should be versioned, reviewed and rolled back like code, not edited in a console, and it is not a security boundary, because content in the conversation can attempt to override it.</li>\n<li><b>What does temperature do?</b> It scales the model's output distribution before sampling. Low temperature makes the highest-probability tokens dominate, so output is more deterministic and repetitive; high temperature flattens the distribution, so output is more varied and more likely to be wrong. Consequence: for extraction, classification or anything parsed downstream, use a low temperature; variety is a feature only when the task is generative.</li>\n<li><b>What is an agent?</b> A model given tools and allowed to choose which to call, in a loop, until a goal is met - typically a planner deciding the next step, an executor calling the tool, and memory of what has happened. Consequence: the failure modes are loops, wrong tool choice and compounding errors across steps, so agents need step limits, tool-level permissions and observability per step, and they are the wrong choice when a fixed sequence would do.</li>\n<li><b>How do you know a model is trained enough?</b> When held-out performance stops improving while training performance continues to - that gap is overfitting, and the stopping point is chosen on validation data, never on test. Consequence: \"enough\" is defined against the requirement, not against the loss curve; if the model already meets the metric the product needs, more training costs money for no return.</li>\n<li><b>What is fine-tuning, and when instead of prompting?</b> Continued training on task data (<a href=\"#llm-fine-tuning-and-alignment/lft-f1\">When to fine-tune, when to prompt, when to retrieve</a>). Prompt first, because it is instant and reversible; fine-tune when the behaviour is stable, high volume, and expensive or unreliable to obtain by prompting - or when a smaller fine-tuned model can replace a larger prompted one at much lower cost.</li>\n<li><b>What is hallucination, and what actually reduces it?</b> Confident output not supported by any source. Grounding in retrieved passages, requiring citations, constraining the output format, and giving the model a permitted way to say it does not know. Consequence: a model with no refusal path will always produce something.</li>\n</ul>",
    "deeper": "<p>One structural thing to notice about that list: half of the answers end in an operational consequence rather than in a definition, and that is deliberate. An interviewer in an AI round has heard the definition of retrieval-augmented generation many times that week. What separates an engineer from someone who read a summary is the second sentence - that changing the embedding model means reindexing, that a system prompt is a versioned file rather than a setting, that temperature is a correctness control for anything parsed downstream.</p>\n<p>The other habit worth building is being willing to say the limits of a technique at the same time as the technique. \"Retrieval-augmented generation, and the honest caveat is that most of its failures are retrieval failures, so I would measure retrieval separately\" is one sentence longer and considerably more convincing than the definition alone.</p>",
    "check": {
     "question": "A team parses the model's output as JSON downstream and occasionally gets malformed responses. Which single setting is most likely wrong?",
     "options": [
      "The embedding model is mismatched with the index",
      "Temperature is too high, flattening the sampling distribution so lower-probability tokens are chosen and the format drifts",
      "The system prompt is too short",
      "The context window is being exceeded by retrieved passages"
     ],
     "answer": 1,
     "explain": "Temperature scales the output distribution before sampling. For anything parsed downstream, a low temperature is a correctness control, not a style preference."
    }
   },
   {
    "id": "gp-f5",
    "part": "field",
    "title": "When not to use AI, preventing misuse, and the live-tool exercise",
    "body": "<p>AI rounds ask three judgement questions as often as they ask concept questions: when would you not use this, how do you stop it being misused, and - in several reports - solve something in front of us with the tool and tell us what you think of the result.</p>\n<p><b>When not to use it.</b> Have a decision rule rather than a list of opinions. The rule: use a model when the mapping from input to output is genuinely too complex or too variable to write down, when you can measure whether the answer is good, and when a wrong answer is affordable or recoverable. If any of those three fails, do not.</p>\n<ul>\n<li><b>The rule is writable.</b> Tax thresholds, eligibility logic and business policy are rules. A model that approximates a rule is strictly worse than the rule: less accurate, less explainable, and it drifts.</li>\n<li><b>The problem is an optimisation problem.</b> Scheduling, assignment and routing under hard constraints are solved by solvers, not learned by models. Published engineering accounts of expert scheduling problems make exactly this point.</li>\n<li><b>You cannot measure success.</b> Without a metric and a label there is no way to know whether it works or when it stops working, and an unmeasurable model in production is a liability with a launch date.</li>\n<li><b>A wrong answer is unaffordable and unrecoverable</b> - a legal determination, a filed return, an irreversible financial action - unless there is a human in the loop who is genuinely able to catch it rather than nominally responsible for it.</li>\n<li><b>There is no data, or it is not representative.</b> A model trained on a population that is not the one it will serve is a confident source of wrong answers about the people who were missing.</li>\n<li><b>A cheaper thing works.</b> A lookup, a heuristic, a small tabular model. The strongest version of this answer is that the baseline should be built first and the model should have to beat it.</li>\n</ul>\n<p><b>Preventing misuse.</b> Four layers, and naming them as layers is what makes the answer an engineer's rather than a policy document's.</p>\n<ul>\n<li><b>Access.</b> Who may call it, on which data, with the data's classification deciding which models and regions may process it.</li>\n<li><b>Input.</b> Scope checks, and treating retrieved or user-supplied content as data rather than as instructions - which is the whole of prompt-injection defence.</li>\n<li><b>Output.</b> Sensitive-field redaction, grounding and citation, format validation, and a permitted refusal.</li>\n<li><b>After the fact.</b> Every request and response logged with its prompt and model version, so misuse is detectable and auditable rather than deniable.</li>\n</ul>\n<p><b>The live-tool exercise.</b> Reported in more than one interview account: solve a problem now, using an AI assistant, while we watch. The moment that counts is before you type. State the problem and your acceptance criteria - the cases, the edge cases, the complexity you would accept - then ask for something small enough to review, read it critically out loud, write the tests yourself, and say plainly what is wrong with what it produced. The rule to state: the tool compresses the writing, not the responsibility.</p>",
    "deeper": "<p>There is a reason employers ask the \"when would you not\" question so consistently. A company running a very large classical machine-learning estate alongside a generative platform has to hire people who will not reach for the expensive tool by default, because at that volume the choice is a budget line rather than a preference. Answering with genuine willingness to say no is therefore aligned with a real constraint, not a display of caution.</p>\n<p>A useful pairing to have ready: the case where you would not use a language model, and the case where you would not use machine learning at all. They are different answers. The first is usually about cost, latency or determinism and resolves to a smaller model or a classifier. The second is usually about a writable rule or a constrained optimisation and resolves to code or a solver. Being able to distinguish them at the same time counts for more than either answer alone.</p>",
    "check": {
     "question": "Which situation most clearly argues against using a learned model at all, rather than against using a large language model specifically?",
     "options": [
      "The latency budget is small and the cost per call is high",
      "The task is assigning experts to shifts under hard legal and availability constraints, which is a constrained optimisation problem",
      "The output must be valid JSON for a downstream parser",
      "The corpus changes frequently and the answers must reflect the latest version"
     ],
     "answer": 1,
     "explain": "Constrained scheduling and assignment are solved by optimisation, not learned. The other three are arguments for a smaller model, a lower temperature, or retrieval - all still machine learning."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "gp-a3",
    "type": "followup",
    "title": "AI round: \"When would you not use AI for a problem?\"",
    "prompt": "An interviewer asks: \"When would you not use AI for a problem?\" Answer in first person, in about 90 seconds. Give a decision rule first, then two or three examples, and distinguish the case where you would not use a language model from the case where you would not use machine learning at all.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) the broad decision rule stated first - use a learned model only when the mapping from input to output is genuinely too complex or too variable to write down, when success can be measured, and when a wrong answer is affordable or recoverable - with the explicit note that if any of the three fails, do not; (2) the two questions distinguished: when not to use a language model, which usually resolves to a smaller model or a classifier on grounds of cost, latency or determinism, and when not to use machine learning at all, which usually resolves to a written rule or a constrained optimisation problem; (3) a stated decision rule for the narrower question rather than a list, for example: if the task has a well-defined output space and labelled data exists, a classical model is cheaper, faster, more explainable and easier to evaluate; (2) at least three concrete disqualifiers - a hard low-latency budget, a per-call cost that does not survive the volume, a requirement for a deterministic or auditable decision, or an output that must be exactly right rather than plausibly right; (3) at least one clear case where a language model is the right answer, such as unstructured text where the output space is open or where writing rules would be endless; (4) the point that a combination is often correct, with the model handling extraction or summarisation and a deterministic component making the decision; (5) an explicit connection to evaluation - a task with a clear metric favours the classical route, since it can be gated automatically; (6) no dismissiveness toward language models, since most employers asking this have invested heavily in them. Bonus: notes that engineers at large product companies have published the argument that a hard scheduling problem was best solved with mathematics rather than generative AI, which makes this judgement a stated engineering value rather than a contrarian pose. Common mistakes: an opinion with no rule; dismissing LLMs generally; ignoring cost and latency; no hybrid option. {{HONESTY}}",
    "model": "<p>\"I would separate two questions, because they get asked as one. When not to use a language model, and when not to use machine learning at all.</p>\n<p>The broad rule I work from has three conditions, and if any one fails I would not build a model. Is the mapping from input to output genuinely too complex or too variable to write down - because if it is writable, a rule is more accurate, more explainable and does not drift. Can I measure whether the answer is good, because an unmeasurable model in production is a liability with a launch date. And is a wrong answer affordable or recoverable, because if it is neither, either a human has to be genuinely able to catch it or the thing should not be automated. Two clear cases where I would not use machine learning at all: anything that is really business policy, like an eligibility threshold, and anything that is really a constrained optimisation problem, like scheduling or assignment under hard constraints - those are solver problems, and engineering teams at large product companies have published exactly that argument.</p>\n<p>Now the narrower question, language model or classical model. My rule there is about the output space. If the task has a well-defined output - a class, a score, a number - and there is labelled data, a classical model is almost always the better engineering choice: cheaper per call by orders of magnitude, faster, more explainable, and far easier to evaluate and gate automatically.</p>\n<p>The specific disqualifiers I would name. A hard latency budget - if the decision has to happen in 200 milliseconds inside a transaction, that is not where a language model belongs. A per-call cost that does not survive the volume, because unlike a tree model the marginal cost never goes away. A requirement that the decision be deterministic and auditable, which matters a lot in financial software. And any case where the answer must be exactly right rather than plausibly right.</p>\n<p>Where a language model genuinely wins is unstructured text with an open output space - summarising a call, extracting fields from a messy document, answering a question that has no fixed schema - or where writing the rules would never end.</p>\n<p>And often the right answer is both: let the model do the extraction or the summarisation, and let a deterministic component make the actual decision. That way the auditable part stays auditable.</p>\n<p>It is worth saying that engineers at large product companies have published this argument themselves, about hard scheduling problems where the right answer was mathematics rather than generative AI. So it is not a contrarian position; it is one the field has written down.\"</p>"
   },
   {
    "id": "gp-a4",
    "type": "explain",
    "title": "AI round rehearsal: ten concept answers, two sentences each",
    "prompt": "Answer these out loud, in order, two sentences each, without notes: what is retrieval-augmented generation; what is a vector database; what is an embedding and how is similarity measured; how does an image become a vector; what is a system prompt; what does temperature do; what is an agent; how do you know a model is trained enough; when do you fine-tune instead of prompting; what actually reduces hallucination. Time yourself. The whole set should take under five minutes.",
    "timeboxSec": 300,
    "rubric": "Must-haves, one per question, each correct and each with an operational consequence attached rather than a bare definition: (1) retrieval-augmented generation as retrieving passages into the prompt so the model answers from supplied context, with the consequence that knowledge is updated by changing documents rather than retraining and that most failures are retrieval failures; (2) a vector database as an embedding store with an approximate nearest-neighbour index, with the consequence that recall trades against latency and that filters such as tenant must be applied inside the search rather than after it; (3) an embedding as a fixed-length vector positioning an item in a learned space, with cosine similarity or a dot product on normalised vectors as the measure, and the consequence that vectors from different models are incomparable so changing the model means reindexing; (4) an image embedding as activations taken from a vision model, with joint image-text training explaining cross-modal search, and the consequence that the training objective determines what the vector encodes; (5) a system prompt as the standing instruction setting role, constraints and format, with the consequences that it is a versioned production file and that it is not a security boundary; (6) temperature as a scaling of the output distribution before sampling, with the consequence that low temperature is a correctness control for anything parsed downstream; (7) an agent as a model choosing tools in a loop toward a goal, with loops, wrong tool choice and compounding error named as the failure modes and step limits, tool permissions and per-step observability as the controls; (8) \"trained enough\" as held-out performance ceasing to improve while training performance continues, chosen on validation and never on test, with the consequence that enough is defined against the product requirement; (9) fine-tuning versus prompting decided by stability, volume and cost, with prompting first because it is instant and reversible; (10) hallucination as unsupported confident output, reduced by grounding, citation, output constraints and a permitted refusal, with the consequence that a model given no way to decline will always produce something. Also required: the whole set delivered inside five minutes, with no answer running past three sentences. Common mistakes: definitions with no consequence; long answers that consume the round; explaining cosine similarity as a distance without saying it ignores magnitude; describing an agent as a chatbot; claiming to have built agent frameworks or evaluation setups you have never built. {{HONESTY}}",
    "model": "<p><b>Retrieval-augmented generation.</b> Retrieve relevant passages and put them in the prompt so the model answers from supplied context rather than from memorised parameters. It means knowledge is updated by changing documents rather than retraining - and most of its failures are retrieval failures, so I measure retrieval separately.</p>\n<p><b>Vector database.</b> A store of embeddings with an approximate nearest-neighbour index, so you can find the items closest in meaning to a query. Because the index is approximate there is a trade-off between recall and latency, and filters like tenant or date have to be applied inside the search rather than after it.</p>\n<p><b>Embedding and similarity.</b> A fixed-length vector placing an item in a space where nearby means similar according to the model that produced it. Similarity is cosine - the angle between the vectors, ignoring magnitude - or equivalently a dot product on normalised vectors; vectors from different models are not comparable, so changing the embedding model means reindexing the corpus.</p>\n<p><b>Images to vectors.</b> A vision model processes the pixels and you take an internal layer's activations, usually the last before the classifier, as the representation. Models trained to align images and text put both in the same space, which is what lets you search images with a sentence - and the vector encodes whatever the training objective rewarded, so a classification embedding and a retrieval embedding behave differently.</p>\n<p><b>System prompt.</b> The standing instruction above the conversation that sets role, constraints and output format for every turn. Two things follow: it is a production file that should be versioned and rolled back like code, and it is not a security boundary, because content in the conversation can try to override it.</p>\n<p><b>Temperature.</b> It scales the output distribution before sampling - low makes the likeliest tokens dominate and output more deterministic, high flattens the distribution so output is more varied and more often wrong. For anything parsed downstream I treat a low temperature as a correctness setting rather than a style one.</p>\n<p><b>Agent.</b> A model given tools and allowed to choose which to call, in a loop, until a goal is met - a planner deciding the step, an executor calling the tool, and memory of what has happened. The failure modes are loops, wrong tool choice and errors compounding across steps, so it needs step limits, per-tool permissions and observability at each step - and a fixed sequence is the better answer when one will do.</p>\n<p><b>Trained enough.</b> When held-out performance stops improving while training performance keeps going - that gap is overfitting, and the stopping point is chosen on validation data, never on test. In practice enough is defined against the requirement: if it already meets the metric the product needs, more training costs money for no return.</p>\n<p><b>Fine-tune or prompt.</b> Prompt first, because it is instant and reversible. Fine-tune when the behaviour is stable, high-volume and expensive or unreliable to get by prompting - or when a small fine-tuned model can replace a large prompted one at much lower cost, which is the case I have actually built.</p>\n<p><b>Hallucination.</b> Confident output not supported by any source. What reduces it is grounding in retrieved passages, requiring citations, constraining the output format, and giving the model a permitted way to say it does not know - because a model with no refusal path will always produce something.</p>"
   },
   {
    "id": "gp-a2",
    "type": "design",
    "title": "Exercise: guardrails for RAG on customer tax documents",
    "viz": "guardrails-io",
    "prompt": "Design the guardrail and evaluation layer for a retrieval-augmented assistant answering questions over customers' own tax documents. Cover isolation, corpus freshness, grounding, refusal, injection, and how you evaluate. Say which failure would end the project and how you prevent it.",
    "timeboxSec": 600,
    "rubric": "Must-haves: (1) tenant and permission scoping enforced at query time rather than by post-filtering, named as the project-ending failure if it goes wrong; (2) corpus freshness handled with versioning and an effective-date concept, with the point that stale tax guidance produces confidently wrong answers; (3) grounding and citation - answers traceable to retrieved passages, and the passages producible - justified as both trust and audit; (4) a defined refusal and escalation behaviour when retrieval is empty or confidence is low, with the observation that in a legally consequential domain a confident guess is the worst outcome; (5) retrieved content treated as untrusted input, covering instruction injection from documents and preventing sensitive fields being echoed outward; (6) evaluation split into retrieval quality and generation faithfulness measured separately, with the point that most failures are retrieval failures; (7) a golden set that includes cases where the correct answer is a refusal, and any automatic judge checked against human labels on a sample; (8) at least one operational guardrail such as logging that itself respects data classification, or an approval step before prompt or model changes reach production. Bonus: connects to the published registries-and-policies description and to a blended manual-plus-automatic evaluation against a golden set, each cited with its date; notes data classification levels constraining what may be indexed or logged. Common mistakes: leading with embedding-model choice; post-hoc filtering as the isolation mechanism; no refusal path; conflating retrieval and generation evaluation; no golden set. {{HONESTY}}",
    "model": "<p><b>The failure that ends the project.</b> One customer's document appearing in another customer's answer. So isolation is not a filter applied to results - the retrieval query itself must be scoped by tenant and by permission, enforced in the retrieval layer, and tested as a case in its own right, with adversarial prompts that try to widen the scope. Everything else on this list is recoverable; that one is not.</p>\n<p><b>Corpus freshness.</b> Tax guidance changes annually. Documents and rule content carry an effective date, retrieval is filtered by the tax year in question, and the corpus is versioned so an answer can be reproduced later against what was actually indexed at the time.</p>\n<p><b>Grounding.</b> Answers cite the passages they came from, and the system can produce them. That is a trust feature for the customer and an audit requirement for everyone else.</p>\n<p><b>Refusal and escalation.</b> If retrieval returns nothing relevant, or the answer would go beyond what was retrieved, the system says so and offers a human. In a domain with legal consequence a confident guess is the worst possible output, and the refusal path has to be a designed behaviour rather than an accident of prompting.</p>\n<p><b>Injection.</b> Retrieved content is untrusted. Instructions inside a document must never be executed, so the prompt structure has to separate instructions from evidence, and outputs are checked so sensitive fields are not echoed outside the boundary. Logging is itself a data-classification decision - what gets written to a log is as much a compliance question as what gets returned.</p>\n<p><b>Evaluation.</b> Two separate measurements. Retrieval: was the right passage in the retrieved set at all, at some cut-off. Generation: is the answer faithful to what was retrieved. Conflating them hides the usual truth, which is that the generator was fine and the retriever missed. A golden set of representative questions with agreed expected behaviour, explicitly including questions where the correct answer is a refusal. Any automatic judge is checked against human labels on a sample before it is trusted - otherwise you have swapped an unknown error rate for a different unknown error rate. Published platform accounts describe a comparable blend of manual expert evaluation with automatic evaluation against a golden set, and registries and policies enforcing authorisation, data handling, privacy and compliance inside a GenAI runtime.</p>\n<p><b>Change control.</b> A prompt change is a production change. It goes through the same checks as a model change, with the golden set run automatically and a human approval recorded.</p>"
   },
   {
    "id": "gp-a1",
    "type": "explain",
    "title": "Exercise: make an LLM feature ten times cheaper",
    "prompt": "A customer-facing LLM feature is too expensive to launch at full volume. List the options in order of impact, say what each costs in quality or complexity, and name the one you would try first and why. Then say what you would measure to prove the saving did not damage the product.",
    "timeboxSec": 300,
    "rubric": "Must-haves: (1) model size named as the biggest single factor, with the reason that it multiplies cost on every call, and the cost stated as possible task-quality loss requiring evaluation; (2) prompt length addressed, with retrieved context identified as the usual culprit; (3) caching covered with its two prerequisites - repetition rate and staleness tolerance - and prefix or system-prompt caching distinguished from full response caching; (4) continuous batching named as the way to raise throughput on fixed hardware; (5) routing or a cascade described with a defined escalation rule; (6) output length bounding named, with the point that generation dominates cost; (7) streaming named as changing perceived rather than actual cost; (8) a first choice with a reason, normally model size or a cascade, and the measurement plan: task-level evaluation on a golden set before and after, side-by-side on identical inputs, plus a shadow or canary period and a business proxy such as escalation or abandonment rate. Common mistakes: starting with hardware; proposing caching without a hit-rate or staleness argument; treating streaming as a cost saving; no measurement plan; benchmark scores instead of task-level evaluation. {{HONESTY}}",
    "model": "<p><b>In order of impact.</b> Model size first - it multiplies cost on every single call, and the largest model is rarely the right default. Cost: possible task-quality loss, which has to be measured rather than assumed. Then prompt length, because cost and prefill latency are roughly linear in input tokens, and retrieved context is almost always the largest and least disciplined part of the prompt. Then caching - a shared system prompt can share prefix computation, identical requests can be served from a result cache - but only if the repetition rate is real and the staleness tolerance is stated. Then continuous batching, which is the serving engine's job and the biggest throughput win on fixed hardware. Then routing: a cheap model handles the easy majority and escalates on a defined rule, which is the cascade pattern from classical serving. Then bounding output length, since generation dominates cost. Streaming I would list separately, because it does not save anything - it changes what the user feels, replacing total generation time with time to first token.</p>\n<p><b>First choice.</b> A cascade with a smaller default model. It attacks the dominant term, it degrades gradually rather than dropping off suddenly, and the cheap stage doubles as a fallback if the expensive stage is saturated - so it buys reliability as well as cost.</p>\n<p><b>Proof it did not damage the product.</b> A golden set of representative prompts, including cases where the correct answer is a refusal, scored before and after on identical inputs. Human review on a sample to check any automatic judge. A shadow period, then a canary, with a business proxy watched throughout - escalation to a human, abandonment, or repeat-question rate. Cost per resolved request rather than cost per call, because a cheap answer that causes a second question is not cheap.</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "scenario-questions": {
  "id": "scenario-questions",
  "title": "Scenario questions",
  "day": 2,
  "level": "danger",
  "levelLabel": "HR said these questions are likely",
  "why": "Many interview processes include scenario questions: a hiring manager round built on situations from your career, design disagreements and how you learn; a round described as \"how you handle a given scenario\"; and published engineering accounts of scenario discussions about choosing technology. A scenario question is not a behavioural question under another name. A behavioural question asks what you did. A scenario question asks what you would do, and what they judge is the structure of your answer far more than the answer itself. One structure covers all of them, and the fourteen questions below start with the most likely.",
  "learn": [
   {
    "id": "sq-0",
    "part": "field",
    "title": "Key terms",
    "viz": "incident-scenario-spine",
    "body": "<ul>\n<li><b>Scenario question</b> - a hypothetical situation you are asked to handle: \"a model you shipped starts misbehaving during peak, what do you do\". The interviewer is watching your process, not waiting for a specific answer.</li>\n<li><b>Situation question</b> - the past-tense version: \"tell me about a time when\". Same structure, with the added requirement that it must be true.</li>\n<li><b>The structure</b> - the five steps that make up a competent response, in order: <b>clarify</b>, <b>contain</b>, <b>diagnose</b>, <b>fix and verify</b>, <b>prevent</b>.</li>\n<li><b>Contain</b> - stop the harm before you understand it. Restoring service and understanding the cause are two different jobs, and doing them in the wrong order is the most common failure in these answers.</li>\n<li><b>Blast radius</b> - how much is affected. Narrowing it is usually the fastest containment available.</li>\n<li><b>Reversible versus irreversible</b> - whether an action can be undone. Reversible decisions are made quickly and alone; irreversible ones are made slowly and with other people, and saying which kind you are facing shows real judgement.</li>\n<li><b>Escalation</b> - bringing in another person or team. Not a failure; a scoped request with a reason and a deadline.</li>\n</ul>\n<p><b>The structure, in detail.</b> <i>Clarify</i>: one or two questions that change the answer - what is the actual impact, who is affected, what changed recently, is it still happening. <i>Contain</i>: the fastest safe action that reduces harm - roll back, disable the feature, fall back to rules, shed the affected traffic - stated as reversible, with who you tell. <i>Diagnose</i>: form hypotheses and order them by what changed most recently and what is cheapest to test; say the ordering rule out loud. <i>Fix and verify</i>: the actual repair, plus how you know it worked, plus what you do about the damage already done. <i>Prevent</i>: the change that stops this whole class of problem, not just this one case - a test, a release check, an alert, a runbook, or a design change.</p>\n<p><b>What they are really looking for.</b> Whether you stop the harm before you investigate. Whether you say what you would ask rather than assuming facts. Whether you name who you would tell and when. Whether you separate the fix from the prevention. And whether you can say \"I would not know that yet\" without it destabilising the rest of the answer.</p>",
    "deeper": "<p>The single most common mistake is investigating before containing. It feels rigorous - you do not want to act without understanding - but in an operational context it is the wrong instinct, and an experienced interviewer hears it immediately. The point to make is that containment is cheap and reversible, so it costs almost nothing to be wrong about it, whereas an extra twenty minutes of customer impact is not recoverable. Say that reasoning out loud and you get the credit even if your containment guess is imperfect.</p>\n<p>The second most common mistake is answering a scenario as a technical puzzle when it is a coordination question. Half of these prompts contain a person - a scientist who disagrees, a manager under deadline pressure, a stakeholder who wants certainty. For those, the technical content is the easy part and the credit is in how you handle the disagreement: state the shared goal, make the disagreement concrete and testable, and say what would change your mind. An answer where you are simply right and the other person is simply wrong comes across badly, however correct it is.</p>",
    "check": {
     "question": "A scenario question describes a live customer-facing regression. What should the first move in your answer be?",
     "options": [
      "Form a hypothesis about the root cause and describe how you would test it",
      "Establish the current impact, then contain it with a fast reversible action, and only then diagnose",
      "Escalate to the hiring manager's equivalent so the decision is not yours",
      "Describe the monitoring you would have built to prevent it"
     ],
     "answer": 1,
     "explain": "Containment is cheap and reversible; customer impact is not recoverable. Diagnosing before containing is the most common and most visible error in these answers."
    }
   },
   {
    "id": "sq-1",
    "part": "field",
    "title": "The four most likely: production regression, silent quality drop, no rollback path, leakage after launch",
    "viz": "degrade-to-fallback",
    "body": "<p>These four come first because they are the ones an ML-focused panel reaches for. Each is given with the structure applied and a model answer in your own voice.</p>\n<p><b>1. A model you shipped is causing a customer-visible regression during the busiest week of the year.</b></p>\n<ul>\n<li><i>Clarify:</i> what is the observed harm, how many customers, is it still happening, what changed in the last few days - model version, feature definition, upstream data, traffic mix.</li>\n<li><i>Contain:</i> roll back to the previous model version, which is a traffic switch rather than a rebuild, and revert the feature contract with it. If the regression is confined to a slice, restrict that slice first, because the blast radius is smaller than a full rollback.</li>\n<li><i>Diagnose:</i> order hypotheses by what changed most recently and what is cheapest to test - version, feature freshness or null rates, upstream schema, a traffic shift into a slice the model was never good at.</li>\n<li><i>Fix and verify:</i> repair, then re-verify on the same evidence that revealed the problem, plus a shadow comparison before the model takes traffic again. Address the customers already affected explicitly.</li>\n<li><i>Prevent:</i> whichever check would have caught it - a per-slice check before release, a shadow period, an alert on the leading indicator rather than the lagging one.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"First, impact: what is the customer actually seeing, how many of them, and is it still happening. While someone answers that I am rolling back, because at peak the cost of another twenty minutes is much higher than the cost of being wrong about a reversible action - and where rollback is a traffic switch through the mesh it takes seconds, with the model and its feature contract reverting together. Then I would say in one message what happened, what I did and when I will update. Only then do I diagnose, in order of what changed most recently: model version first, then feature freshness and null rates, then whether an upstream schema moved, then whether traffic shifted into a slice the model was never good on - peak weeks change the traffic mix, not just the volume. When I have a fix, I verify it on the same evidence that showed the problem, and I would want it in shadow before it takes traffic again. Then prevention: whatever check would have caught this, added to the checks before release rather than to a runbook.\"</p>\n<p><b>2. Precision has dropped and no infrastructure alarm has fired.</b></p>\n<ul>\n<li><i>Clarify:</i> how do we know precision dropped, given labels are delayed - is this a matured-label measurement or a proxy such as override rate or alert volume? Over what window? Which slices?</li>\n<li><i>Contain:</i> nothing is broken in the infrastructure sense, so containment is the threshold, which is the fastest action available - move the operating point to protect the expensive error while you investigate, and say what that costs on the other side.</li>\n<li><i>Diagnose:</i> three families - the data changed, the world changed, or we changed something. Check feature freshness, null rates and distributions first, because a broken feature is the most common and the cheapest to confirm; then a population shift; then concept drift, which is the residual after the first two are excluded.</li>\n<li><i>Fix and verify:</i> a data fix is a repair; a population shift may need a threshold change; genuine concept drift needs retraining, which is bounded by label availability, not by compute.</li>\n<li><i>Prevent:</i> the two health questions monitored separately, with proxies for the delayed one, and seasonal baselines compared year-over-year rather than to last month.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The first thing I would ask is how we know - because if labels take weeks, then a precision number today is either a proxy or a stale measurement, and which one it is changes everything. If it is a proxy like reviewer override rate or alert volume against capacity, I treat it as a hint, not a conclusion. Containment here is not a rollback, it is the threshold, which is the fastest action I have: move the operating point to protect whichever error costs more, and say out loud what that costs on the other side, because it is a business trade-off and not mine alone to make. Then diagnosis in three families - the data changed, the world changed, or we changed something. I check the data first because it is cheapest: feature freshness, null rates, distributions per feature, and whether any upstream job is late. Then population shift, which looks like the same model doing the same thing to a different mix of customers. Concept drift is what is left after I have excluded the other two, and it is the one that actually needs retraining. In most systems the honest gap is exactly this: infrastructure monitoring is solid, and decision-quality monitoring is the thinner half. It is the first thing I would build if I joined a team in that state.\"</p>\n<p><b>3. You are asked to ship a model with no rollback path because the deadline is tomorrow.</b></p>\n<ul>\n<li><i>Clarify:</i> what specifically is missing - no previous version deployable, no traffic switch, no way to revert the feature definition, or no tested procedure? Each has a different cost and a different shortcut.</li>\n<li><i>Contain:</i> reduce exposure rather than refusing outright - ship to a small slice, ship behind a flag, ship in shadow, or ship with a rules fallback that can be switched on. There is almost always a version of \"yes\" with a smaller blast radius.</li>\n<li><i>Diagnose:</i> what is the actual deadline pressure, and what is the cost if this goes wrong at full exposure - the two numbers that decide it.</li>\n<li><i>Fix and verify:</i> the cheapest real rollback is usually a configuration switch to the previous version plus a documented, tested procedure - hours, not days.</li>\n<li><i>Prevent:</i> make a working rollback path one of the checks before release, so the question cannot arise again.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"I would not make it a yes-or-no conversation, because that usually ends with either a bad deployment or a bad relationship. First I would find out what is actually missing - whether there is no previous version deployable, or no traffic switch, or no way to revert the feature definition alongside the model, or just no tested procedure. Those are very different problems and the last one is a couple of hours of work. Then I would offer the smaller-exposure version: ship to a slice, or behind a flag, or in shadow so it produces predictions nobody acts on, or with the rules path ready to switch on. That is still a yes, and it caps the damage. What I would say plainly is that shipping something I cannot take back is the one class of decision I would want on the record as a joint call rather than mine alone - and I would put in writing what the exposure is and what we do if it goes wrong. In the system I ran, rollback was a traffic switch through the mesh plus a Git revert, and the reason that mattered was not elegance, it was that reverting the model and its feature contract together is the only version that actually works - rolling a model back onto a changed feature definition is a second incident, not a fix.\"</p>\n<p><b>4. After launch you discover a feature was leaking label information.</b></p>\n<ul>\n<li><i>Clarify:</i> which feature, how it leaks, whether the leak exists at serving time too or only in training - the answer determines whether the model is merely overrated or actively wrong.</li>\n<li><i>Contain:</i> if the leaked value is unavailable at serving time, the live model is worse than its offline numbers suggested and the reported metrics are invalid; fall back to the previous model or to rules while you quantify.</li>\n<li><i>Diagnose:</i> rebuild the training rows with a point-in-time correct join, retrain without the offending feature, and compare - that difference is the real performance, and the gap is what was being oversold.</li>\n<li><i>Fix and verify:</i> correct the feature definition so the value attached to an event is the as-of value, then re-run the release checks from scratch.</li>\n<li><i>Prevent:</i> point-in-time correctness enforced in the training-data build rather than left to the author, plus a serving-path comparison in those checks, which catches a feature the request path cannot actually produce.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The first question is whether the leaking value is available at serving time or not, because that decides how bad this is. If it is not available live, then the offline numbers were never real and the live model is quietly worse than everyone believes, so I would fall back while I quantify it. If it is available live, the model may be fine and the evaluation was flattering - still a problem, but a reporting problem rather than a customer one. Then I rebuild the training rows with a point-in-time correct join, retrain without the feature, and compare. That gap is the honest number, and I would communicate it rather than quietly fixing it, because someone made a decision on the old figure. The prevention is the part I care about: point-in-time correctness should be a property of how training data is built, not something each author remembers, and the release checks should score the candidate model through the serving path, so a feature the request path cannot actually produce fails there instead of in production. That is exactly why, in a streaming fraud pipeline, features are computed once at event time and published to both read paths - one computation, two readers, so there is nothing to get out of step.\"</p>",
    "deeper": "<p>Notice what these four have in common: none of them is solved by knowing more machine learning. Each is solved by having an operational reflex, a reversible action available, and the willingness to communicate before the picture is complete. That is what a strong answer is actually made of, and it is why the same structure works whether the prompt is about a model, a pipeline or a person.</p>\n<p>One refinement for the leakage scenario, because it is the one where people most often show the wrong instinct. The temptation is to fix it quietly and move on - it is embarrassing, and the fix is easy. The correct behaviour is to restate the number, because a decision was made on the old one. Saying \"someone chose to launch this on a figure that turned out to be wrong, so the figure has to be corrected out loud\" is what an interviewer is listening for, and it costs you nothing in the technical round.</p>",
    "check": {
     "question": "A feature is found to leak label information, and the leaking value is NOT available at serving time. What is the immediate consequence?",
     "options": [
      "The model is unaffected in production; only the offline report was wrong",
      "The live model is performing worse than its reported metrics implied, so the reported numbers are invalid and a fallback is warranted while the real performance is quantified",
      "The model must be deleted and rebuilt from scratch before anything else",
      "It only matters if the feature is also used by other teams"
     ],
     "answer": 1,
     "explain": "If the leaked signal exists only in training, the offline evaluation was inflated by information the request path never has. The production model is genuinely weaker than believed, so the numbers must be corrected and exposure reduced while the true gap is measured."
    }
   },
   {
    "id": "sq-2",
    "part": "field",
    "title": "Data, plans and dependencies: backfills, infeasible schedules, a failing dependency, 2 a.m.",
    "viz": "circuit-breaker-states",
    "body": "<p>Four more, still in priority order. These are operational rather than modelling.</p>\n<p><b>5. A feature pipeline backfill changed the offline evaluation number, and nobody is sure which number is right.</b></p>\n<ul>\n<li><i>Clarify:</i> what did the backfill change - the value of an existing feature, its availability window, or the set of rows? Was the old number computed with a point-in-time correct join or not?</li>\n<li><i>Contain:</i> freeze promotions that depend on the metric until it is trustworthy, because a release check reading an untrustworthy number is worse than no check at all.</li>\n<li><i>Diagnose:</i> recompute both versions on one frozen holdout - old feature definition and new - and compare on identical rows. If the two disagree on the same rows, the definition changed; if they agree on shared rows but the row set moved, the backfill changed availability.</li>\n<li><i>Fix and verify:</i> version the feature definition explicitly, retrain against the version the serving path will actually use, and re-baseline the incumbent on the same rows so the comparison is like for like.</li>\n<li><i>Prevent:</i> feature definitions versioned with additive changes treated as minor and breaking changes as major, training rows built point-in-time correct by construction, and evaluation always pinned to a frozen dataset plus a recent window rather than to whatever the pipeline last produced.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The first thing I do is stop trusting the metric and say so, because a release check reading a number nobody believes is more dangerous than no check at all. Then I would recompute both on one frozen holdout - the old feature definition and the new one, on identical rows - because that separates the two possible causes. If they disagree on the same rows, the definition itself changed and someone has to decide which one is correct. If they agree on the shared rows and the row set is different, the backfill changed which history exists, and the older number was computed on a population that no longer exists. Either way I would rebaseline the incumbent on the same rows before comparing anything, because a champion scored on last quarter's rows against a challenger scored on this quarter's is not a comparison. The prevention is versioning: a feature definition should have a version, additive changes should be minor and breaking changes major, and training rows should be built point-in-time correct by construction rather than by whoever wrote the query being careful.\"</p>\n<p><b>6. A scheduling or assignment engine starts producing infeasible plans on a surge day.</b></p>\n<ul>\n<li><i>Clarify:</i> infeasible how - the solver returned nothing, returned something that violates a hard constraint, or returned something feasible on paper that operations cannot execute? Those are three different bugs.</li>\n<li><i>Contain:</i> fall back to the last known good plan or to a simple, obviously-feasible heuristic, and tell operations which one they are running. An optimisation system's degraded mode is a worse plan, not the absence of one.</li>\n<li><i>Diagnose:</i> a surge changes the inputs, so check the inputs first - the demand forecast for that day, capacity and availability data, and whether any constraint was newly added. Then check whether the solver hit its time limit and returned its best incumbent rather than an optimum, which is the classic surge-day failure.</li>\n<li><i>Fix and verify:</i> relax the right constraint deliberately rather than letting the solver fail - soften the ones that are preferences and keep the ones that are legal or safety limits hard - and re-verify by replaying the surge day's inputs.</li>\n<li><i>Prevent:</i> feasibility checked and reported as an output in its own right, a time limit with a defined behaviour when it is hit, hard and soft constraints separated explicitly, and the surge day replayed as a regression test.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"I would first separate three different failures that all look the same from outside: the solver returned nothing, it returned something that breaks a hard constraint, or it returned something feasible on paper that the people executing it cannot actually run. Containment is the same in all three - fall back to the last known good plan or to a simple heuristic that is obviously feasible, and tell operations explicitly which one they are on, because the worst version of this is people quietly working around a plan they do not trust. Then I check inputs before I touch the model, because a surge changes the inputs first: the demand forecast for that day, the availability data, and whether a constraint was added recently. The specific failure I would expect on a surge day is the solver hitting its time limit and returning its best incumbent rather than an optimum - which is not a bug, it is the configuration being wrong for that day's problem size. The design fix is to separate hard constraints from soft ones so the solver degrades by relaxing preferences instead of failing, and to make feasibility an output in its own right rather than something the consumer discovers later. The general lesson holds across domains - a solution the operators will not execute is not a solution, so the constraints have to match reality before the objective matters.\"</p>\n<p><b>7. An outage is caused by a dependency you do not own.</b></p>\n<ul>\n<li><i>Clarify:</i> is it down or slow, is it total or partial, and which of your paths actually need it.</li>\n<li><i>Contain:</i> stop calling it - open the circuit breaker, serve the degraded path, and if the dependency is optional, disable the enrichment entirely. Slow is more dangerous than down, because down fails fast and slow consumes your concurrency.</li>\n<li><i>Diagnose:</i> confirm it is really them, with your own evidence - error rate and latency from your side, plus a trace - before you tell another team their service is broken.</li>\n<li><i>Fix and verify:</i> theirs to fix; yours to verify that your degraded path actually held and to measure what customers experienced during the window.</li>\n<li><i>Prevent:</i> a timeout and a breaker on every dependency, a retry budget, and a rehearsed fallback - plus an explicit agreement about what you are entitled to expect from them.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The first distinction I would make is down versus slow, because slow is the more dangerous one - a dependency that fails fast is survivable, and one that hangs eats my threads until my own callers start failing, which is how a small outage becomes a platform outage. So containment is to stop calling it: open the breaker, serve the degraded path, and if that call is an optional enrichment, turn it off entirely rather than degrading everything for it. Then, before I tell another team their service is broken, I want my own evidence - my error rates and latencies against theirs, and a trace showing where the time is going - because arriving with a conclusion and no data is how those conversations go badly. The fix is theirs; what is mine is verifying that my fallback actually held, and being honest afterwards about what customers experienced during the window rather than reporting that we stayed up. The prevention I would push for is boring and effective: a timeout on every remote call smaller than the budget left at that point, a breaker in front of anything I call repeatedly, a cap on retries as a fraction of traffic, and an actual drill where we turn that dependency off in a test environment - because a fallback nobody has actually tried is a claim, not a capability.\"</p>\n<p><b>8. It is 2 a.m., you are on call, and the alert is ambiguous.</b></p>\n<ul>\n<li><i>Clarify:</i> is anything customer-visible right now - that single question decides whether this is an incident or a ticket.</li>\n<li><i>Contain:</i> if yes, the reversible action first: roll back if something shipped, shed or restrict if a slice is affected, fall back if a dependency is at fault. If no, write down what you saw and stop.</li>\n<li><i>Diagnose:</i> what changed in the last few hours, ordered by recency; check the cheap things first; and time-box - if you have not found it in a set period, escalate rather than continuing alone at 3 a.m.</li>\n<li><i>Fix and verify:</i> the minimum change that restores service, not the elegant one. The elegant one is written down for the morning.</li>\n<li><i>Prevent:</i> the alert itself is part of the postmortem - an alert that cannot distinguish \"customers are affected\" from \"a number moved\" will keep waking people up and will eventually be ignored.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The only question that matters in the first minute is whether anything is customer-visible right now. If it is not, I record what I saw and go back to sleep, and it becomes a ticket in the morning - and I would say that plainly, because being willing not to act at 2 a.m. says as much about you as being willing to act. If it is customer-visible, I take the reversible action before I understand it: roll back if something shipped recently, restrict the affected slice if it is confined, switch to the fallback if a dependency is the cause. Then I diagnose by recency and by cost - what changed in the last few hours, cheapest checks first - and I time-box it, because the failure mode at 3 a.m. is one person quietly making it worse for two hours. If I have not found it inside that box, I wake someone, and I do not treat that as a failure. The fix I make at night is the minimum one that restores service; the good fix is written down for the morning. And the alert goes in the postmortem too - an alert that cannot tell me whether customers are affected is an alert that will eventually be ignored, and that is worse than not having it.\"</p>",
    "deeper": "<p>Scenario six is the one worth extra rehearsal, because this team's published problem is a nested hierarchy of forecasting, assignment, scheduling and supply-planning engines, and \"the plan is infeasible today\" is the characteristic failure of that whole family. The general point is that an optimisation system fails differently from a model: it does not degrade smoothly, it either returns something or it does not, and the thing it returns can be perfectly optimal against constraints that no longer describe reality. So the operational requirements are different - feasibility as an output, a time limit with defined behaviour, hard and soft constraints separated, and a last-known-good plan always available.</p>\n<p>On scenario seven, one refinement that works well: the right time to negotiate what you expect from a dependency is not during an outage. If you can say that you would want a written expectation - a latency target and an availability target you can design against - and that in its absence you design for the worst case you have observed, you have converted a complaint into an engineering position.</p>",
    "check": {
     "question": "Why is a slow dependency often more dangerous than one that is completely down?",
     "options": [
      "Because slow responses are usually incorrect as well",
      "Because a fast failure is contained, while a hanging call consumes the caller's concurrency and propagates saturation upstream",
      "Because circuit breakers cannot detect slowness",
      "Because monitoring systems do not record latency"
     ],
     "answer": 1,
     "explain": "A hard failure returns immediately and can be handled. A slow call holds threads or connections, so the caller saturates, and its callers saturate in turn - the cascading failure pattern."
    }
   },
   {
    "id": "sq-3",
    "part": "field",
    "title": "People and judgement: a scientist, a teammate, a stakeholder, and simple against complex",
    "body": "<p>Four prompts where the technical content is easy and the credit is in the handling. Hiring-manager rounds ask about design conflicts explicitly.</p>\n<p><b>9. You disagree with a scientist about the method.</b></p>\n<ul>\n<li><i>Clarify:</i> what exactly are we disagreeing about - the objective, the metric, the method, or the deployment constraint? Most method disagreements are actually constraint disagreements in disguise.</li>\n<li><i>Contain:</i> agree the goal and the measure first, in writing, before arguing about the approach. Two people who agree on how they would tell who was right can stop arguing and go and find out.</li>\n<li><i>Diagnose:</i> make it empirical - what is the cheapest experiment that would distinguish the two positions, and can it be run in days rather than weeks.</li>\n<li><i>Fix and verify:</i> run it, and honour the result publicly, including when you lose.</li>\n<li><i>Prevent:</i> establish the practice - a stated objective, a stated metric, a baseline and a bake-off - so the next disagreement resolves itself.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The first thing I do is work out what we are actually disagreeing about, because in my experience a method argument is usually a constraint argument in disguise - the scientist is optimising for accuracy and I am optimising for a latency budget or an operability requirement, and neither of us has said so. So I would try to get the objective and the measure agreed and written down before anyone defends an approach, because once two people agree how they would tell who is right, the argument becomes an experiment. Then I would look for the cheapest test that separates the positions and can run in days, not weeks - usually a baseline plus both options on one frozen holdout, scored on the metric we just agreed. And I would commit up front to honouring the outcome, including when it goes against me, because that is the only thing that makes the next disagreement cheap. The one place I would hold ground regardless of the accuracy number is deployability: if a method cannot be served inside the budget, or cannot be rolled back, that is not a preference, it is a constraint, and I would say so as a constraint rather than as an opinion. A common version of this is choosing gradient-boosted trees for a fraud path - the argument for something deeper is real, and yet the tabular data, the inference budget and the retraining cost all point the same way.\"</p>\n<p><b>10. You think a teammate's design is wrong.</b></p>\n<ul>\n<li><i>Clarify:</i> is it wrong, or is it different from what you would do? Only the first justifies escalation.</li>\n<li><i>Contain:</i> raise it privately first and early, when changing it is cheap, and ask a question rather than making an assertion - what happens to this when the store is slow, what does rollback look like here.</li>\n<li><i>Diagnose:</i> separate the categories - a correctness or safety problem, a scaling problem that appears later, or a style preference. Fight for the first, flag the second with a trigger, and let the third go.</li>\n<li><i>Fix and verify:</i> if it is genuinely a correctness problem and you do not convince them, escalate with the disagreement stated as a risk with a consequence, not as a complaint about a person.</li>\n<li><i>Prevent:</i> design review as a normal habit rather than an event, so this is a conversation rather than a confrontation.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The first thing I make myself do is check whether it is wrong or just not what I would have done, because I have been wrong about that distinction before and it costs a lot of goodwill. If it is genuinely a problem, I raise it privately and early, while changing it is still cheap, and I try to raise it as a question rather than a judgement - what happens here when the store is slow, or what does rollback look like in this design. Questions bring out the thing they already worried about, and they let the person fix it themselves, which is a much better outcome than winning. I would also separate three categories out loud: correctness or safety, which I will push on; scaling problems that show up at a volume we are not at yet, which I would rather flag with a trigger - if we pass this load, this becomes a problem - than block on now; and style, which I let go. If it is genuinely a correctness issue and I have not convinced them, I escalate, and I would state it as a risk with a consequence rather than as a disagreement with a person. The nearest thing most people have is getting a standard adopted across an organisation - that works because it is easier to use than what teams had, not because its author was right, and the same lesson applies to design disagreements.\"</p>\n<p><b>11. Choosing between a simple approach and a complex one.</b></p>\n<ul>\n<li><i>Clarify:</i> what does the complexity buy, measured in the metric the business cares about, and what does it cost in latency, operability, and the number of people who can maintain it?</li>\n<li><i>Contain:</i> build the simple one first and put it in production. It becomes the baseline, and the baseline is what makes the complex option's value measurable rather than assumed.</li>\n<li><i>Diagnose:</i> if the simple version is within a small margin of the complex one, the complex one is not worth the extra operational burden - and if nobody has measured the margin, that is the finding.</li>\n<li><i>Fix and verify:</i> commit to one and say why, with the trigger that would change the decision - a volume, a metric gap, a new requirement.</li>\n<li><i>Prevent:</i> make a baseline one of the checks every model has to pass, so the comparison always exists.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"I default to the simple one and I try to be explicit about why, because it is not laziness - it is that the simple one gets to production sooner, and a thing in production generates the evidence that tells us whether the complex one is worth building. So I would build the simple version, ship it, and make it the baseline. Then the complex option has to beat a number rather than beat a hunch. The comparison I would insist on is not just the metric: it is the metric gain against the latency cost, the operational burden, and how many people on the team could debug it at 2 a.m. A two percent gain that only I can maintain is a liability, and I would say that to my own proposal as readily as to someone else's. The other thing I would do is commit, and name the trigger that would change my mind - if we pass this volume, or if this metric gap opens up, we revisit. What I would try hard not to do is present a list of options with no recommendation, because that is not a decision, it is a survey.\"</p>\n<p><b>12. A stakeholder wants a single number with no uncertainty attached.</b></p>\n<ul>\n<li><i>Clarify:</i> what decision are they making with it? A number for a go/no-go decision and a number for a capacity plan need very different treatments.</li>\n<li><i>Contain:</i> give them the number. Refusing to answer is not integrity, it is unhelpfulness - then attach the range and, crucially, say what the range means for their decision.</li>\n<li><i>Diagnose:</i> find the decision threshold - if the whole plausible range sits on one side of it, the uncertainty does not matter and you should say so; if it straddles the threshold, that is the finding and it is more useful than the point estimate.</li>\n<li><i>Fix and verify:</i> express the uncertainty in their units - customers, hours, cost - rather than in statistical language.</li>\n<li><i>Prevent:</i> make intervals normal in the reporting, so a range is not a special-case caveat.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"I give them the number, because refusing is not rigour, it is just unhelpful. Then I attach a range, and I translate the range into their units - not a confidence interval, but 'between forty and sixty people, and the plan is fine anywhere in that band' or 'we cannot tell yet which side of your threshold this falls on'. The question I actually want answered first is what decision the number is for, because that tells me whether the uncertainty matters at all. If every plausible value points the same way, I say so and the uncertainty is a footnote. If the range straddles their decision point, then that is the real finding and it is more valuable than the point estimate, because it tells them either to buy more information or to build the plan so it survives both outcomes. And I would say what would narrow the range and what it would cost. The version of this I would give is a forecast delivered with 95 percent intervals - the intervals only become useful when they are expressed as a planning band rather than as a statistic.\"</p>",
    "deeper": "<p>There is a reliable trap in scenario nine and ten, and it is the temptation to be the hero of your own story. Interviewers hear a great many answers in which the speaker was right, persisted, and was vindicated. Nobody can check those answers, and they land badly, because what the interviewer is looking at is not whether you were right. It is how you behave when the outcome is uncertain and the other person has a fair point.</p>\n<p>The strongest version includes a cost you paid: you conceded something, you were wrong about part of it, the relationship needed repair afterwards, or you escalated and it was awkward. A scenario answer with a cost in it is believable in a way that a clean victory never is, and it makes the technical content that surrounds it more credible too.</p>",
    "check": {
     "question": "In a design disagreement, which category justifies escalating rather than flagging and moving on?",
     "options": [
      "A style or naming preference",
      "A correctness or safety problem that has not been resolved after being raised directly",
      "A scaling limit that appears only above traffic volumes the system will not reach this year",
      "Any disagreement, since unresolved disagreements should always go up"
     ],
     "answer": 1,
     "explain": "Correctness and safety justify escalation, stated as a risk with a consequence. Future scaling limits are better flagged with a trigger condition, and style preferences are let go."
    }
   },
   {
    "id": "sq-4",
    "part": "field",
    "title": "Change and tools: a platform migration under deadline, and using an AI tool live",
    "body": "<p>Two more, both reported directly. The second is a scenario and an exercise at once: some interview processes include an AI round in which you are asked to solve a problem live with an AI tool.</p>\n<p><b>13. Migrate a production model to a new platform, under a deadline.</b></p>\n<ul>\n<li><i>Clarify:</i> what is actually moving - training, serving, features, or all three? What is the deadline driven by, and what is the cost of missing it against the cost of a bad cutover?</li>\n<li><i>Contain:</i> never a big-bang switch. Run both, compare, then move traffic - the old platform stays able to serve until the new one has proved itself on real traffic.</li>\n<li><i>Diagnose:</i> the risk in a migration is almost never the model, it is the edges - feature definitions that differ subtly, a different default for a missing value, a different tokenisation or type coercion, a different rounding. Compare predictions row by row on the same inputs and investigate every disagreement rather than checking that aggregate metrics match.</li>\n<li><i>Fix and verify:</i> shadow the new platform on live traffic, compare score distributions and per-row differences, then canary, then ramp, with the old path still available.</li>\n<li><i>Prevent:</i> a re-runnable comparison script that checks the two paths agree - that script is what makes the second migration cheap.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"The first thing I would establish is what is actually moving, because training, serving and the feature path have completely different risk profiles and people say 'migrate the model' meaning any of them. Then I would refuse the big-bang approach regardless of the deadline, because the safe version is not much slower: run both, compare, then move traffic. On the comparison, the mistake I would avoid is checking that the aggregate metrics match - they will, and it proves nothing. I want a row-by-row comparison of predictions on identical inputs, and I want to investigate every disagreement, because migration bugs live in the edges: a missing value that defaults differently, a type coercion, a rounding difference, a feature definition that is subtly not the same. Then shadow on live traffic, then canary, then ramp, with the old path still able to serve the whole load until the last moment. If the deadline genuinely cannot take that, then what I would negotiate is scope, not safety - move one model, or one traffic slice, by the date, and be explicit that the rest follows. And I would build the comparison as a reusable tool rather than a throwaway script, because the second migration is the one where it pays for itself. I want to be accurate about my own experience here: I name the platforms I have actually worked on, and say plainly that a migration onto one I have not used is reasoning I would bring rather than something I have already done.\"</p>\n<p><b>14. Solve a problem live using an AI tool, then critique what it produced.</b></p>\n<ul>\n<li><i>Clarify:</i> state the problem back and state your acceptance criteria <b>before</b> you prompt. The interviewer is watching whether you know what correct looks like independently of the tool.</li>\n<li><i>Contain:</i> keep the scope small - ask for one function, one query, one design sketch, not a whole solution - because you can only meaningfully review what you can read.</li>\n<li><i>Diagnose:</i> read the output critically and out loud: is it correct on the stated case, what are the edge cases it ignored, what did it invent, what would it do on empty input, on duplicates, at scale, and is any library or API it used real.</li>\n<li><i>Fix and verify:</i> write the test yourself. The test is the part that must not come from the tool, because it is the thing that decides whether the tool was right.</li>\n<li><i>Prevent:</i> state the general rule you work by - the tool accelerates the writing, and the review, the tests and the accountability stay with you.</li>\n</ul>\n<p><b>Model answer, first person.</b> \"Before I prompt anything I would say what the problem is and what correct looks like - the cases it has to handle, the edge cases, the complexity I would accept - because if I let the tool define the requirement I have lost the only advantage I have over it. Then I would ask for something small enough to actually review: one function, or one query, not a finished solution. When it comes back I would read it out loud and be specific about what I am checking: does it handle the empty case, duplicates, the boundary, does it do anything quadratic that could have been linear, and is every API it used real - because the most expensive failure mode is a confident call to something that does not exist. Then I write the tests myself, deliberately, because the test is the thing that decides whether the tool was right and it is the one part I would never delegate to it. If it got something wrong I would say what and why rather than just regenerating, because regenerating until it looks right is how you end up shipping something nobody understands. The general rule I work by is that these tools compress the writing, not the responsibility - the review, the tests and the accountability stay with me, and I would rather ship a smaller thing I can defend than a larger thing I am hoping about.\"</p>",
    "deeper": "<p>The live-AI scenario deserves one more layer, because it is really a question about how you work, dressed up as a technical one. What they are watching for is an engineer who has handed their judgement to the tool: prompts, pastes, runs, and declares success because it executed. What they want to see is someone who uses the tool hard and still owns the result.</p>\n<p>Two specific things show this cheaply. Say what you would <i>not</i> use it for in this context - anything touching customer data, credentials, or a decision with a legal consequence, where a plausible-looking wrong answer is worse than no answer. And when it produces something correct, say why it is correct rather than accepting it, because agreeing for the right reason and agreeing out of habit look identical from outside unless you say why.</p>",
    "check": {
     "question": "In a live exercise where you must solve a problem with an AI tool, what is the highest-scoring thing to do first?",
     "options": [
      "Write the most detailed possible prompt to get a complete solution in one attempt",
      "State the problem and your acceptance criteria - the cases, edge cases and complexity you would accept - before prompting at all",
      "Ask the tool to write its own tests so the verification is automated",
      "Regenerate until the output runs without errors, then explain it"
     ],
     "answer": 1,
     "explain": "Stating acceptance criteria first proves you know what correct looks like independently of the tool. It turns the exercise from a typing test into a demonstration of judgement, and it is the step almost everyone skips."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "sq-a1",
    "type": "followup",
    "title": "Hiring manager: a model you shipped is hurting customers at peak",
    "prompt": "The hiring manager says: \"You shipped a model change on Monday. On Wednesday, during your busiest week of the year, support tells you customers are being blocked who should not be. Walk me through what you do.\" Answer in first person, in about two minutes.",
    "timeboxSec": 150,
    "rubric": "Must-haves: (1) impact established first - how many customers, still happening or not, which slice - as one or two questions rather than five; (2) containment before diagnosis, explicitly named as reversible: roll back the model version, with the feature contract reverted alongside it, or restrict the affected slice if the blast radius is smaller that way; (3) the reasoning for containing first stated out loud - at peak, the cost of further customer impact exceeds the cost of being wrong about a reversible action; (4) communication named: who is told, when, and what the message contains, including a next-update time; (5) diagnosis ordered by recency and cost - model version, feature freshness and null rates, upstream schema change, traffic-mix shift into a weak slice - with the ordering rule stated rather than just the list; (6) verification on the same evidence that revealed the problem, plus shadow before the model takes traffic again; (7) the already-affected customers addressed explicitly rather than only the system; (8) prevention that is structural - a per-slice check before release, a shadow period, or an alert on a leading indicator - not \"be more careful\"; (9) honest grounding: rollback as a traffic switch through the mesh with a Git revert is something you have actually done, and decision-quality monitoring being the weaker half should be admitted rather than described as solved. Common mistakes: diagnosing before containing; no communication step; a list of hypotheses with no ordering rule; forgetting the affected customers; prevention that is a document; claiming automated metric-driven rollback, a shadow platform, or per-slice release checks you never actually had. {{HONESTY}}",
    "model": "<p>\"First question: how many customers, and is it still happening. Second: is it concentrated anywhere - a channel, a geography, a customer segment - because if it is, restricting that slice contains it with a much smaller blast radius than a full rollback.</p>\n<p>While that is being answered, I am rolling back, and I would say why I am doing it before I understand the cause: it is Wednesday of the busiest week, the rollback is reversible and takes seconds, and another twenty minutes of blocked customers is not recoverable. Where the rollback is a traffic switch through the mesh plus a Git revert, the model and its feature contract go back together - reverting a model onto a changed feature definition is a second incident, not a fix.</p>\n<p>Then I tell people, before I have an answer: support, the on-call channel, and whoever owns the customer relationship. What happened, what I have done, what customers should expect, and when I will update next. That last part is the one people skip and it is the one that stops everyone asking.</p>\n<p>Then diagnosis, ordered by what changed most recently and what is cheapest to check. Monday's model version first, since that is the obvious candidate. Then feature freshness and null rates, because a broken feature looks exactly like a bad model and is far more common. Then whether an upstream schema moved. Then the traffic mix, which is the one people forget at peak - a busy week is not just more traffic, it is a different population, and a model can be fine everywhere it was tested and bad on the slice that only shows up in season.</p>\n<p>When I have a fix, I verify it on the same evidence that revealed the problem - the blocked-customer reports and the slice they came from - and I want the model in shadow on live traffic before it makes decisions again, so I can compare it against the incumbent on real inputs with nothing at stake.</p>\n<p>The customers already affected are their own workstream: identify them from the prediction log, unblock them, and tell them. Fixing the system and leaving the people is half a job.</p>\n<p>Prevention: whichever check would have caught this becomes one of the checks before release, not a line in a runbook. If it was a slice, per-slice evaluation becomes one of those checks. If it was a feature, a serving-path comparison joins them. I would be honest that this is exactly where the gap usually is - the infrastructure monitoring is solid, and the decision-quality half is thinner than anyone would want, and closing that is the first thing I would build.\"</p>"
   },
   {
    "id": "sq-a2",
    "type": "followup",
    "title": "Hiring manager: ship it tomorrow, without a rollback path",
    "prompt": "The hiring manager says: \"The deadline is tomorrow and the rollback path is not ready. Your lead wants to ship anyway. What do you do?\" Answer in first person, in about 90 seconds.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) refusal to make it binary - the answer finds a version of yes rather than a yes or a no; (2) clarification of what is actually missing, with the distinct cases separated: no previous version deployable, no traffic switch, no way to revert the feature definition, or no tested procedure - and the observation that the last of these is hours of work; (3) at least two exposure-reducing alternatives offered: a traffic slice, a feature flag, shadow deployment producing predictions nobody acts on, or a rules fallback held ready; (4) the two numbers that decide it named - the real cost of missing the deadline and the cost of the failure at full exposure; (5) the irreversibility point made explicitly: a decision that cannot be undone is one to make jointly and to put in writing, with the exposure and the contingency stated; (6) the disagreement handled without either capitulating silently or refusing flatly, and without making the lead the antagonist; (7) grounded in what you have actually done - rollback as a traffic switch plus Git revert, and model and feature contract reverting together - without claiming an approval process you never ran. Common mistakes: a flat refusal presented as integrity; silent compliance; escalating over the lead's head as the first move; no alternative offered; treating \"put it in writing\" as a threat rather than as a record; claiming formal approval steps or a release-management process you never had. {{HONESTY}}",
    "model": "<p>\"I would try hard not to make this a yes-or-no conversation, because that ends either with a deployment I cannot undo or with a relationship I have damaged for nothing.</p>\n<p>So first, what is actually missing. Is there no previous version deployable, or no traffic switch, or no way to revert the feature definition alongside the model, or just no tested procedure? Those are very different. If it is the last one, it is a couple of hours and we should just do it before we ship, and I would offer to do it myself tonight.</p>\n<p>If it is genuinely not available by tomorrow, then I look for the version of yes with a smaller blast radius. Ship to one slice of traffic. Ship behind a flag that can be turned off, which is a rollback by another name. Ship in shadow so it produces predictions nobody acts on, and take the decision path next week. Or ship with the rules fallback wired up and ready to switch on. One of those is almost always acceptable, and all of them hit the date.</p>\n<p>What decides it is two numbers: what missing the deadline actually costs - and I would ask, because deadlines vary a lot in how real they are - and what this failing at full exposure costs. If the second is large and the first is a slipped internal date, that is not a close call.</p>\n<p>The thing I would say plainly is that shipping something I cannot take back is the one category of decision I do not want to make alone or informally. Not as a threat - I would put the exposure and the contingency in the ticket so we both know what we agreed, and if it goes wrong nobody is reconstructing it from memory.</p>\n<p>And I would be honest that this is not theoretical for me: in the fraud system, rollback being a traffic switch through the mesh plus a Git revert was the single thing that made shipping frequently safe, and the detail that mattered most was that the model and its feature contract reverted together.\"</p>"
   },
   {
    "id": "sq-a3",
    "type": "followup",
    "title": "Hiring manager: you and a scientist disagree about the method",
    "prompt": "The hiring manager says: \"Tell me about a time you disagreed with a data scientist about the right method - or if you would rather, tell me how you would handle it. How does it end?\" Answer in first person, in about 90 seconds.",
    "timeboxSec": 120,
    "rubric": "Must-haves: (1) the disagreement diagnosed before it is argued - the observation that a method disagreement is usually a constraint disagreement in disguise, with the two people optimising for different things and neither having said so; (2) the shared goal named first, and the objective and metric agreed and written down before any approach is defended; (3) the disagreement converted into something empirical - the cheapest experiment that separates the positions, runnable in days, on a frozen holdout with a baseline; (4) a stated commitment to honour the result publicly including when it goes against him; (5) one thing held as a constraint rather than a preference - deployability inside the latency budget, or the existence of a rollback path - stated as a constraint, with the reason; (6) a real cost acknowledged somewhere: a concession, a partial error, or the relationship needing repair, rather than a clean victory; (7) a concrete grounding - for example the choice of gradient-boosted trees for a fraud path, on tabular data, inside a 200 millisecond decision budget, with retraining cost as a factor - rather than an abstract disagreement. Common mistakes: an answer where the speaker is simply right and is vindicated; no agreed measure, so the disagreement stays a matter of opinion; escalation as the first move; treating deployability as an opinion rather than a constraint; inventing a named colleague or a specific dispute. {{HONESTY}}",
    "model": "<p>\"The first thing I try to do is work out what we are actually disagreeing about, because a method argument is very often a constraint argument in disguise. The scientist is optimising for accuracy on a holdout and I am optimising for a decision inside a latency budget that has to be operable at 2 a.m., and if neither of us says that out loud we will argue about architectures for a week.</p>\n<p>So I would try to get the objective and the measure agreed and written down before either of us defends an approach. Once two people agree how they would tell who is right, the argument stops being an argument and becomes an experiment - and that is much cheaper for both of us.</p>\n<p>Then I look for the smallest test that separates the positions and can run in days rather than weeks. Usually that is a baseline plus both options on one frozen holdout, scored on the metric we just agreed, with the slices we both care about. And I would commit up front to honouring the outcome including when it goes against me, because that commitment is the thing that makes the next disagreement cheap instead of expensive.</p>\n<p>There is one place I would hold ground regardless of the accuracy number, and I would state it as a constraint rather than a preference: if a method cannot be served inside the budget, or cannot be rolled back, that is not my opinion about modelling, it is a property of the system we have to operate. Saying it as a constraint keeps it out of the ego territory.</p>\n<p>The version of this I would give is choosing gradient-boosted trees for a fraud path. The case for something deeper was real and I do not think it was silly - what decided it was that the data was tabular, the inference had to fit inside a 200 millisecond decision budget end to end, and the thing needed retraining regularly on a schedule bounded by label arrival. What I gave up was some headroom on the metric, and I would rather say that than pretend the choice was free.\"</p>"
   },
   {
    "id": "sq-a4",
    "type": "followup",
    "title": "AI round: solve it with the tool, then tell us what is wrong with it",
    "prompt": "An interviewer says: \"Use an AI assistant to write this function now, share your screen, and then tell us what you think of what it gave you.\" Narrate the whole thing in first person, in about two minutes: what you say before you prompt, what you ask for, how you review it, and what you conclude.",
    "timeboxSec": 150,
    "rubric": "Must-haves: (1) acceptance criteria stated before prompting - the cases, the edge cases, the complexity that would be acceptable - explicitly so that correctness is defined independently of the tool; (2) a deliberately small scope requested, on the stated grounds that you can only meaningfully review what you can read; (3) a critical read performed out loud with specific checks named: empty input, duplicates, boundaries, whether anything is quadratic that could be linear, and whether every API or library used actually exists; (4) hallucinated APIs identified as the most expensive failure mode, because the code looks confident and fails only at runtime; (5) tests written by you rather than by the tool, with the reason given - the test is what decides whether the tool was right, so it is the one part not delegated; (6) a refusal to fix by regeneration, with the reason: regenerating until it looks right produces code nobody understands; (7) a stated general rule that the tool compresses the writing but not the responsibility - review, tests and accountability stay with the engineer; (8) at least one named situation where you would not use the tool at all, such as anything touching customer data, credentials, or a decision with legal consequence. Common mistakes: prompting first and reviewing afterwards; accepting correct output without saying why it is correct; asking the tool to write its own tests; treating \"it ran\" as verification; a critique that is vague scepticism rather than specific faults; claiming an AI-tool policy you did not set. {{HONESTY}}",
    "model": "<p>\"Before I type anything into it, let me say what correct looks like, because if I let the tool define the requirement I have given away the only advantage I have over it. So: here is the input, here is the output, here are the cases it has to handle - and specifically the empty input, duplicates, and the boundary case at the end of the range. I would accept linear time; anything quadratic I would push back on. That is my acceptance criteria and I am writing it down first.</p>\n<p>Now I would ask for something small - this one function, not a finished solution - because I can only meaningfully review what I can read, and a hundred lines that look plausible is worse than twenty I have actually checked.</p>\n<p>Reading what came back, out loud. Does it handle the empty input, or does it index into the first element? What does it do with duplicates - and is that what I said I wanted, or what it assumed? What happens at the boundary. Is there a nested loop where a hash map would do. And the one I check every time: is every function it called real. A confident call to an API that does not exist is the most expensive failure mode with these tools, because it looks exactly as authoritative as the correct code around it, and it only fails at runtime.</p>\n<p>Then I write the tests myself. Deliberately, and I would say why: the test is the thing that decides whether the tool was right, so it is the one part I would never take from it. If I ask it for the tests too, I have asked it to grade its own work.</p>\n<p>If something is wrong, I would say what and why and fix that specific thing, rather than regenerating until it looks right - regenerating is how you end up shipping code nobody on the team understands, including the person who shipped it.</p>\n<p>And where it is correct, I would still say why it is correct, because agreeing for the right reason and agreeing out of habit look the same from outside.</p>\n<p>The rule I actually work by is that these tools compress the writing, not the responsibility - the review, the tests and the accountability stay with me. And there are places I would not reach for one at all: anything where I would be pasting customer data or credentials, and anything where the answer carries a legal consequence and a plausible-looking wrong answer is worse than no answer.\"</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "coding-drills": {
  "id": "coding-drills",
  "title": "The coding round",
  "day": 3,
  "level": "danger",
  "levelLabel": "Format unknown - practise a little every day",
  "why": "Expect a coding or pseudocode round, often without being told in advance which kind it will be. That is the whole problem: a data-structures round and an ML-focused data-processing round need different reflexes. The good news is that they overlap in one place - writing short, correct, commented Python by hand while you talk. You need the patterns worth knowing, worked pseudocode for the tasks you are most likely to get in an ML context, the SQL you cannot skip, and a routine for the first ninety seconds that makes freezing much less likely.",
  "learn": [
   {
    "id": "cdr-0",
    "part": "field",
    "title": "Key terms",
    "body": "<p>Precision here is cheap and it is noticed. Define these once.</p>\n<ul>\n<li><b>Time complexity, big-O</b> - how running time grows with input size, ignoring constants. <code>O(1)</code> means constant, not growing with input size at all; <code>O(n)</code> means proportional to input size; <code>O(n log n)</code> is typical of sorting; <code>O(n squared)</code> means every pair.</li>\n<li><b>Space complexity</b> - the same for additional memory used, not counting the input.</li>\n<li><b>Amortised</b> - averaged over a sequence of operations. Appending to a dynamic array is amortised constant time even though an occasional append copies everything.</li>\n<li><b>In-place</b> - modifying the input rather than allocating a new structure.</li>\n<li><b>Streaming or online algorithm</b> - one pass, bounded memory, no ability to revisit earlier items. The default assumption in any event-processing question.</li>\n<li><b>Idempotent</b> - applying it twice has the same effect as applying it once.</li>\n<li><b>Deterministic</b> - the same input always yields the same output. Matters for tests and for audit.</li>\n<li><b>Edge case</b> - empty input, one element, all elements identical, values at the boundary, negative or zero, duplicates, unsorted input assumed sorted, integer overflow in languages that have it.</li>\n<li><b>Invariant</b> - something that stays true at every step of a loop. Stating one aloud is the fastest way to convince someone your loop is correct.</li>\n<li><b>Unit test</b> (<a href=\"#mlops-tooling/mot-f1\">Source control and project layout for ML</a>) - a small automated check of one behaviour, including at least one edge case.</li>\n<li><b>Window function (SQL)</b> - a computation over rows related to the current row, written with <code>OVER</code> and usually <code>PARTITION BY</code> and <code>ORDER BY</code>, which does not collapse the rows the way <code>GROUP BY</code> does.</li>\n<li><b>Common table expression (CTE)</b> - a named subquery introduced with <code>WITH</code>, used to make multi-step SQL readable.</li>\n</ul>",
    "deeper": "<p>One habit worth more than any pattern: after writing a loop, say the invariant out loud. \"At the top of each iteration, the window holds exactly the elements within k of the current index, and the running sum equals their total.\" If that sentence is true, the loop is almost certainly correct, and if you cannot say it, you have found your bug without running anything.</p>\n<p>The second habit is to name your edge cases before you write, not after. Empty input, single element, all duplicates, and the boundary value. Saying them first turns them into requirements; saying them afterwards turns them into corrections.</p>",
    "check": {
     "question": "What is the fastest way to convince an interviewer your loop is correct?",
     "options": [
      "Run it on several test cases",
      "State the loop invariant aloud - what is true at the top of every iteration",
      "Explain the time complexity",
      "Add comments to each line"
     ],
     "answer": 1,
     "explain": "An invariant is a correctness argument rather than evidence. It also finds your own bugs faster than testing does."
    }
   },
   {
    "id": "cdr-f1",
    "part": "field",
    "title": "The six patterns, with pseudocode models",
    "body": "<p>If the round is data structures, it is very likely one of these. Each is given as a model you should be able to reproduce.</p>\n<p><b>1. Sliding window.</b> For contiguous subarrays or time windows.</p>\n<ul>\n<li><code>left = 0; running = 0</code></li>\n<li><code>for right in range(n): running += a[right]</code></li>\n<li><code>while window_invalid(left, right): running -= a[left]; left += 1</code></li>\n<li><code>best = max(best, right - left + 1)</code></li>\n</ul>\n<p>Time <code>O(n)</code>, space <code>O(1)</code>. Each element enters and leaves once, which is the invariant to say aloud.</p>\n<p><b>2. Heap for top-k.</b> Keep the k best seen so far.</p>\n<ul>\n<li><code>heap = []  # min-heap of size at most k</code></li>\n<li><code>for x in stream: heappush(heap, x)</code></li>\n<li><code>if len(heap) &gt; k: heappop(heap)  # drop the smallest</code></li>\n</ul>\n<p>Time <code>O(n log k)</code>, space <code>O(k)</code>. Note why a min-heap gives the k largest: the root is the weakest survivor.</p>\n<p><b>3. Breadth-first search on a graph.</b> Shortest path in an unweighted graph, or level order.</p>\n<ul>\n<li><code>queue = deque([start]); seen = {start}</code></li>\n<li><code>while queue: node = queue.popleft()</code></li>\n<li><code>for nbr in adj[node]: if nbr not in seen: seen.add(nbr); queue.append(nbr)</code></li>\n</ul>\n<p>Time <code>O(V + E)</code>. Mark on enqueue, not on dequeue, or nodes get queued twice.</p>\n<p><b>4. Depth-first search.</b> Connectivity, cycle detection, topological order. Iterative with an explicit stack avoids recursion limits on large graphs.</p>\n<p><b>5. Dynamic programming.</b> State, transition, base case, order.</p>\n<ul>\n<li><code>dp[0] = base</code></li>\n<li><code>for i in range(1, n+1): dp[i] = best over choices of f(dp[i-1], ...)</code></li>\n</ul>\n<p>Say the state definition in words before writing anything: \"dp[i] is the best value using the first i items\". Most dynamic-programming failures are undefined state, not wrong arithmetic.</p>\n<p><b>6. Hash map and two pointers.</b> Counting, grouping, deduplication, pair-finding on sorted data. Usually the simplest correct answer, and simplest correct wins.</p>",
    "deeper": "<p>Pattern recognition cues, which are worth more than the implementations. Contiguous subarray or a time window means sliding window. Top-k or \"k largest or smallest\" means a heap. Shortest path in an unweighted graph means breadth-first search. Reachability, components or ordering means depth-first search. \"Count the ways\" or \"best over choices with overlapping subproblems\" means dynamic programming. \"Have I seen this before\" means a hash map. Sorted input with a pair or triple to find means two pointers.</p>\n<p>If none of them fits, say so and start with the brute force, stating its complexity. A working brute force plus a stated improvement path is a pass; twenty minutes of silence looking for the clever solution is not.</p>",
    "check": {
     "question": "A stream of billions of items, and you need the 100 largest. What structure and what complexity?",
     "options": [
      "Sort the stream, then take the last 100 - O(n log n) time",
      "A min-heap of size 100, pushing then popping the smallest - O(n log k) time and O(k) space",
      "A max-heap of the whole stream - O(n) space",
      "A hash map of counts - O(n) time"
     ],
     "answer": 1,
     "explain": "Bounded memory is the point in a stream. A size-k min-heap keeps the k largest because the root is the weakest survivor and is evicted first."
    }
   },
   {
    "id": "cdr-f2",
    "part": "field",
    "title": "ML-style coding tasks, part 1: streaming statistics and windows",
    "body": "<p>If the round is ML-focused, these are the forms it takes. Each comes with a model to reproduce.</p>\n<p><b>Streaming mean and variance (Welford).</b> One pass, bounded memory, numerically stable - the naive sum-of-squares approach loses precision badly on large streams, and saying so is the point of the question.</p>\n<ul>\n<li><code>n = 0; mean = 0.0; m2 = 0.0</code></li>\n<li><code>def update(x): n += 1; d = x - mean; mean += d / n; m2 += d * (x - mean)</code></li>\n<li><code>variance = m2 / (n - 1) if n &gt; 1 else 0.0</code></li>\n</ul>\n<p>Edge cases: n equals zero and n equals one. Say why <code>n - 1</code>: the sample variance is unbiased with Bessel's correction.</p>\n<p><b>Rolling window feature over a time window.</b> Count or sum of events for a key within the last <code>w</code> seconds, assuming events arrive in non-decreasing time order.</p>\n<ul>\n<li><code>windows = defaultdict(deque)  # key -&gt; timestamps</code></li>\n<li><code>def add(key, ts): dq = windows[key]; dq.append(ts)</code></li>\n<li><code>while dq and dq[0] &lt;= ts - w: dq.popleft()</code></li>\n<li><code>return len(dq)</code></li>\n</ul>\n<p>Amortised constant time per event, memory proportional to events in the window. The follow-up to be ready for: what if events arrive out of order. Then a deque is not enough and you need either a bounded reorder buffer with a watermark, or a sorted structure, and you must say what you do with an event that arrives after its window closed.</p>\n<p><b>Exponentially weighted moving average.</b> Constant memory, no window to manage, smooth decay.</p>\n<ul>\n<li><code>ewma = x if ewma is None else alpha * x + (1 - alpha) * ewma</code></li>\n</ul>\n<p>Trade-off to state: no explicit window boundary and much less memory, at the cost of a less interpretable \"last N minutes\" meaning.</p>",
    "deeper": "<p>The deeper version of the rolling-window question is what happens under skew: one key with a million events in the window while a million keys have one each. The deque-per-key structure degrades in both directions - memory for the hot key, and per-key overhead for the long tail.</p>\n<p>Answers worth having: cap the per-key structure and fall back to an approximate count for hot keys; expire idle keys on a timer rather than holding empty deques forever; or switch to a bucketed count - fixed-width time buckets per key with a rolling sum - which trades exactness at the window edge for constant memory per key. That last one is what most production systems actually do, and naming it counts for a lot.</p>",
    "check": {
     "question": "Why use Welford's method rather than accumulating the sum and the sum of squares?",
     "options": [
      "It is faster",
      "It is numerically stable - the naive approach subtracts two large nearly-equal numbers and loses precision on large streams",
      "It uses less memory",
      "It handles out-of-order data"
     ],
     "answer": 1,
     "explain": "Both are constant memory and one pass. The difference is catastrophic cancellation in the naive formula when the mean is large relative to the variance."
    }
   },
   {
    "id": "cdr-f3",
    "part": "field",
    "title": "ML-style coding tasks, part 2: dedup, top-k per key, rate limiting",
    "viz": "rate-limiter-token-bucket",
    "body": "<p><b>Deduplication with idempotency keys.</b> The event stream is at-least-once, so the same event may arrive twice. Bounded memory means you cannot remember every key forever.</p>\n<ul>\n<li><code>seen = OrderedDict()  # key -&gt; timestamp, oldest first</code></li>\n<li><code>def is_duplicate(key, ts): evict_older_than(ts - horizon)</code></li>\n<li><code>if key in seen: return True</code></li>\n<li><code>seen[key] = ts; return False</code></li>\n</ul>\n<p>The design point to say aloud: you cannot deduplicate forever, so you choose a horizon based on the realistic redelivery window, and you accept that a redelivery beyond the horizon is not caught. That is a stated trade-off, not a bug. Mention that at-least-once delivery plus an idempotency key gives effectively-once processing.</p>\n<p><b>Top-k per key.</b> The k highest-scoring items for each entity, in one pass.</p>\n<ul>\n<li><code>heaps = defaultdict(list)  # key -&gt; min-heap of size at most k</code></li>\n<li><code>for key, score, item in stream: h = heaps[key]</code></li>\n<li><code>heappush(h, (score, item))</code></li>\n<li><code>if len(h) &gt; k: heappop(h)</code></li>\n</ul>\n<p>Time <code>O(n log k)</code>, space <code>O(number of keys times k)</code>. The follow-up is what to do when the key count does not fit in memory: partition by key hash across workers, or spill.</p>\n<p><b>Rate limiter, token bucket.</b> Allow bursts up to a capacity, refill at a steady rate.</p>\n<ul>\n<li><code>def allow(key, now): b = buckets[key]</code></li>\n<li><code>b.tokens = min(capacity, b.tokens + (now - b.last) * rate)</code></li>\n<li><code>b.last = now</code></li>\n<li><code>if b.tokens &gt;= 1: b.tokens -= 1; return True</code></li>\n<li><code>return False</code></li>\n</ul>\n<p>Compare with a fixed-window counter, which is simpler but allows twice the rate across a window boundary, and with a sliding-window log, which is exact but stores every timestamp. Naming that comparison is what the question is for.</p>",
    "deeper": "<p>The distributed follow-up on the rate limiter is worth preparing, because it is the natural escalation: with several instances, per-instance buckets allow the total rate to exceed the limit by a factor equal to the instance count. The options are a shared store with an atomic operation, which costs a network round trip on every request; sharding by key so each key is owned by one instance, which keeps it local but complicates rebalancing; or allocating each instance a share of the budget, which is cheap and imprecise.</p>\n<p>The right answer depends on whether the limit is a hard contractual one or a protective one. Saying that - and then choosing - is the answer, because it shows the decision follows the requirement rather than a preference.</p>",
    "check": {
     "question": "Why must a deduplication cache have a horizon rather than remembering every key?",
     "options": [
      "Because keys expire on the producer side",
      "Because memory is bounded, so you choose a horizon matching the realistic redelivery window and accept that later redeliveries are not caught",
      "Because duplicates only occur immediately after the original",
      "Because the hash function collides over time"
     ],
     "answer": 1,
     "explain": "Unbounded state is not an option in a stream. The horizon is a deliberate trade-off between memory and the probability of catching a late redelivery."
    }
   },
   {
    "id": "cdr-f4",
    "part": "field",
    "title": "ML-style coding tasks, part 3: train-eval by hand, batch inference, SQL",
    "body": "<p><b>A train and evaluate loop with no libraries.</b> Sometimes asked to check that you understand what a framework does. Logistic regression by gradient descent is the standard example.</p>\n<ul>\n<li><code>w = [0.0] * d; b = 0.0</code></li>\n<li><code>for epoch in range(epochs): for x, y in data:</code></li>\n<li><code>z = dot(w, x) + b; p = 1 / (1 + exp(-z))</code></li>\n<li><code>g = p - y  # gradient of log loss with respect to z</code></li>\n<li><code>for j in range(d): w[j] -= lr * g * x[j]</code></li>\n<li><code>b -= lr * g</code></li>\n</ul>\n<p>Then evaluation, and this is the part they are really watching: split the data before you fit anything; compute a confusion matrix; derive precision as true positives over predicted positives and recall as true positives over actual positives; and say why accuracy is useless on imbalanced data.</p>\n<p><b>Batch inference under a latency budget.</b> Score a large set within a deadline.</p>\n<ul>\n<li><code>for batch in chunks(items, batch_size): scores = model.predict(batch)</code></li>\n<li><code>if elapsed() &gt; budget: break  # or degrade</code></li>\n</ul>\n<p>The content is not the loop, it is the reasoning: larger batches raise throughput and raise per-item latency; the deadline decides the batch size; and you must define what happens to unscored items - a default decision, a queue for later, or a cheaper model.</p>\n<p><b>SQL window functions.</b> The SQL topics that come up are windows, common table expressions and joins. The three patterns to have ready:</p>\n<ul>\n<li><code>ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY ts DESC)</code> then filter to 1 - the latest row per group.</li>\n<li><code>SUM(amount) OVER (PARTITION BY customer_id ORDER BY ts ROWS BETWEEN 6 PRECEDING AND CURRENT ROW)</code> - a rolling total over the last seven rows.</li>\n<li><code>LAG(ts) OVER (PARTITION BY customer_id ORDER BY ts)</code> - the previous event time, for computing gaps between events.</li>\n</ul>\n<p>Know the difference between <code>ROW_NUMBER</code>, <code>RANK</code> and <code>DENSE_RANK</code> on ties, and between <code>ROWS</code> and <code>RANGE</code> in a window frame.</p>",
    "deeper": "<p>A likely SQL follow-up in this domain: compute a point-in-time correct feature in SQL. The pattern is a self-join or a window with a frame bounded by the label's timestamp - for each labelled event, aggregate only the rows strictly before it, usually with <code>RANGE BETWEEN INTERVAL ... PRECEDING AND CURRENT ROW</code> or an inequality join on time.</p>\n<p>Getting the strictness right is the whole question: <code>&lt;=</code> versus <code>&lt;</code> on the boundary decides whether the labelled event contributes to its own feature. Saying that out loud, unprompted, is exactly what a machine-learning-aware interviewer is listening for in a SQL question.</p>",
    "check": {
     "question": "In a batch inference loop with a hard deadline, what is the most important thing to specify?",
     "options": [
      "The optimal batch size",
      "What happens to items that are not scored before the deadline - a default decision, a deferred queue, or a cheaper model",
      "The model's memory footprint",
      "Whether to use multithreading"
     ],
     "answer": 1,
     "explain": "The deadline guarantees some items will be unscored. Defining their treatment is the design; the batch size is tuning."
    }
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "cdr-a1",
    "type": "code",
    "title": "Write the streaming feature computer",
    "prompt": "Write, in Python or clear pseudocode, a class that consumes a stream of events - each with a key, a timestamp and an amount - and can answer at any moment: the count of events for a key in the last W seconds, the sum of amounts in that window, and a running mean and variance of amounts for that key over all time. Assume at-least-once delivery, so duplicates are possible. State complexity, edge cases, and the tests you would write.",
    "timeboxSec": 1800,
    "rubric": "Must-haves: (1) a deque per key holding timestamps and amounts, with expiry of entries older than the window on each access, giving amortised constant time per event; (2) a running window sum maintained incrementally rather than recomputed, with subtraction on eviction; (3) Welford's method for running mean and variance, with the explicit reason - numerical stability against catastrophic cancellation in the naive sum-of-squares approach - and correct handling of n equals zero and n equals one; (4) duplicate suppression via an idempotency key with a bounded horizon, and the stated reasoning that unbounded state is impossible so the horizon is chosen from the realistic redelivery window; (5) out-of-order events addressed explicitly, either by assuming non-decreasing timestamps and saying so, or by describing a bounded reorder buffer with a watermark and a stated policy for events arriving after their window closed; (6) complexity given for both time and space, per operation and overall; (7) edge cases named before coding: empty state, first event, all events outside the window, a key never seen, and the hot-key skew case; (8) at least three tests named, including one edge case and one duplicate case. Bonus: names the bucketed-count alternative for hot keys as the constant-memory-per-key option and states its cost as inexactness at the window edge; names idle-key expiry so empty structures are not retained forever. Common mistakes: recomputing the window sum on every query; naive variance; unbounded dedup state; ignoring out-of-order arrival; no complexity statement; no tests. {{HONESTY}}",
    "model": "<p><b>Assumptions stated first.</b> Timestamps are non-decreasing within a key; I will say what changes if they are not. Memory is bounded, so nothing is retained forever.</p>\n<ul>\n<li><code>class FeatureComputer:</code></li>\n<li><code>  def __init__(self, window_s, dedup_horizon_s):</code></li>\n<li><code>    self.win = defaultdict(deque)   # key -&gt; deque of (ts, amount)</code></li>\n<li><code>    self.wsum = defaultdict(float)  # key -&gt; sum of amounts in window</code></li>\n<li><code>    self.stats = defaultdict(lambda: [0, 0.0, 0.0])  # key -&gt; n, mean, m2</code></li>\n<li><code>    self.seen = OrderedDict()       # event_id -&gt; ts, oldest first</code></li>\n<li><code></code></li>\n<li><code>  def add(self, event_id, key, ts, amount):</code></li>\n<li><code>    self._expire_seen(ts)</code></li>\n<li><code>    if event_id in self.seen: return False   # duplicate, drop</code></li>\n<li><code>    self.seen[event_id] = ts</code></li>\n<li><code>    dq = self.win[key]; dq.append((ts, amount)); self.wsum[key] += amount</code></li>\n<li><code>    self._expire_window(key, ts)</code></li>\n<li><code>    n, mean, m2 = self.stats[key]</code></li>\n<li><code>    n += 1; d = amount - mean; mean += d / n; m2 += d * (amount - mean)</code></li>\n<li><code>    self.stats[key] = [n, mean, m2]; return True</code></li>\n<li><code></code></li>\n<li><code>  def _expire_window(self, key, now):</code></li>\n<li><code>    dq = self.win[key]</code></li>\n<li><code>    while dq and dq[0][0] &lt;= now - self.window_s:</code></li>\n<li><code>      old_ts, old_amt = dq.popleft(); self.wsum[key] -= old_amt</code></li>\n<li><code></code></li>\n<li><code>  def count(self, key, now): self._expire_window(key, now); return len(self.win[key])</code></li>\n<li><code>  def total(self, key, now): self._expire_window(key, now); return self.wsum[key]</code></li>\n<li><code>  def mean_var(self, key):</code></li>\n<li><code>    n, mean, m2 = self.stats[key]</code></li>\n<li><code>    return (0.0, 0.0) if n == 0 else (mean, 0.0 if n == 1 else m2 / (n - 1))</code></li>\n</ul>\n<p><b>Why these choices.</b> The window sum is maintained incrementally, added on insert and subtracted on eviction, so a query is amortised constant rather than linear in window size. Variance uses Welford because the naive sum-of-squares subtracts two large nearly-equal numbers and loses precision badly on long streams - the running form never forms that difference. Dedup has a horizon because unbounded state is not an option; I would size it from the realistic redelivery window, and I would say plainly that a redelivery beyond the horizon is not caught. That is a stated trade-off, not a bug. At-least-once delivery plus this key gives effectively-once processing within the horizon.</p>\n<p><b>Complexity.</b> Add is amortised constant time - each event is appended once and evicted once. Queries are amortised constant after expiry. Space is proportional to events in the window plus dedup entries in the horizon, and it is bounded rather than growing without limit.</p>\n<p><b>Edge cases.</b> No events for a key. First event, where variance is zero and n minus one would divide by zero. All events outside the window, leaving an empty deque and a zero sum. A hot key with a very large window, and a long tail of keys with one event each - the deque-per-key structure suffers in both directions, so I would cap the per-key structure and, for hot keys, switch to fixed-width time buckets with a rolling sum, which is constant memory per key at the cost of exactness at the window edge. I would also expire idle keys on a timer so empty structures are not retained.</p>\n<p><b>Out of order.</b> As written I assume non-decreasing timestamps. If not, the deque is wrong, because an old event appended at the tail will not be evicted correctly. I would either buffer briefly and release by watermark, or use a sorted structure, and either way I must define what happens to an event arriving after its window closed - here, it is dropped for the live feature and still recorded in history, and the training set must use the decision-time value, not the corrected one.</p>\n<p><b>Tests.</b> Two events for one key inside the window gives count two. An event older than the window is evicted and the sum drops. The same event identifier submitted twice is counted once. Variance of a single event is zero, and of two known values matches the hand-computed sample variance. And a test that the window sum after many insertions and evictions equals a recomputation from the deque, which catches drift in the incremental sum.</p>"
   },
   {
    "id": "cdr-a2",
    "type": "code",
    "title": "Top-k per key, then make it distributed",
    "prompt": "Write a function that takes a stream of (key, score, item) and returns the k highest-scoring items per key. State complexity. Then answer: what changes if the number of keys does not fit in memory, and what changes if the stream is split across ten machines?",
    "timeboxSec": 1200,
    "rubric": "Must-haves: (1) a min-heap of size at most k per key, with push then conditional pop, and the explanation that a min-heap gives the k largest because the root is the weakest survivor; (2) complexity stated as O(n log k) time and O(number of keys times k) space; (3) tie handling addressed - either a deterministic tiebreak in the heap tuple or an explicit statement that ties are broken arbitrarily; (4) edge cases named: fewer than k items for a key, k equal to zero, equal scores, and a key seen once; (5) the memory answer covering partitioning by key hash, external sort or spill to disk, and the observation that per-key state is the scaling limit rather than stream length; (6) the distributed answer stating that top-k is decomposable - each machine computes local top-k per key and a merge step takes the top-k of the union - with the reason that an item outside a machine's local top-k cannot be in the global top-k for that key; (7) the caveat that this holds because the key partition is respected, and that if keys are split across machines the merge must still be per key; (8) at least two tests named. Bonus: notes that approximate top-k structures exist when even local state is too large, and names the exactness cost; notes skew where one key dominates and the mitigation of splitting that key's work by a secondary partition then merging. Common mistakes: a max-heap of everything; sorting per key; no complexity; claiming top-k is not decomposable; ignoring ties. {{HONESTY}}",
    "model": "<ul>\n<li><code>def top_k_per_key(stream, k):</code></li>\n<li><code>  heaps = defaultdict(list)</code></li>\n<li><code>  for i, (key, score, item) in enumerate(stream):</code></li>\n<li><code>    h = heaps[key]</code></li>\n<li><code>    heappush(h, (score, i, item))   # i breaks ties deterministically</code></li>\n<li><code>    if len(h) &gt; k: heappop(h)       # evict the smallest</code></li>\n<li><code>  return {key: sorted(h, reverse=True) for key, h in heaps.items()}</code></li>\n</ul>\n<p><b>Why a min-heap.</b> The root is the weakest of the k kept so far, so it is exactly the element to evict when a better one arrives. A max-heap would put the strongest at the root, which is the one you never want to remove.</p>\n<p><b>Complexity.</b> O(n log k) time, since each item does at most one push and one pop on a heap of size k. Space is O(number of keys times k), which is the real constraint - it scales with distinct keys, not with stream length.</p>\n<p><b>Edge cases.</b> Fewer than k items for a key returns all of them. k equal to zero returns empty. Equal scores are broken deterministically by the sequence index, which I would state rather than leave to chance, because non-determinism makes tests flaky. A key seen once returns one item.</p>\n<p><b>Too many keys for memory.</b> The limit is per-key state, so partition by a hash of the key and process each partition independently, or spill partitions to disk and process them one at a time. If even that is too much, an approximate structure - a sketch that tracks heavy hitters - trades exactness for constant memory, and I would say which exactness is being given up.</p>\n<p><b>Split across ten machines.</b> Top-k is decomposable, which is the useful property. Each machine computes its own top-k per key over the shard it sees, then a merge step takes the top-k of the union of those per key. This is exact, because an item that is not in a machine's local top-k for a key cannot be in the global top-k for that key - there are already k better items on that machine alone. The merge only needs to move k items per key per machine rather than the whole stream, which is why this scales.</p>\n<p>Two cautions. The merge must still be grouped by key, so if the partitioning does not keep a key on one machine, the merge is a shuffle rather than a local operation. And skew is the practical problem: if one key dominates, that machine does most of the work. The mitigation is a secondary partition on that key, computing partial top-k independently and merging, which is safe precisely because the operation is decomposable.</p>\n<p><b>Tests.</b> A key with fewer than k items returns all of them, in order. A key with more than k returns exactly the k highest, verified against a sorted brute force. Ties produce a deterministic result across runs. And a merge test: splitting a stream in half, computing local top-k, merging, and asserting equality with the single-pass result.</p>"
   },
   {
    "id": "cdr-a3",
    "type": "code",
    "title": "Train and evaluate, no libraries",
    "prompt": "Implement logistic regression training by gradient descent and evaluation, from scratch, with no machine-learning libraries. Then compute precision, recall and a precision-recall trade-off on an imbalanced test set. State every design decision you make and every place where a mistake would leak information.",
    "timeboxSec": 1800,
    "rubric": "Must-haves: (1) correct logistic regression: linear score, sigmoid, log-loss gradient reducing to predicted minus actual with respect to the linear score, and a weight update with a learning rate; (2) the split performed before any fitting-based preprocessing, with an explicit statement that fitting a scaler or imputer on the full dataset leaks test information into training; (3) numerical care named - overflow in the exponential for large negative or positive scores, and the standard guard of clipping or a branch on the sign; (4) a confusion matrix computed, with precision defined as true positives over predicted positives and recall as true positives over actual positives, and division-by-zero handled when no positives are predicted; (5) accuracy explicitly rejected for imbalanced data with the majority-class baseline given as the reason; (6) a threshold sweep producing the precision-recall trade-off, with the point that the operating point is a business choice tied to the cost asymmetry or to review capacity; (7) at least three leakage sources named: fitting preprocessing on all data, a feature only knowable after the label, and a random split on temporal data; (8) tests named, including a separable toy dataset where the model must reach near-perfect separation, and a check that precision and recall match hand-computed values on a tiny confusion matrix. Bonus: mentions class weighting or resampling and its effect on calibration; mentions shuffling and a fixed seed for reproducibility; mentions convergence checking rather than a fixed epoch count. Common mistakes: wrong gradient; scaling before splitting; accuracy as the metric; no threshold sweep; no leakage discussion; unguarded exponential. {{HONESTY}}",
    "model": "<ul>\n<li><code>def sigmoid(z):</code></li>\n<li><code>  if z &gt;= 0: return 1.0 / (1.0 + exp(-z))</code></li>\n<li><code>  e = exp(z); return e / (1.0 + e)     # guards overflow for large |z|</code></li>\n<li><code></code></li>\n<li><code>def train(X, y, lr=0.1, epochs=50, seed=0):</code></li>\n<li><code>  d = len(X[0]); w = [0.0] * d; b = 0.0</code></li>\n<li><code>  idx = list(range(len(X))); rng = Random(seed)</code></li>\n<li><code>  for _ in range(epochs):</code></li>\n<li><code>    rng.shuffle(idx)</code></li>\n<li><code>    for i in idx:</code></li>\n<li><code>      x = X[i]; z = sum(w[j] * x[j] for j in range(d)) + b</code></li>\n<li><code>      g = sigmoid(z) - y[i]           # d(log loss)/dz</code></li>\n<li><code>      for j in range(d): w[j] -= lr * g * x[j]</code></li>\n<li><code>      b -= lr * g</code></li>\n<li><code>  return w, b</code></li>\n<li><code></code></li>\n<li><code>def confusion(y_true, y_score, thr):</code></li>\n<li><code>  tp = fp = fn = tn = 0</code></li>\n<li><code>  for yt, s in zip(y_true, y_score):</code></li>\n<li><code>    yp = 1 if s &gt;= thr else 0</code></li>\n<li><code>    if yp and yt: tp += 1</code></li>\n<li><code>    elif yp and not yt: fp += 1</code></li>\n<li><code>    elif not yp and yt: fn += 1</code></li>\n<li><code>    else: tn += 1</code></li>\n<li><code>  return tp, fp, fn, tn</code></li>\n<li><code></code></li>\n<li><code>def prec_rec(tp, fp, fn):</code></li>\n<li><code>  p = tp / (tp + fp) if (tp + fp) else 0.0</code></li>\n<li><code>  r = tp / (tp + fn) if (tp + fn) else 0.0</code></li>\n<li><code>  return p, r</code></li>\n</ul>\n<p><b>Design decisions, stated.</b> The sigmoid is branched on the sign of z to avoid overflow in the exponential - a large positive z overflows the naive form. The gradient of log loss with respect to the linear score is simply predicted minus actual, which is why the update is that clean; I would say that rather than leave it looking like magic. Shuffling with a fixed seed makes runs reproducible. A fixed epoch count is a shortcut; in real use I would check convergence on a validation loss rather than count epochs.</p>\n<p><b>Where leakage would happen.</b> Three places. First, any preprocessing fitted before the split - a scaler, an imputer, an encoder - carries test information into training, so the split comes first and the transforms are fitted on train only. Second, a feature that could only be populated after the label existed; on this kind of data that is the commonest and most damaging error, and it produces excellent offline metrics and a useless model. Third, a random split on temporal data, which lets the model see the future; if there is a time dimension, the split must be by time and, if features use lags, with a gap at the boundary.</p>\n<p><b>Metrics, and why not accuracy.</b> If two percent of rows are positive, predicting all-negative scores 98 percent accuracy and catches nothing. So the baseline is exactly that, and it is the number every result is compared against. I report precision and recall from the confusion matrix, and I sweep the threshold to produce the precision-recall trade-off, because a single threshold is an arbitrary point on a curve. Choosing where to sit on that curve is not a modelling decision - it follows from the cost asymmetry between a miss and a false positive, and from how many alerts a review team can actually absorb.</p>\n<p><b>Tests.</b> On a small linearly separable dataset the model must reach near-perfect separation, which catches a sign error in the gradient immediately. On a hand-built confusion matrix, precision and recall must match values computed by hand. And a degenerate case: if nothing is predicted positive, precision must return zero rather than raising a division error.</p>"
   },
   {
    "id": "cdr-a4",
    "type": "code",
    "title": "SQL: window functions on event data",
    "prompt": "Write SQL for three things over a table of events with columns customer_id, event_ts, amount and event_type: (1) the most recent event per customer; (2) a rolling seven-day sum of amount per customer at each event; (3) for each event, the number of prior events for that customer in the preceding hour, computed point-in-time correctly. State the assumptions and the trap in each.",
    "timeboxSec": 1500,
    "rubric": "Must-haves: (1) latest-per-customer implemented with ROW_NUMBER over a partition ordered by timestamp descending, filtered to 1 in an outer query or common table expression, with the tie trap named - equal timestamps produce an arbitrary winner unless a deterministic secondary ordering is added, and the difference between ROW_NUMBER, RANK and DENSE_RANK on ties stated; (2) the rolling sum written with a window frame, and the ROWS versus RANGE distinction made explicitly - a seven-day sum requires a RANGE frame over an interval, not a ROWS frame over a row count, unless one row per day is guaranteed; (3) the prior-events count written with a frame or inequality join bounded strictly before the current event, with the boundary strictness called out - whether the current row contributes to its own feature is decided by strict versus non-strict comparison, and it must be strict for point-in-time correctness; (4) common table expressions used per logical step rather than deep nesting, with readability named as deliberate; (5) assumptions stated: timestamp type and time zone, duplicate events, null amounts, and whether event_ts is event time or ingestion time; (6) at least one performance note - partitioning or indexing on customer and timestamp, or the cost of an inequality self-join versus a window frame. Bonus: connects the third query explicitly to point-in-time correct feature engineering and to leakage, and notes that using ingestion time rather than event time silently changes what the feature means. Common mistakes: GROUP BY with MAX for the latest row, which loses the other columns or requires a second join; a ROWS frame for a time-based window; a non-strict boundary that leaks the current event into its own feature; one deeply nested query; no assumptions. {{HONESTY}}",
    "model": "<p><b>Assumptions.</b> event_ts is a timestamp in a single time zone and represents event time rather than ingestion time - that distinction matters, because using ingestion time silently changes what every one of these features means. Duplicates are possible, amounts may be null, and I would confirm whether a null amount should be treated as zero or excluded.</p>\n<p><b>1. Most recent event per customer.</b></p>\n<ul>\n<li><code>WITH ranked AS (</code></li>\n<li><code>  SELECT *, ROW_NUMBER() OVER (</code></li>\n<li><code>    PARTITION BY customer_id ORDER BY event_ts DESC, event_id DESC) AS rn</code></li>\n<li><code>  FROM events)</code></li>\n<li><code>SELECT * FROM ranked WHERE rn = 1;</code></li>\n</ul>\n<p>The trap is ties. With equal timestamps, ROW_NUMBER picks arbitrarily and the result is not reproducible, so I add a deterministic secondary key. Also worth naming: RANK would give both tied rows rank 1 and return two rows; DENSE_RANK differs only in how it numbers subsequent groups. Using GROUP BY with MAX(event_ts) is the common wrong answer - it gives you the timestamp but not the rest of the row without a second join.</p>\n<p><b>2. Rolling seven-day sum per customer.</b></p>\n<ul>\n<li><code>SELECT customer_id, event_ts, amount,</code></li>\n<li><code>  SUM(amount) OVER (</code></li>\n<li><code>    PARTITION BY customer_id ORDER BY event_ts</code></li>\n<li><code>    RANGE BETWEEN INTERVAL '7' DAY PRECEDING AND CURRENT ROW) AS sum_7d</code></li>\n<li><code>FROM events;</code></li>\n</ul>\n<p>The trap is ROWS versus RANGE. A ROWS frame counts rows, so ROWS BETWEEN 6 PRECEDING would be the last seven events, not the last seven days - correct only if there is exactly one row per day. A time window needs a RANGE frame over an interval.</p>\n<p><b>3. Prior events in the preceding hour, point-in-time correct.</b></p>\n<ul>\n<li><code>SELECT customer_id, event_ts,</code></li>\n<li><code>  COUNT(*) OVER (</code></li>\n<li><code>    PARTITION BY customer_id ORDER BY event_ts</code></li>\n<li><code>    RANGE BETWEEN INTERVAL '1' HOUR PRECEDING AND CURRENT ROW) - 1 AS prior_1h</code></li>\n<li><code>FROM events;</code></li>\n</ul>\n<p>The trap here is the whole point of the question: the boundary. CURRENT ROW includes the event itself, so the count must exclude it - here by subtracting one, which is only correct if no other row shares the exact timestamp. If ties are possible, the safer form is an inequality self-join with a strict comparison on time, which makes the exclusion explicit rather than arithmetic.</p>\n<p>This is exactly point-in-time correct feature engineering. A feature that includes the event it is describing is leakage, and it produces a model that looks excellent offline and fails on the first day in production. Getting strict versus non-strict right on this boundary is the difference.</p>\n<p><b>Performance.</b> All three depend on ordering within a customer, so the table wants clustering or an index on customer_id and event_ts. The window-frame versions are single passes over the sorted partitions; the inequality self-join alternative is far more expensive, which is worth saying if someone proposes it.</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 },
 "question-bank-fundamentals": {
  "id": "question-bank-fundamentals",
  "title": "Question bank: decision systems, forty-eight questions",
  "day": 2,
  "level": "warning",
  "levelLabel": "Answers are hidden - try first",
  "why": "Forty-eight questions on forecasting, optimisation, the control-loop view, sequential decisions, statistics, production ML and judgement. The answers are hidden on purpose. Try each question first, so you find out what you do not know instead of reading past it.",
  "learn": [
   {
    "id": "qb-1",
    "part": "field",
    "title": "Forecasting fundamentals — 8 questions",
    "body": "<p>The warm-up set. Almost every forecasting interview starts somewhere in here.</p>\n<ul>\n<li><b>Q1.</b> How do you evaluate a forecast?</li>\n<li><b>Q2.</b> MAE, RMSE, MAPE — what is the difference, and when does each mislead you?</li>\n<li><b>Q3.</b> How do you validate a time-series model, and why not a random train/test split?</li>\n<li><b>Q4.</b> How do you handle trend and seasonality?</li>\n<li><b>Q5.</b> What is a prediction interval, and how would you check yours is calibrated?</li>\n<li><b>Q6.</b> What is the difference between a confidence interval and a prediction interval?</li>\n<li><b>Q7.</b> A scheduler consumes your forecast. Should it consume the mean?</li>\n<li><b>Q8.</b> What do you do when you only have a short history?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Pick an error metric that matches the cost of the decision, measure it on data the model never saw, and compare it against a naive baseline such as \"same slot last week\". Report error by horizon and by segment rather than one aggregate number — a forecast that is fine one day out and useless at fourteen is a different product. And if you ship intervals, evaluate the interval too: coverage and width, not just the point error.</li>\n<li><b>A2.</b> MAE, mean absolute error, is the average absolute miss and treats every miss linearly. RMSE, root mean squared error, squares before averaging, so it punishes a few large misses much harder — use it when one big miss really is worse than several small ones. MAPE, mean absolute percentage error, divides by the actual, so it explodes near zero, is undefined at zero, and penalises over-forecasting more than under-forecasting. On spiky or intermittent demand, MAPE is the one that misleads most often.</li>\n<li><b>A3.</b> Rolling-origin validation, also called walk-forward: train on everything up to a cut-off, predict the next window, move the cut-off forward, repeat, and average the errors over many folds. A random split leaks the future into training — the model sees Wednesday and Friday and is asked to predict Thursday — so it flatters the model and the number will not survive production. Leave a gap at each cut-off when features use lags, so no training row contains information from after the prediction moment.</li>\n<li><b>A4.</b> Either model them explicitly or difference them out. Explicit means a trend term plus seasonal dummies or Fourier terms for the recurring cycles, which is what a driver-based regression does; differencing hands the job to a model such as ARIMA (autoregressive integrated moving average; see <a href=\"#forecasting-uq\">Forecasting and uncertainty</a>) working on the remainder. What matters most is naming which seasonalities exist at once — half-hourly demand typically carries time-of-day, day-of-week and holiday effects together, and a missing holiday calendar is the usual source of the biggest errors.</li>\n<li><b>A5.</b> A prediction interval is a range for a single future outcome with a stated probability, say 95%. You check it by backtesting: over many rolling-origin folds, count how often the actual fell inside the band. If a nominal 95% band only covers 80% of the time, it is not a 95% band. The simple correction is to measure the miss once on a held-out calibration set, widen the band by that amount, and apply the same widening going forward — that is conformal prediction in plain words.</li>\n<li><b>A6.</b> A confidence interval is for the average — where the true mean response sits, given uncertainty in the fitted parameters. A prediction interval is for one actual future observation, so it carries the parameter uncertainty plus the residual noise of a single draw, and is always the wider of the two. Reporting a confidence interval where the decision needs a prediction interval makes the future look more certain than it is, which is quietly wrong in a lot of dashboards.</li>\n<li><b>A7.</b> Not necessarily. The mean is the right input only when being over and being under cost the same, and in staffing they rarely do — a shortfall costs a missed customer, a surplus costs an idle hour. So plan to a quantile: if a shortfall is three times as expensive as an idle hour, staff nearer the 75th percentile of demand than the 50th. The general form is the newsvendor rule — the right quantile is the underage cost divided by the sum of underage and overage costs.</li>\n<li><b>A8.</b> Borrow strength from elsewhere. Pool similar series and fit one model with a group effect rather than many thin ones, rely on external drivers rather than on history, and keep the model small, because twenty observations cannot support fifteen features. Hold yourself to beating a naive baseline, widen the intervals honestly rather than pretending precision, and for a genuinely new series say so and plan for human override until it has enough history.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb-2",
    "part": "field",
    "title": "Optimisation and operations research — 9 questions",
    "body": "<p>The vocabulary set. What is being tested is whether you can model a problem, not whether you can call a solver.</p>\n<ul>\n<li><b>Q1.</b> Walk me through how you set up an optimization problem. What are the pieces?</li>\n<li><b>Q2.</b> What is the difference between an LP, a MIP and a CP model?</li>\n<li><b>Q3.</b> What is the difference between a hard and a soft constraint, and how do you model a soft one?</li>\n<li><b>Q4.</b> What is an optimality gap, and how do you decide when to stop the solver?</li>\n<li><b>Q5.</b> When do you use an exact solver, and when a heuristic or metaheuristic?</li>\n<li><b>Q6.</b> Describe an assignment problem in words, with no mathematics.</li>\n<li><b>Q7.</b> What do you do when the problem is too big to solve?</li>\n<li><b>Q8.</b> Beyond the objective value, how do you know a solution is good?</li>\n<li><b>Q9.</b> What is a warm start, and what does re-solving daily actually mean?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Three pieces. Decision variables — what you are allowed to choose, for example a binary variable for \"this person works this slot\". An objective — the single number you maximise or minimise, which is where the business trade-off actually gets written down. Constraints — what makes a solution legal: capacity, availability, coverage, no double-booking. In practice I would add a fourth, the data contract on the inputs, because most real failures are a wrong input rather than a wrong model.</li>\n<li><b>A2.</b> An LP, linear program, has continuous variables with a linear objective and linear constraints, and solves fast and reliably. A MIP, mixed-integer program, adds integer or binary variables — assign or do not — and is solved by branch-and-bound over LP relaxations. CP, constraint programming, works over discrete variables with rich logical constraints such as all-different or no-overlap, and searches by propagation rather than relaxation, which often suits scheduling better. The follow-up is always why integers make it hard: you lose convexity, so the feasible set is a lattice of points rather than a smooth region, you cannot simply walk downhill, and the solver must explore an exponential tree, pruning with bounds.</li>\n<li><b>A3.</b> A hard constraint must hold or the solution is invalid — legal rest periods, one person in one place at a time. A soft constraint is a preference you would like to hold, such as continuous blocks of work or a fair share of unpopular slots. You model a soft one by moving it into the objective: add a slack variable that measures the violation, let the constraint hold up to that slack, and penalise the slack with a weight. The trap is making everything hard — an over-constrained model comes back infeasible and tells you nothing, whereas the soft version comes back with a solution and a bill.</li>\n<li><b>A4.</b> A branch-and-bound solver holds two numbers: the best feasible solution found so far, and a bound proving nothing can be better than some value. The distance between them, usually quoted as a percentage, is the optimality gap, and a gap of zero means a proven optimum. In practice you stop on a gap threshold or a time limit, because the last one per cent of gap often costs more time than everything before it. Where to stop is a business question — for a daily plan I would rather have a 2% gap in five minutes than a proven optimum in three hours.</li>\n<li><b>A5.</b> Exact solvers when the constraints are hard and fully specified, the model fits the solver's structure, and you want a feasibility guarantee plus a stated gap you can show an auditor. Heuristics and metaheuristics — local search, tabu search, simulated annealing, genetic algorithms — when the instance is too large, the objective is messy or noisy, or you need a good answer under a clock; they give no proof of optimality but they scale and they tolerate an ugly objective. It is a trade of guarantee against scale and flexibility, not a ranking of seriousness. Note that a routing solver applies guided local search under a time limit, which is metaheuristic optimization whether or not anyone calls it that.</li>\n<li><b>A6.</b> You have two sets — jobs on one side, the people or machines that can do them on the other — and a cost or value for each possible pairing. You choose pairings so that every job gets one worker and every worker gets at most one job, or at most their capacity, maximising total value or minimising total cost. The plain version is solvable exactly and quickly, by the Hungarian algorithm or as a minimum-cost flow. It stops being plain the moment you add time slots, skills, travel or fairness, and then it becomes a MIP or a CP model.</li>\n<li><b>A7.</b> Decompose or shrink. Split along a natural boundary — region, day, skill group — and solve the pieces, accepting the loss of global optimality across the cut. Shrink the variable space before solving: pre-filter impossible pairings, cluster, or generate only a promising subset of columns rather than all of them. If it is still too big, drop to a heuristic under a time limit, because a dispatch decision has a deadline and a good feasible answer on time beats a better answer too late.</li>\n<li><b>A8.</b> Sanity-check it the way an operator would. Compare it against the incumbent process and a naive baseline on the metrics people actually care about, not only the objective. Look at the distribution rather than the total — who got the worst schedule, which route looks absurd — because a good objective value hides a terrible tail. Then check stability by perturbing the inputs slightly and seeing whether the plan changes wildly, and put the solution in front of a planner, who will spot the \"no human would do that\" cases in seconds.</li>\n<li><b>A9.</b> A warm start hands the solver a known good starting solution, usually yesterday's plan repaired, so it has an incumbent immediately and spends its time improving rather than hunting for anything feasible. Re-solving daily means the plan is rebuilt each day against the latest demand, availability and commitments instead of being held for a week. The subtlety is continuity: a fresh solve can produce a very different but equally good plan, which is disruptive to people, so you add a penalty for deviating from the published plan. The usual pattern is a daily re-solve against that day's demand, resources and time windows, with the continuity penalty holding the plan steady.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb-3",
    "part": "field",
    "title": "Forecast then optimise, and the control-loop view — 6 questions",
    "body": "<p>The boundary between prediction and decision, in the vocabulary a job description reaches for.</p>\n<ul>\n<li><b>Q1.</b> A system is described as \"optimal control\". What does that mean for a business decision loop?</li>\n<li><b>Q2.</b> Open loop versus closed loop — what is the difference?</li>\n<li><b>Q3.</b> What is the difference between the horizon you plan over and the part you commit to?</li>\n<li><b>Q4.</b> How often should you re-solve, and what goes wrong when the data is stale?</li>\n<li><b>Q5.</b> Noisy inputs make the plan jump around every run. How do you stop the thrashing?</li>\n<li><b>Q6.</b> What feedback signal do you actually monitor on a loop like this?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> It means you are not only predicting, you are choosing actions over time against a cumulative objective while the system reacts to what you do. The pieces are a state, such as staffing, backlog and commitments; a model of how that state evolves; a set of admissible actions; a cost you minimise over a horizon; and re-planning as reality arrives. Concretely here that is forecast demand, solve for the assignment, act, observe what happened, and fold it back in.</li>\n<li><b>A2.</b> Open loop computes a plan once and executes it regardless of what happens. Closed loop measures the outcome and feeds it back so the next plan is adjusted, which is what makes it control rather than a one-off optimization. The standard industrial pattern is model predictive control: plan over a long horizon, execute only the near part, then re-plan with fresh measurements. A daily re-solve with event-triggered reruns is exactly that pattern, whether or not anyone calls it that.</li>\n<li><b>A3.</b> You plan over a long horizon so the plan accounts for what is coming, because a schedule that is excellent today and impossible on Friday is not a plan. You commit only the near part, because forecast quality decays with horizon and people need certainty about the next shift, not the next month. So the pattern is a long horizon for the mathematics and a short commitment for the humans, with everything beyond it treated as provisional. Both failure modes are common — commit too far and you are locked into a bad forecast, plan too short and you make locally sensible, globally silly decisions.</li>\n<li><b>A4.</b> Re-solve as often as new information actually changes the decision, and no more often. If demand signals refresh every half hour but staffing is committed the day before, a half-hourly re-solve produces churn nobody can act on, so the cadence should follow the commitment point plus event-triggered reruns for genuine shocks. Stale data shows up as a plan optimised for a world that has moved — you keep serving yesterday's peak. I would monitor the age of the inputs at solve time as a metric in its own right, because a silently stale feed is the most common way these loops fail.</li>\n<li><b>A5.</b> Add inertia. Penalise deviation from the currently published plan in the objective, so the solver only moves when the gain exceeds the disruption cost — the same mechanism as any soft constraint. Freeze the committed near-term window so it cannot be rewritten at all. Do not chase noise upstream either: smooth the input, or require a change to persist or exceed a threshold before it triggers a rerun. Then measure churn explicitly, as changed assignments per re-solve, because if nobody measures it, the first you hear about it is an angry operator.</li>\n<li><b>A6.</b> Three layers, kept separate. Forecast accuracy and interval coverage against realised demand, which tells you whether the input is still good. Plan quality — service level or coverage achieved versus planned, and how much of the plan survived contact with the day. And the operational layer: churn, overrides, and how often humans edited the plan, which is the honest measure of whether the system is trusted. Above all three sits the business outcome, and the loop is only healthy if you can trace a bad outcome back to the layer that caused it.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb-4",
    "part": "field",
    "title": "RL and sequential decisions — 5 questions",
    "body": "<p>These are judgement questions. The strong answer is usually a reason not to reach for reinforcement learning.</p>\n<ul>\n<li><b>Q1.</b> What makes a problem sequential rather than a series of independent predictions?</li>\n<li><b>Q2.</b> When is a bandit enough?</li>\n<li><b>Q3.</b> When is reinforcement learning the wrong tool?</li>\n<li><b>Q4.</b> How would you evaluate a policy without deploying it?</li>\n<li><b>Q5.</b> What is reward hacking?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Carry-over. The decision you make now changes the state the next decision is made from, so you cannot optimise each step independently — putting your best person on the morning changes what you can do at night. That brings credit assignment with it: an outcome may have been caused by a decision several steps earlier, which is the hard part. If there is no carry-over and each decision faces a fresh, independent situation, it is a prediction problem with a decision rule, not a sequential one.</li>\n<li><b>A2.</b> A bandit is enough when you choose among a small set of actions, you observe the outcome only of the action you chose, and your choice does not change the next situation you face. Choosing which of four candidate plans to publish, or how aggressively to buffer, fits that pattern. It is far cheaper to build, evaluate and operate than full reinforcement learning, so it is the right first step whenever the carry-over is genuinely absent.</li>\n<li><b>A3.</b> When constraints are hard and fully specified and you need a feasibility guarantee, because a solver gives you that and a learned policy does not. When labels or outcomes are dense enough that supervised learning plus a tuned threshold answers the question. When the decision has to be auditable, or when you cannot afford exploration on live traffic, which covers most money-moving and safety-relevant systems. In a forecast-then-optimize setting the mature version is that learning supplies the optimizer's parameters rather than replacing the optimizer.</li>\n<li><b>A4.</b> Offline first, on logged data, with off-policy estimators such as inverse propensity scoring or a doubly robust estimator — which require that you logged the probability of each action taken, so you have to have planned for it before you needed it. Be honest that those estimates degrade exactly where the new policy does something the old one rarely did. Then shadow mode: run it beside the incumbent, log both decisions and compare, while the incumbent still acts. Then a guarded ramp with a kill switch. Most teams already own the rollout half; the off-policy evaluation half is the one that has to be built deliberately.</li>\n<li><b>A5.</b> The policy optimises the metric you wrote rather than the outcome you meant, usually by finding a degenerate case you did not anticipate. Reward completed jobs and it quietly starves the hard ones — remote addresses, awkward windows — because easy ones score faster, so the aggregate improves while the worst-served customers get worse. The mitigation is measurement rather than clever reward shaping: watch the tail, not just the mean, and alert on it. It is the same failure as a badly specified objective in an optimizer, in a different vocabulary.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb-5",
    "part": "field",
    "title": "Statistics a data scientist will ask about — 7 questions",
    "body": "<p>Statistics questions come with a follow-up almost every time, so answer at the depth you actually hold.</p>\n<ul>\n<li><b>Q1.</b> What is overfitting, and how do you detect it?</li>\n<li><b>Q2.</b> Give me bias and variance together.</li>\n<li><b>Q3.</b> What does regularization actually do?</li>\n<li><b>Q4.</b> What does a p-value tell you?</li>\n<li><b>Q5.</b> AIC, BIC or cross-validation — how do you select a model?</li>\n<li><b>Q6.</b> Your forecast is driver-based. How do you keep correlation from being read as causation?</li>\n<li><b>Q7.</b> How do you tell whether an effect is real or just noise?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> The model has learned the noise in the training data as if it were structure, so it looks good in sample and fails on data it has not seen. You detect it by holding data out — for a time series, a rolling-origin backtest rather than a random split — and watching the gap between training and validation error widen as you add capacity. The subtler versions matter more in practice: leakage from a feature that would not exist at prediction time, and overfitting the validation set itself by tuning against it repeatedly, which is why a final untouched holdout is worth keeping.</li>\n<li><b>A2.</b> Bias is error from the model being too simple to represent the truth; variance is error from the model being too sensitive to the particular sample it was trained on. Adding capacity lowers bias and raises variance, and total error is minimised somewhere in between. The practical read-off is simple: high training error and high validation error together means bias, while a large gap between them means variance.</li>\n<li><b>A3.</b> It constrains the model so it cannot chase noise, trading a little bias for a larger reduction in variance. L2 means the penalty is the sum of the squared coefficients - that is ridge, and it shrinks all of them toward zero and copes gracefully with correlated drivers. L1 means the penalty is the sum of their absolute values - that is lasso, and it drives some coefficients exactly to zero, so it also performs feature selection. With correlated macro drivers, ridge is usually the safer default, and the strength is chosen by cross-validation rather than by taste. Early stopping, dropout and limiting tree depth are the same idea in other model families.</li>\n<li><b>A4.</b> It is the probability of seeing an effect at least this large if the null hypothesis were true, and nothing more. It is not the probability that the hypothesis is true, and it says nothing about whether the effect is large enough to matter. In practice I care about the effect size and its interval far more than whether it crossed 0.05, and I watch for multiple comparisons — test twenty things and one will look significant by chance alone.</li>\n<li><b>A5.</b> Cross-validation when the data allows it, because it estimates the thing I actually care about, out-of-sample error, with the fewest assumptions; for a time series that means rolling-origin folds, not random ones. AIC (Akaike information criterion) and BIC (Bayesian information criterion) are cheaper analytic alternatives that score fit minus a complexity penalty, with BIC penalising extra parameters more heavily, so it prefers smaller models and is aimed more at identifying a true model than at minimising prediction error. Corrected AIC matters when the sample is small relative to the number of parameters. Whichever I use, I compare against a naive baseline, because a model that loses to \"same as last week\" is not a model.</li>\n<li><b>A6.</b> By being explicit that a regression coefficient is an association under the model, not something you can adjust to change the outcome. Practically: prefer drivers with a plausible mechanism and a real lead time over ones that merely co-move, check whether the coefficient is stable across periods and holdouts, and watch for a confounder driving both sides, or for two drivers that are proxies for the same thing, which makes coefficients unstable and unreadable. The moment someone wants to act on a coefficient — raise this and demand rises — that is a causal claim, and it needs an experiment or an explicit causal design, not a fit.</li>\n<li><b>A7.</b> Ask whether it survives out of sample. A pattern that holds across several rolling-origin folds and several segments is far more credible than one that appears once. Quantify how surprising it is under a null, with an interval or a permutation test, and count honestly how many things you looked at before you found it. Then ask for a mechanism: with no plausible story for why the relationship should exist, I treat it as noise until it repeats on data collected after I formed the hypothesis.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb-6",
    "part": "field",
    "title": "Production ML systems — 8 questions",
    "body": "<p>Answer these in specifics from systems you have actually run, not in generalities.</p>\n<ul>\n<li><b>Q1.</b> What is training/serving skew, and how do you prevent it?</li>\n<li><b>Q2.</b> What problem does a feature store solve?</li>\n<li><b>Q3.</b> How do you set a latency budget?</li>\n<li><b>Q4.</b> How do you monitor drift and decide to retrain?</li>\n<li><b>Q5.</b> How do you roll out a model safely — shadow, canary, blue-green?</li>\n<li><b>Q6.</b> Batch or streaming — how do you decide?</li>\n<li><b>Q7.</b> What does reproducibility mean here in practice?</li>\n<li><b>Q8.</b> You are handed a new problem. How do you design the evaluation before building anything?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> The features the model sees in production differ from the ones it was trained on — a different code path, different freshness, a transformation applied in training and forgotten at serving, or a value that was not truly available at prediction time. The prevention is structural: share one transformation path between training and serving instead of reimplementing it, and build training data with point-in-time correctness so no feature carries information from after the prediction moment. Then verify it — log serving features and compare their distributions against training, and periodically re-score a stored production request offline to check the answer still matches.</li>\n<li><b>A2.</b> One definition of a feature, computed once and used by both training and serving, which is the structural fix for skew. It also gives point-in-time correct historical lookups so backfills do not leak the future, reuse across teams, and an online store fast enough to serve at request time alongside an offline store for training. It is not free — it is real infrastructure to run — and for a single model with a handful of features, a shared transformation library and a disciplined pipeline is often the better call.</li>\n<li><b>A3.</b> Start from the user-facing requirement and work backwards. If the end-to-end budget is 200 milliseconds, subtract network, authentication, feature fetch and post-processing, and what remains is the model's share, which then dictates model size and whether you can afford a remote feature lookup at all. Budget the tail rather than the mean: p99 is the value below which 99 percent of requests fall, the slow tail rather than the average, and it is what users feel and where timeouts happen. Then enforce it: load-test against it, alert on it, and treat a breach as a defect rather than a tuning opportunity.</li>\n<li><b>A4.</b> Monitor three things separately: input drift, meaning feature distributions moving; prediction drift, meaning the output distribution moving; and actual performance against labels once they arrive. Only the third is proof — the first two are early warnings, which matters because labels are usually delayed. I would trigger a retrain on a performance threshold tied to the business metric, with a scheduled retrain as a minimum, rather than retraining on every drift alert. And I would check that retraining is even the right fix, because drift caused by a broken upstream feed is fixed upstream, not by training on bad data.</li>\n<li><b>A5.</b> Shadow means the new model scores real traffic but its output is not used, so you compare against the incumbent at zero risk. Canary means a small slice of real traffic gets the new answer, watched closely, then ramped. Blue-green means two full environments with traffic switched across at once, and the rollback is switching back. In all three the non-negotiables are a kill switch, a rollback trigger agreed before launch, and keeping the previous version deployable. A service mesh with traffic splitting gives you all three from one mechanism.</li>\n<li><b>A6.</b> Follow the decision, not the technology. If the decision is made on a schedule — a daily plan, a nightly score — batch is simpler, cheaper, easier to backfill and easier to reason about. Stream when the value of the answer decays in seconds or minutes, or when the state feeding it changes that fast. Many real systems are honestly both: batch the heavy plan, stream the fast corrections, which is the usual pattern of a forecast-then-optimize loop.</li>\n<li><b>A7.</b> Being able to rebuild the same artefact and get the same answer. Concretely: version the data as well as the code, pin the environment, record the exact feature set and training window, fix the random seeds, and store the model with the metadata that says what produced it. The test is a question you will genuinely be asked — why did the plan change on the 14th — and you can only answer it if you can rebuild the model and the inputs as they stood on the 14th.</li>\n<li><b>A8.</b> Write down the decision first, then the metric that reflects its cost, then the baseline you must beat, then the split that makes the number honest — all before any modelling. For a forecast feeding a schedule that means error per horizon and per segment, interval coverage if intervals are shipped, a naive baseline such as the same slot last week, rolling-origin folds, and the downstream service metric the forecast is meant to move. Agree it with the people who will act on it so the goalposts cannot move later. Doing this first is also the fastest way to find out the problem does not need machine learning at all.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb-7",
    "part": "field",
    "title": "Judgement and staff-level scope — 5 questions",
    "body": "<p>The answers that separate Senior from Staff. Give the reasoning, not just the conclusion.</p>\n<ul>\n<li><b>Q1.</b> When would you not use machine learning at all?</li>\n<li><b>Q2.</b> How do you choose between a simple and a complex method?</li>\n<li><b>Q3.</b> How do you get another team to adopt a standard?</li>\n<li><b>Q4.</b> A stakeholder wants a single number with no uncertainty. What do you do?</li>\n<li><b>Q5.</b> First thirty days on a forecast-then-schedule system — what do you do?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> When the rules are known and stable, a rule engine is better — auditable, testable, and readable by the people who own the policy. When the problem is really optimization under hard constraints, a solver beats a learned policy and gives you a feasibility guarantee. When labels are absent, when the cost of being wrong cannot be measured, when the volume does not justify the operational burden, or when a simple statistical baseline already meets the requirement. The honest way to put it is that a model is an operating cost you carry forever, so it has to be worth it.</li>\n<li><b>A2.</b> Start simple, measure, and make the complex method prove itself against the simple one on the metric that matters — including the interval, the tail and the failure modes, not only the headline average. Then price the complexity: latency, retraining, debuggability, and how many people can maintain it at two in the morning. I will pay for complexity when the gain is material to the decision and survives a backtest, and not when it improves a number nobody acts on. Naming the simple baseline out loud also protects you, because if it wins you shipped in a week.</li>\n<li><b>A3.</b> Not by mandate. Make the standard the easiest path — a working template, a paved road, a migration someone can finish in an afternoon — and start with one team that has a real problem it solves, so you have evidence instead of an opinion. Then socialise the result, put the check into review and continuous integration so the default is correct, and carry the support burden yourself early. A standard spreads because it is demonstrably less work, not because it was announced.</li>\n<li><b>A4.</b> Give them a number, and give it a range, but lead with the decision rather than the statistics. I would ask what they would do differently at the high end and at the low end — if the answer is nothing, the range is not the issue and a single number is fine. If the answer is that they would hire differently, then the uncertainty is the whole point, and I would present it as a plan with a cost rather than as an interval: staff to this level and you cover nine days in ten. Refusing to give a number is not an option, and neither is hiding the uncertainty.</li>\n<li><b>A5.</b> Listen, and measure what is already happening, before you change anything. Learn what decision the system produces and who lives with it, then measure the current state honestly — forecast error by horizon and segment against a naive baseline, interval coverage if intervals exist, how often humans override the plan, and how old the inputs are at solve time. Talk to the planners, because their overrides are a labelled dataset of everything the model gets wrong. Then pick the smallest change with a defensible payoff and ship it, so the first thing the team sees from me is a working improvement rather than a redesign proposal.</li>\n</ul>",
    "check": null
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "qb-a1",
    "type": "drill",
    "title": "Five questions at random, in writing, in ten minutes",
    "prompt": "Pick five questions at random from the bank, from at least three different areas, and answer them in writing in 10 minutes. Do not reveal the answers first. Then reveal the answers and compare.",
    "timeboxSec": 600,
    "rubric": "Must-haves, applied to each of the five answers: (1) substantively correct; (2) concise enough to say aloud in under a minute — a spoken answer, not an essay; (3) it names what the answer changes about a decision, rather than stopping at a definition; (4) it includes the follow-up trap wherever the bank's answer flags one, for example why integer variables make a problem hard, why MAPE breaks near zero, confidence versus prediction interval, or stale inputs at solve time; (5) a sentence saying plainly what you have and have not done, wherever the question touches something you have read about rather than run yourself. Common mistakes: writing an essay instead of something you could say out loud; reciting a definition with no consequence attached; leaving out that sentence where it is needed; drawing all five questions from one area. {{HONESTY}}",
    "model": "<p>There is no single model answer for this exercise — the reference answers are the ones you reveal in each section. Check yourself against those, mark each of the five as solid, shaky or wrong, then answer the two weakest again out loud, without notes.</p>\n<p>A useful pass mark: three of five solid on the first attempt, and every boundary sentence present where it was needed. If a whole area comes back shaky, that area is the next thing to study rather than the next thing to re-drill.</p>"
   },
   {
    "id": "qb-a2",
    "type": "followup",
    "title": "Put it together: \"How would you know the forecast is good enough?\"",
    "prompt": "Answer aloud in first person, in about 90 seconds: \"A schedule is built from a demand forecast. How would you know the forecast is good enough, and what would you change in the schedule if it is not?\"",
    "timeboxSec": 90,
    "rubric": "Must-haves: (1) defines \"good enough\" by the downstream decision rather than by an abstract error threshold; (2) a rolling-origin backtest with error broken out by horizon and by segment, compared against a naive baseline such as the same slot last week; (3) interval coverage checked as a separate question from point error; (4) at least three schedule-side responses for when the forecast is not good enough — plan to a quantile because over and under do not cost the same, shorten the commitment window and re-solve more often, widen buffers only where the error is worst, hold flex capacity as a deliberate hedge; (5) says plainly where the answer touches interval methods you have read about rather than shipped. Common mistakes: answering only the forecasting half and never touching the schedule; naming a magic MAPE threshold as the definition of good enough; proposing a better model as the only remedy; omitting the naive baseline. {{HONESTY}}",
    "model": "<p>\"Good enough is defined by the decision, not by the metric, so I would start at the other end — what does the schedule do differently when the forecast moves. Then I would measure three things on a rolling-origin backtest. Error by horizon, because a forecast that is fine one day out and useless at seven changes what we are willing to commit. Error by segment, because the aggregate hides the slot that actually hurts. And interval coverage — whether the 95% band contained the outcome 95% of the time. All of it against a naive baseline like the same slot last week, because if we cannot beat that, we do not have a forecast.\"</p>\n<p>\"If it is not good enough, I would not stop scheduling — I would change how the schedule consumes it. Plan to a quantile instead of the mean, because a shortfall and an idle hour do not cost the same; if a shortfall is three times as expensive, staff nearer the 75th percentile. Widen the buffer only where the error is worst. Shorten the commitment window and re-solve more often, so a bad forecast is corrected sooner. And hold some flex capacity, on-call or overtime, as the deliberate hedge against what is left.\"</p>\n<p>\"And I would say plainly which half of that I have operated and which half I would be building. Shipping an interval and validating an interval are two different skills, and claiming the second because you have done the first is exactly the kind of exaggeration that gets found out.\"</p>"
   }
  ],
  "sayItOutLoud": null,
  "sayQuestion": null
 },
 "question-bank-mle": {
  "id": "question-bank-mle",
  "title": "Question bank: production ML engineering, fifty-two questions",
  "day": 3,
  "level": "warning",
  "levelLabel": "Answers are hidden - try first",
  "why": "Fifty-two questions covering the platform and the lifecycle, data and features, serving and scale, reliability and operations, modelling in production, generative AI, and staff-level judgement. The answers are hidden so you have to try each one first.",
  "learn": [
   {
    "id": "qb2-3",
    "part": "field",
    "title": "Serving and scale - 8 questions",
    "body": "<p>Precision with numbers is what turns an answer here into credit: a percentile and a scope, never a bare figure.</p>\n<ul>\n<li><b>Q1.</b> You have a 200 millisecond budget. How do you spend it?</li>\n<li><b>Q2.</b> Why do you quote p99 (the value below which 99 percent of measurements fall) rather than the mean?</li>\n<li><b>Q3.</b> How many replicas do you need?</li>\n<li><b>Q4.</b> What does horizontal pod autoscaling do, and where does it stop?</li>\n<li><b>Q5.</b> Traffic will rise sixty-fold in two hours. What do you do?</li>\n<li><b>Q6.</b> When does batching help, and what does it cost?</li>\n<li><b>Q7.</b> What is your degraded mode when the feature store is down?</li>\n<li><b>Q8.</b> How do you load test something honestly?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Decompose by hop - ingress and authorisation, feature fetch, last-mile transformation, inference, policy, response - and leave headroom deliberately unallocated, because a budget allocated to the last millisecond is already breached. Every hop gets a timeout smaller than the remaining budget and a defined behaviour when it fires. Logging and feature writes go off the critical path. The feature fetch is normally the largest and most variable piece.</li>\n<li><b>A2.</b> Because the mean hides the tail and the tail is what customers experience. It also compounds: if a request fans out to five calls each with a p99 of 20 milliseconds, the chance that at least one is slow is far above one percent, so the request p99 is much worse than any single call's. Mitigations are batching the reads to cut fan-out, hedging, and making some reads optional.</li>\n<li><b>A3.</b> Measure the sustained throughput of one replica at which the target latency percentile is still met - not its maximum, its maximum while still fast enough. Divide the forecast peak by that. Add headroom so steady-state utilisation stays away from saturation, and add failure-domain redundancy so losing a zone still leaves peak capacity. Then repeat the arithmetic for every downstream, because system capacity is the smallest link.</li>\n<li><b>A4.</b> It observes a metric, compares to a target and changes replica count, with a stabilisation window to prevent thrashing. Three limits. The metric choice matters more than the target, and CPU is often wrong for an inference service - scale on the metric most correlated with load. It schedules pods but does not create nodes, so without cluster capacity you get pending pods. And it cannot outrun cold start: if loading weights takes forty seconds, a thirty-second reaction is still a minute behind.</li>\n<li><b>A5.</b> Recognise that a predictable ramp is a readiness problem, not an autoscaling one. Pre-scale on the calendar and hold warm headroom, because detection plus scheduling plus node provisioning plus warmup is slower than the ramp. Size from measurement, size the downstreams too, and hold node-level buffer - one published approach is spare nodes held by low-priority pause pods that are preempted when real workloads need them. Then rehearse: load, stress, soak and failure injection, not volume alone.</li>\n<li><b>A6.</b> Batching amortises per-call overhead and uses hardware far better, so it raises throughput. It costs waiting - a dynamic batcher holds requests to fill a batch - so it is affordable only if the wait fits the budget, which usually means small windows in a real-time path and large ones offline. For language models, continuous batching adds and removes sequences from a running batch and is much more effective than naive batching.</li>\n<li><b>A7.</b> A specific trigger and a specific behaviour, not a promise of resilience. On a feature-store timeout, score with request-only features and mark the decision degraded so the policy layer treats it more conservatively; if the model is unavailable, fall back to a rules layer. The principle is that a failed dependency should produce a worse decision, never no decision - and the degraded path needs its own metric, because a service can be fast, available and error-free while quietly running on its fallback.</li>\n<li><b>A8.</b> Load at expected peak, stress beyond it until something breaks and confirm the breaking component is the one you predicted, and soak for hours to catch leaks and connection exhaustion. Then inject failures - lose a zone, kill a dependency - because volume is only one failure mode. And shape the synthetic traffic to match the real key distribution, because uniform synthetic load is reassuring rather than predictive.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb2-4",
    "part": "field",
    "title": "Reliability and operations - 8 questions",
    "body": "<p>The half most people skip, which is exactly why it is where the most credit is available.</p>\n<ul>\n<li><b>Q1.</b> What SLOs (service level objectives) would you write for a model service?</li>\n<li><b>Q2.</b> What is an error budget and what is it for?</li>\n<li><b>Q3.</b> How do you monitor whether a model is still right, when labels are late?</li>\n<li><b>Q4.</b> Data drift versus concept drift - what is the difference and what do you do?</li>\n<li><b>Q5.</b> A drift alert fires on eight features at once. What is your first hypothesis?</li>\n<li><b>Q6.</b> Design the rollout of a new model version.</li>\n<li><b>Q7.</b> What does rolling back an ML system actually involve?</li>\n<li><b>Q8.</b> It is two in the morning and precision has dropped. Go.</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Availability as the share of requests receiving a decision, including the degraded path. Latency as a percentile, a threshold and a window. Feature freshness, because stale features are a quality failure invisible to latency and error metrics. Degraded-path share, capped - the one most people omit and the best indicator of hidden decay. And a quality objective on a lagged window with the lag stated. I would refuse to promise a quality number over a window shorter than the label delay, because it cannot be measured.</li>\n<li><b>A2.</b> The shortfall an SLO permits - 99.9 percent over thirty days allows roughly forty-three minutes. It converts an argument into arithmetic: instead of debating whether to ship, you observe how much budget is left and what this change historically costs. When the budget is exhausted, you stop shipping and stabilise. For ML I would also propose a quality budget on the lagged metric, governing how aggressively new models are promoted.</li>\n<li><b>A3.</b> Two questions, and the second is delayed. Day to day you watch proxies: score distribution against last week and the same period last year, alert volume against review capacity, reviewer override rate, feature null rates and freshness, and degraded-path share. The discipline is that a proxy triggers investigation, not rollback - a genuine change in traffic mix produces exactly the same signal as a broken model.</li>\n<li><b>A4.</b> Data drift is the input distribution moving; detect it by comparing recent feature distributions to a reference, which needs no labels and is therefore immediate. Concept drift is the input-to-label relationship changing; it cannot be seen from inputs alone and needs labels or a strong proxy such as reviewer override rate. What you do is a sequence of checks, not a reflex: confirm it is real, rule out expected seasonality, check for an upstream data defect, then respond - threshold first because it is fast and reversible, then retraining, then feature redesign.</li>\n<li><b>A5.</b> An upstream data defect or pipeline change, almost always. Correlated shifts across many features share a common cause - a schema change, a failed job, a partial outage - far more often than they indicate model decay. It is also worth remembering that per-feature drift tests on a wide feature set generate constant false alarms by chance, which is why you alert on a small set of features that matter and require persistence across windows.</li>\n<li><b>A6.</b> Offline checks, then shadow, then canary, then ramp, then switch. Shadow is the stage worth insisting on, because machine learning uniquely allows the prediction to be separated from the action - the candidate scores real traffic with real features while its output is logged and discarded, catching skew, latency and distribution surprises at zero customer risk. Canary abort criteria are defined before it starts, and the soak is set by how long it takes for a real difference to show up, which on a rare-event problem is a serious constraint.</li>\n<li><b>A7.</b> Reverting the model version and its feature definitions together, because restoring a model onto changed features puts it on inputs it never trained on. If delivery is GitOps the mechanism is a revert and the controller reconciles. And you name what cannot be undone: decisions already made, notifications already sent, and a training set already shaped by the bad version's own outputs - which should be marked in the pipeline rather than silently absorbed.</li>\n<li><b>A8.</b> First, notice that with weeks-long label delay you cannot know tonight's precision tonight - so what dropped is a proxy, and the queue volume is the hard fact. Then stabilise before diagnosing, with reversible actions: raise the threshold to bring alert volume inside review capacity, revert to the previous version, or fall back to rules - and say out loud that raising the threshold trades recall, which is a business call to flag. Then diagnose cheapest first: recent deploys, upstream feed health, null rates and distributions, traffic mix, seasonality, and only then the model. Afterwards, a blameless postmortem whose test is whether someone who was not there could now resolve it faster.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb2-1",
    "part": "field",
    "title": "Platform and lifecycle - 8 questions",
    "body": "<p>The core of production ML. These should be automatic.</p>\n<ul>\n<li><b>Q1.</b> Walk me through the lifecycle of a model, from data to retraining.</li>\n<li><b>Q2.</b> What is the difference between a pipeline and a platform?</li>\n<li><b>Q3.</b> What does GitOps (the pattern of holding desired state in Git and reconciling reality to it) give you for machine learning specifically?</li>\n<li><b>Q4.</b> What has to be pinned for a training run to be reproducible?</li>\n<li><b>Q5.</b> What goes in a model registry, and why does it matter?</li>\n<li><b>Q6.</b> What are the three families of test in an ML continuous-integration pipeline?</li>\n<li><b>Q7.</b> How do you get another team to adopt your platform?</li>\n<li><b>Q8.</b> If you joined a team with models in production and no platform, what would you build first?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Nine stages: data ingestion, feature engineering, training, evaluation, registration, approval, deployment, serving and monitoring, retraining. Put it another way: the model occupies one of nine boxes and most production incidents live in the other eight. Naming registration and approval separately is what distinguishes a system that can be audited from one that cannot.</li>\n<li><b>A2.</b> A pipeline is one team's automated path for one model. A platform factors out the common parts - packaging, promotion, monitoring, rollback - so a second team that did not build it gets them for free. Reuse by someone else is the defining property; the technology is incidental.</li>\n<li><b>A3.</b> Deployment, audit and rollback become one mechanism. The desired state - serving manifest with the model version reference, pipeline definitions, feature definitions, infrastructure, scaling policy, alert definitions - lives in Git, and a controller reconciles. Who changed what, when, with whose approval is then a Git history, and rollback is a revert. Weights and data are referenced by immutable version rather than stored in Git.</li>\n<li><b>A4.</b> Four things: the code commit, the data window or snapshot identifier, the configuration and hyperparameters, and the environment image - plus recorded random seeds. Hyperparameters alone are not enough; without the other three the same settings produce a different artefact.</li>\n<li><b>A5.</b> An immutable version per artefact, its metrics, lineage back to commit and data window, approval state and approver, and a stage label. Deployments reference a registry version rather than a file path. That one rule is what makes rollback and audit possible, which is why it is usually the first thing worth building.</li>\n<li><b>A6.</b> Data tests on the inputs - schema, null rates, ranges, cardinality, freshness. Model tests on the artefact - metrics against a frozen holdout, comparison to the incumbent, per-slice metrics, behavioural checks, a degeneracy check. And integration tests on the deployed path - score fixed records through the real serving container with production-style feature fetches and assert they match the offline scores. The third is the only one that can see training-serving skew.</li>\n<li><b>A7.</b> By making the safe path the fast path, not by policy. Working templates rather than documentation, migrating the first workload yourself, and removing a pain people already have - manual rollback is the usual one. If the compliant route is slower than a shell script, people route around it and the platform team becomes a police force. And plan for the road going stale: an escape hatch with review, and a stated schedule for revisiting what is mandatory.</li>\n<li><b>A8.</b> A registry with lineage, then the checks a model must pass before release, then a serving-path integration test. In that order, because until you can say exactly which artefact is serving and rebuild it, nothing else you claim about reliability is checkable - and the integration test catches the most common ML production failure. Everything else can wait a quarter.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb2-2",
    "part": "field",
    "title": "Data and features - 8 questions",
    "body": "<p>Where design questions cluster hardest.</p>\n<ul>\n<li><b>Q1.</b> What is a feature store for?</li>\n<li><b>Q2.</b> Explain point-in-time correctness.</li>\n<li><b>Q3.</b> What causes training-serving skew, and how do you detect it?</li>\n<li><b>Q4.</b> How do you handle features that depend on the request itself?</li>\n<li><b>Q5.</b> Your labels arrive six weeks late. What changes?</li>\n<li><b>Q6.</b> How do you handle duplicate events in a stream?</li>\n<li><b>Q7.</b> A feature definition changes. How do you roll it out?</li>\n<li><b>Q8.</b> How do you decide between streaming and batch features?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Four things, and consistency comes first: one definition computed once so training and serving see the same quantity. Then reuse and discovery, so the same feature is not reimplemented five slightly different ways. Then point-in-time correct training data. Then low-latency serving. Leading with latency describes a cache, not a feature store.</li>\n<li><b>A2.</b> Building a training row using only values that were knowable at the label's timestamp. The mechanism is an as-of join: for each row, take the feature value with the greatest timestamp at or before the event time - and ideally the cut-off is when the value was available, not merely when it was true. The symptom of getting it wrong is excellent offline metrics and immediate, not gradual, production underperformance.</li>\n<li><b>A3.</b> Five causes: two implementations of the same feature; different data freshness between training and serving; different missing-value handling; filtering applied in one path and not the other; and sampling that does not cover the full daily or seasonal cycle. Detection is one test - score a fixed record set offline and again through the deployed serving path with production-style feature fetches, and assert they match within tolerance. It belongs in continuous integration as a blocking test, not in a debugging session.</li>\n<li><b>A4.</b> Last-mile transformations: store the base statistic and do the request-time arithmetic just before invoking the model, using the same code in training and serving. \"Amount relative to this customer's median\" cannot be precomputed for every possible amount, but the median can be, and the division is cheap.</li>\n<li><b>A5.</b> Four things. Quality is not measurable today, so you run on proxies - score distribution, alert volume, override rate, feature freshness and null rates - and treat them as reasons to investigate rather than as conclusions. The training set gets a maturity window, and recent rows are censored rather than negative. How often you retrain is limited by labels rather than compute, with the threshold as the fast, reversible control in between. And evaluation must compare models over comparable label maturity.</li>\n<li><b>A6.</b> At-least-once delivery plus an idempotency key derived from the event, checked against a store of recently seen keys. The horizon is bounded by the realistic redelivery window rather than kept forever, and you accept that a redelivery beyond the horizon is not caught. That combination gives effectively-once processing within the horizon.</li>\n<li><b>A7.</b> As a new versioned feature rather than a mutation in place, because mutating silently mixes two definitions inside one training set. Publish the new version, backfill history through the same transforms, compare, then cut over. Minor versions are additive and backwards compatible; major versions are breaking - and a breaking version is exactly the case where rolling back a model without rolling back its feature contract goes wrong.</li>\n<li><b>A8.</b> By how quickly the signal decays. A velocity feature over the last few minutes is worthless if it is an hour old, so it must be streaming. A ninety-day customer aggregate changes slowly and batch is cheaper and simpler. The cost of streaming is real - windows, watermarks, late data, duplicates, a harder backfill - so the question is whether the freshness buys enough signal to pay for that.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb2-5",
    "part": "field",
    "title": "Modelling in production - 7 questions",
    "body": "<p>Modelling questions asked from an engineering point of view, which is how they are usually asked.</p>\n<ul>\n<li><b>Q1.</b> Why gradient boosting rather than a neural network for tabular data?</li>\n<li><b>Q2.</b> Your data is two percent positive. Which metrics, and which not?</li>\n<li><b>Q3.</b> How do you choose the decision threshold, and who owns it?</li>\n<li><b>Q4.</b> What is calibration and when do you need it?</li>\n<li><b>Q5.</b> How do you evaluate a candidate model against the incumbent?</li>\n<li><b>Q6.</b> How would you detect leakage in someone else's training pipeline?</li>\n<li><b>Q7.</b> When would you tell a team not to use a model at all?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> On tabular data with mixed types, missing values and moderate row counts, boosted trees usually match or beat neural networks with far less tuning, train in minutes rather than hours, and infer fast enough for a small latency budget. They also handle monotone and interaction structure well without feature scaling. The cost is less capacity to learn from raw sequences or unstructured inputs, which is the case where a neural model genuinely wins.</li>\n<li><b>A2.</b> Not accuracy - predicting all-negative scores ninety-eight percent and catches nothing, which is exactly the baseline every result must beat. Use precision and recall at a stated operating point, precision-recall area as the threshold-free summary, and calibration if the score drives a policy. Report per slice as well as in aggregate, because an aggregate improvement routinely hides a segment that got worse.</li>\n<li><b>A3.</b> From two constraints: the cost asymmetry between a false positive and a false negative, and the capacity of whoever handles the alerts. It is a business control, not a hyperparameter, and it should be owned jointly rather than tuned quietly by the modeller. Describing it that way also makes it the fastest reversible action in an incident, precisely because it is the control the business already understands.</li>\n<li><b>A4.</b> A model is calibrated when a predicted probability of 0.3 corresponds to the event happening about thirty percent of the time. You need it whenever the score is combined with costs, compared against a threshold with business meaning, or fed to another system - which is most production uses. You do not strictly need it if only the ranking matters. Boosted trees are often poorly calibrated as trained and are usually corrected with a post-hoc fit on held-out data.</li>\n<li><b>A5.</b> Paired, on the same rows: a frozen holdout for comparability across releases plus a recent window for current relevance, with the incumbent scored in the same run. Aggregate and per-slice metrics, with a hard block if any named slice regresses beyond tolerance. Plus a serving-path comparison and a latency check. And an explicit statement of what those checks cannot catch - drift after deployment, feedback effects, and anything whose labels have not arrived yet.</li>\n<li><b>A6.</b> Three checks. First, ask of every feature whether it could have been known before the label existed - the commonest leak is a field populated by the outcome process itself. Second, look at the join: was the training row built with an as-of join, or a plain join on the entity key that picked up today's value. Third, look at the split: a random split on temporal data lets the model see the future. And the symptom that should make you suspicious in the first place is offline metrics that look too good and production performance that is bad from day one rather than degrading.</li>\n<li><b>A7.</b> When the task has a well-defined output space and rules already express it adequately - a model then adds operational cost, opacity and a training-data dependency for no gain. Also when there is no reliable label, when the decision must be deterministic and auditable, when the volume cannot pay for the inference cost, or when nobody will own it after launch. That last one is the most common real reason and the least often said.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb2-7",
    "part": "field",
    "title": "Staff-level judgement - 7 questions",
    "body": "<p>These are judged on your reasoning and on how much you take responsibility for, not on what you know. Answer with a decision rule, not an opinion.</p>\n<ul>\n<li><b>Q1.</b> You have one quarter and an immature ML setup. What do you build, in what order?</li>\n<li><b>Q2.</b> How do you decide whether to build or buy a platform component?</li>\n<li><b>Q3.</b> A team wants to ship a model you think is unsafe. What do you do?</li>\n<li><b>Q4.</b> How do you balance shipping speed against reliability?</li>\n<li><b>Q5.</b> How do you decide what to standardise and what to leave free?</li>\n<li><b>Q6.</b> What do you do when the metric improves and the business outcome does not?</li>\n<li><b>Q7.</b> What is the most important thing you have changed your mind about?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Registry with lineage, then the checks a model must pass before release, then a serving-path integration test, then a lagged quality dashboard with the label delay stated on it. The ordering rule is: make things checkable first. Until you can say which artefact is serving and rebuild it, nobody can check any other reliability claim you make. And I would deliberately not build a feature platform in the first quarter unless several teams already need it, because platforms without users are the most expensive thing an engineer can build.</li>\n<li><b>A2.</b> Buy the parts that are common across companies and build the parts that encode your own domain. Storage, serving runtimes and orchestration are commodities; the release checks, the feature semantics and the degraded-mode policy encode decisions only you can make. The second question is who operates it at three in the morning - a bought component with a support contract and a built one with a single expert are very different risks.</li>\n<li><b>A3.</b> Make the risk concrete and checkable rather than making it a matter of opinion. Name the specific failure - a slice that regressed, an untested serving path, no rollback - and propose the cheapest thing that would resolve the disagreement: a shadow run, a slice metric, a canary with abort criteria. If it still goes ahead, make sure the decision and its reasoning are written down, and that the fallback is in place. Escalation is a last resort; converting a judgement dispute into an experiment is almost always available first.</li>\n<li><b>A4.</b> With an error budget, so it stops being a values argument. The budget quantifies the failure the objective already permits; you spend it on releases and stop shipping when it is gone. That changes the conversation from \"are we moving fast enough\" to \"how much budget is left and what does this change cost\", which anyone can check.</li>\n<li><b>A5.</b> Declare the tiers explicitly: fixed, flexible and free is the widely used way of describing it. Standardise what creates cross-team risk or cost - delivery, secrets, observability, data classification - and leave free what only affects one team's productivity. Then make the standard path faster than the alternative, because a standard maintained by enforcement decays and one maintained by convenience does not. And set a schedule to revisit what is fixed, or the road goes stale.</li>\n<li><b>A6.</b> Believe the business outcome. Usually one of three things: the metric is measuring the wrong thing, the improvement is real but in a segment that does not matter, or the model improved and the decision policy around it did not change. Check the last one first - a better score with the same threshold and the same downstream action often changes nothing. It is also why I track two separate things, model quality and decision quality, and expect them to move independently.</li>\n<li><b>A7.</b> That the model is the small part. I used to think the interesting work was in modelling and the rest was plumbing. Then I watched good models fail because an upstream feed changed its structure, or a feature was computed one way offline and another way online, or there was no clean way to undo a release. Now I would rather have a mediocre model in a system I can reproduce, check, monitor and roll back than an excellent one in a system I cannot.</li>\n</ul>",
    "check": null
   },
   {
    "id": "qb2-6",
    "part": "field",
    "title": "Generative AI in production - 6 questions",
    "body": "<p>Secondary material. Enough to be fluent if they open it, never the thing you open with.</p>\n<ul>\n<li><b>Q1.</b> Why is generation slow compared with processing the prompt?</li>\n<li><b>Q2.</b> How do you make an LLM (large language model) feature cheaper, in order of impact?</li>\n<li><b>Q3.</b> What is the first design question for retrieval over customer documents?</li>\n<li><b>Q4.</b> How do you evaluate a retrieval-augmented system?</li>\n<li><b>Q5.</b> When is fine-tuning the right answer rather than prompting or retrieval?</li>\n<li><b>Q6.</b> What changes operationally when you self-host rather than call an API?</li>\n</ul>",
    "deeper": "<ul>\n<li><b>A1.</b> Prefill processes the whole prompt at once and is compute-bound and parallel; decode generates one token at a time and is memory-bandwidth-bound and sequential. The key-value cache stores state for tokens already processed so each new token does not reprocess the sequence, and it grows with sequence length and concurrency - which is usually what actually bounds how many requests a server can hold. That is also why throughput drops suddenly rather than degrading: once the cache is full, new requests queue.</li>\n<li><b>A2.</b> Model size first, because it multiplies cost on every call and the largest model is rarely the right default. Then prompt length, where retrieved context is usually the least disciplined part. Then caching, if the repetition rate is real and the staleness tolerance is stated. Then continuous batching. Then routing - a cheap model with a defined escalation rule, which is a cascade. Then bounding output length, since generation dominates. Streaming is separate: it changes perceived latency, not cost.</li>\n<li><b>A3.</b> Tenancy and permissions, enforced in the retrieval query rather than by filtering results afterwards. One customer's document appearing in another's answer is the failure that ends the project, and no amount of retrieval quality compensates for it. Embedding-model choice is a long way down the list.</li>\n<li><b>A4.</b> Split it. Retrieval quality is whether the right passage was in the retrieved set at all; generation quality is whether the answer is faithful to what was retrieved. Conflating them hides the usual truth, which is that the generator was fine and the retriever missed. Use a golden set that includes cases where the correct answer is a refusal, and calibrate any automatic judge against human labels on a sample before trusting it.</li>\n<li><b>A5.</b> When you need behaviour rather than knowledge - a consistent format, a tone, a task-specific decision - or when the prompt needed to get the behaviour has grown so long that it costs more than training would. Retrieval is the right answer for knowledge that changes, because retraining every time a fact changes is absurd. Low-rank adaptation makes the trade-off cheaper, and it has an operational benefit: many adapters share one base model, so you serve one set of weights rather than one model per task.</li>\n<li><b>A6.</b> A variable per-call cost becomes a fixed infrastructure cost, and with it you take on capacity planning, upgrades and availability for something you previously rented. That is only the right trade-off above a volume threshold and with stable enough demand to amortise the fixed cost. You also gain control - data never leaves your boundary, and you can fine-tune - and you lose the vendor absorbing model improvements on your behalf.</li>\n</ul>",
    "check": null
   }
  ],
  "connect": null,
  "activities": [
   {
    "id": "qb2-a1",
    "type": "explain",
    "title": "Test yourself: eight questions, no notes",
    "prompt": "Pick one question from each of the seven sections plus one more, and answer all eight out loud without notes, each in under 90 seconds. Write down which ones came out fluently and which did not. Then say which chapter you are going to reread as a result.",
    "timeboxSec": 900,
    "rubric": "Must-haves: (1) eight questions answered, at least one from each of the seven sections; (2) each answer under 90 seconds and delivered without notes; (3) each answer names a mechanism rather than a tool - the stage, the test, the trade - and where a number appears it carries units and scope; (4) an honest self-assessment marking each answer fluent or not, rather than a uniform pass; (5) a stated remediation - which chapter to reread and why - tied to the specific weak answers rather than a general intention; (6) no answer that claims production experience you do not have, particularly on feature stores, streaming platforms, managed ML platforms, workflow engines or multi-region operation. Common mistakes: reading the answer rather than recalling it; answering with a tool name; marking everything fluent; no remediation plan. {{HONESTY}}",
    "model": "<p>The point of this exercise is diagnosis, not performance, so the model answer is a method rather than a script.</p>\n<p>Answer each question aloud with a timer running. Immediately after each, mark it in one of three ways: <b>fluent</b>, meaning it came out in your own words without hesitation and included a mechanism and a trade-off; <b>recalled</b>, meaning you got there but had to assemble it; or <b>absent</b>, meaning you produced a general statement rather than an answer.</p>\n<p>The remediation rule: anything marked absent sends you back to the chapter it came from, and you reread only the section that covers it, then answer the question again the same day. Anything marked recalled gets answered again the following day without rereading. Fluent answers are done.</p>\n<p>Do not reread whole chapters because one answer was weak. With one to two weeks, the scarce resource is spoken repetition, not reading time, and the failure in the interview is retrieval under pressure rather than comprehension.</p>"
   },
   {
    "id": "qb2-a2",
    "type": "formulate",
    "title": "Write your own five hardest questions",
    "prompt": "Write the five questions you would least like to be asked, given what is actually in your experience. For each, write the honest answer - including what you have not done - in under 100 words. These are the answers that protect everything else you say.",
    "timeboxSec": 600,
    "rubric": "Must-haves: (1) five questions that genuinely target thin areas rather than comfortable ones - the usual ones are whether there were automated checks before release, whether you built a component or used a managed one, which percentile a quoted latency figure was, a named tool you have not used, multi-region operation, which drift statistic was actually computed, and whether SLOs and error budgets were formal or informal; (2) each answer says what you have not done, flatly, in one clause, without apologising and without over-explaining; (3) each answer pivots immediately to the adjacent real experience and to the transferable concerns, rather than to a promise of learning quickly; (4) each answer under 100 words; (5) no answer that quietly inflates what you did in order to sound competent; (6) at least one answer that turns the gap into a design you would build, with specifics. Common mistakes: choosing easy questions; apologising; over-explaining the gap; claiming one tool is basically the same as another. Bonus: identifies the question that would be most damaging if answered badly and rehearses it first. {{HONESTY}}",
    "model": "<p>The structure of a good answer, in five worked examples. Substitute your own systems and tools; the structure is the transferable part.</p>\n<p><b>1. They ask which percentile a number you quoted refers to.</b> \"That figure was the end-to-end decision budget the service was held to. I would want to confirm which percentile the objective was stated at before I quote one - and the distinction matters, because a mean-based objective and a p99-based objective describe very different systems. The tail is what customers feel.\"</p>\n<p><b>2. \"Did you build that component, or use a managed one?\"</b> \"I used it as a managed service. What I built was the pipeline that computed and published the features, and the arrangement where training and serving both read that same output. The properties I cared about were consistency, point-in-time correct history, and a single-key read fast enough for the budget.\"</p>\n<p><b>3. \"What did a model have to pass before you released it?\"</b> \"That was the weakest stage. Delivery and rollback were strong; the champion-challenger comparison before promotion was not as automated as the rest, and we ended up promoting on judgement. It is the first thing I would build: frozen holdout plus recent window, incumbent scored on the same rows, per-slice hard blocks, and a serving-path comparison.\"</p>\n<p><b>4. \"How did you detect drift?\"</b> \"Not with a formal drift-detection statistic. What I had was service monitoring and alerting; the decision-quality side was proxy metrics rather than a proper detector. What I would build is a small set of monitored features rather than all of them, persistence across windows before alerting, and year-over-year baselines in a seasonal business.\"</p>\n<p><b>5. \"You have not used the tools we run on.\"</b> \"Correct. My equivalents are the ones I have named. What transfers is not the interface, it is what I would insist on: a registry with lineage, checks a model must pass before release, a reversible deployment - and for streaming, event time versus processing time, watermarks, late data and idempotency. The operational instinct for those specific tools would take real time, and I would not pretend otherwise.\"</p>"
   }
  ],
  "sayQuestion": null,
  "sayItOutLoud": null
 }
};
